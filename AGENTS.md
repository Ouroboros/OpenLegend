# OpenLegend Agent 规则

## 1. 沟通与正确性

- 默认使用中文沟通；文档、提交信息、TG 汇报和解释均使用中文，除非用户明确要求其他语言。
- 一切以当前原版 `Z.COM` / `Z.DAT` 的机器码和完整汇编为准；原版汇编是唯一正确的行为真值。
- 修改必须最小且聚焦，不顺手重构、重命名、重新设计接口或添加无关测试。
- 不添加占位 TODO、dummy 实现或仅为通过测试而伪造的逻辑。
- 文档只写事实、证据地址、验证等级和未决项，不把推测写成完成结论。

### 1.1 禁止子代理

- 禁止使用 `subagent` 工具启动、恢复、转向、等待或管理任何子代理。
- 禁止通过 `session_handoff` 或其他机制变相委派给子代理、外部 coding agent 或独立 AI 会话。
- 调研、逆向、实现、审计、测试、提交和汇报全部由当前主会话直接完成。
- 禁止使用 `pi-interactive-shell`、`interactive_shell` 及其任何模式执行任何命令。
- Windows 宿主命令必须由主会话通过 `bash` 直接调用 `cmd.exe` 或目标 `.exe`，不得通过交互式 shell 中转。

## 2. 仓库与数据边界

- 产品代码、工具和研究产物只放在本仓库。
- 原版游戏文件保持只读，且不得复制或提交到本仓库。
- 构建产物、本地工具、用户环境输出和原版 EXE/COM/DAT 不得误提交。

## 3. Git、阶段提交与工作区保护

- 工作区可能包含用户改动。编辑前读取当前文件和 diff，只处理当前阶段相关内容，不覆盖或混入无关变化。
- 禁止使用 `git reset --hard`、`git checkout --` 等破坏性命令清理工作区。
- 不使用 `git -C`；Git 命令通过工具的仓库工作目录执行。
- 每到一个已经验证、可独立回退的阶段边界，必须自动完成：

  ```text
  精确暂存本阶段文件
  → 使用 $commit Skill 检查并提交已暂存内容
  → 确认暂存区为空
  → 立即 git push
  → 继续下一阶段
  ```

- `$commit` Skill 不负责暂存或推送。主 Agent 在调用 Skill 前只暂存本阶段文件，Skill 完成后再推送。
- 不绕过 `$commit` Skill 直接执行 `git commit`。
- 不等待用户提醒提交或 push。只有用户明确要求“下次提交后暂停”时，完成该次提交和 push 后暂停；用户直接要求暂停时立即停止，不补做提交。
- 测试失败、证据未收敛或改动跨越未完成边界时不得抢先提交。
- 提交前检查完整 staged diff 和近期历史，提交后确认实际 commit 与远端结果。命令未成功时不得声称提交或 push 成功。

### 3.1 提交标题

- 提交标题必须让玩家和普通开发者直接看懂，并根据本次实际代码、测试、原版文字和可观察结果逐条编写。
- 标题必须回答：
  1. 改的是哪个真实游戏界面、功能或明确的数据处理职责；
  2. 玩家执行了什么操作，或程序在什么明确时机执行；
  3. 画面、数据或运行结果具体发生了什么变化。
- 已确认的游戏名称直接使用，如标题画面、读取进度、世界地图、场景、对话、角色属性、物品、武功和战斗。
- 共用内部函数不属于单一界面时，标题写清实际数据操作，不虚构业务页面。
- 标题禁用不能独立说明结果的内部黑话，例如：`闭环`、`分派`、`编排`、`transition`、`runtime`、`owner`、`helper`、`entry`、`page`、`mode`。
- “恢复”“完成”“处理”“准备”“更新”“支持”等宽泛动词不能单独承担标题含义，必须写明具体操作和结果。
- 不得根据草拟标题反过来修改代码行为；标题与原版汇编或代码不一致时修正标题。
- 如果真实界面、按钮或结果尚未查清，继续检查原版字符串、调用者、测试和证据，不得发明名称并提交。
- 提交前人工阅读完整 staged diff，并用一句普通中文复述“玩家或程序能观察到什么变化”；复述不清楚则标题不合格。

合格示例：

- `feat: 启动程序后按原调色板显示标题画面`
- `feat: 读取进度时按原槽位字节恢复角色状态`
- `fix: 角色走到世界边界时保持原版碰撞坐标`
- `docs: 记录场景精灵索引末帧延伸到文件结尾的汇编证据`

## 4. TG 阶段汇报

每达到一个已验证、可独立回退的阶段边界并由主 Agent 检查后，发送：

```bash
python3 /mnt/d/Dev/Source/Project/stockkit/scripts/tg_notify.py "CONTENT"
```

`CONTENT` 固定为五段，段首和顺序不得改变，每段之间恰好一个空行：

```text
OpenLegend <模块或阶段>：<功能或工作包>已完成。

实现：<高层业务结果>。

兼容：<关键兼容行为与调用方回收；没有新增事项时写“保持既有兼容边界。”>。

验证：<实际执行的验证门>全部通过。

进度：<整体进度>；下一项<下一工作包或明确动作>。
```

- 五段之外不增加标题、问候、总结或备注；阻塞事实压缩写入“兼容”段。
- TG 命令失败时明确记录，不得声称已经发送。
