# 函数证据：`sub_2B3B4` `0x2B3B4..0x2B483`

状态：`implemented_pending_review`

机器码无显式目标坐标，读取玩家当前格的事件层。事件为 `-1` 或事件 word4（event_3）恰为 `-1` 时返回0且不呈现；其余值包括0和其他非正数都会先恢复方向基础图、重绘并呈现场景，保存 event index 与当前 x/y，仅 script id `>0` 时调用解释器，最后返回1。

现代 `SceneSession::run_auto_event/prepare_event/resume` 已把禁用条件从笼统 `<=0` 修正为严格 `==-1`，并恢复 `present` continuation 及非正脚本的事件上下文写入。独立 oracle 固定 `-1` fallback、script0 `present->stay` 和真实 script825 `present->notice` 序列；原资产 event_3 三类分布已登记。最终双向 REVIEW 尚未执行。
