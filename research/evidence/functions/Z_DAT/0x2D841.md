# 函数证据：`sub_2D841` `0x2D841..0x2DBF4`

状态：`implemented_pending_review`

opcode3 固定宽度14 words：参数为目标场景、目标事件和十一个事件字段。场景 `-2` 使用当前场景；事件 `-2` 使用当前事件，事件 `-1` 还会先把触发格写为 `-1`。每个字段仅在参数不为 `-2` 时写入。

坐标基本块 `0x2D9CD..0x2DA3A` / `0x2DB7A..0x2DBE7` 在写新 x/y 前分别保存旧 x/y；任一坐标改变后，先清当前工作场景事件格的旧位置，再以事件号写入新位置。当前1,018条 KDEF 共2,320次 opcode3，其中30次坐标修改全部同时给出 x/y；首次为 script147 pc0，事件6从原坐标迁移到 `(14,40)`。现代 `SceneSession::modify_event` 原先在写入后才读取“旧坐标”，会清新格并留下旧格；本切片已改为先读旧坐标并由 synthetic opcode3 固定旧格清除、新格写入。

显式外部场景路径通过 `ALLDEF.IDX/GRP` 临时载入、修改、回写后保留当前会话；现代全100场景常驻 `GameSnapshot` 直接写目标事件记录。原坐标格副作用始终落在当前工作场景，现代映射同样写当前 `scene_id_` 的 event-index 层。最终 REVIEW 仍需从入口复核当前/外部归档分支、`-1/-2` 哨兵和全部调用者。
