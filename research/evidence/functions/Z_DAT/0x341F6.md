# 函数证据：`sub_341F6` `0x341F6..0x343DA`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::choose_ai_medicine_target`。

当前原始`Z.DAT`物理范围为484 bytes、117条连续指令、16条分支、28处重定位和4个direct call；raw SHA256为`001f0798d08e00b532d1e3650ee4cfafdd6796bb5112447eb88da1a8f12743b2`，IDA加载后SHA256为`b0ee1653f7d268d5b780e3fdd70d63da11e1e9e4d06db15e6569e8e8ada8ae86`。唯一caller为AI入口医疗目标门命中后的调用点；四个call依次为栈探测和三个独立RNG调用点。随机实现为独立owner，本函数只关闭调用次序与短路消费。

函数按signed combatant槽序扫描，跳过actor自身、不同side与hidden非0目标。每个候选先要求actor医术严格大于candidate伤势减30；两值先符号扩展至32位，减30不发生16位回绕。候选已有请求医疗动作8、HP严格小于20或伤势严格大于40时直接选择，均不消费RNG。

仍未选择时依次判断HP严格小于最大HP的1/2、1/3和1/4；除法为signed截零。只有对应HP条件成立才消费一次`bounded(10)`，输出分别严格小于7、8、9时选择，选中后不消费后续RNG。前三档均失败后，HP严格小于最大HP的1/5时确定选择，不再消费RNG。首个命中保存目标坐标与slot，给actor写动作5并返回5；全部未命中返回0，不写actor动作，也不清理机器中此前残留的目标共享状态。现代typed返回用空目标表达该不可用状态；越界actor、role或count安全失败属于合法域外平台适配。

定向回归覆盖：actor自身/hidden/异side低HP均跳过；医术20对伤势50的等号门拒绝；请求动作8、HP19、伤势41三类免RNG命中；HP20与伤势40等号不直接命中；HP49/max100用seed5输出5首档命中，seed1输出8只消费一次后拒绝；HP32/max100用seed1消费`[8,8]`后拒绝；HP24/max100消费`[8,8,3]`在第三概率门命中；HP20/max105用seed331消费`[9,9,9]`全失败后由1/5确定门命中。对应终态依次为1222621274、1103527590、2524885223、662824084和3382126054。正式battle golden由独立Python两次生成逐字节一致，只新增十项医疗目标边界，SHA256为`07ed00f26acb04390fdfd087a2802239a4cc01b815f1a4ec8c00679957f12f4c`。

首次完整机器推导后逐块对照未发现产品实现差异，只发现边界与RNG短路覆盖不足；补测试与独立golden后再次从入口覆盖全部117条指令、16分支、28重定位、唯一caller、三处RNG调用、全部优先/概率/确定门和两个返回，零新增差异或未决项。机器门、静态/golden门、closure门、逆向框架及根`./build.sh app --config Debug` 14/14均通过，归类`platform_adapted / converged_no_new_differences`。
