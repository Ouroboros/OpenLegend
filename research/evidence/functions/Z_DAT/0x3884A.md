# 函数证据：`sub_3884A` `0x3884A..0x38910`

状态：`implemented_pending_review`

已映射状态边界：`BattleSetup::effect_animation_plan`；玩家暗器调用点已接入 `BattleSession`。

函数先把固定sample 13与effect id载入channel 1/2，立即分派channel 1，render/present后wait100，再分派channel 2。effect visible随后置1；初始effect frame为 `2*sum(word_55ACE[0..effect_id))`，循环 `word_55ACE[effect_id]` 帧，每帧render→present→wait17后frame加2，结束清effect visible。

固定effect2：前奏100 tick，17个可见帧，effect frame `48..80`，完整hash `0x2b5c87d8e0c754d5`。玩家暗器真实effect30已分派sample13、render/present、等待三次tick变化、分派effect sample并执行11帧EFT；前奏与首EFT整帧FNV64为`0xdbee20f394fd7219`、`0x370a4078e9de6172`。AI暗器也实际执行同一前奏及其后EFT/damage链，但保留独立owner，不调用玩家库存/行动尾部；固定AI前奏像素hash为`0x49aac6569a28fe89`，sample13后按参数100等待三次BIOS tick变化，再进入11帧effect30。最终完整汇编↔C++ REVIEW仍待执行。
