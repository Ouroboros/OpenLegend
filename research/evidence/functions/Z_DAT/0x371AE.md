# 函数证据：`sub_371AE` `0x371AE..0x371F7`

状态：`implemented_pending_review`

映射：`BattlePathing::build` 的 enqueue。

按当前 signed 写下标分别写 x/y，再把写下标设为 `(index+1)%255`；没有 full guard。C++ 同样使用255槽且不把它替换为无界 STL queue。
