# 函数证据：`sub_296E6` `0x296E6..0x29819`

状态：`implemented_pending_review`

机器码清零事件计数/游标、四个动画 word 与步行 frame/delay，按 jump flag 从52字节 scene metadata 的 normal words14/15 或 jump-return words24/25 取得玩家坐标；视口分别为 coordinate-11 后按 signed `[0,36]` clamp，并扫描完整64×64 event-index layer 统计非 `-1` 格。玩家基础图片沿用当前朝向索引。

`SceneSession` 新实例的成员初值、metadata 入口选择、`update_view_origin` 与 `player_frame` 映射同一合法资产合同；独立 oracle 与单测固定 `(0,63)→(0,36)`、jump `(63,0)→(36,0)`、direction2/frame5030，真实 scene70 normal入口 `(44,29)`、jump入口 `(0,0)` 与 event-cell count 12 亦记录。scene id超出84条 metadata 的原越界域由现代保护拦截，不宣称该非法域等价；最终 REVIEW 尚未执行。
