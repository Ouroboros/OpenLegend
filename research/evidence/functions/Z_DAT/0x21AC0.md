# 函数证据：`sub_21AC0` `0x21AC0..0x22066`

状态：`implemented_pending_review`

该函数先按`word_C0B78`取连续队伍前缀，再筛选解毒能力`>=10`的施术者party slot。没有合格施术者时，保留调用前已显示的游戏主菜单，在其上叠加`(70,18,154,26)`提示框、“隊員中無人解毒夠格”，present后由`sub_20C32`等待任意非零键返回主菜单。

存在合格施术者时，每轮按`word_C0BF2`重画世界或场景背景，再绘制施术者标题框`(70,18,108,26)`、列表框`(70,45,90,20*count+30)`、“誰要幫人解毒”、“解毒能力”、过滤后的姓名和`%2d`能力值。上下键在过滤列表内回绕；Enter、Space和keypad Insert确认；Escape直接返回主菜单。

确认施术者后以参数1调用`sub_22090`选择全部队员中的解毒目标。目标选择Escape返回原施术者光标；确认后经队伍表把两个party slot映射为角色ID，再调用`sub_39DA3(-1,-1,actor,target)`。现代实现复用`apply_role_detox_value`，保持两次`bounded(10)`的消费顺序、`ability/3+first-second`、0..99夹值、毒值超过能力+20时归零、目标毒值夹值及16位写回顺序；该路径不扣体力。

效果完成后再次按当前上下文重画背景，绘制`(112,47,96,26)`结果框、“幫助解毒”和`%3d`结果值，present后等待任意非零键再返回主菜单。结果值0仍走结果提示。

现代映射由`GameMenuController`保存双层选择与过滤slot映射，`LegacyGameRuntime`执行party slot到角色ID映射及状态提交，`BattleRenderer`负责能力列表、参数1目标列表和两类提示像素。定向回归覆盖能力阈值、过滤与取消、三确认键、两次RNG后的毒值写入、结果等待、世界整帧hash及无合格消息；与医疗路径共享的场景测试固定双层选择框外背景不变。

逐指令实现审计同步修正了能力/目标/结果数字的`%2d/%3d`宽度，以及无合格提示应保留主菜单底图的时序差异。Linux app Debug、Windows app Debug及Linux app ASan+UBSan均14/14通过；sanitizer后普通Linux Debug cache已恢复并复验。独立B5 golden与tracked证据一致，五项原版资产摘要未变化。功能映射状态提升为`implemented_pending_review`；最终双向逐基本块REVIEW仍为`not_started`。
