# 函数证据：`sub_21AC0` `0x21AC0..0x22066`

状态：`platform_adapted`

## 独立机器恢复

本轮先仅依据当前 `Z.DAT` 的 `0x21AC0..0x22065` 指令、向前共享尾块和原始字符串字节，从入口独立恢复行为，再读取现代实现。入口建立六项 `-1` 临时槽；从队伍槽1扫描 `word_C0B78`，首个 `<=0` 的槽号作为连续队伍长度，没有哨兵则为6。随后按槽序筛选角色记录步长 `0xB6`、解毒能力字段 `word_901AC >= 10` 的施术者，并保存原party slot。

无合格施术者时，函数不重画背景，而是在调用前主菜单帧上叠加 `(70,18,154,26)` 提示框、“隊員中無人解毒夠格”，随后跳入 `sub_21496` 的共享present/`sub_20C32`等待尾块。存在合格施术者时，每轮按 `word_C0BF2` 的0/1值重画世界或场景背景，再调用 `sub_269AB` 绘主菜单，叠加 `(70,18,108,26)` 标题框、`(70,45,90,20*count+30)` 列表框、“誰要幫人解毒”和“解毒能力”。姓名 `%s` 缓冲只检查字节1..6中的首个NUL，X依次为95/91/87/83/79/75；能力以 `%2d` 绘于 `x=130`。普通项颜色 `0x2321`，当前项重绘为 `0x6663`。

Down `0x98` 和Up `0x9E` 在过滤数组内回绕并分别清自身translated state与last-key。Enter、Space、keypad Insert共同确认，先清last-key与三项确认键态，再以参数1调用 `sub_22090`。目标Escape返回 `-1` 后保持原施术者光标；确认目标后经队伍表把目标和过滤后的施术者party slot映射为角色ID，再调用 `sub_39DA3(-1,-1,actor,target)`。外层Escape清last-key及Esc状态后退出。

效果完成后按当前上下文重画世界或场景背景，绘制 `(112,47,96,26)` 结果框，压入“幫助解毒”后跳入 `sub_21496 @ 0x21A37` 的共享格式化/绘制尾块，以 `%3d` 显示返回值，present并等待任意非零键。结果值0仍显示结果框；退出使用 `sub_21496 @ 0x21AB6` 的共享EAX=0 epilogue。

## C++ 映射与平台适配

- `LegacyGameRuntime::update_menu_counts` 保留槽1哨兵扫描和六人上限；`GameMenuController::set_ability_party_options` 按signed解毒能力 `>=10` 保存原party slot。
- `GameMenuController` 的source/target两级状态保留过滤光标、目标Escape回到原施术者、三确认键和上下回绕；共享 `party_select` 的 `menu_key_state_reset` 精确回报Down、Up、Escape和确认组写零。
- runtime按party slot取得角色ID并调用 `apply_role_detox_value`；合法数据路径保持两次 `bounded(10)`、signed `ability/3+first-second`、16位回绕、0..99夹值、严格 `poison > ability+20` 门槛、当前毒值限制及写回后 `<0`/`>100` 夹值，且不扣体力。
- `BattleRenderer` 的能力列表、参数1目标选择和结果/无合格提示保留原Big5字节、框坐标、姓名NUL居中、`%2d/%3d`前导空格和颜色。
- 同步自旋与跨函数向前共享尾块由现代事件续体和共享renderer调用承接；无效资源、角色ID及宿主绘制边界的安全拒绝属于平台适配。

从 `0x21AC0` 入口逐块复核队伍边界、signed阈值、临时过滤槽、全部输入分支、参数1目标调用、`sub_39DA3`参数顺序、世界/场景绘制分支、列表像素、向前共享结果/等待尾块和EAX=0出口。此前在 `sub_21496` 审计中发现并修正的共享 `party_select` 键态差异已覆盖本函数同构写集；本函数单向汇编→C++终审未发现新增差异。

## 验证

- Linux app Debug：14/14，`proc_a3be`。
- 解毒真实资产整帧：施术者 `0xB4778AEC4384B308`、目标 `0x13C793584FEBEBB0`、结果 `0x985B2010DF3F15EB`、无合格提示 `0x9F12EC5BDE262B27`。
- 固定解毒结果：目标毒值 `90 -> 59`；该路径不修改施术者体力。
- 原程序动态差分因本机无DOS运行器登记为 `blocked_runtime_oracle`；当前机器码静态闭环、真实资产像素与独立状态oracle不受影响。

```text
final_review = converged_no_new_differences
remaining =
```
