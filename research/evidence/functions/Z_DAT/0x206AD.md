# 函数证据：`sub_206AD` `0x206AD..0x20899`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；调用者经`sub_3D88A`传入粒子屏幕坐标、CLOUD帧和6..8混合权重。

独立汇编恢复：入口把source权重换算为palette分量表偏移`64*weight`，destination权重为`8-weight`；读取RLE的宽、高、x/y offset后分别处理上、下、左、右裁剪。纵向负坐标以逐行跳过RLE行，横向负坐标在每行内跨skip/run；可见区限制为320×200。每个可见源像素最终调用`sub_20899/sub_208A9`，透明skip只推进destination坐标。全部循环出口均保持RLE源指针与行边界一致。

独立向量：完全出界、四边部分裁剪、空行、跨左边界run、weight 6/7/8和真实CLOUD四帧。现代映射为`WorldSession::draw_weather_particle`：`SpriteFrameView`恢复相同RLE行/run，显式坐标裁剪，逐像素计算后写回indexed framebuffer。

差异与适配：原固定全局指针由只读`PackedArchive`/span替代；损坏RLE在现代实现中失败返回，不执行原程序未定义的越界读取。真实资产路径由300 tick天气hash与逐像素oracle覆盖。最终汇编→C++与C++→汇编逐基本块REVIEW均为`not_started`。