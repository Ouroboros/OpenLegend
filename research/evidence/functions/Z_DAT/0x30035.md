# 函数证据：`sub_30035` `0x30035..0x30094`

状态：`implemented_pending_review`

KDEF opcode53。机器码以原 Big5 格式串 `你現在個人聲望指數為%4d` 格式化 role0 word59（fame），调用 `sub_2CEBF(50,40,220,27,255,0,...,4)` 画圆角暗化面板，再由公共文字调用在 `(60,45)` 以颜色 `0x0705`、16点字绘制；其尾部跳入 opcode52 的呈现、任意键等待与场景重绘链。

C++ case53 生成相同原始 Big5/ASCII 字节和 `%4d` 空格宽度，以 notice style53 使用对应面板尺寸、文字坐标和颜色，并复用相同同步恢复。真实 script828、fame123 固定文本十六进制和 framebuffer FNV1a64 `0x5678c57a93ec10c4`；独立 B7 oracle 从真实 scene70 原帧与字体/调色板计算同一结果。Linux Debug BUILD脚本门禁通过；最终双向 REVIEW 尚未执行。
