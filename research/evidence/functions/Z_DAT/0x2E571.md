# 函数证据：`sub_2E571` `0x2E571..0x2E5E6`

状态：`implemented_pending_review`

无 UI 的共享库存 mutator。线性扫描全部200槽，对每个匹配 item ID 的 count 做 int16 加法，不短路；若一个都未匹配，再找首个 ID=-1 槽，写目标 ID，并把 delta 加到该槽既有 count。不会过滤非正 count、不会合并重复槽、不会压缩/清零其它槽。

调用者包括队员加入时归还携带物、shop 增加购买物品。C++ `SceneSession::add_inventory` 按此实现；真实 script149/497 固定重复槽全更新、顺序不变及空槽残留 count 相加。shop 使用同 helper 增加物品。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
