# 函数证据：`sub_39A45` `0x39A45..0x39B1F`

状态：`implemented_pending_review`

映射：`BattleSetup::apply_poison_value`。

毒值先计算signed `(actor.use_poison-target.anti_poison)/4`，负数除法向零截断；结果先夹到0..99，再按 `99-target.poison` 限制剩余空间。随后以16位加法写目标poison，并依次夹到≤99和≥0；返回实际加值。该函数不消费RNG。

固定actor use_poison80、target anti_poison20、poison90：raw15、实际9、目标99；差值为负时返回0；raw超过99时返回99。Linux Debug 14/14通过。现代实现只对坏combatant/role索引安全拒绝；最终仍需完整汇编↔C++ REVIEW。
