# Z_DAT `sub_39A45` final evidence

## 定位

- 入口：`0x39A45`
- 结束：`0x39B1F`
- 长度：218 bytes
- 最终状态：`platform_adapted / converged_no_new_differences`
- 现代owner：`openlegend::battle::BattleSetup::apply_poison_value`
- fresh临时数据库：`tmp/Z_DAT-b8-order60.i64`
- 完整机器导出：`tmp/b8-order60-machine-all.txt`
- 独立机器审计：`tmp/b8-order60-audit.json`
- 独立入口复核：`tmp/b8-order60-independent-review.md`

## 机器身份

- 完整函数：52条指令、11个基本块、5个条件分支、0个无条件跳转、1个本地`RET`（`0x39B1E`）。
- 唯一direct call：`0x39A4A -> sub_3ED1E`，为16-byte栈探测；本owner不直接消费RNG。
- 唯一caller：`sub_397E5:0x3998A`。
- 9处加载时fixup偏移：`0x1C,0x31,0x69,0x8B,0x9D,0xA4,0xAE,0xC2,0xCC`；每处loaded值均比原始值高`0x20000`，全部反归一化后与只读`Z.DAT`切片逐字节一致。
- raw SHA256：`1362e94661933be1546599511f67fdd2ceb4d2eedc8c7a30f15693bb754bf622`
- loaded SHA256：`e401b2bac12421aba49d0a079cc5df45d2f9e623116171502e3c32af755b5c2a`

## ABI与caller合同

`sub_397E5`依次压入target role、actor role、target slot、actor slot；本函数只读取后两个显式role参数。caller在返回后立即把EAX复制到EDX，清理16字节参数，再把DX写到目标combatant damage word，因此返回低16位的signed值可观察。

合法caller域中，现代API接收actor/target slot后立即解析出同一对role；caller从读取role到调用之间没有call或写入，故参数重构逐块等价。无外部xref进入函数内部基本块。

## 完整机器合同

1. `0x39A53..0x39A7D`：以signed role id乘182，读取actor `use_poison`和target `anti_poison`两个signed word，执行32位减法。
2. `0x39A7F..0x39A89`：sign-adjust序列实现signed除4向零截断。输入word差限定商在`[-16383,16383]`，AX不丢失结果。
3. `0x39A8C..0x39AA0`：先按signed `>99`夹99，再按signed `<0`夹0。
4. `0x39AA0..0x39AD4`：signed扩展当前target poison和amount，以32位相加；若和严格大于99，amount改为low16 `99-current_poison`。当现存poison大于99时，该值可以为负。
5. `0x39AD4..0x39AF9`：以16位加法把amount写入target poison，随后signed `>99`夹99。
6. `0x39AF9..0x39B17`：target poison signed `<0`时夹0。
7. `0x39B17..0x39B1E`：DX sign-extend到EAX并返回。返回的是容量限制后的amount，不是最终poison经末两次夹值后的实际delta。

## C++逐块结论

`BattleSetup::apply_poison_value`与机器合法域一致：

- `std::int32_t(use_poison)-std::int32_t(anti_poison)`与机器32位signed差一致；
- C++ signed `/4`与机器向零截断一致；
- quotient、capacity差和目标写回均显式采用`wrapping_i16`，保留low16语义；
- 0/99初始夹值、capacity strict `>99`、写回后high clamp再low clamp的次序一致；
- 现存poison100时返回-1并写99，现存32767时可返回-32668，均保留机器异常但可观察的signed返回；
- actor/target解析到同一role时，所有源字段读取仍先于poison写入；
- 不消费RNG，不修改其他role字段。

现代对invalid combatant slot和invalid role id安全拒绝，替代机器越界数组读取，登记为平台适配；不改变合法域。

## 独立向量与验证

原资产oracle覆盖：negative divisible与negative fractional向零截断、raw恰99/超过99、capacity和恰99/超过99、现存poison为99/100/32767/-32768/-1、role word差的正负极值，以及same-role alias。向量SHA256为`fec49732797f1f546f4e3cbcf234d6640e6c4d04a1edb4938d8ea8699e995477`。

C++回归逐项锁定amount与最终poison，并以fresh fixture验证invalid target slot和invalid role拒绝。首轮完整入口REVIEW及回归后核对均为零合法域产品差异，无产品源码修改。

`battle-goldens.json`由只读原始资产三次独立生成并逐字节一致，order60正式文件SHA256为`96406b67eecdda8e638ba945800dd15eab2ef4819a79c388dc756fe7ec38516a`。closure gate后根`./build.sh app --config Debug`精确通过`100% tests passed, 0 tests failed out of 14`。

## Closure边界

- `sub_3ED1E`栈探测保持独立owner。
- sole caller `sub_397E5`已按order59独立关闭，其方向、effect、动画、sprite和action提交行为不由本owner传播状态。
- 本owner只关闭毒值kernel自身的字段读取、signed算术、目标poison写回与返回值合同。
