# 函数证据：`sub_300D9` `0x300D9..0x300FF`

状态：`implemented_pending_review`

KDEF opcode55。机器码按 `event_index*22` 读取当前场景事件 record word2（`event_1`），与参数相等返回 true offset，否则返回 false offset。C++ `SceneSession::run_event(case55)` 读取同一字段并调用 `conditional`。

真实 script464 的首个 `55,2,-1,14,0` 已固定两条分支：event_1=-1 时跳过14-word modify-event；event_1=999 时执行并写为862，同时 `-2` 保留 current_picture=1234。独立 B7 oracle 同步输出该向量；Linux Debug `openlegend.scene` 通过。最终双向 REVIEW 尚未执行。
