# 函数证据：`sub_30094` `0x30094..0x300D9`

状态：`platform_adapted`；最终 REVIEW：`converged_no_new_differences`

来源：当前 `Z.DAT` 机器码与原始文件字节；headless IDA完整导出；当前 `KDEF.IDX/GRP`；当前RANGER场景metadata布局。

## 1. 机器身份与caller

- 函数物理范围 `0x30094..0x300D9`，69 bytes，14条指令。
- loaded SHA256 `c8d966040fd8e75500eb9ebb9326ce0bc8207f971f65fdda4fe41cf1af6ad9a1`；原始 `Z.DAT[0x29A94:0x29AD9]` SHA256 `02eae42c7e04357aedc4009826569c45f0f0d2e1dddad227c6cd6395e01781c7`。
- 5个差异字节全部由5项场景metadata绝对地址操作数的 `raw+0x20000` 搬移解释，归一化后逐字节等于loaded。
- 唯一代码caller是事件解释器opcode54；没有脚本参数，忽略callee返回0，经公共块把PC增加1 word。
- 入口 `sub_3ED1E(4)` 仅为Watcom栈探测合同。

## 2. 不看C++的开放场景恢复

1. EAX从0开始；每轮以 `index*52` 定位场景metadata记录，把word0写为0，再自增索引。
2. `cmp eax,84; jl`使循环精确覆盖scene0..83共84条记录；84不是100份场景地图/事件数据的数量。
3. 循环后按固定顺序覆盖scene2=2、scene38=2、scene75=1、scene80=1。
4. 因此结果恰有80条word0为0，两条为2，两条为1；不读取旧值，不修改记录其他25个word，不执行场景I/O或UI，最后返回0。

## 3. 汇编→C++ REVIEW

首轮及从入口复核确认C++ opcode54以range-for覆盖合法快照中的84条scene metadata，并仅清 `entrance_condition`，之后写入相同四个特例值；PC固定增加1，没有产品差异。

现代快照以固定84条vector承载原内存块，并在四项覆盖前保留边界保护；损坏或手工缩短快照不执行原机越界写入，归类 `platform_adapted`。合法84条域中循环范围、写入顺序和值完全一致。

从入口重新逐条复核14条指令、5项搬移、52字节步长、84次循环、四项覆盖、返回值和caller PC，零新增差异。

## 4. KDEF与oracle

当前1,018条KDEF只有script821 PC145一次opcode54。按little-endian `<II>` 编码 `(script,PC)` 的完整调用流SHA256为 `0e2d7c1475a48b05d9f2c47d7bb245f87d23a929f9e38a51bb9234298d77f4d5`。

真实script821由前置对话链到达该指令。测试先把全部84条入口条件置777，再逐项验证最终80个0及四特例。按little-endian `<84h>` 编码最终结果的SHA256为 `0ab5bc8a2f315a8c30460744a7cbc42329873810f9d4082c97c327c292335745`。

独立oracle双次生成逐字节一致；正式 `scene-goldens.json` SHA256 `f359f735b2e81b5dad6be60d827f4eeeecc62491560828c73897f01ffb2bc636`。

## 5. 验证与结论

- Linux app Debug根构建：14/14 tests通过（`proc_dbee`）。
- 原程序动态差分：`blocked_runtime_oracle`。

```text
closure_status = platform_adapted
final_review = converged_no_new_differences
remaining =
```
