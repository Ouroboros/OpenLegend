# 函数证据：`sub_30094` `0x30094..0x300D9`

状态：`implemented_pending_review`

KDEF opcode54。循环 `i=0..83`，把84条 RANGER scene metadata 的 word0 `entrance_condition` 全清0；随后固定 scene2=2、scene38=2、scene75=1、scene80=1。这里84是 metadata 记录物理数量；100份场景地图/事件数据不是100条 metadata，不能把84..99当作可索引 metadata。

C++ case54 range-for `snapshot.ranger.scenes`，其固定 size=`kSceneMetadataCount=84`，再写四特例。真实 script821 从29段 dialogue 驱动至 opcode54；scene0/83清0，2/38/75/80分别为2/2/1/1。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
