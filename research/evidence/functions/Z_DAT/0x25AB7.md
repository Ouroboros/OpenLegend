# 函数证据：`sub_25AB7` `0x25AB7..0x25BBA`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；审计包 `tmp/b9-review/0x25AB7.txt`

## 1. 独立机器恢复

- 本函数只重置scene map/event工作副本，不读取或复制RANGER。
- 先以只读方式打开 `ALLSIN.GRP`，创建/截断 `ALLSINBK.GRP`，两端均seek到0。
- 固定循环2次：每次从源读取 `0x258000`（2,457,600）字节到共享scratch，再向目标写出同样字节；总计4,915,200字节。
- 随后打开 `ALLDEF.GRP`，创建/截断 `ALLDEFBK.GRP`，两端seek到0；一次读取并写出 `0x6B6C0`（440,000）字节。
- 关闭顺序严格为目标DEF → 源DEF → 目标SIN → 源SIN；前三次close结果丢弃，函数返回最后一次close结果。
- 机器路径不检查open/seek/read/write/close结果，也不复制S/D IDX。

## 2. 汇编 → C++ 映射

- baseline scene字节读取：`persistence::load_baseline`、`load_snapshot`、`decode_scene_archive`。
- 固定S/D尺寸：`model::kSceneCount * kSceneMapBytesPerScene = 4,915,200`，`model::kSceneCount * kSceneEventBytesPerScene = 440,000`。
- 工作态owner：`model::GameSnapshot::scene_maps/scene_events`；`LegacyGameRuntime::begin_new_game`在baseline载入成功后原子提交到 `GameState`。
- 后续scene脚本只修改该内存工作态；编号槽保存由 `write_numbered_slot_scene_archives`直接运输当前S/D字节。

## 3. 单向终审

- 从 `0x25AB7` 入口独立恢复后才读取现代persistence实现。
- 逐块覆盖四文件打开模式、两端seek、2×S chunk循环、单次D chunk、精确字节数和四次close顺序。
- 本轮零新增差异，无需修正或重启。

## 4. 平台适配

- 原版创建并持续修改 `ALLSINBK.GRP/ALLDEFBK.GRP`；现代明确不写原始游戏目录，以内存 `GameSnapshot`替代会话期BK文件。
- 因此BK文件创建/截断、文件锁、部分写入和close返回值不是assembly-exact；终态必须为 `platform_adapted`。
- 现代增加完整长度/IDX布局校验并在成功后原子提交，避免机器无检查路径的部分工作态；合法当前资产的S/D字节、脚本写集和编号槽运输不变。
- RANGER baseline运输属于相邻新游戏边界，不伪装成本函数职责。

## 5. 验证与结论

- Linux app Debug `proc_07d5`：14/14。
- persistence回归固定S为4,915,200字节、D为440,000字节，验证baseline载入、逐字节snapshot round-trip、单word修改隔离、编号槽S→D运输及原始资产目录不被写入。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
