# 函数证据：`sub_2F8D1` `0x2F8D1..0x2F966`

状态：`implemented_pending_review`

KDEF opcode41，角色四个携带物槽。先找第一个 item ID 等于参数的槽，对其 count 做 int16 加法并停止；结果0/负数也保留 ID/count。若无已有，再找第一个 ID **恰为 -1** 的槽，写 item ID 和原始 count 参数（包括0/负数）；其它负 ID（如-2）不算空。四槽都无可用则无写入。

旧 C++ 拒绝 count0、把任意负 ID 当空，并在结果<=0时清槽。现 `add_role_item` 按两个阶段精确实现。真实 script289 opcode41(36,78,1)：已有 item78 count=-1 后变0仍保留ID；另一个 snapshot 的 slot0 ID=-2 不动，item78写入slot1的 exact -1 槽、count1。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
