# 函数证据：`sub_2B308` `0x2B308..0x2B3B4`

状态：`implemented_pending_review`

机器码显式参数为目标 x、y。先按 `y*64+x` 读取事件层；值为 `-1` 时立即返回0且不呈现。命中事件时先把玩家图恢复为当前方向基础图，重绘并呈现场景，再保存 event index、x、y；读取事件 word3（event_2），仅 script id `>0` 时调用解释器，但无论脚本是否正数都返回1。

现代 `SceneSession::use_item/prepare_event/resume` 已恢复命中事件后的 `present` continuation，并在非正脚本时仍保存事件上下文；无事件保持 `stay`。`SceneSession::use_menu_item` 在 `sub_2A86C` 对应的菜单恢复呈现后调用该路径，脚本结束再返回场景主菜单。独立 oracle 固定无事件、script0 与真实 script825 三条输出序列，并登记原资产 event_2 的 `-1/非正/正数` 分布；runtime 测试固定库存槽到 item ID 的转换。最终双向 REVIEW 尚未执行。
