# 函数证据：`sub_33599` `0x33599..0x33C4D`

状态：`pending_implementation`

映射：`BattleSetup::begin_ai_turn`、`BattleSetup::choose_ai_turn_action`、`BattleSetup::finish_ai_turn`。

入口先按actor side对全部combatant的`HP+attack`以int16逐次回绕累计双方总值与人数，不排除隐藏或死亡槽；随后固定执行战场重绘、present、调用原延迟参数300。`BattleSession`现实际计算四项int16累计值、重绘并present；present完成时捕获BIOS tick，并按`legacy_delay_tick_count(300)=8`等待八次tick变化后才调用selector。日志保留双方总值/人数、等待次数及最终动作/handler/目标。

决策严格按低HP→中毒→低MP→医疗队友→解毒队友→逃跑→攻势selector短路。低HP使用HP<20、hurt>50及最大HP的1/2、1/3、1/4、1/5门槛，随机阈值依次3/5/7/9；中毒入口无条件消费一次`bounded(10)`并与`poison/10`比较；低MP四档随机阈值为2/4/6/8。体力>50时医术/解毒能力达到20/40/60分别消费RNG并比较4/6/8，达到80最终无RNG兜底。逃跑先消费一次`bounded(10)<5`，再按HP<20或1/4、1/5档追加6/8门槛。

原顺序BUG保留：体力<10先把局部动作设为等待7，但入口仍继续判断低HP；若低HP selector被调用后返回0，会把等待覆盖为0并继续后续随机决策。固定seed1向量得到`[8,8,13]`、终态662824084；seed10逃跑向量得到`[3,4]`、终态1849040536。

动作0和7映射休息handler，1移动、2攻击、3用毒、4解毒、5医疗、6物品、8请求医疗、9请求解毒、10暗器、11逃跑；只有handler返回后才写combatant word7为1。动作0/7实际调用休息状态核心，随后由外层完成word7并进入逐actor胜负/清理continuation；固定seed1连续两个角色休息的体力增量依次为5、4。其余handler均进入各自typed计划；其中mode0..3移动需要的目的格、逐格状态、render/present和每格两次BIOS tick变化已同步执行，移动返回后恢复原攻击/用毒/支持/物品/请求/暗器计划。真实battle2逃跑动作11执行三格移动后休息并由外层写word7，同时保留该局部动作不写word10。攻击和用毒handler已完成直接/移动后状态、FIGHT/EFT、damage、音效、提交、攻击/休息回退和外层actor continuation；医疗/解毒handler也完成直接及移动后支持效果、无flash同步动画、攻击/休息回退和外层完成；请求医疗/解毒在可选mode0移动后无条件进入完整自动攻击并保留请求动作码。AI普通物品与暗器实际效果/动画/输入仍未执行，因此本函数保持`pending_implementation`。Linux Debug完整BUILD全部14项测试通过；最终完整汇编↔C++ REVIEW仍待执行。
