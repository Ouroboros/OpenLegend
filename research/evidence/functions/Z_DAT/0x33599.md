# 函数证据：`sub_33599` `0x33599..0x33C4D`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::begin_ai_turn`、`BattleSetup::choose_ai_turn_action`、`BattleSetup::finish_ai_turn`、`BattleSession`。

## 机器身份与边界

当前原始`Z.DAT`物理范围为1716 bytes、440条连续指令、64条分支、78处重定位和39个direct call；raw SHA256为`fe66621722777e5ada2987c67e39b5a56dc275feacc48e53684467ad4bbdeb68`，IDA加载后SHA256为`d5a59d8b4e7fd757b088169c19223937575ea33ac11fcfb26676bee7f3ce77aa`。两个caller固定为回合actor分派和玩家automatic分派。前置12项dispatch表从填充后的表首开始，表SHA256为`4ff226b5a41d10d4d7b96fd71c4072ef0b3fef190c91194134e725ce8be6b8f0`；目标依次为`33BAD,33BBE,33BD3,33BDE,33BE9,33BF4,33BFF,33BAD,33C0A,33C15,33C20,33C2B`，动作7与动作0共享入口。

## 汇编合同

入口先按actor side扫描全部combatant，不排除隐藏或死亡槽；每槽依次把role attack与HP以16位加法逐次回绕到双方总值，并以16位计数。四项值在绘制前写定，随后固定调用actor战场/状态绘制、present和参数300的延迟；延迟期间不重绘，末级攻势selector继续读取入口冻结的四项值而非重新扫描。

决策严格按低HP→中毒→低MP→医疗队友→解毒队友→逃跑→攻势selector短路。低HP使用HP<20、hurt>50及最大HP的1/2、1/3、1/4、1/5门槛，随机阈值依次3/5/7/9；中毒入口无条件消费一次`bounded(10)`并与signed `poison/10`比较；低MP四档随机阈值为2/4/6/8。体力严格大于50时，医术/解毒能力达到20/40/60分别消费RNG并比较4/6/8，达到80最终无RNG兜底。逃跑先消费一次`bounded(10)<5`，再按HP<20或1/4、1/5档追加6/8门槛。所有除法均按signed截零。

原顺序BUG保留：体力<10先把局部动作设为等待7，但入口仍继续判断低HP；若低HP selector被调用后返回0，会把等待覆盖为0并继续后续随机决策。固定seed1向量得到`[8,8,13]`、终态662824084；seed10逃跑向量得到`[3,4]`、终态1849040536。

动作0和7映射休息handler，1移动、2攻击、3用毒、4解毒、5医疗、6物品、8请求医疗、9请求解毒、10暗器、11逃跑；handler同步返回后才把当前combatant word7写1。现代typed handler把原同步callee拆成可呈现/等待的phase，但所有分支最终都在callee语义完成后调用`finish_ai_turn`，再进入逐actor胜负/清理continuation。各handler内部仍由其独立owner关闭，本owner只验证入口调用、参数、分派表和统一返回尾，不传播closure。

## 现代接线与回归

`BattleSession::begin_ai_action`在任何绘制前保存prelude；`ai_prelude_present`重绘battlefield和actor status panel，宿主present完成后才开始`legacy_delay_tick_count(300)=8`次BIOS tick变化；`ai_wait`保留已呈现缓冲，不重新读取底层状态或重画。第八次变化后，Session把保存的prelude显式传入selector并立即分派handler。

定向回归覆盖：330/220与3/2四项累计、present前计算、八次tick变化、等待期修改actor HP后整帧hash仍为`0xb02104139829a80d`、等待期把可重算总值改为1530/620后仍按冻结330/220选择攻击2而非医疗5，且RNG边界`[10,10,10,50]`输出`[8,8,3,15]`、终态3295386429。正式battle golden由原始资产和独立Python两次生成逐字节一致，仅新增该冻结prelude向量；SHA256为`f80e016364dc2512ae6a3294e9e1c72e4080f5a6afcae82d30d33954d46bf691`。

## 收敛结论

第一轮从入口审计发现延迟期会被现代render重复重绘，修正并补冻结像素回归后作废该轮结论；第二轮从入口重审发现攻势selector延迟后重新累计双方值，改为显式传递入口prelude并补共享状态/RNG向量后再次作废。第三轮重新从`0x33599`入口覆盖全部440指令、64分支、78重定位、39调用、两个caller、12项dispatch和统一尾，零新增差异或未决项。order9机器门、静态/golden门、closure门、逆向框架与根`./build.sh app --config Debug` 14/14均通过，归类`platform_adapted / converged_no_new_differences`；平台适配仅为同步present/阻塞延迟拆成宿主回调和tick phase，以及非法现代输入的受检失败。
