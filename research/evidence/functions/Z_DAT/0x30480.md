# 函数证据：`sub_30480` `0x30480..0x30510`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；直接调用者 `sub_302E0`。

## 汇编独立合同

参数 cell 0..29：

1. dialogue `2854+cell`，head 为机器码内 30-word 表，style0；render。
2. 调 `sub_31C75(cell+102, get_exp=0)` 建立战斗。
3. 返回值等于1：render→fade_from_black→dialogue2890(head0,style1)→render，返回1。
4. 其他返回：调用 `sub_2E659` 死亡 UI，返回0（实际死亡路径可转移控制）。

30-word head 表：`8,21,23,31,32,43,7,11,14,20,33,34,10,12,19,22,56,68,13,55,62,67,70,71,26,57,60,64,3,69`；由 `Z.DAT` VA `0x2C1CD`（raw offset `0x25BCD`）独立解码。

## C++ 映射与验证

`TournamentTrialState::choose_opponent/awaiting_battle` 输出 dialogue→present→battle；victory 输出 present→fade_from_black→dialogue2890→present；非 victory 同步 quit。真实 script936 全胜固定15个 battle/talk/head，失败反例固定首场后停止 RNG。Linux Debug scene/ui 通过。

`sub_31C75` 战斗主体属于 B8；`sub_2E659` 死亡 UI保持独立 pending。最终双向 REVIEW 尚未执行。
