# 函数证据：`sub_39188` `0x39188..0x395EC`

状态：`implemented_pending_review`

映射：`BattleSetup::apply_hp_damage`。

函数先扫描全部存活且未隐藏参战者，将 knowledge>80 的角色按 actor side 分成双方并各累加 `2*knowledge`。从当前熟练度 unsigned `/100` 向下寻找内力可支付的最高层，写出 cost scale；攻击总量为 `(3*attack + magic attack[cost_scale])/2 + 两件装备add_attack + 特殊加成 + 我方知识`，防御总量为 `defence + 两件装备add_defence + 敌方知识`，均保留16位路径。首次伤害为 `2*(attack-3*defence)/3 + bounded(20)-bounded(20)`；非正时退化为 `attack/10 + bounded(4)-bounded(4)`。非负值再加攻击者体力`/15`和目标受伤`/20`，距离1..10乘 `100-3*(distance-1)` `/100`，距离>10固定乘2/3，最终最小1。

写回顺序固定为 actor word13加damage/5、目标HP减damage；只有16位减法结果严格小于0才清0并给 actor word13加 `10*目标level`，恰好减到0不加击杀奖励。随后目标hurt加damage/10并封顶99；攻击者attack_with_poison加当前武功等级攻击值，超过目标anti_poison且目标anti_poison<90时，目标poison加差值/15，严格>100时写99，负数写0。

固定主向量：attack30、magic attack30、defence5、seed1两次bounded(20)均18，damage30、cost scale3、RNG state2524885223；HP30恰好归零只得6点计数，HP29下溢归零得6+40=46；hurt3、poison4。fallback向量固定双方知识162/164、装备攻防6/2、特殊加成3、RNG `[18,18,1,3]`，原伤害非正后退化为21，再因distance11缩为14，state3295386429。

玩家攻击四种area type现实际调用本函数；line与十字强制HP，方形type0/3按hurt type分派。跨百双击保留入口缓存范围但每次命中重新读取当前熟练度，因此第二击cost scale由3升4，提交时对应内力20→15→5。Linux Debug 14/14。现代实现对坏slot/role/item安全拒绝；最终仍需完整汇编↔C++ REVIEW。
