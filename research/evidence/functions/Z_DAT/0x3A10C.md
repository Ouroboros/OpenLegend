# 函数证据：`sub_3A10C` `0x3A10C..0x3A29C`

状态：`implemented_pending_review`

映射：`BattleSetup::apply_medicine_value`。

actor体力<50时立即返回0，不消费RNG且不写状态。否则medicine负值先夹0；按目标hurt分四档计算治疗基数：`<=25`为`4/5`、`26..50`为`3/4`、`51..75`为`2/3`、`>75`为`1/2`，均为signed除法向零截断；随后只消费一次 `bounded(5)`。若目标hurt严格大于原medicine+20，治疗量与减伤量都归零；治疗量再受maximum_hp-hp限制，hurt按完整非负medicine扣减并夹0，最后actor体力减2且不在本函数夹0。

固定medicine80、hurt40、HP100/200、seed1：RNG输出3、state1103527590，治疗63、HP163、hurt0；hurt25/26/51/76分别治疗67/63/56/43。medicine20且hurt41严格超门槛得到0但仍消费RNG并扣2体力；体力49直接返回且不消费RNG；HP190/200时返回10并封顶。Linux Debug 14/14通过。最终仍需完整汇编↔C++ REVIEW。
