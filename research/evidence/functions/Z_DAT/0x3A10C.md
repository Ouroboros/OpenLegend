# 函数证据：`sub_3A10C` `0x3A10C..0x3A29C`

状态：`platform_adapted / converged_no_new_differences`

映射：`apply_role_medicine_value`与`BattleSetup::apply_medicine_value`。

## 机器身份、入口与owner边界

fresh临时数据库`tmp/Z_DAT-b8-order66.i64`仅由`/mnt/d/Dev/Crack/IDA/idat.exe -A`配合`tmp/ida_dump_b8_order66.py`分析。owned范围固定为400 bytes、103条连续指令、25个基本块、12个条件分支、3个无条件跳转、20处绝对数据fixup、2次direct call、2个入口xref和1个本地RET。raw SHA256为`d5f8fa8f412f555f98c496abb0dbe933fbf58591b98298da0729ceeb17293868`，IDA loaded SHA256为`0eafabf1a8517ec451a14e08ddc29135e94e68fcb0767003c1e41a281ad315f8`；20处DWORD均严格增加`0x20000`，逐处反变换后与原始400字节完全一致。

调用仅为入口20-byte栈探测`sub_3ED1E`及`sub_3D612(5)`。两个入口xref分别是菜单owner `sub_21496:0x2163B`和战斗医疗动作owner `sub_39EF7:0x3A093`，没有外部xref进入内部基本块。机器实际忽略前两个actor/target slot参数，只消费后两个signed actor/target role id；菜单传`-1,-1`并保存完整EAX，战斗传真实slot并只把DX写入目标combatant damage word。栈探测、RNG helper与两个caller均按独立owner关闭，不随本owner传播closure。

## 完整汇编合同

EAX/EBX先清零；actor signed physical power严格小于50时直接到RET，返回0，不消费RNG且不写任何角色状态。其余路径把actor signed medicine读入ESI；负数只把本地ESI夹0，不回写role word。

目标signed hurt分四个闭区间：`<=25`以32位`4*medicine` signed除5，`26..50`以等价有符号序列计算`3*medicine/4`，`51..75`以`2*medicine` signed除3，`>75`以等价有符号序列除2；除法均向零截断。四路汇合后无条件恰消费一次`sub_3D612(5)`并把结果加到32位EBX，发生在所有threshold、cap和写回之前。

随后重新读取actor原始signed medicine而不是本地非负值。只有目标signed hurt严格大于`raw_medicine+20`才同时清EBX治疗量和ESI减伤量；恰等于不清。机器再按signed32比较`current_hp+EBX`与signed maximum HP；超出时把完整EBX改为`maximum-current`，该返回值可小于-32768。

目标HP word直接加BX低16位并回绕，再signed重读；仅当重读值严格大于maximum时写maximum。目标hurt word减SI低16位并回绕，signed负数才夹0，回绕成正数则保留。最后actor physical power word减2且不夹值；入口门槛使正常路径结果至少48。函数以完整32位EBX返回，因此例如EBX=-65535时EAX保留-65535而AX为1；返回值不保证等于可观察HP差。

## 汇编到现代实现逐块对照

从入口重新覆盖全部103条指令、25个基本块和15条分支/跳转，对照`apply_role_medicine_value`与`BattleSetup::apply_medicine_value`。signed体力门、局部负medicine夹值、四档hurt边界、32位乘除、单次RNG位置、使用原medicine的strict threshold、signed HP cap、低16位回绕写、hurt回绕/负夹值、体力成本、完整32位返回及same-role alias写序均在合法role域逐项一致。

`BattleSetup`只负责把有效combatant slot解析成role id；role级helper对应机器真正消费的两个参数，也覆盖菜单与战斗共同kernel语义。合法域首轮完整入口REVIEW零产品差异，无需修改产品源码。

## 平台适配、回归与验证

原机器接受任意signed role id并可能越界读取/写入；现代两层入口在RNG和写回前安全拒绝非法role，BattleSetup还拒绝机器本体实际忽略的非法slot。合法battle caller总是传真实slot，菜单owner则由role级helper表达；这些差异只替代机器不安全域，归类`platform_adapted`。

新增回归覆盖physical power 49/50、hurt边界25/26/50/51/75/76、strict threshold相等/超出、负medicine和-32768基于原值的threshold、medicine/hurt signed最大值、hurt最小值减法回绕、正常HP cap、当前HP已高于maximum的负返回、`maximum=-32768/current=32767`产生完整EAX=-65535而HP写-32768、same-role alias及非法role不消费RNG。独立机器向量SHA256为`f758d1f4dae67a61868a007a13336a7c94e1c11ed21332904c6f7b26b5b1f472`。

正式Golden由两次独立原资产生成逐字节一致后更新，再以第三次生成核对；文件SHA256为`0d3a179c201f2d65dd29181e2db715c818b57be1f05e3724cf1d338745d08067`，历史59键逐值不变。根`./build.sh app --config Debug`精确通过`100% tests passed, 0 tests failed out of 14`。

原程序动态差分继续登记为`blocked_runtime_oracle`；最终结论为`platform_adapted / converged_no_new_differences`。
