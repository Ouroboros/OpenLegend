# 函数证据：`sub_2D501` `0x2D501..0x2D590`

状态：`implemented_pending_review`

机器码固定连续调用四次 `sub_3D8D8`：依次绘制 `(x,y,width,1)` 上边、`(x,y,1,height)` 左边、`(x+width-1,y,1,height)` 右边、`(x,y+height-1,width,1)` 下边，颜色与目标 framebuffer 原样传递；函数返回最后一次填充结果，不填充或修改内部像素。

唯一直接调用者 `sub_2A186` 是物品选择界面：先对5×3个格子在 `(55+42*column,62+42*row)` 绘制 `40×40`、颜色0的边框并画物品图片，再以颜色255重画当前选中格边框。现代映射为 `IndexedFramebuffer::outline_rectangle`，按相同上/左/右/下顺序复用 `fill_rectangle`；现有 `BasicUiRenderer::draw_box` 实际调用该 primitive 后再执行自身的内部填充，因此既使用映射又保持既有菜单输出。

独立 oracle 以背景色7固定普通色0与选中色255的完整 framebuffer FNV-1a64，并验证 `(56,63)` 内部像素仍为7；render 单测使用同一原调用几何和 hashes。此 closure 只覆盖四边框 helper；`sub_2A186` 整体物品图标、分页与详情仍在 `ui-closure.tsv` 保持 `pending_mapping`，不得随本函数误关闭。
