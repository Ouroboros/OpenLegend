# 函数证据：`sub_2E29C` `0x2E29C..0x2E2D7`

状态：`implemented_pending_review`

opcode16 helper 无条件扫描全部六个队伍槽；任一 signed role ID 等于参数1就把匹配标志置1，但不提前退出，也不因中间非正槽停止。最后严格按标志是否等于1返回 true/false offset；dispatcher 固定推进4 words 后叠加返回值。现代 `party_contains` 同样遍历六槽。

synthetic 向量把槽1设为`-1`、槽5设为55，证明空隙后的最后槽仍命中并执行后续加物品；最终 REVIEW 仍需反查当前资产全部 case16 参数与偏移。
