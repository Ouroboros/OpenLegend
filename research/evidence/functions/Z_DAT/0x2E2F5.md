# 函数证据：`sub_2E2F5` `0x2E2F5..0x2E337`

状态：`implemented_pending_review`

KDEF opcode18。线性扫描200个库存 slot，首个 `item_id==argument1` 即 found 并结束；不读取 count。found 返回 true_offset，否则 false_offset，caller 宽度4。

旧 C++ 用汇总 count>0；现用 `inventory_contains_id`。真实 script37：slot0=(item173,count0) 仍走 true 分支修改事件并 opcode7→stay；ID=-1 才走 false→dialogue139。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
