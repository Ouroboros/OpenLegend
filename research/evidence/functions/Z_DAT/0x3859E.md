# 函数证据：`sub_3859E` `0x3859E..0x3884A`

状态：`implemented_pending_review`

已映射状态边界：`BattleSetup::magic_animation_plan`。

函数按actor role head加载对应FIGHT包，并将magic sound/effect id分别载入sample channel 1/2。actor动作帧数取role frame word `25+magic_type`；FIGHT sprite基址为包帧数加此前magic type各动作帧数的四倍，逐帧sprite为 `int16(2*actor_frames*direction + 2*base + 2*frame)`。effect起点为role frame word `30+magic_type-1`，channel 2在该帧首次分派；channel 1在role frame word `35+magic_type-1`首次分派。effect frame从 `-2 + 2*sum(word_55ACE[0..effect_id))` 开始，进入可见帧前先加2；总帧数为 `role.frame[30+magic_type] + word_55ACE[effect_id] - 1`。每帧严格render→present→wait17，结束后清effect visible。

固定向量magic type2/effect2/fight frames100/direction1：19帧，actor sprite `248..254`，effect从frame2显示 `48..80`，channel2在frame2、channel1在frame4各分派一次，完整hash `0x5aaffbb1d5697a73`。

玩家用毒、解毒、医疗现已分别以magic type0/effect30、36、0实际载入FIGHT包，按动态FIGHT基址8000与EFT基址6500逐帧更新actor/effect，执行render→present→一次BIOS tick变化，并把武功槽0 sample与effect sample分派到attack/effect双bank。真实battle4 Session固定得到11/9/10帧，首帧音频命令分别为`(attack,7)+(effect,30/36/0)`。

玩家攻击四种area type、AI automatic flag1攻击、AI用毒、AI医疗和AI解毒现均实际进入该逐帧链；固定武功type0/effect0得到每击10帧、首帧`(attack,7)+(effect,0)`，每帧present后按参数17等待一次BIOS tick变化，最后一帧后先清effect visible再进入damage。跨百双击会完整重复FIGHT载入、音频和逐帧continuation。AI固定纵向直线向量首帧hash为`0xe1d1b3cff84bc0c4`，mode1移动一格后首帧为`0xacc58834b066ca07`；AI用毒首帧为`0x47286fa4af30fce4`；AI医疗/解毒首帧分别为`0xbec9ef2738ca79b4`/`0xae0f13fbbc4c8083`，移动后医疗为`0x2138cfdf8041c6bb`。AI暗器现通过`sub_3884A`前奏后复用独立AI owner的EFT/damage同步相位；固定直接与移动后首EFT hash分别为`0xc65b523bd75389e2`和`0x16a8f10ce319622b`，双bank音频、逐帧present/tick与最终effect visible清理均被回归锁定。该helper的攻击、用毒、医疗、解毒调用族已全部执行；最终完整汇编↔C++ REVIEW仍待执行。
