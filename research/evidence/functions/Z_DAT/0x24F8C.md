# 函数证据：`sub_24F8C` `0x24F8C..0x2558B`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；32×32世界深度列表重建。

独立汇编恢复：清三个1024-word输出数组；以local X/Y各`center-11 .. center+20`，X外层、Y内层扫描。候选为BUILD owner(X,Y)非零、玩家格或船格。玩家优先注入sprite5000并暂写owner表，船次优先注入6000；普通owner以(X,Y)去重。重复owner若不是当前列表尾项，向同X列上方回扫不同owner关系，必要时把尾项插到已有项位置；sprite id来自BUILDING owner格。任何id按unsigned16大于8292触发原错误等待。末尾记录count并把玩家、船current、船next的临时owner注入清零。

现代映射：`render::build_legacy_world_depth_list`复制owner层后注入两个typed actor，按相同扫描/去重/重排生成entries；输入cache不被修改，因此末尾清理等价。错误id返回typed failure。

真实初始depth、连续移动/重载和玩家存在性有定向测试；原错误阻塞路径为平台适配。最终双向逐基本块REVIEW为`not_started`。