# 函数证据：`sub_2A10F` `0x2A10F..0x2A186`

状态：`implemented_pending_review`

现代映射：`GameMenuController::set_inventory_count`、`LegacyGameRuntime`的200格库存前缀输入，以及`BattleSetup::begin_item_selection`。

函数先把200项临时slot表填为-1，再按库存slot 0..199顺序扫描；只要求item ID非负，不检查数量。过滤参数与item type相等时收录；参数为0时收录全部；另保留type3在过滤参数4时也收录的兼容分支。因此 `sub_3A29C` 的battle调用固定得到type3/type4并集，顺序严格保持原库存slot顺序，数量为0的合法item ID仍列入。

现代battle向量以库存type4、type4、type3(count0)、type2得到三个slot `[0,1,2]`，并由玩家物品Session实际进入5×3选择器；世界/场景物品菜单以参数0保存全部连续库存槽，由外层按物品类型进入装备、修炼、消耗品或事件分支。Linux/Windows `core/app × Debug/Release`及Linux app ASan+UBSan全部通过；当前保持`implemented_pending_review`，最终双向逐基本块REVIEW仍为`not_started`。
