# 函数证据：`sub_35803` `0x35803..0x3582B`

状态：`pending_implementation`

部分映射：`BattleSetup::begin_ai_item_plan`、`resume_ai_item_after_relocation`。

函数先以参数1调用`sub_34AEC`，因此复用逃跑目的格算法但移动后不休息；随后无条件调用`sub_3598C(actor,0)`使用此前AI selector写入的物品槽。真实field2 synthetic向量source `(10,20)`、round value3时重定位到`(7,20)`，敌方曼哈顿和20，use mode为0。party side0从200槽inventory取item，非零side从角色4槽taking-item取item；该handler自身不检查库存数量。

现代已生成side相关item source/slot/id、重定位move步骤及移动后mode0使用continuation，但未实际执行`sub_3650E`逐格移动/render/present/tick，也未执行`sub_3598C`物品效果、动画、库存扣减和外层AI完成continuation，故不得升级为完整实现。Linux Debug 14/14和独立oracle通过。
