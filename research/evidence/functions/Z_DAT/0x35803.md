# 函数证据：`sub_35803` `0x35803..0x3582B`

状态：`implemented_pending_review`

部分映射：`BattleSetup::begin_ai_item_plan`、`resume_ai_item_after_relocation`。

函数先以参数1调用`sub_34AEC`，因此复用逃跑目的格算法但移动后不休息；随后无条件调用`sub_3598C(actor,0)`使用此前AI selector写入的物品槽。真实field2 synthetic向量source `(10,20)`、round value3时重定位到`(7,20)`，敌方曼哈顿和20，use mode为0。party side0从200槽inventory取item，非零side从角色4槽taking-item取item；该handler自身不检查库存数量。

现代按side生成item source/slot/id、重定位move步骤及移动后mode0使用continuation；`BattleSession`实际执行`sub_34AEC`目的格、逐格状态、render/present和两次BIOS tick变化，移动后不休息并恢复mode0使用。随后状态先应用，实际效果面板present并由任意键确认，再等待九次BIOS tick变化后才按来源提交库存/携带槽并完成AI外层。真实battle4固定执行`(26,24)→(26,23)`、round value `0→-1`，效果面板像素hash为`0xa7542240e4172664`，最终RNG状态为`662824084`。Linux Debug完整BUILD全部14项测试通过；最终完整汇编↔C++ REVIEW仍待执行。
