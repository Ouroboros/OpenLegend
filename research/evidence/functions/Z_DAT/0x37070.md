# 函数证据：`sub_37070` `0x37070..0x37166`

状态：`implemented_pending_review`

映射：`BattlePathing::build` 的单节点 flood-fill 步骤。

每次出队；若 y<0，则 distance=`(distance+1)%128`、重新入队 sentinel并再次出队；连续 sentinel 返回-1结束。否则按上、右、左、下扫描，值254才入队并写当前 distance，正常返回0。机器码边界允许坐标等于64；现代保留可落在0..4095的线性别名，仅对线性 index>=4096作安全拒绝，登记为最终 REVIEW 的错误路径差异。
