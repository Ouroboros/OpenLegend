# 函数证据：`sub_23C98` `0x23C98..0x23F28`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；X轴世界移动入口。

独立汇编恢复：按`a5=±1`更新陆地或船方向/动画offset，候选X限制0..479；在任何碰撞提交前以候选(X,currentY)调用`sub_25911`。场景返回后不是提前return，而是继续原调用者：陆地依次探测船双格、步行阻挡；乘船依次探测水域、下船。成功时更新坐标/船双格，调用position/cache提交和天气滚动；失败仍提交方向、动画及船next状态。

现代映射：`WorldSession::move`保存`WorldMoveContinuation{direction,target_x,target_y}`，`resume_move_after_scene -> complete_move`恢复同一X轴后半段；`LegacyGameRuntime`在同步scene返回后反向朝向，保留原`WorldSession`的cache/weather/idle/50步计数/palette，经返回侧present/fade后续跑。审计修正了旧实现scene返回直接停步、跳过碰撞/提交，以及重建world清空瞬态的差异。

向量覆盖scene70候选(356,235)返回后建筑阻挡stay、合成可行入口移动到(358,235)、X边界、陆地/船四分支。最终双向逐基本块REVIEW为`not_started`。