# 函数证据：`sub_35372` `0x35372..0x3540E`

状态：`platform_adapted / converged_no_new_differences`

映射：`openlegend::battle::BattleSetup::choose_ai_nearest_target`。

## 1. 机器身份与调用边界

- 范围`0x35372..0x3540E`，raw file offset `0x2ED72`，共156字节、39条指令、5个显式跳转、12处加载时fixup。
- raw SHA256：`c20a0bad9f0b84b7d6c757dd0f2338f9bf6427e022c3c7644c8a378d06ff17f1`。
- loaded SHA256：`d5a95b43e79f688fad6798458fd71c41bc91e1e00aaa225b380e22a24e8a457c`。
- 12个fixup加载值均相对raw增加`0x20000`；归一化后156字节逐字节等于原始`Z.DAT`切片。
- 两个caller：自动攻击`sub_34C47`在`0x34F59`调用后直接读取actor word11，攻击策略`sub_3505B`在`0x3512F`调用后直接返回；均不读取EAX。
- direct call仅入口栈检查`0x35377 -> sub_3ED1E`和合格候选循环内的`0x353CF -> sub_36E7F` targeting图构建；函数不消费RNG。

## 2. 完整机器合同

1. local best以32位1000初始化，EDI槽从0起；循环尾以signed 16位`slot < combatant_count`扫描全部槽。
2. 候选必须side不同于actor且hidden严格等于0；不检查HP。
3. 对每个合格候选，先把actor signed x/y写入全局source坐标，再调用一次`sub_36E7F`重建targeting图。source在函数内不变，但原版仍按合格候选次数重复构建。
4. 随后按candidate signed y乘128字节、signed x乘2字节读取word path map，即signed `path[candidate_y*64+candidate_x]`。
5. 仅signed `candidate_distance < best`时写actor word11并更新best低16位；strict比较使同距保留早槽。
6. best初值1000，所以普通距离、未访问值254以及blocked值555都可能写目标；仅等于或大于1000的signed值不写。无合格候选时保留调用前word11。
7. 两个caller都只依赖word11，不依赖返回寄存器；该函数无独立布尔返回合同。

## 3. 汇编→C++逐块对照

- 现代`choose_ai_nearest_target`按槽序应用相同side/hidden过滤，以signed `int16_t best_distance=1000`和strict `<`写actor `ai_target`；不读HP且不消费RNG。
- 现代`BattlePathing`对同一actor source只构建一次确定性targeting图；原版每个合格候选重建一次，但每次source、upper layer和构图规则相同，所有候选读取值及最后保留的图均相同。此调用次数差异不引入RNG或可观察宿主时序。
- targeting图只以upper layer阻挡；blocked常量为555，仍小于best1000。固定battle3原资产格`(23,9)`upper layer非零，唯一可见敌人位于该格时仍被选中。
- 固定边界回归：真实距离`[6,8]`选slot3；两个敌人同坐标同距且早槽HP0仍选slot3；slot3 hidden为`-1`时跳过并选slot4；两敌人hidden为`1/-1`时不写并保留stale slot4；blocked555写slot3。
- 现代越界坐标由`legacy_index`安全映射为blocked555，替代原版线性越界读取；正常0..63坐标域未发现行为差异，因此归类`platform_adapted`。
- 差异审计后从入口重新覆盖全部39条指令、5个跳转、12个fixup、两个caller及两个call，零新增差异。

## 4. 验证与结论

- 独立机器审计：`proc_25f5`，156/39/5/12身份、两个caller、栈检查和逐候选targeting call全部通过。
- 原始只读资产双生成：`proc_a56f`与`proc_fd1a`逐字节一致；正式battle golden SHA256为`83376071c3aa86e8fcae0a8221bae7b64cbe36bd33abdad12dc99211a65b31f1`。
- Linux app Debug根BUILD：`proc_c8b9`，14/14通过，包含新增battle边界回归与reverse framework。
- 本owner结论：`platform_adapted / converged_no_new_differences`。两个caller及`sub_36E7F`各保持独立owner状态，不由本函数传播关闭。
