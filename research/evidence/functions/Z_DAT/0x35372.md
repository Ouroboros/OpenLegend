# 函数证据：`sub_35372` `0x35372..0x3540E`

状态：`implemented_pending_review`

映射：`BattleSetup::choose_ai_nearest_target`。

以actor坐标建立targeting距离图，按combatant槽序检查不同side且未隐藏目标，以signed best=1000起步；距离严格更小时写actor target word11，因此同距保留早槽。targeting图只受upper layer阻挡，不看occupancy和ground九段。真实field2 synthetic目标距离`[6,8]`选择slot3，且函数不消费RNG。Linux Debug 14/14和独立oracle通过；最终仍需完整汇编↔C++ REVIEW。
