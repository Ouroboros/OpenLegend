# 函数证据：`sub_343DA` `0x343DA..0x34550`

状态：`implemented_pending_review`

映射：`BattleSetup::choose_ai_detox_target`。

按combatant槽序扫描同side、非自身、未隐藏目标，先要求actor解毒能力严格大于target中毒值减30。target动作已为请求解毒9时立即选择；否则依次判断中毒值>10、>20、>30、>40，前三档分别消费一次`bounded(10)`并要求结果<4、<6、<8，最后>40无RNG直接选择。各分支严格短路，首个命中写actor动作4及目标slot/坐标。

固定中毒35、seed1消费`[8,8,3]`后选择slot1，终态662824084。Linux Debug 14/14和独立oracle通过；最终仍需完整汇编↔C++ REVIEW。
