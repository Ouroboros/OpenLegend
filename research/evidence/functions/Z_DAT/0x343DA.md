# 函数证据：`sub_343DA` `0x343DA..0x34550`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::choose_ai_detox_target`。

当前原始`Z.DAT`物理范围为374 bytes、88条连续指令、14条分支、22处重定位和4个direct call；raw SHA256为`08aca9ac49ae4fa8e78aea453a2adb9551f19cada9c9f98915469b5931df52ed`，IDA加载后SHA256为`9a5dd17949445c233ddc9d97345902759a003e85545569420aaee0acae10dc3e`。唯一caller为AI入口解毒目标门命中后的调用点；四个call依次为栈探测和三个独立RNG调用点。随机实现为独立owner，本函数只关闭调用次序与短路消费。

函数按signed combatant槽序扫描，跳过actor自身、不同side与hidden非0目标。每个候选先要求actor解毒能力严格大于candidate中毒值减30；两值先符号扩展至32位，减30不发生16位回绕。能力门在请求动作之前，请求不能绕过该门。候选已有请求解毒动作9时直接选择且不消费RNG。

仍未选择时依次判断中毒值严格大于10、20和30；只有对应条件成立才消费一次`bounded(10)`，输出分别严格小于4、6、8时选择，选中后不消费后续RNG。前三档均失败后，中毒值严格大于40时确定选择，不再消费RNG；等于40不命中。首个命中保存目标坐标与slot，给actor写动作4并返回4；全部未命中返回0，不写actor动作，也不清理机器中此前残留的目标共享状态。现代typed返回用空目标表达该不可用状态；越界actor、role或count安全失败属于合法域外平台适配。

定向回归覆盖：actor自身/hidden/异side中毒50均跳过；解毒能力20对中毒50的等号门拒绝；请求动作9免RNG命中；中毒10不消费；中毒11用seed9输出2首档命中；中毒20用seed1输出8后停止；中毒21用seed6消费`[6,5]`在第二档命中；actor解毒能力1且中毒30用seed1消费`[8,8]`后停止，确保独立隔离30等号阈值；中毒35用seed1消费`[8,8,3]`在第三档命中；中毒40用seed331消费`[9,9,9]`后不命中，中毒41以相同三次输出后由确定门命中。对应终态为1341714958、1103527590、1672197364、2524885223、662824084和3382126054。正式battle golden由独立Python两次生成逐字节一致，只新增十项解毒目标边界，SHA256为`d69ad12af9ef846af0835e4b816b4967240b601cacf1c2ce06028109ed163555`。

首次完整机器推导后逐块对照未发现产品实现差异，只发现过滤、strict能力门、四个阈值等号和RNG短路覆盖不足。首次根构建揭示中毒30夹具因默认解毒能力0未通过前置strict门；将actor能力设为1并同步双生成golden后，根构建14/14通过。最终再次从入口覆盖全部88条指令、14分支、22重定位、唯一caller、三处RNG调用、全部请求/概率/确定门和两个返回，零新增差异或未决项。机器门、静态/golden门、closure门、逆向框架及根`./build.sh app --config Debug`均通过，归类`platform_adapted / converged_no_new_differences`。
