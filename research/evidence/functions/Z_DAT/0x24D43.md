# 函数证据：`sub_24D43` `0x24D43..0x24DB8`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；`research/ida/reports/Z_DAT.b6_world_xrefs.txt`；当前原版`EARTH.002`。

## 1. 独立机器恢复

- 物理范围`0x24D43..0x24DB8`，117 bytes、39条指令、3个基本块；唯一caller为总加载`sub_24CE8`。
- 栈检查为28 bytes；EDI=origin X、ESI=origin Y、EBX从0开始，每轮加1并以有符号`<128`回跳，恰好处理row 0..127。
- 每行先以EARTH句柄`word_C0BF0`执行绝对seek：`960*(origin_y+row)+2*origin_x` bytes；960来自480个int16的完整世界行宽。
- 随后把全局读长写为256并从同一句柄读256 bytes到`word_7FE2C + row*256`，即EARTH缓存第row行128个int16。
- 返回值是row127的read结果；总入口随后调用BUILDING loader并覆盖该值。除文件游标、读长和目标缓存外无状态写入，不消费RNG。

## 2. 汇编 → C++ 与适配

- `WorldCache::reload`的`WorldLayer::earth`轮次用源索引`(origin_y+row)*480+origin_x`和目标索引`row*128`执行`copy_n(...,128)`，与机器偏移和256-byte行宽同构。
- `WorldMapData`已把`EARTH.002` little-endian words预载为只读向量，因此seek/read改为内存复制；合法origin下值与布局不变。
- 现代统一预验证地图和origin，见总入口`0x24CE8.md`；这是机器合法调用域外的安全适配。

## 3. 验证与结论

- origin `(293,171)`、`(0,0)`和`(352,352)`三份16384-word EARTH缓存完整FNV1a64分别锁定初始、左上和右下最大边界；后两份独立oracle来自`proc_087a`。
- Linux app Debug：14/14通过，受管进程`proc_c005`。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
