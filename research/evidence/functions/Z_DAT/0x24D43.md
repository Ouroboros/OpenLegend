# 函数证据：`sub_24D43` `0x24D43..0x24DB8`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；EARTH cache loader。

独立汇编恢复：EBX行号0..127；每行seek偏移`960*(origin_y+row)+2*origin_x`，设置读长256字节，读入EARTH cache的`128*row`位置；循环恰好128次。

现代映射：`WorldCache::reload`对`WorldLayer::earth`执行128次`copy_n`，源索引`(origin_y+row)*480+origin_x`，目标索引`row*128`。预加载int16le后与原256字节读完全同构。

真实初始cache hash与独立oracle一致。最终双向逐基本块REVIEW为`not_started`。