# 函数证据：`sub_24667` `0x24667..0x246F9`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；陆地阻挡探测。

独立汇编恢复：候选moved coordinate必须严格在11..458；BUILDING、BUILDX、BUILDY均为0；EARTH不得位于八段封锁区间358..362、374..380、458..464、506..670、818..824、838、934..936、1016..1022。全部条件通过才返回可移动。

现代映射：`WorldSession::target_is_walkable`与`kBlockedWalkingRanges`/tail；使用相同inclusive比较且保留单点838。scene入口探测先于本函数，因此入口格即使建筑阻挡也先同步进入scene，返回continuation后才由本函数决定stay。

向量覆盖各区间端点、building owner层和scene70真实阻挡。最终双向逐基本块REVIEW为`not_started`。