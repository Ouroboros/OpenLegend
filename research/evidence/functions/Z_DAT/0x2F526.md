# 函数证据：`sub_2F526` `0x2F526..0x2F62F`

状态：`implemented_pending_review`

KDEF opcode34。角色 IQ 做 int16 加法后 clamp `[0,100]`；仅最终值比旧值大时显示实际增量 notice。C++ case34 使用 `clamped_add` 和 positive-delta notice。真实 script673 opcode34(0,3)：role0 IQ99 结果100，不越界到102。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
