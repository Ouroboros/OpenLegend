# 函数证据：`sub_2F526` `0x2F526..0x2F62F`

状态：`platform_adapted`；最终 REVIEW：`converged_no_new_differences`

来源：当前 `Z.DAT` 机器码与原始文件字节；headless IDA完整导出；当前 `KDEF.IDX/GRP`、`RANGER.GRP`；`research/ida/reports/Z_DAT.b7_scene_xrefs.txt`。

## 1. 机器身份与caller

- 函数物理范围 `0x2F526..0x2F62F`，265 bytes，67条指令。
- loaded SHA256 `de9e5e2a9c5e490ac7b4de1c3503e1f768ce6ed20d726db3d9901ee60c75cc08`；原始文件 `Z.DAT[0x28F26:0x2902F]` SHA256 `57b5bf86312f9e011a1c6db794a0e486b2d9af1a550a00947f9347ac0000c0ad`。
- 15个差异字节全部是角色资质、格式串、文本缓冲区、framebuffer和呈现状态地址raw加加载基址 `0x20000` 的重定位；规范化后与loaded逐字节相同，未解释差异为零。
- 唯一物理代码caller是 `sub_2C319:0x2C879..0x2C895` 的opcode34；`0x55644`只是函数地址表数据引用。
- caller把两个KDEF word sign-extend为 `(role_id,delta)`，公共尾部清理8字节栈并把PC固定增加3 words；返回值不使用。
- 入口 `sub_3ED1E(40)` 仅为Watcom栈探测合同。

## 2. 不看C++的资质写入恢复

1. 以 `role_id*182` 定位角色记录，sign-extend并保存word60资质原值。
2. 对该word执行低16位add；结果先按signed `>100`钳到100，再按signed `<0`钳到0。
3. sign-extend最终值并减去原signed值，得到32位实际gain。
4. gain小于等于0时直接返回0，不显示提示，也不重绘场景。
5. gain严格大于0时显示实际gain；因此提示数字不必等于传入delta。例如原值99、delta3最终提示1；原值-10、delta0最终提示10。
6. 可见与不可见两路均固定返回0。

## 3. 原Big5资质提示与时序

可见路径使用含末尾NUL的15字节原格式串 `25 73 20 B8 EA BD E8 BC 57 A5 5B 20 25 64 00`，即 `%s 資質增加 %d`。机器从角色记录byte8开始按C串读取名字并格式化实际gain。

令角色名字节数为N、`A=N+10`，机器绘制：

- 圆角混色面板 `(150-(4A+24),40,8A+68,27)`；
- 文字位置 `(160-(4A+24),45)`，颜色5/7；
- A不包含末尾十进制gain的位数。

面板直接叠加在caller已有framebuffer上，随后present、等待任意非零翻译键，再重绘并present裸场景后返回。委托panel、Big5文字、present、按键和裸场景绘制closure不由本行传播关闭。

## 4. 首轮差异与从入口重审

首轮汇编→C++ REVIEW确认资质word回绕、signed钳位、actual-gain条件和PC一致，发现同一UI链两项差异：

- 原C++只构造ASCII `role <id> +<gain>`，没有使用角色名和原Big5 `資質增加`；
- 原默认notice使用固定黑色矩形、颜色21/23并重绘scene底图，机器使用按角色名字节长度变化的混色圆角面板、颜色5/7和冻结caller framebuffer。

最小修正仅给opcode34新增专用资质notice：拼接角色原名字节、固定Big5中缀和实际gain，按机器公式绘制并复用冻结底图运输；case45/47现有提示不变。随后废弃首轮结论，从入口重新逐条复核67条指令、15项重定位、word60 add/clamp、signed gain、无提示分支、格式串、角色C串、十进制实际gain、名称长度几何、颜色、present/等待/恢复、返回0和caller PC，零新增差异。

机器对非法role索引和损坏无NUL名字执行越界访问；现代仅在该资产未使用域内做记录边界保护。宿主以非阻塞notice/response运输替代函数内同步等待，归类 `platform_adapted`。

## 5. 全KDEF、文本与像素oracle

当前1,018条KDEF只有4次opcode34。按little-endian `<IIhh>` 编码 `(script,PC,role_id,delta)` 的完整参数流SHA256为 `94d52336ff60b9eea2fa7387558ce60cdf63e656efe40d17b0616a27aa0a73ee`：

- 位置为 `(284,179,35,10)`、`(284,185,0,5)`、`(484,185,53,20)`、`(673,74,0,3)`；全部role合法，delta为3/5/10/20。
- 按当前RANGER基线分别单次执行，固定原文与像素为 `令狐沖 資質增加 10`、`胡斐 資質增加 5`、`段譽 資質增加 20`、`胡斐 資質增加 3`。
- 合成边界固定：99+3钳100并提示1；32766+3回绕负后钳0且无提示；-10+0钳0并提示10；-32768-1回绕正后钳100并提示32868；32767+1回绕负后钳0且无提示。
- 三种可见合成帧FNV-1a64分别为 `0x6e7fe3eb3d0961b7`、`0x4e4523ee5f7afda0`、`0x225ecf10614f7cb8`，串联SHA256 `a0386afac0565c3002f5b51d53f9a9b4325ddc4681e50d2458a5cefb8603b89d`；角色0共同面板 `(62,40,196,27)`。
- 重复render固定同帧且不消费RNG；确认后固定scene present。

双次独立生成字节一致；正式 `scene-goldens.json` SHA256 `5ce9fa25278dc076220db7bc3ec764574c79f6490f0fa26dee27cec94e8464bb`。

## 6. 验证与结论

- Linux app Debug根构建：14/14 tests通过（`proc_2ea7`）。
- 原程序动态差分：`blocked_runtime_oracle`。

```text
closure_status = platform_adapted
final_review = converged_no_new_differences
remaining =
```
