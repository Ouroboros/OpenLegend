# 函数证据：`sub_2F526` `0x2F526..0x2F62F`

状态：`implemented_pending_review`

KDEF opcode34。角色 IQ 先做 16-bit `add word` 回绕，再以 signed 值 clamp `[0,100]`；仅最终值比旧值大时显示实际增量 notice。C++ case34 使用显式 uint16→int16 回绕的 `clamped_add` 和 positive-delta notice。真实 script673 opcode34(0,3) 固定 IQ99→100，以及 IQ32766+3 回绕为负后 clamp0 的反例；独立 B7 oracle 同步计算两向量。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
