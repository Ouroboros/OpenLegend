# 函数证据：`sub_23B3E` `0x23B3E..0x23C98`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码。

独立汇编恢复：天气active时三个x各加1；仅当三者均`>500`才同tick清active并继续生成。inactive路径先调用`bounded(1)`（固定返回0且不消费RNG），随后按批次生成3个kind=`bounded(4)`、3个speed=`bounded(3)+6`、3个x=`bounded(100)-300`；最后每粒子先`bounded(2)`，非零写`-3000/-4000/-5000`，零则再消费`bounded(50)`并加0/75/150。

现代映射：`WorldSession::update_weather`/`periodic_tick`，循环按机器批次而非按粒子交错，保留无效上界不消费状态和同tick重启。scene同步返回后在保留的weather状态上恢复该原外层调用，不因重建world额外生成天气；后续医疗/解毒序列保持HP80、poison59及原整帧hash。

seed1、300 tick粒子状态和天气framebuffer由独立oracle固定。最终双向逐基本块REVIEW为`not_started`。