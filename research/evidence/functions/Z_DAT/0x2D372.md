# 函数证据：`sub_2D372` `0x2D372..0x2D501`

状态：`implemented_pending_review`

该函数只用十二段 `sub_3D8D8` 颜色255矩形画圆角白边，不执行底色混合。`sub_2CC21` 在 HDGRP sprite 已绘制后调用，因此顺序为“混色底 → 头像 → 白边”。

现代 `SceneSession::draw_panel_border` 保持相同十二段坐标，并在 `draw_portrait` 后调用。独立 oracle 的 style0/1/4 framebuffer hashes 固定该叠放次序。最终双向 REVIEW 尚未执行。
