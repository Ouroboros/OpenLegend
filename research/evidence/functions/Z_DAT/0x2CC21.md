# 函数证据：`sub_2CC21` `0x2CC21..0x2CEBF`

状态：`implemented_pending_review`

机器码按 talk ID 从 `TALK.IDX/GRP` 读取仍为 XOR `0xFF` 的正文，并按 style 选择固定布局：0/2 为正文 `(94,17)`，1/3 为 `(8,130)`，4 为 `(8,17)`，5 为 `(94,130)`；style2/3 不画头像，其余分别在 `(23,12)`、`(237,125)`、`(237,12)`、`(23,125)` 绘制 `60×62` 头像框。正文框固定 `218×57`，文字入口为框坐标 `(+13,+3)`。

每次 `sub_3D950` 返回本页已消费字节；随后固定呈现并等待按键。返回非零时先 `sub_29D2D` 重绘裸场景，再重画框、头像并继续下一页；返回零时结束，清最后键状态。现代 `SceneSession::queue_dialogue/draw_overlay` 与 `paginate_dialogue` 保留同一同步页边界、style 布局、重绘和最终空页行为。

独立 oracle 使用 scene70、真实 TALK/KDEF、MMAP.COL、FONT 与 HDGRP 固定 style0/1/2/4 帧；style4 使用 script142 opcode25 后的真实 view origin `(33,14)`，script515 的344像素超长行另固定线性 framebuffer 帧。最终双向 REVIEW 尚未执行。
