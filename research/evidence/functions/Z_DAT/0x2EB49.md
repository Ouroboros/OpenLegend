# 函数证据：`sub_2EB49` `0x2EB49..0x2ED7D`

状态：`implemented_pending_review`

opcode24 先把 framebuffer 清为 palette index0并执行从黑淡入，再进入四项 modal 菜单。正式 IDA raw bytes 解码为“載入進度一”“載入進度二”“載入進度三”“離開睡覺去”；主面板为 `(109,40,101,90)`，文字位于 `(119,45+20*i)`，普通颜色 word `0x2321`、选择颜色 `0x6663`。下键 `0x98` 为3→0，否则加1；上键 `0x9E` 在0时跳到2而不是3，这是载入菜单独有原 BUG。Enter `0x0D`、Space `0x20`、键码 `0x96` 都确认。

选择0..2时写槽位、执行淡出并交给载入链；选择3时叠加 `(71,140,177,31)` 确认框与 `(75,145)` 的“真要離開遊戲（Ｙ／Ｎ）”。只有大写 `Y` 执行终止链，任意其他键回主菜单；原 framebuffer 不清除，因此确认框像素继续留在后续菜单帧。现代 `LoadMenuState`、`load_menu_framebuffer_` 与 app 原始键路由保持这些同步边界，并与死亡菜单的0→3上键回绕分离。

独立 oracle hashes：黑帧 `0xdd14fcc6528cab25`、selection0 `0x65c8da776eac540f`、0→2 `0x816757297e6cb14b`、selection3 `0x726a3cccc0e9c1d2`、确认及非Y返回 `0xb908953a6570b8a0`。原资产无 opcode24，故使用只在测试临时目录生成的1,018条 synthetic KDEF 索引执行整链；最终双向 REVIEW 尚未执行。
