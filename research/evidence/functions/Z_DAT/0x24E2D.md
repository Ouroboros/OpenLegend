# 函数证据：`sub_24E2D` `0x24E2D..0x24EA2`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；SURFACE cache loader。

独立汇编恢复：固定128行，每行从SURFACE句柄按`960*(origin_y+row)+2*origin_x` seek并读取256字节到SURFACE cache行。

现代映射：`WorldCache::reload`的`WorldLayer::surface`复制，机器顺序位于BUILDING之后。原枚举顺序差异已修正，层索引本身仍保持earth/surface/building以供render语义访问。

真实cache hash与oracle覆盖。最终双向逐基本块REVIEW为`not_started`。