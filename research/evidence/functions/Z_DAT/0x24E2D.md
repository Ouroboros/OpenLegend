# 函数证据：`sub_24E2D` `0x24E2D..0x24EA2`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；`research/ida/reports/Z_DAT.b6_world_xrefs.txt`；当前原版`SURFACE.002`。

## 1. 独立机器恢复

- 物理范围`0x24E2D..0x24EA2`，117 bytes、39条指令、3个基本块；唯一caller为总加载`sub_24CE8`。
- EDI/ESI保存origin X/Y，EBX严格遍历0..127；每行以SURFACE句柄`word_C0BEC`绝对seek到`960*(origin_y+row)+2*origin_x`。
- 每次读256 bytes到`word_7300C + row*256`，恰为SURFACE缓存一行128个int16；row127结果返回后由BUILDX调用覆盖。
- 无分支、提前退出、RNG或游戏状态写入。

## 2. 汇编 → C++ 与适配

- `WorldCache::reload`显式在BUILDING之后处理`WorldLayer::surface`，保持机器加载顺序；`WorldLayer`枚举仍按earth/surface/building保存语义索引，不发生层交换。
- 现代源索引、目标索引和128-word长度分别等价于机器960-byte世界行、256-byte缓存行和row目标；预载与输入检查属于平台适配。

## 3. 验证与结论

- origin `(293,171)`、`(0,0)`、`(352,352)`三份SURFACE完整缓存哈希逐项命中；两端独立oracle `proc_087a`锁定最大源坐标479及BUILDING→SURFACE次序下的层归属。
- Linux app Debug：14/14通过，受管进程`proc_c005`。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
