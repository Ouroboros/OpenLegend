# 函数证据：`sub_395EC` `0x395EC..0x39776`

状态：`implemented_pending_review`

映射：`BattleSetup::apply_mp_damage`。

等级下标严格使用攻击者对应武功熟练度 unsigned `/100`。调用顺序为两次`bounded(3)`形成攻击者内力随机差，攻击者当前内力先加 magic add_mp[level]；最大内力再加 `bounded(add_mp/2)` 并在 signed值>=999时写999；当前内力加随机差后若>=最大内力则夹到最大值。随后再调用两次`bounded(3)`形成目标随机差，目标内力依次减 magic hurt_mp[level] 和该差值，signed值<=0时清0，返回攻击前后目标内力之差。

`LegacyRandom::bounded` 对上界<=1不消费状态，因此 add_mp/2过小时第三次调用仍保留原序列。固定向量 seed1、bounds `[3,3,10,3,3]`、outputs `[2,1,3,1,1]`、state4182499122；add_mp20、hurt_mp15下攻击者 mp/max_mp 由10/20变23/23，目标50→35，返回15。Linux Debug 14/14。坏slot/role安全拒绝；最终仍需完整汇编↔C++ REVIEW。
