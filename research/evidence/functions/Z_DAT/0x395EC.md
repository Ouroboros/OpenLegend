# 函数证据：`sub_395EC` `0x395EC..0x39776`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::apply_mp_damage`。

## 机器身份与边界

fresh临时IDB完整导出确认本体为394字节、96条连续指令、3个显式条件跳转、22处加载时fixup、6次direct call和一个本地RET。raw SHA256为`ce826212a2e10ca85304d3fc172700a76764ea80f571dfa9dbcd01f604007cc1`，IDA loaded SHA256为`df416c290c6f739366caa15baf2e1a5696f197aa875e740fcc4fbcd9d66a7eb0`；22个绝对地址均为raw dword加`0x20000`，逐项反转后394字节与原始`Z.DAT`完全一致。

调用点为`0x395F1→sub_3ED1E`栈探测，以及`0x3963A/0x39646/0x39693/0x396E8/0x396F4→sub_3D612`五次RNG。函数在`0x39775`本地RET，不进入共享尾。

唯一caller是方形area hurt-type1分支`0x37B76`。它先写机器effect kind3，再依次压入distance、magic slot、target role、attacker role、target combatant和actor combatant，调用后回收24字节并把返回AX写目标combatant word9。本体只读取attacker role、target role和magic slot；两个combatant参数及distance完全不读。

## 完整汇编合同

入口先保存signed目标当前MP。攻击者武功熟练度按unsigned16 `/100`得到level；合法十层资产域为0..9。随后无条件调用两次`bounded(3)`，保存first-second为攻击者随机差。

攻击者当前MP先以16位加选中level的`add_mp`。同一`add_mp`按signed向零除2作为第三次RNG上界；该helper调用无条件发生，但已独立关闭的原版RNG在上界`<=1`或`>30000`时返回0且不推进状态。返回值以16位加到最大MP；signed结果`>=999`写999，负回绕值不封顶。当前MP再以16位加先前随机差，signed current严格小于maximum时保留，否则写maximum；没有下限夹取。

之后无条件调用第四、第五次`bounded(3)`形成目标随机差。函数重读attacker role、magic id及保存的level，目标当前MP以两次16位写依次减`hurt_mp[level]`和first-second；signed结果`<=0`清0，仅严格正值保留。返回值是`signed目标MP旧值 - signed目标MP最终值`的32位差；caller只保存低16位。

## 汇编→C++ REVIEW

机器合同冻结后才读取现代实现。`attack_profile`在合法caller域解析出相同attacker role、magic id、unsigned level和magic slot；target combatant也解析出机器caller传入的同一target role。现代五次`LegacyRandom::bounded`调用顺序、第三次调用但条件消费、add_mp/hurt_mp字段、所有16位回绕、maximum signed `>=999`、current signed `>=maximum`、target signed `<=0`及32位返回均逐基本块一致。

现代把目标两次16位减法写成一次`wrapping_i16(target-hurt-variance)`；机器在两次写之间无分支或观察，因此最终模65536值完全相同。新增actor/target共享同一role的别名向量进一步确认：目标旧MP在攻击者增长前保存，目标扣减读取增长后的同一记录，返回2，与机器一致。唯一caller继续写低16位damage并发布kind3，不修改HP cost-scale scratch。

首轮完整入口REVIEW没有发现产品代码差异。补齐边界回归和Golden后，从`0x395EC`重审至`0x39775`，再次覆盖全部96条指令、3个跳转、22处fixup、6个call、唯一caller、五次RNG调用/消费路径、全部状态写入和返回，仍无新增差异。现代对非法combatant、role、magic、slot及level索引安全拒绝，替代机器线性越界访问，归类平台适配。

## 回归、Golden与结论

聚焦回归覆盖seed1正常bounds `[3,3,10,3,3]`与输出`[2,1,3,1,1]`、actor MP/max `10/20→23/23`、target `50→35`、返回15；新增add_mp0/3不推进第三次RNG、add_mp4推进、负add_mp、maximum999封顶、current/max MP回绕、target恰零/低于零、seed2目标随机差、正负target回绕、actor/target role别名及非法参数不消费RNG。根`./build.sh app --config Debug`由Clang 23完成并精确通过`100% tests passed, 0 tests failed out of 14`。

正式battle Golden新增`battle_mp_damage_machine`，记录本体identity、22处重定位、6次call、唯一caller、本地RET、六个机器切片和独立公式向量；两次临时生成逐字节一致，第三次正式生成与两份临时文件一致。独立向量集SHA256为`b3eaaa9b520a536c2a1f2d00956a2bbb33c83c9b748e5dcc542889d99ac4c67f`，正式Golden SHA256为`fb4d4ec7a49114ca5361904e3f5fff1bff2f32371ba675b85f22709184f1a636`。

因此本battle owner最终归类`platform_adapted / converged_no_new_differences`；不传播给`sub_3ED1E`、`sub_3D612`或方形area caller `sub_37734`。原程序动态差分仍登记为`blocked_runtime_oracle`。
