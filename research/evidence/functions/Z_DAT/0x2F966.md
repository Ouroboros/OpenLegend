# 函数证据：`sub_2F966` `0x2F966..0x2F9B5`

状态：`implemented_pending_review`

KDEF opcode42。机器码固定遍历六个队伍槽；槽值 signed `>=0` 时读取对应角色 record word14，任一值等于1即返回 true offset，否则返回 false offset。C++ `SceneSession::run_event(case42)` 保留六槽、`>=0` 和 `sexual==1` 条件。

当前 KDEF 中 opcode42 共2处。真实 script445 已固定无女性到 TALK1575、女性队员存在且尾槽为空到 TALK1574 的两分支；独立 B7 oracle 同步输出该向量。角色上界保护只覆盖当前脚本不可达的损坏 ID。Linux Debug `openlegend.scene` 通过；最终双向 REVIEW 尚未执行。
