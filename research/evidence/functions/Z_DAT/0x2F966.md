# 函数证据：`sub_2F966` `0x2F966..0x2F9B5`

状态：`platform_adapted`；最终 REVIEW：`converged_no_new_differences`

来源：当前 `Z.DAT` 机器码和原始文件字节；headless IDA完整导出；当前 `KDEF.IDX/GRP`；队伍头记录与角色182-byte记录布局。

## 1. 机器身份与caller

- 函数物理范围 `0x2F966..0x2F9B5`，79 bytes，24条指令。
- loaded SHA256 `2c10b674c8bf3ab2d00dfbd220fdf28d0deaf830f361629430ff9065d3d78c69`；原始文件 `Z.DAT[0x29366:0x293B5]` SHA256 `8b5596e0e9c33a8ceda55589320d757cb0a2f04264513aed06abc1b26e765e82`。
- 三个差异字节分别是队伍槽基址两次、角色sexual字段基址一次raw加加载基址 `0x20000` 的三项重定位；规范化后与loaded逐字节相同，未解释差异为零。
- 唯一物理代码caller是 `sub_2C319:0x2C971..0x2C98B` 的opcode42；`0x55664`只是函数地址表数据引用。
- caller先压false offset、再压true offset；callee返回所选signed offset后，共享块清理8字节栈，先把PC增加3 words，再叠加返回offset，即 `old_PC + 3 + returned_offset`。
- 入口 `sub_3ED1E(8)` 仅为Watcom栈探测合同。

## 2. 不看C++的女性队员合同

1. 命中标志初始为0；固定按slot0到slot5遍历全部六个队伍槽，不因命中或空槽提前终止。
2. 槽值按signed word判断；小于0时只跳过该槽。
3. 槽值大于等于0时，以该ID乘182定位角色记录，读取word14 `sexual`。
4. 仅 `sexual == 1` 把命中标志设为1；0、负数、2或其它值均不命中。标志一旦置1不会被后续槽清除，但循环仍继续。
5. 最终标志精确等于1则返回第一个参数true offset，否则返回第二个参数false offset。
6. 不修改队伍或角色状态，不present、不等待输入、不播放音频。

## 3. 汇编→C++ REVIEW

C++ case42固定遍历 `kTeamMemberCount=6`，对signed负槽跳过，对每个合法role读取同一sexual word并仅在值等于1时置present；代码没有break，合法角色域的全部六槽扫描、持久命中标志和offset选择均与机器一致。本轮没有产品代码差异。

机器对非负但超出角色表的ID直接越界读取；现代增加role上界检查，归类 `platform_adapted`。修正合成测试构造后，从入口重新逐条复核24条指令、三项重定位、六槽无短路循环、signed空槽判断、角色乘182寻址、sexual精确比较、两个返回和caller PC组合，零新增差异。

## 4. 全KDEF与独立oracle

当前1,018条KDEF共有2次opcode42：script444 PC158与script445 PC24，参数均为true offset6、false offset0。按little-endian `<IIhh>` 编码 `(script,PC,true_offset,false_offset)` 的完整参数流SHA256为 `0a3894fb1e8ff92f7c8e0dbc15a3e73b601f522f076b9ca4179822c6fd9e2e96`。

- 真实script445固定无女性到TALK1575、女性存在且slot5为空到TALK1574。
- 合成六槽全空固定false；仅slot5角色sexual1固定true，证明空洞不终止；slot5 sexual2固定false；slot0 sexual1固定true。
- 合成slot0女性且slot5为越界角色32767固定现代继续扫描并稳定保持true，覆盖损坏ID保护。
- 双次独立生成字节一致；正式 `scene-goldens.json` SHA256 `1e20c57c567227b750e7b309a9cf45de48d2463884b7fc4f124d011bbd81683a`。

## 5. 验证与结论

- Linux app Debug根构建：首次因合成script75跳过IDX写入使后续脚本错位而失败；修正构造器后重跑14/14通过（`proc_ebb4`）。
- 原程序动态差分：`blocked_runtime_oracle`。

```text
closure_status = platform_adapted
final_review = converged_no_new_differences
remaining =
```
