# 函数证据：`sub_3A30B` `0x3A30B..0x3A8A4`

状态：`pending_implementation`

已映射状态边界：`BattleSetup::throwing_weapon_targeting_range`、`BattleSetup::apply_throwing_weapon_target`。

目标射程为actor signed `hidden_weapon/15+1`。确认目标后按与状态动作相同的绝对差规则更新方向并清effect；友军格完全跳过，空格只写effect1，敌方格写effect1并使用当前选择库存槽的暗器。原坐标判断重复检查x<64而漏掉y<64；现代实现安全拒绝y越界，登记待最终REVIEW。

命中敌方时先取item `hidden_weapon_effect_id`。HP增量按target hurt分档：hurt等于0取item `add_hp/4`，1..33取`/3`，34..66取`/2`，大于66取完整值；随后减一次`bounded(5)`，再计算signed `(分档值-2*actor.hidden_weapon)/3`。该负增量加到target HP并夹0..maximum HP；hurt按`hurt-增量/4`夹0..99，显示值为新旧HP差绝对值。

item `add_poison`为正时不再消费RNG：先算`(add_poison-actor.hidden_weapon)/2-target.anti_poison`，target抗毒不少于100或结果为负则归零，再除2并加到poison。item `add_poison`非正时严格再消费两次`bounded(5)`，以`add_poison/2+first-second`修改poison；最终夹0..99。真实带毒暗器向量得到伤害21、hurt40→45、poison10→12，只消费一次RNG；真实无毒暗器仍消费三次RNG并得到伤害16、hurt0→4、poison10→8。

命中后执行正常闪烁的damage kind0动画，库存数量按int16减1；结果不大于0时调用共享压缩函数，把后续199-slot全部左移并把尾槽写`(-1,0)`。actor只写`action_done=1`，不增加攻击计数也不扣体力；所有非取消目标路径最后重算全体sprite。现代状态核心返回暗器effect ID供后续同步动画使用；目标光标、effect动画、damage render/present/wait仍未接入，故保持 `pending_implementation`。
