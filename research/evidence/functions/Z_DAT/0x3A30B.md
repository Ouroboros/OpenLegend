# 函数证据：`sub_3A30B` `0x3A30B..0x3A8A4`

状态：`implemented_pending_review`

映射：`BattleSetup::throwing_weapon_targeting_range`、`apply_throwing_weapon_target`、`finish_throwing_weapon_action`及 `BattleSession` type4 continuation。

目标射程为actor signed `hidden_weapon/15+1`。确认目标后按与状态动作相同的绝对差规则更新方向并清effect；友军格完全跳过，空格只写effect1，敌方格写effect1并使用当前选择库存槽的暗器。原坐标判断重复检查x<64而漏掉y<64；现代实现安全拒绝y越界，登记待最终REVIEW。

命中敌方时先取item `hidden_weapon_effect_id`。HP增量按target hurt分档：hurt等于0取item `add_hp/4`，1..33取`/3`，34..66取`/2`，大于66取完整值；随后减一次`bounded(5)`，再计算signed `(分档值-2*actor.hidden_weapon)/3`。该增量加到target HP并夹0..maximum HP；hurt按`hurt-增量/4`夹0..99，显示值为新旧HP差绝对值。

item `add_poison`为正时不再消费RNG：先算`(add_poison-actor.hidden_weapon)/2-target.anti_poison`，target抗毒不少于100或结果为负则归零，再除2并加到poison。item `add_poison`非正时严格再消费两次`bounded(5)`，以`add_poison/2+first-second`修改poison；最终夹0..99。真实带毒暗器向量得到伤害21、hurt40→45、poison10→12，只消费一次RNG；真实无毒暗器仍消费三次RNG并得到伤害16、hurt0→4、poison10→8。

现代Session已接入type4 targeting：Escape回原动作菜单，空目标不扣库存且不完成行动，敌方目标先写状态，再按 `sub_3884A` 分派attack sample13、present、等待参数100对应三次tick变化、分派effect sample、执行11帧EFT；随后按 `sub_38910(0)`执行10帧damage与前四帧flash。前奏、首EFT、首damage整帧FNV64分别为`0xdbee20f394fd7219`、`0x370a4078e9de6172`、`0xd41fa068222d444a`。动画完成后才按int16减库存；数量不大于0时左移后续199槽并清尾槽，随后写actor `action_done=1`、刷新全体sprite并推进下一actor。actor不增加攻击计数也不扣体力。Linux与Windows Debug完整BUILD均14/14；最终逐基本块双向REVIEW尚未执行，故为 `implemented_pending_review`。
