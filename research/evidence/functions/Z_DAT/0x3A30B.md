# 函数证据：`sub_3A30B` `0x3A30B..0x3A8A4`

状态：`platform_adapted / converged_after_timing_and_wrap_fix`

映射：`BattleSetup::throwing_weapon_targeting_range`、`prepare_throwing_weapon_target`、`apply_throwing_weapon_payload`、`apply_throwing_weapon_target`、`finish_throwing_weapon_action`及 `BattleSession` type4 continuation。

## 机器身份

- fresh临时IDA数据库：`tmp/Z_DAT-b8-order68.i64`，仅由`/mnt/d/Dev/Crack/IDA/idat.exe -A`配合`tmp/ida_dump_b8_order68.py`打开。
- 完整owner范围：`0x3A30B..0x3A8A4`，1433 bytes、340条连续指令、62个IDA基本块、31个条件分支、11个无条件跳转、85处绝对数据fixup、13次direct call、1个入口xref、1个本地RET。
- raw SHA256：`8958cd95aa8764fe422c784e74ff1a40933a2c7e5cdd191931a55517618b5d9c`。
- fresh IDA loaded SHA256：`b34fb5b6496f43631d89d5e68ebfa5d10e061a5871c6a17854e1d6232b9533f6`。
- 85处loaded DWORD均严格等于raw值加`0x20000`；逐处减回后1433字节与只读`Z.DAT`原切片完全一致。
- 唯一入口xref为`sub_3A29C:0x3A2E6`；没有外部xref进入函数内部块。
- 13次call依次为栈探测`sub_3ED1E`、targeting selector `sub_36AF7`、两次绝对值`sub_3F50B`、EFT动作`sub_3884A`、一次伤害RNG `sub_3D612`（另一路同一callsite）、伤害绝对值`sub_3F50B`、两次中毒RNG `sub_3D612`、damage动画`sub_38910`、库存槽删除`sub_2B227`及循环sprite计算`sub_3B1E6`。

## 完整机器合同

1. 以actor signed role的`hidden_weapon/15+1`向零截断并回绕到signed低16位，调用`sub_36AF7(actor, range, 1, &cancel)`。cancel低字恰1返回`-1`；其余确认路径继续且最终返回0。
2. 方向使用`abs(dx)`与`abs(dy)`的signed低字比较；纵向仅在`abs(dy)>abs(dx)`时胜出，tie走横向。`dx>0/<=0`分别写1/2，`dy>0/<=0`分别写3/0；同格`dx=dy=0`也明确写方向2。随后清零全部64×64 effect。
3. 坐标门依次检查`x>=0`、`x<64`、`y>=0`，最后错误地再次检查`x<64`而漏掉`y<64`。现代拒绝`y>=64`，归类平台安全适配。
4. 机器在判断empty前短暂读取`combatants[target_slot].side`，所以empty会读`combatants[-1].side`；可观察分派仍为友军不标记直接退出、empty写effect1后退出、敌军写effect1并继续。现代避免负槽读取，合法域结果一致。
5. EFT前从当前选择库存槽读取item及`hidden_weapon_effect_id`，同步完成`sub_3884A`的sample13/effect双bank前奏、等待和全部EFT帧。EFT返回后重新读取目标occupancy、actor/target role和当前库存item，因此HP/毒payload与RNG状态提交严格晚于整段EFT。
6. signed target hurt分档：0取`item.add_hp/4`，1..33取`/3`，34..66取`/2`，大于66取完整值；随后无条件消费一次`bounded(5)`。减随机值后的基数先按低16位回绕，再计算signed `int16((base-2*actor.hidden_weapon)/3)`。
7. target hurt按`hurt-hp_delta/4`低字回绕后夹0..99；HP按`hp+hp_delta`低字回绕，先signed夹到maximum HP，再将`<=0`夹0。damage word为夹值后新旧HP差的绝对值；非零damage才设置kind1。
8. `item.add_poison>0`时不再消费RNG：计算signed `((add_poison-actor.hidden_weapon)/2-target.anti_poison)`并先回绕低16位，anti-poison不少于100或结果为负则清0，再除2后加到poison。`add_poison<=0`时严格再消费两次`bounded(5)`，按`add_poison/2+first-second`修改；最终poison低字夹0..99。
9. `sub_38910(0)`完整执行10帧damage及前四帧flash并清kind后，才将所选库存count低字减1；结果不大于0调用`sub_2B227`左移后续槽。随后写actor `action_done=1`，按slot顺序为全部combatant调用`sub_3B1E6`并写sprite，再返回0。该动作不增加attack counter，也不扣physical power。

## 汇编到现代实现REVIEW与修正

首轮先冻结机器合同，再逐块对照现代`BattleSetup`和`BattleSession`，发现三项合法域差异：

1. 同格确认时现代代码跳过方向写入，机器明确写方向2。
2. 玩家暗器伤害在`add_hp/divisor-bounded(5)`后未先做int16回绕；`add_hp=-32768`、hurt>66可与机器产生相反符号结果。
3. 现代Session在target确认时立即提交HP/hurt/poison和RNG，而机器先完整执行`sub_3884A`，EFT返回后才重新读取payload并提交状态。

最小修正将玩家暗器拆为目标准备与动画后payload提交：准备阶段写方向、清/标effect并冻结EFT effect id；11帧EFT结束后重新读取目标与库存item，才消费RNG并写状态，然后进入damage。方向分派现在对同格写2；随机基数使用`wrapping_i16`后再进入最终公式。保留一体化`apply_throwing_weapon_target`供同步测试，它严格按准备→payload组合。

修正后废弃首轮结论，从`0x3A30B`入口重新覆盖全部340条指令、62块、42个函数体跳转、85处fixup、13次call、唯一RET和`sub_3A29C` caller；合法caller域无剩余产品差异。原机器漏检upper-y、empty负槽side读取，以及非法actor/role/item/occupancy、负hurt访问由现代安全拒绝，统一归类`platform_adapted`。

## 判别性回归、Golden与验收

- 新增同格目标将actor direction从1写为2的回归。
- 新增`add_hp=-32768`、hurt67、首个RNG输出4的回绕向量，锁定damage=`10921`、HP `100→11021`、hurt `67→0`，且只消费一次RNG。
- Session在target确认及完整EFT期间锁定HP/hurt/poison与RNG不变；第11帧EFT延迟完成后才锁定HP `100→79`、hurt `40→45`、poison `10→12`、damage21及RNG终态`1103527590`，随后执行10帧damage，库存/action/sprite仍在damage结束后提交。
- 既有cancel、friendly、empty、真实带毒/无毒暗器、effect30双bank/三tick前奏、11 EFT帧、10 damage帧、库存压缩及action完成回归继续通过。
- 独立机器向量SHA256：`4de5134d6363b727de38a09d0b2cc7cc335dd0cf70831c0e9d498e6a663decd1`。
- 两次独立原资产Golden生成逐字节一致，第三次写正式文件并再次与两份临时文件比较；仅新增`battle_throwing_weapon_action_machine`，历史61键逐值不变。正式Golden SHA256：`cc94651bde1b784f51f2b9173cf1b8e0c0ec07330cba4c674734878a11614c8a`。
- 根统一入口`./build.sh app --config Debug`成功，精确结果为`100% tests passed, 0 tests failed out of 14`。
- 原程序动态差分仍登记为`blocked_runtime_oracle`；独立只读资产oracle不冒充原程序runtime输出。

## closure边界

本owner结论为`platform_adapted / converged_after_timing_and_wrap_fix`。栈探测、cursor、EFT、RNG、damage、库存压缩、sprite helper、物品wrapper caller及后续行动调度均保持独立owner，不传播closure。
