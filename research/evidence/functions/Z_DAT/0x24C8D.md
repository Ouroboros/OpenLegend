# 函数证据：`sub_24C8D` `0x24C8D..0x24CE8`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码。

独立汇编恢复：按打开的严格逆序BUILDY→BUILDX→SURFACE→BUILDING→EARTH逐个调用close；所有调用都执行，返回最后一次close结果。

现代映射：五层文件由`DataRoot::read`局部RAII对象在读取完成/错误展开时关闭，`WorldMapData`只持有解析后的只读vector，因此没有显式逆序close函数。该差异是宿主平台资源适配；不改变合法资产cache或像素。

文件锁/运行中替换属于适配后的外部行为差异，最终只能标`platform_adapted`而非声称逐指令相同；当前closure仍为`implemented_pending_review`。最终双向REVIEW为`not_started`。