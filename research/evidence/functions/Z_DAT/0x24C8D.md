# 函数证据：`sub_24C8D` `0x24C8D..0x24CE8`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；审计包 `tmp/b9-review/0x24C8D.txt`

## 1. 独立机器恢复

- 函数无条件执行五次 `sub_3DB2B(close)`，没有分支、循环或提前返回。
- 16位有符号句柄读取与调用顺序严格为：`word_C0BE6 → word_C0BEE → word_C0BEC → word_C0BF4 → word_C0BF0`。
- 对应 `sub_24C23` 的严格逆打开顺序：`BUILDY.002 → BUILDX.002 → SURFACE.002 → BUILDING.002 → EARTH.002`。
- 所有五次close均执行；前四次返回值被丢弃，函数返回第五次close的结果。
- 本函数不清零句柄、不检查close结果，不关闭TITLE archive、music、Miles或键盘平台。

## 2. 汇编 → C++ 映射

- 五层打开/解析owner：`world::WorldMapData`，打开顺序由 `kLayerLoadOrder` 固定为 `EARTH → BUILDING → SURFACE → BUILDX → BUILDY`。
- 文件字节读取：`resource::DataRoot::read` 的局部文件owner在每次完整读取或错误展开时关闭句柄；`WorldMapData`只持有解析后的五层 `std::vector<int16_t>`。
- world/session关闭：`LegacyGameRuntime::activate_pending_load/start_world` 的 `world_session_/world_map_` reset及应用退出时的runtime RAII析构。
- music、音频设备和平台在runtime资源owner之后关闭，由SDL `main`外层生命周期单独承接，不归入本函数映射。

## 3. 单向终审

- 从 `0x24C8D` 入口独立恢复后才读取现代owner与打开路径。
- 覆盖五个全局句柄读取、五次无条件close、精确顺序、返回值归属及本函数未触及的资源边界。
- 本轮零新增差异，无需修正或重启。

## 4. 平台适配

- 原版五个DOS文件句柄从startup常驻到world/shared shutdown，并在本函数逆序关闭；现代每层读完即由宿主RAII关闭，不保留可伪造的DOS句柄。
- 因此运行期文件锁、文件替换可见性和close错误返回值不是assembly-exact；终态必须为 `platform_adapted`。
- 合法只读资产的打开顺序、完整长度校验、int16le解析、world cache值与渲染结果保持；错误路径以首个失败文件显式终止，属于受控宿主错误处理。

## 5. 验证与结论

- Linux app Debug `proc_07d5`：14/14。
- 资源回归覆盖五层严格打开顺序、每层460,800字节、当前资产解析及world真实资产渲染；runtime reset/load/exit均由RAII与泄漏门禁覆盖。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
