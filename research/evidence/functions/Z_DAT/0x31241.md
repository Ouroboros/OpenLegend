# 函数证据：`sub_31241` `0x31241..0x31280`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；`research/ida/reports/Z_DAT.b7_scene_xrefs.txt`

函数物理范围：`0x31241..0x31280`，63 bytes，`noreturn`

直接调用者：`sub_30C3D` 职员表结束出口。

## 1. 不看 C++ 的独立汇编恢复

```text
sub_3ED1E(8)
sub_24C8D()
sub_3E25B()
sub_3DD57()
sub_3CF19()
printf(" Thanks for playing this game ! \n")
printf(" Oriental Software Studio 1996  \n")
exit(0)
```

前五个调用按原平台链依次关闭游戏输入/声音/图形与运行环境；字符串字节包含首部空格，第二行在换行前有两个空格。退出状态固定0。

## 2. 现有 C++ 映射

- `SceneStepResult::ending_complete` 区分完整结局退出与普通战败/菜单退出。
- `LegacyGameRuntime` 在完整结局 `quit` 时记录该边界。
- SDL main 把 platform、audio device/controller 和 runtime 放在内层作用域；循环退出后先完成析构/宿主配置回收，再输出原两行字节。
- `ending_terminal_message()` 提供可单测的精确字符串；platform main 用 `fwrite` 不做本地化或格式改写。
- main 返回0对应 `sub_3EE5F(0)`。

## 3. LST/机器码 → C++ 正向追溯

- [ ] 最终 REVIEW 尚未执行。

## 4. C++ → LST/机器码反向追溯

- [ ] 最终 REVIEW 尚未执行。

## 5. 验证

- `openlegend.core` 固定终端消息的完整字节序列。
- Linux Debug app target 构建通过；scene 完整结局只在第二次按键、最终 fade 后设置 `ending_complete`。
- 最终平台析构顺序仍须在 B9 platform REVIEW 中反向审计。
- Windows/sanitizer 留到 B7/B9 对应收口。

## 6. 最终结论

```text
final_review = not_started
remaining = B7/B9 收口时审计 shutdown helper 与 SDL/audio 析构的双向平台映射
```
