# 函数证据：`sub_31241` `0x31241..0x31280`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；审计包 `tmp/b9-review/0x31241.txt`

函数范围：`0x31241..0x31280`，63 bytes，`noreturn`；唯一结局调用者为`sub_30C3D`。

## 1. 独立机器恢复

```text
sub_3ED1E(8)                  // 编译器栈帧/探针前导
sub_24C8D()                   // 按BUILDY, BUILDX, SURFACE, BUILDING, EARTH关闭资源
sub_3E25B()                   // 音乐向0淡出2000ms并等待1000ms
sub_3DD57()                   // 音频后端关闭
sub_3CF19()                   // 再执行幂等音频关闭，恢复键盘并回文本/宿主显示
printf(" Thanks for playing this game ! \n")
printf(" Oriental Software Studio 1996  \n")
exit(0)
```

两行字符串均含首部空格；第二行在换行前含两个空格。终端消息只在完整结局路径出现，普通主循环退出不打印；进程状态固定为0。

## 2. 汇编 → C++ 映射

- `sub_30C3D`最终淡黑后才发出`SceneStepKind::quit`且`ending_complete=true`；`LegacyGameRuntime::handle_scene_result`清键态/effect，设置`ending_complete_`与`fade_music_on_exit_`并进入`LegacyGameView::exited`。
- runtime内层作用域结束先释放scene/world/archive等会话资源；`LegacyAudioController::fade_out_music`执行目标音量0/2000ms并等待1000ms。
- `SdlAudioDevice`、`AudioMixer`及`SdlRuntimePlatform`依RAII关闭音频与SDL输入/显示；重复的DOS音频shutdown由幂等宿主析构合并。
- platform析构及lambda正常返回后，仅当`run_status==0 && ending_completed`时以`fwrite`原样写出`ending_terminal_message()`；`main`返回`run_status`即0。

## 3. 逐基本块复核

- 从`0x31241`入口独立覆盖全部八次call、两个push-string/printf及`exit(0)`，再按调用顺序核对现代资源、音乐、音频、输入/显示、消息和退出承接点。
- terminal helper字节为`" Thanks for playing this game ! \n Oriental Software Studio 1996  \n"`，与机器两个printf顺序及空格完全一致。
- 完整结局标志在第二次任意键和最终64级淡黑完成后才设置；普通quit不设置该标志，故不会输出结局消息。
- 本轮零新增差异，不需要实现修正或重启。

## 4. 平台适配

- DOS全局句柄关闭、音频shutdown两次、键盘中断向量与文本模式恢复改为内存archive和SDL RAII；可观察顺序仍为游戏资源结束→音乐淡出/等待→音频与平台关闭→终端消息→状态0。
- 现代平台在SDL析构前读取窗口状态并保存宿主窗口配置；这是DOS不存在的宿主配置副作用，不改变游戏数据、结局消息或退出状态。
- `sub_3ED1E(8)`为编译器栈前导，无现代业务承接点。

## 5. 验证与结论

- Linux app Debug `proc_c33b`：14/14。
- `openlegend.core`固定完整终端字节；`scene_test`固定第二次按键、最终淡黑后才产生`quit(ending_complete=true)`。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
