# 函数证据：`sub_3570F` `0x3570F..0x35803`

状态：`implemented_pending_review`

映射：`BattleSetup::choose_ai_first_poison_target`。

资格条件与`sub_35667`相同，但原循环对每个合格候选都重复建立actor targeting图，随后错误读取陈旧全局`word_E6EE0`指向坐标的距离，而不是当前候选i。固定stale slot4距离8，候选真实距离`[6,8]`均被忽略；best从1000开始，首个合格slot3写入后，后续候选因同一距离不再strict更小而无法替换。因此该函数不是最近目标selector。

无合格候选时原版从不读取陈旧slot，现代也先完成资格短路；存在候选但stale slot越界时现代安全拒绝原线性越界读取，留待最终REVIEW。Linux Debug 14/14和独立oracle通过。
