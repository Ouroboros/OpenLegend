# 函数证据：`sub_3570F` `0x3570F..0x35803`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::choose_ai_first_poison_target`及`BattleSession`的legacy AI目标scratch时序。

当前原始`Z.DAT`物理范围为244字节、56条连续指令、8个显式跳转、18处加载时fixup和2次direct call；raw SHA256为`3bf9b8e1b03074465262d0a5b08257cde7d2ce0243a56f51cef7892cee30562e`，IDA加载后SHA256为`b06d503b356f1c7587888e743a9dfcda7851d38e86999acf2d7098156322d2b9`。18处加载重定位相对raw均增加`0x20000`，归一化后逐字节相等。唯一caller为`sub_355FF`的`0x35657`；两个call依次是入口栈探测`sub_3ED1E`和targeting图构建`sub_36E7F`，本函数不消费RNG。

DI最佳距离初始化为signed int16 1000，SI候选槽从0开始按signed int16与combatant count比较并完整扫描。候选必须异side且hidden恰等于0；role poison按signed严格小于95，anti_poison按signed严格小于actor use_poison；函数不读取候选HP。每个合格候选都重新把actor x/y写入targeting source并调用一次`sub_36E7F`，命中首槽后仍对后续合格候选重复建图。

距离读取故意不使用当前候选SI。`0x357AB` sign-extend全局`word_E6EE0`，按该陈旧槽的x/y查询图；全部合格候选因此得到同一距离。AX与DI作signed strict `<`：距离小于1000时首个合格候选写actor word12，后续相同距离因等号拒绝；距离等于或大于1000时不写。blocked值555和负距离仍可命中。每次strict improvement保存候选SI及距离低16位；无命中保留旧word12。best仍为1000返回32位-1，否则返回32位1。

机器合同固定后才读取现代实现。`choose_ai_first_poison_target`的int16 best1000、候选过滤、每合格候选重建actor-source图、显式stale slot坐标查询、strict更新、逐次word12写入和bool控制效果均逐块一致；非法role、stale slot、异常count或坐标由现代安全拒绝，替代原版线性越界。

首轮宿主REVIEW发现一处产品差异：原版`sub_3540E`在`0x35421`先调用`sub_355FF/sub_3570F`，成功返回后才于`0x35444`把actor word12复制到`word_E6EE0`；现代`BattleSession`却在selector前直接把actor旧word12作为stale slot。全xref证明`word_E6EE0`是独立AI目标scratch，写点来自AI目标selector及攻击、用毒、暗器handler，不是actor word12别名；其`dseg02`地址无文件映射。

现代现以值初始化的Session级`legacy_ai_target_slot_`映射该scratch：AI动作选择得到合法target时更新，攻击初选及移动后重选、成功用毒目标和暗器目标均按原handler写入时序更新；用毒selector入口只读取该镜像，不再读取actor word12。修正后从入口重新覆盖全部56条指令、8个跳转、18处fixup、两个call、唯一caller、所有过滤/建图/strict更新路径和两个出口，零新增差异。

回归固定stale slot4距离8而候选真实距离`[6,8]`仍选择首个slot3；IQ60不消费RNG；无合格候选且stale slot99时短路不读取并保留word12；negative hidden跳过且HP0候选slot4可选；actor use_poison=-10时anti`[-20,-10]`仅首项通过signed strict门。完整Session将actor旧word12设为99，仍由独立scratch初值槽0选中slot1并覆写word12，直接证明旧word12不再污染陈旧距离来源。

正式battle golden由原始只读资产独立双生成逐字节一致，SHA256为`c19c2c95f241882966f965803f8355202b929d3d74beca305494f38e691d0d5c`。修正后根BUILD `proc_b6bf`及正式证据落位后的最终根BUILD `proc_0f5a`均完成Linux app Debug 14/14。`sub_36E7F`callee及其他`word_E6EE0`读者继续按owner独立审计，不传播closure。故本owner归类`platform_adapted / converged_no_new_differences`。
