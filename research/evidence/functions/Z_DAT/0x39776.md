# 函数证据：`sub_39776` `0x39776..0x397E5`

状态：`platform_adapted`

映射：`BattleSetup::poison_targeting_range`与`BattleSession`玩家用毒目标选择wrapper。

## 机器身份、入口与owner边界

- fresh临时IDB导出完整`111` bytes、`36`条指令、`1`个条件分支、`2`处32位重定位、`3`次direct call、`1`个caller和`2`个本地RET。
- 原文件偏移`0x33176`；raw SHA256为`94d03eff4ea1c9da0efeb51a4fbda40bc3bb81426f5aaa5f4067e98faea31609`，加载重定位后SHA256为`9549158d1cb4e5570427a7aead98a07c073892c1bd7360309eed75441b8dfe7b`。fixup位于函数偏移`0x20/0x2D`，加载值均比原文件大`0x20000`，逐处逆变换后完整111字节与只读`Z.DAT`一致。
- 三个call依次为`0x3977B -> sub_3ED1E`的28-byte栈探测、`0x397BC -> sub_36AF7`同步目标光标、`0x397D6 -> sub_397E5`用毒动作。
- 唯一caller为`sub_32E59:0x33381`动作菜单case2；caller无条件跳到公共参数回收，随后从actor重新计算战斗员地址并检查action-done，因此完全忽略wrapper的EAX。
- `sub_36AF7`光标循环已由B8 order38的battle owner独立关闭；`sub_397E5`状态/动画/提交属于B8 order59；栈探测和caller同样不传播本行closure。

## 完整机器合同

1. 入口把四字节local初始化为0，并把actor参数低16位sign-extend。以`actor*28`读取战斗员signed role word，再以`role*182+170`读取signed `use_poison` word。
2. `IDIV 15`按signed向零截断，商加1后执行`CWDE`。完整signed skill域结果为`trunc(use_poison/15)+1`，范围`-2183..2185`；正常`0..100`域为`1..7`。
3. 参数按`actor, range, mode=1, &local`传给`sub_36AF7`。本体直接RNG消费为0。
4. selector返回后仅比较local低16位是否**恰等于1**：等于1从`0x397D4`返回EAX=-1，不调用用毒动作；所有其他低字值进入确认边，调用`sub_397E5(actor)`，忽略callee返回并从`0x397E4`返回EAX=0。
5. 正常`sub_36AF7`入口先把取消word清0，Escape写1，合法确认保持0；所以合法域只有上述取消/确认两条出口。异常的其他local值不属于同步callee正常合同。

## 汇编到现代实现逐块对照

- `BattleSetup::poison_targeting_range`逐项保持actor→role→`use_poison`、signed C++向零除15、加1与16位结果；`BattleSession::begin_player_targeting`只在玩家动作case2调用该射程，并固定进入`BattleCursorSelectionMode::targeting`。
- 已关闭的`begin_cursor_selection/apply_cursor_selection`保持机器mode1光标合同、入口两次present以及每次后续输入前一次present。Escape产生cancelled，Session清path/cursor后经`finish_player_action_call`回到同一菜单ordinal；action-done仍为0，与唯一机器caller忽略-1后检查word7的路径一致。
- 确认产生selected并立即把当前cursor传入`begin_player_target_effect(use_poison)`；其首个状态写调用`apply_poison_target`，对应机器立即调用`sub_397E5`。现代将同步callee拆成可呈现continuation；`sub_397E5`内部状态、动画、音频与action-done必须在order59独立审计。
- 首轮从入口逐块覆盖全部36条指令、唯一分支、两个RET、三次call与唯一caller，合法域零产品差异；补齐signed阶梯和非法索引回归后再从入口完整复核，仍无新增差异。现代对非法actor或role安全返回`nullopt`，替代机器越界读取，归类平台适配。

## 判别性回归与验证

- 新增signed射程向量：`-32768→-2183`、`-15→0`、`-14/-1/0/14→1`、`15→2`、`89→6`、`90→7`、`32767→2185`；非法actor与负role安全拒绝。
- 既有真实battle4 Session回归锁定use_poison30得到path limit3、两次入口present、mode1逐键移动、Escape回原菜单ordinal，以及确认敌方坐标后立即进入用毒effect continuation。
- 正式Golden由两次独立原资产生成逐字节一致后更新，再以第三次生成核对；formula vector SHA256为`336881d39e28183cdc37c8970c27b0c2048a88e70ea123334a80b342e9b447d6`，正式文件SHA256为`30863e17bdd3fa340acfb74b55ea425a33a0c1e7b3146c25ec8c928371a43b71`。
- 根`./build.sh app --config Debug`以Clang 23通过精确14/14。
- 最终归类：`platform_adapted / converged_no_new_differences`。
