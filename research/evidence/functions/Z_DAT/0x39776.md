# 函数证据：`sub_39776` `0x39776..0x397E5`

状态：`pending_implementation`

已映射状态边界：`BattleSetup::poison_targeting_range`。

函数把actor role `use_poison/15+1`（signed除法向零截断）作为 `sub_36AF7` 的选择射程，target mode固定1。目标选择out flag为1时返回-1；否则调用 `sub_397E5` 并返回0。

固定use_poison80得到射程6。公式已由Linux Debug 14/14验证；`sub_36AF7`目标光标、input/render/present continuation及对 `sub_397E5` 的同步接线尚未实现，因此保持 `pending_implementation`。
