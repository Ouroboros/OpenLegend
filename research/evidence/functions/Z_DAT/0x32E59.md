# 函数证据：`sub_32E59` `0x32E59..0x33567`

状态：`pending_implementation`

本函数是 side word=0 且自动标志为0时的玩家行动菜单。headless IDA 从 `0x55A06` 读取10个固定20字节 Big5 项：

| action id | 文案 | 可用条件 |
|---:|---|---|
| 0 | 移動 | physical_power word21 > 5 且参战者 word6 > 0 |
| 1 | 攻擊 | physical_power > 10；最低耗内初值1000，扫描十个非0武功ID取最小 need_mp word16，最终要求该值 <= 当前 mp word41 |
| 2 | 用毒 | physical_power > 10 且 use_poison word47 >= 20 |
| 3 | 解毒 | physical_power > 50 且 detoxification word48 >= 20 |
| 4 | 醫療 | physical_power > 50 且 medicine word46 >= 20 |
| 5 | 物品 | 恒可用 |
| 6 | 等待 | 恒可用 |
| 7 | 狀態 | 恒可用 |
| 8 | 休息 | 恒可用 |
| 9 | 自動 | 恒可用 |

函数按 action id 建10个0/1可用表，再把 cursor 维护为“可用项 ordinal”而非 action id；每次绘制都重新扫描 ordinal 对应的原 action id。上下键在可用数量内回绕，三个确认 flag 任一命中后全部清零并按原 action id调用对应 handler。移動返回后重新检查门槛，失效时从表中移除并减少可用数。

handler 后若参战者 word7仍为0，则重建菜单、重画战场并继续；word7==1、结果 word>=1或本次 action id==6（等待）任一成立即退出。函数返回最后 action id。

`BattleSetup::player_action_availability`现按原顺序构造十项0/1表，保留武功最低耗内初值1000，所以无已学武功但当前内力达到1000时仍会开放攻击；非法负武功ID只采用现代安全拒绝。`BattleSession`现以可用项ordinal维护光标，执行上下回绕和Enter/Space/keypad Insert确认，再映射回原action id。每帧先重画战场，再按`(20,19,42,17*count+10)`绘制圆角混色菜单，按原Big5字节以普通/选中色绘制可用项，最后在右侧绘制当前actor状态面板。十项全可用、cursor0的独立Python oracle与C++整帧FNV64均为`0x7d062c289e7f933a`。

当前菜单可用性、像素、输入和action-id确认边界已实际实现；确认后的十个动作handler、移動返回后单独重检、handler后菜单重建及word7/战果/等待退出continuation仍待实现，因此状态保持`pending_implementation`。
