# 函数证据：`sub_37355` `0x37355..0x37734`

状态：`implemented_pending_review`

映射：`openlegend::battle::BattleSetup::move_one_marked_step`、`advance_player_movement`、`advance_ai_movement`与`BattleSession`同步移动相位。

函数从当前 `(word_556D6,word_556D8)` 开始，按上、右、左、下找首个 path value=250 的邻格。每移动一格的可观察写入顺序为：旧 path写255；旧 occupancy写-1；新 occupancy写 actor slot；参战者 x/y写新格；direction word4写方向下标；调用 `sub_3B1E6` 重算 sprite word8；若 round word6 恰等于角色 speed word44/10，则 physical_power word21执行16位 DEC并将负数夹0；最后 round word6执行16位 DEC。其后才更新视图、重画、present并等待40 tick。

完整函数还按 mode/geometry 控制停止：普通玩家移动直到目标格；AI mode0/3在目的格或行动值耗尽时停，mode1进入 Manhattan 范围也停，mode2还要求与目标同x或同y。现代`advance_ai_movement`每次只推进一个同步移动边界，实际消费path并改写occupancy、坐标、方向、sprite、体力和行动值，同时返回新中心、夹0..32视图偏移、render/present与原延迟参数40；测试覆盖连续左移、固定battle4首步、255消费标记、occupancy、direction、sprite、体力10→9、round8→7，以及mode0行动值耗尽、mode1范围和mode2轴向停止。

玩家与AI路径现均在每格状态提交后更新视图、重画、present，并等待`legacy_delay_tick_count(40)=2`次BIOS tick变化再继续。battle4玩家Session两次单格移动验证word6 2→1→0和菜单返回；battle2 AI逃跑Session从`(30,24)`经`(30,23)`、`(31,23)`到`(31,22)`验证mode0 word6 3→2→1→0、三次present、每格两次tick变化及移动后休息/actor推进。mode1/2/3复用同一Session同步相位，其目标距离、轴向与目的格停止条件继续由独立setup向量覆盖。本函数推进为`implemented_pending_review`，仍待最终完整汇编↔C++ REVIEW。
