# 函数证据：`sub_37355` `0x37355..0x37734`

状态：`pending_implementation`

部分映射：`openlegend::battle::BattleSetup::move_one_marked_step`。

函数从当前 `(word_556D6,word_556D8)` 开始，按上、右、左、下找首个 path value=250 的邻格。每移动一格的可观察写入顺序为：旧 path写255；旧 occupancy写-1；新 occupancy写 actor slot；参战者 x/y写新格；direction word4写方向下标；调用 `sub_3B1E6` 重算 sprite word8；若 round word6 恰等于角色 speed word44/10，则 physical_power word21执行16位 DEC并将负数夹0；最后 round word6执行16位 DEC。其后才更新视图、重画、present并等待40 tick。

完整函数还按 mode/geometry 控制停止：普通移动直到目标格；AI mode会在行动值耗尽、进入 Manhattan 范围，或范围类型2下同时与目标同 x/y 时停止。现代每次 `move_one_marked_step` 只推进一个同步移动边界，`movement_should_stop` 保留上述三类停止条件；测试覆盖连续左移两格、255消费标记、occupancy、direction2、sprite5110、体力1→0、round5→4→3、距离13与 aligned 分支。视图/render/present/40 tick continuation 未实现，因此不得推进为 `implemented_pending_review`。
