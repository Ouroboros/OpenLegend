# 函数证据：`sub_2D590` `0x2D590..0x2D653`

状态：`implemented_pending_review`

机器码按传入 head ID 直接索引 `HDGRP.IDX/GRP`，不执行 legacy sprite ID 除二；读取该记录后调用 `sub_3D8FF(x,y,frame)`。`sub_2CC21` 的锚点为头像框 `(x+2,y+59)`。

当前原资产 HDGRP 共115帧，GRP SHA256 为 `9b3ea687e0a2d82cc0dd83f580cff6a5cb48e7b62b6fd98dc4b63dad93a9c037`，全部按现有 RLE 格式解析。现代 `SceneSession::portraits_/draw_portrait` 直接按 head ID 读取 `PackedArchive` 并以同一锚点调用 RLE renderer；style2/3 不读取无效 head ID。最终双向 REVIEW 尚未执行。
