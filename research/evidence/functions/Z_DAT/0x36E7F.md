# 函数证据：`sub_36E7F` `0x36E7F..0x36EF8`

状态：`implemented_pending_review`

映射：`openlegend::battle::BattlePathing::build(..., BattlePathMode::targeting)`。

队列、source覆盖和 flood-fill 顺序与 `sub_36E06` 相同；唯一入口差异是先调用 `sub_36FF9`，因此目标/射程距离只看 WARFLD upper layer，不看 occupancy 与九段 ground tile。battle0 target `(21,23)` 距离14，battle93 target `(12,29)` 距离22；两张完整图 hash 已固定。
