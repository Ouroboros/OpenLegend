# 函数证据：`sub_2F39C` `0x2F39C..0x2F3F0`

状态：`implemented_pending_review`

KDEF opcode32 及 shop 扣 money 使用的首槽 mutator。线性扫描至第一个匹配 item ID，只对该槽 count 做 int16 加法并立即结束；结果 `<=0` 时调用库存删除 helper 压缩后续槽。若 ID 不存在，函数不新增任何槽。

旧 C++ 复用全库存 normalize helper，错误更新/合并/过滤并重排。现 `change_first_inventory` 只更新首匹配并在非正时单槽左移压缩。真实 script234：slot0=(174,10)、slot1=(174,20)、扣10后仅删除slot0并把未改的20槽移至slot0。shop 扣货币亦走此 helper。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
