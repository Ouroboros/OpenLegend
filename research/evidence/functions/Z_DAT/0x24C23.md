# 函数证据：`sub_24C23` `0x24C23..0x24C8D`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码。

独立汇编恢复：无条件按EARTH.002→BUILDING.002→SURFACE.002→BUILDX.002→BUILDY.002调用文件打开器，每个16位句柄写入独立全局；无短路和错误检查，返回最后一个句柄。

现代映射：`WorldMapData`显式`kLayerLoadOrder`。审计发现原枚举循环曾把SURFACE提前，已改回机器顺序；每个文件必须精确460,800字节并按int16le解析。

现代`DataRoot::read`读取后立即由RAII关闭，而非持有DOS句柄到世界退出；这是平台资源生命周期适配，合法只读资产的字节与错误首文件顺序保持一致。最终双向逐基本块REVIEW为`not_started`。