# 函数证据：`sub_3540E` `0x3540E..0x355FF`

状态：`implemented_pending_review`

部分映射：`BattleSetup::begin_ai_poison_plan`、`resume_ai_poison_after_move`。

先调用用毒目标selector；无目标立即回退自动攻击。目标存在时range为actor use_poison signed除15加1，距离取targeting图。仅round value恰为0且已在射程时首次检查直接用毒；round value>0时即使已在射程仍调用movement mode3；负round value跳过移动，并真实重复建targeting图后在第二次距离检查决定用毒。typed计划固定立即用毒1次range check、负值与零值超距2次、移动前1次且resume后累计2次。

移动后仍只复检同一目标，不重选。超距时比较`2*target attack`与`2*allied_total_i16/allied_count_i16`，strict更大回退自动攻击，否则休息。固定allied total330/count3阈值220；target attack50休息，200回退攻击。

`BattleSession`实际生成上述typed计划；next_step为move时按mode3逐格执行状态、render/present及两次BIOS tick变化，随后调用`resume_ai_poison_after_move`复检同一目标。命中时调用共享`sub_397E5`状态/动画链，执行11帧effect30、双bank音效、10帧damage、sprite刷新、word7/word13/体力提交，再由AI外层完成actor continuation；无目标或强敌条件命中时重新进入完整`sub_34C47`自动攻击，rest分支实际休息。真实battle4 seed2直接向量锁定目标poison0→25、体力100→98、word13加1、最终RNG2993822286，首magic/damage整帧hash为`0x47286fa4af30fce4`/`0xd76de7fa195a1ac3`；poison95无合法目标向量保留动作码3并进入attack effect sample0。Linux/Windows Debug完整BUILD、全部测试与逆向框架门禁通过；本函数推进为`implemented_pending_review`。
