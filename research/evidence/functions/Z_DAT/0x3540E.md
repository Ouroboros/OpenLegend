# 函数证据：`sub_3540E` `0x3540E..0x355FF`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::begin_ai_poison_plan`、`resume_ai_poison_after_move`、`BattleSession`。

## 机器身份与边界

当前原始`Z.DAT`物理范围为497字节、115条连续指令、8个显式跳转、40处加载时fixup；raw/loaded SHA256分别为`d153ed4a0997bc2735ea31b5a00ecc62ad634b9f574ce57dcf06c867c9546e1a`和`8be7484923c0296d3c6060e77fe945fe29440cc56df79a3c8b8c545ae2c4c335`。40个绝对地址word逐项减去加载基址差`0x20000`后与raw 497字节完全一致。

唯一caller为AI动作3分支`0x33BE2`；返回后直接跳到共享参数清栈和`word7=1`尾，不读取EAX。十个direct call严格为`0x3ED1E,0x355FF,0x34C47,0x3F50B,0x3F50B,0x36E7F,0x397E5,0x3650E,0x36E7F,0x34AD3`。栈检查、目标selector、自动攻击、targeting图、用毒状态、移动和休息保持各自owner，本行不传播closure。

## 汇编合同

函数先调用用毒目标selector；返回signed -1立即调用完整自动攻击，否则从actor combatant word12重读目标并复制到共享目标槽，而不是继续使用EAX。目标/actor坐标分别写入两组targeting全局。actor role use_poison按signed `IDIV 15`向零截断再加1，后续以低16位DI作signed射程。

首次构图前对x/y差各调用一次纯32位abs helper，但两次返回均在使用前被覆盖且callee无副作用。targeting距离以signed word读取；命中条件为`distance <= range`。首次命中且round value恰0直接用毒；round value严格正时即使首次已命中仍调用`sub_3650E(actor,mode3,range)`；负值跳过移动。之后无条件恢复同一目标、重写双方坐标并第二次建图，命中用毒，仍超距才fallback。

fallback的EBX在`0x3556F`已覆写为actor记录偏移，因此左侧严格为`2 * signed actor.attack`，不是target.attack。右侧读取AI外层在handler和300延迟前已冻结的signed 16位allied total/count，按32位`2 * total`再signed除count向零截断；左侧strict greater才自动攻击，相等或更小休息。本handler自身无direct RNG调用；selector及各action callee的RNG按对应owner负责。

## C++对照、修正与回归

第一轮逐块对照发现现代fallback误读target attack，作废结论并改为actor attack；第二轮入口重审发现现代在fallback时重扫盟友attack+HP，而机器读取外层冻结总值，再次作废。最终`BattleSession`把既有`BattleAiTurnPrelude`传入plan，plan保存allied total/count并跨mode3宿主presentation/resume复用；仅actor attack继续在fallback发生时读取，与机器时序一致。

plan级回归锁定：use_poison80射程6；round0距离6一次检查直接用毒；round-1距离6跳移动但两次检查后用毒；round3即使距离6仍mode3并在resume复检；无目标自动攻击。`use_poison=-16`与目标anti_poison=-20锁定signed射程0。移动plan建立后把盟友HP从100改为1000，resume仍使用入口total330/count3和阈值220。actor attack160锁定320等于320时休息；target attack保持50而actor attack200锁定左侧400、冻结total520/count3、阈值346并自动攻击，明确排除target字段。

`BattleSession`命中时执行共享用毒状态和11帧effect30、双bank音效、10帧damage、sprite刷新、word7/word13/体力提交，再由AI外层完成actor；真实battle4 seed2向量锁定目标poison0→25、体力100→98、word13加1、最终RNG2993822286，首magic/damage整帧hash为`0x47286fa4af30fce4`/`0xd76de7fa195a1ac3`。poison95无目标向量进入完整攻击effect sample0。同步callee被拆为逐present/tick phase，但动作提交和外层word7仍只在语义完成后发生。

正式battle golden由只读原资产独立双生成逐字节一致，SHA256为`cf647cc258b768f4b0ddc1162a5f9d1b7a3364a6bb59e0624fa3805555ccf890`；包含497/115/8/40机器身份、十次call、唯一caller、actor攻击和冻结总值向量。Linux app Debug根BUILD `proc_3554`全部14项测试及逆向框架通过。

## 收敛结论

最终从`0x3540E`入口重新覆盖全部115条指令、8分支、40 fixup、10次direct call、唯一caller和自动攻击/用毒/移动/休息全部出口，零新增差异或未决项。原版非法actor/role/target/count/坐标会线性越界或除零；现代只在这些非法输入上安全拒绝，并把同步present/阻塞等待拆成宿主phase，故归类`platform_adapted / converged_no_new_differences`。`sub_355FF`及其他delegated callee继续按各自owner独立审计。
