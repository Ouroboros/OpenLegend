# 函数证据：`sub_2F6E3` `0x2F6E3..0x2F721`

状态：`platform_adapted`；最终 REVIEW：`converged_no_new_differences`

来源：当前 `Z.DAT` 机器码与原始文件字节；headless IDA完整导出；当前 `KDEF.IDX/GRP`；`research/ida/reports/Z_DAT.b7_scene_xrefs.txt`。

## 1. 机器身份与caller

- 函数物理范围 `0x2F6E3..0x2F721`，62 bytes，12条指令。
- loaded SHA256 `b21126ec7ec3a67797d55ee14e342179758c49474730395bc39eadc62c2c1ed8`；原始文件 `Z.DAT[0x290E3:0x29121]` SHA256 `db0dcc63f309397e43036f6ff88cda5998fc0fff0f1ef609bc661b718676b00b`。
- 五个差异字节分别来自同一主角伦理word地址raw加加载基址 `0x20000` 的五项重定位；规范化后与loaded逐字节相同，未解释差异为零。
- 唯一物理代码caller是 `sub_2C319:0x2C8E8..0x2C8FB` 的opcode37；`0x55650`只是函数地址表数据引用。
- caller把唯一KDEF参数sign-extend后传入，忽略返回0，经共享尾块清理4字节栈并把PC增加2 words。
- 入口 `sub_3ED1E(4)` 仅为Watcom栈探测合同。

## 2. 不看C++的修改合同

1. 以参数低16位对角色0记录word56 `morality` 执行原位16位加法，因此先发生low16回绕。
2. 把回绕后的word按signed int16与100比较；严格大于100时写100。
3. 再把当前word按signed int16与0比较；严格小于0时写0。
4. 返回0；无提示、画面、输入或音频边界。

等价公式为 `clamp(signed_int16(low16(old + delta)),0,100)`，且机器实际顺序固定为先上限、后下限。

## 3. 汇编→C++ REVIEW

C++ case37读取同一角色0 morality word，调用共享 `clamped_add(old,delta,0,100)`；该函数先经 `wrapping_add` 执行无符号16位求和并显式还原signed int16，再调用clamp。随后PC增加2。合法快照域与机器逐项一致，本轮没有产品代码差异。

机器固定假设角色0记录存在；现代空角色集合时稳定跳过写入，替代原机无效内存访问，归类 `platform_adapted`。随后从入口重新逐条复核12条指令、五项重定位、word add、两次signed比较、两次条件写入、return0和caller，零新增差异。

## 4. 全KDEF与边界oracle

当前1,018条KDEF共有156次opcode37。按little-endian `<IIh>` 编码 `(script,PC,delta)` 的完整参数流SHA256为 `8146d5aba5f189528a829f58204fde819faca4372b7d8fe20b32a4e84c8bb2c3`；delta集合为 `-14,-10,-6,-5,-4,-3,-2,-1,1,2,3,4,5,10,12`。首项为script10 PC99 delta2，末项为script940 PC87 delta3；全部位置写入正式oracle。

- 真实script149 PC59 `37,-5`：`3 -> -2 -> 0`，`-32768 -> 32763 -> 100`。
- 合成边界另固定：`99+5 -> 100`、`32767+1 -> 0`、`-32768-1 -> 100`、`100+32767 -> 0`、`0-32768 -> 0`、`-1+0 -> 0`。
- 双次独立生成字节一致；正式 `scene-goldens.json` SHA256 `ad6d3725553a2068cce0148bc2bc09a6940a115d5fe4c89326a816a4c601ef9f`。

## 5. 验证与结论

- Linux app Debug根构建：14/14 tests通过（`proc_39ef`）。
- 原程序动态差分：`blocked_runtime_oracle`。

```text
closure_status = platform_adapted
final_review = converged_no_new_differences
remaining =
```
