# 函数证据：`sub_2F6E3` `0x2F6E3..0x2F721`

状态：`implemented_pending_review`

KDEF opcode37。机器码先对 role0 record word56（`morality`）执行 16-bit `add word`，再以 signed 比较依次 clamp 到 `0..100`。C++ `SceneSession::run_event(case37)` 使用共享 `clamped_add`；该 helper 已显式先按 uint16 回绕为 int16，再 clamp，避免提升为 int 后错误跳过原回绕。

真实 script149 `37,-5` 已固定 normal `3->0` 和回绕反例 `-32768 + -5 -> 32763 -> 100`。独立 B7 oracle 同步计算该向量；Linux Debug `openlegend.scene` 通过。最终双向 REVIEW 尚未执行。
