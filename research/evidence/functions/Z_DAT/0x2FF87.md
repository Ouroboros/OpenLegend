# 函数证据：`sub_2FF87` `0x2FF87..0x2FFB3`

状态：`implemented_pending_review`

KDEF opcode51。机器码调用原 RNG `sub_3D612(18)`，把结果加2547作为 TALK ID，并以 head114、style0进入标准对话 helper；随后回到解释器公共尾部。

C++ `SceneSession::run_event(case51)` 以 `random_.bounded(18)` 消耗一次相同 LCG，并排队同一 TALK/head/style。当前 KDEF 中 opcode51 仅真实 script692 一处。seed1 固定 RNG state `0x41c67ea6`、余数8、TALK2555；独立 B7 oracle 同步计算该向量。Linux Debug `openlegend.scene` 通过；最终双向 REVIEW 尚未执行。
