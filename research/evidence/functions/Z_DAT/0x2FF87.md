# 函数证据：`sub_2FF87` `0x2FF87..0x2FFB3`

状态：`assembly_exact`；最终 REVIEW：`converged_no_new_differences`

来源：当前 `Z.DAT` 机器码与原始文件字节；headless IDA完整导出；当前 `KDEF.IDX/GRP`；`research/ida/reports/Z_DAT.b7_scene_xrefs.txt`。

## 1. 机器身份与caller

- 函数物理范围 `0x2FF87..0x2FFB3`，44 bytes，13条指令。
- loaded与原始文件 `Z.DAT[0x29987:0x299B3]` SHA256均为 `9cc9420a01f6dceb68971c812be2ae26a76ba555ae69381b9b34577d81e40d76`；差异字节和重定位均为零。
- 唯一物理代码caller是 `sub_2C319:0x2CAB7` 的opcode51；`0x55688`只是函数地址表数据引用。
- caller无脚本参数；调用后不使用返回值，经公共块把PC固定增加1 word。
- 入口 `sub_3ED1E(16)` 仅为Watcom栈探测合同。
- 函数末尾跳到共享机器尾块 `0x2DE4D`，该块固定置EAX为1并返回；此函数物理末端外的控制流已纳入本次callee审计。

## 2. 不看C++的随机对话恢复

1. 以常量18调用已终审的原RNG边界helper `sub_3D612`，只消费一次底层随机状态并取得0..17的AX余数。
2. `cwde`把AX sign-extend到EAX，再加常量2547，因此TALK编号范围严格为2547..2564。
3. 依次压入style0、头像114和计算出的TALK编号，调用标准对话helper `sub_2CC21`。
4. 对话helper返回后清理12字节参数栈，尾跳到共享块并固定返回1；无第二次随机调用、重试、状态写入或额外present。

## 3. 汇编→C++ REVIEW

首轮及从入口复核均确认C++ opcode51固定调用一次 `random_.bounded(18)`，将结果加2547，并以head114、style0排队标准对话；PC固定增加1。没有产品代码差异。

从入口重新逐条复核13条指令、零重定位、随机上界、单次调用、AX符号扩展、TALK基数、三个对话参数、参数栈清理、外部共享尾块、返回1和caller PC，零新增差异。RNG核心和标准对话内部资源、绘制、分页与宿主运输分别由其既有closure负责，不向本行传播；本行编排归类 `assembly_exact`。

## 4. 全KDEF与确定性oracle

当前1,018条KDEF只有script692 PC0一次opcode51。按little-endian `<II>` 编码 `(script,PC)` 的完整调用流SHA256为 `d3df67cedcb65da7e8e0c36e0a7a9eb1a1feca8b209e83a9ca4eba99bde3b8ed`。

seed1时唯一一次LCG更新后状态为 `0x41c67ea6`，15位输出对18取余为8，故固定选择TALK2555、头像114、style0；真实场景测试同时断言四项结果。独立oracle双次生成与正式基线逐字节一致；正式 `scene-goldens.json` SHA256 `0d0bce29ae7fc1cfc6d08c660e2df310835adc5b0b6b9596b874d843dfef011a`。

## 5. 验证与结论

- Linux app Debug根构建：14/14 tests通过（`proc_f713`）。
- 原程序动态差分：`blocked_runtime_oracle`。

```text
closure_status = assembly_exact
final_review = converged_no_new_differences
remaining =
```
