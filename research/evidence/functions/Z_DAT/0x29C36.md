# 函数证据：`sub_29C36` `0x29C36..0x29D2D`

状态：`implemented_pending_review`

机器码无显式参数。入口先调用 `sub_29D2D` 重绘场景并调用呈现 helper；随后按 direction0/1/2/3 计算正前方 `(x,y-1)/(x+1,y)/(x-1,y)/(x,y+1)`。目标事件层为 `-1` 时直接返回；否则保存 event index 与目标坐标到事件上下文，读取事件 word2（event_1），仅当 script id `>0` 时调用 `sub_2C319`。无事件、非正脚本和脚本返回是全部出口。

现代 `SceneSession::interact/prepare_event/resume` 已用 `present` continuation 保留“先重绘呈现、后运行脚本”的同步边界；有事件的 synthetic script825 固定输出顺序 `present -> notice_style_52`，空目标也固定 `present -> stay`。最终双向 REVIEW 尚未执行。
