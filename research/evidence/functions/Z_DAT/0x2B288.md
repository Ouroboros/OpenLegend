# 函数证据：`sub_2B288` `0x2B288..0x2B308`

状态：`implemented_pending_review`

机器码先把当前场景 x `word_D295C`、y `word_D295A` 复制到临时目标坐标，再按 `word_544F2` 调整相邻格：方向0令 y 减1，方向1令 x 加1，方向2令 x 减1，方向3令 y 加1；其他值保持当前格。最后把目标 x、y 传给 `sub_2B308`，并原样返回该函数的0/1结果。

正式 B7 报告确认唯一物理调用者是 `sub_2A86C` 的 `0x2AA83`。该调用链先从选中的连续库存槽读取 item ID，在 `0x2AA6C` 检查对应 item 记录的 `word_A2796`（现代 `item_word::item_type`）等于0，再以 item ID 设置事件上下文并调用本函数。物品页返回后 `sub_212C0` 从 `0x21438` 回到主菜单循环，而不是结束整个场景菜单。

现代 `GameMenuController` 在确认物品后恢复主菜单页；`LegacyGameRuntime::handle_game_menu_result` 以 `result.index` 读取 `RangerHeader::inventory_item`，只对合法且 `item_type==0` 的 item ID 调用 `SceneSession::use_menu_item`。该 continuation 先呈现被菜单遮住的场景，再由既有 `use_item` 按当前朝向探测相邻格；命中事件时保留 `sub_2B308` 的第二次前置呈现和脚本执行，完成后回到 `open_ui`，只有玩家退出主菜单时才继续原场景 tick。

测试固定了无事件时一次场景呈现后回菜单、script825 命中时两次场景呈现与 notice 后回菜单、退出菜单后才执行 tick 后处理；runtime 整链先确认真实首槽 item0/type3 不触发场景事件，再固定库存槽0转换为 item199，而不是把槽索引0误作 item ID。物品菜单切片提交时的报告 SHA256 为 `8827e8e8643a3a9b4a48488832c945a3f6d04415345fc1fd71d7557feb30aa58`；当时独立 oracle 两次字节一致生成，SHA256 为 `35490b9fd243cc27042aab7b4377530743c444dec520a500dc3e6b0fdce5a917`。最终双向 REVIEW 尚未执行。
