# 函数证据：`sub_33C4D` `0x33C4D..0x33E93`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::choose_ai_low_hp_action`。

当前原始`Z.DAT`物理范围为582 bytes、123条连续指令、24条分支、48处重定位和1个direct call；raw SHA256为`595da6f453cf6f208cd01f24f497f2810ffdd699432be270c03f32a334dce13f`，IDA加载后SHA256为`db1998f97df003535ed67236ef151a8ce0e4b5d9ac6fc4a9cc03ccce6ce1d3ed`。唯一caller为AI入口命中低生命门后的调用点；唯一直接调用是栈探测，不属于游戏逻辑。

按原顺序选择低生命响应。首先检查自身医术不少于20、体力不少于50且医术严格大于自身伤势减30；伤势与医术均先符号扩展到32位，减30不发生16位回绕。命中时保存actor自身坐标与目标槽，写动作5并返回5。

自疗失败后按side分流：side恰为0时按槽序扫描200个队伍inventory，任意非零side时扫描actor四个carried item槽。两条路径均只检查signed item id非负与物品`add_hp`严格大于0，完全不读取数量字段；首个命中保存来源槽、actor目标槽与坐标，写动作6并返回6。现代对超出item表的非法ID安全失败，属于合法域外平台适配。

物品未命中后按combatant槽序寻找同side、非自身、未隐藏，且医术严格大于20并严格大于actor伤势减30的角色；首个命中保存该角色坐标与目标槽，给actor写请求医疗动作8并返回8。全部未命中返回0，不写actor动作，也不清理机器中此前残留的目标共享状态；现代typed返回用空目标表达该不可用状态，动作0调用方不读取目标。

定向回归覆盖：医术21/伤势50/体力50自疗；最低边界医术20/伤势49/体力50自疗；医术20/伤势50的strict失败保持既有动作值；体力49拒绝；队伍inventory slot2与敌方carried slot2在数量0时仍按`add_hp=1`选择物品；普通求援选择slot1，slot1 hidden时跳过并选择slot2。正式battle golden由独立Python两次生成逐字节一致，只新增六项低生命边界，SHA256为`3e8c370da121fca85e3f2e77e4cf90856625862119a6248251d9fae61eac0e92`。

首次完整机器推导后逐块对照未发现产品实现差异，只发现边界覆盖不足；补测试与独立golden后再次从入口覆盖全部123条指令、24分支、48重定位、唯一caller、三类命中副作用和无动作尾，零新增差异或未决项。机器门、静态/golden门、closure门、逆向框架及根`./build.sh app --config Debug` 14/14均通过，归类`platform_adapted / converged_no_new_differences`。
