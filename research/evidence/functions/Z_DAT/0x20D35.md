# 函数证据：`main` `0x20D35..0x20FAA`

状态：`implemented_pending_review`

该629字节函数是完整程序总入口，不是单一场景函数。启动顺序为运行时/键盘/音频初始化、4,000,000字节传统 scratch 分配、80字节状态清零、两份 shadow mask 与世界资源载入、初始音乐和标题/新建/读档流程；主循环退出后依次关闭世界资源、淡出音乐、关闭音频、恢复运行平台并以状态0退出。

世界主循环每帧先保存 BIOS tick，然后按固定优先级只执行一个分支：left、up、down、right、Esc 菜单 edge、idle。四个方向分别读取并清零两份 translated-key 状态：left=`0x9A/0x9D`、up=`0x9E/0x9F`、down=`0x97/0x98`、right=`0x99/0x9C`；因此按住方向后不会在每个宿主帧自动移动，只有后续 make/repeat 再次置位才产生下一步。方向分支不会消费 Esc 的奇数 edge；若方向随后没有再次置位而 Esc 仍按下，后续 tick 才进入菜单。动作之后依次处理待载入槽、天气、条件待机动画、世界重绘/呈现；呈现后把计数加一并取模5，余数为1时将 palette entries 224..231 与 244..252 分别右旋一格，再等待 BIOS tick 值变化。现代必须让本帧先用旧 palette 呈现，旋转结果从下一帧生效。该相位由 `LegacyGameRuntime` 全局持有：进入 scene 时传入 `SceneSession`，每个原外层 scene present continuation 后同步回 runtime，世界/场景往返不重置，对话等待宿主帧也不额外推进。

现代映射跨 `src/platform/sdl3/main.cpp`、`LegacyGameRuntime::handle_world_input/advance/finish_presented_tick/render`、`LegacyKeyboard::world_direction/consume_world_direction/edge/consume_edge`、`WorldSession::idle_tick/periodic_tick/idle_animation_tick/cycle_palette`、scene present continuation 与 RAII 平台资源生命周期。SDL world 分支通过键盘边界按原优先级合并四组成对键态，并在调用移动前清除命中方向的整组状态；SDL repeat keydown 会重新置位并允许下一步。世界态 Esc 不立即派发，而把 odd edge 与方向状态一并交给 runtime；runtime 只在四方向均未命中时打开世界菜单并回报消费，SDL 随后清除 edge。传统固定 scratch、VGA/IRQ、BDA 自旋和 Miles 维护分别由容器、indexed framebuffer、SDL event pump、`SteadyBiosTickSource` 与异步音频后端适配。

独立 oracle 固定 `left→up→down→right→menu→idle` 六级优先级、四组成对键码、命中方向整组清零、repeat 再置位、菜单 edge 是否消费、帧尾/关闭顺序及 MMAP.COL palette hashes：首次与第五次呈现后均为 `0x898e23463574ae76`，第六次呈现后为 `0x6055f0cfd75adaa6`；app 真实资产测试固定“down+menu 同 tick 仍留在世界且不消费菜单，下一无方向 tick 才打开菜单”。该入口 closure 只确认现代顶层编排已有实现；它直接委托的标题/游戏菜单、世界移动/绘制、输入、文件、音频和平台函数仍按各自 inventory 保持 `pending_mapping` 或 `implemented_pending_review`，最终必须逐一完成双向 REVIEW。
