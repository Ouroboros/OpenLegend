# 函数证据：`sub_22090` `0x22090..0x22A59`

状态：`pending_implementation`

这是共享队伍成员选择器。参数2选择“要查閱誰的狀態”标题；扫描`word_C0B78`直到首个`<=0`哨兵得到可选前缀。每轮先调用战场/当前背景重画，再绘制标题框`(70,18,124,26)`和列表框`(70,45,62,20*count+10)`；角色名依据第一个NUL在最多六个Big5字符内居中，普通色`0x2321`，当前项重绘为`0x6663`。下/上分别在队伍前缀内回绕，Enter、Space、keypad Insert确认，Escape返回-1。

`BattleRenderer::render_character_status_selection`现单一实现参数2的圆角标题框、列表框、NUL居中和选中色；`BattleSession`与`LegacyGameRuntime`分别在战斗、世界和场景背景上调用。战斗选择框整帧独立原资产oracle与C++ FNV64均为`0xfa1b21403051335c`；世界路径另锁定整帧hash，场景路径逐像素确认框外背景不变。测试覆盖上下回绕、Escape取消、Enter/Space/keypad Insert三确认键、哨兵计数和不消费RNG。

参数2的所有三种背景上下文已实现；参数0/1/3..6调用点仍待完成，因此状态保持`pending_implementation`，最终双向逐基本块REVIEW仍为`not_started`。
