# 函数证据：`sub_22090` `0x22090..0x22A59`

状态：`implemented_pending_review`

这是共享队伍成员选择器。函数扫描`word_C0B78`直到首个`<=0`哨兵取得可选前缀；每轮按`word_C0BF2`重画世界、场景或战斗背景，绘制参数标题和队伍列表。下/上在队伍前缀内回绕，Enter、Space、keypad Insert确认，Escape返回-1。角色名依据前六个姓名字节中的首个NUL居中；普通色`0x2321`，当前项重绘为`0x6663`。

参数0标题为“要醫治誰”，标题框宽92，列表框`(70,45,118,20*count+30)`；先显示“生命點數”，再以`%3d/%3d`显示当前/最大生命。未选中当前生命按内伤0..33、34..66、>66使用`0x0705/0x0e10/0x1416`，斜线用`0x2322`；当前项全部重绘为`0x6663`。

参数1标题为“替誰解毒”，标题框宽92，列表框`(70,45,86,20*count+30)`；先显示“中毒程度”，再以`%3d`显示毒值。参数2标题为“要查閱誰的狀態”，标题框`(70,18,124,26)`、列表框`(70,45,62,20*count+10)`。参数3/4/5标题分别为“誰要配備”“誰要修練”“誰要使用”，参数6为“要求誰離隊”；四者均为纯姓名列表。参数3..5标题框宽度为动作标题与当前物品名字节长度之和乘8再加28，物品记录`+0x16`的secondary name固定绘于`(145,22)`、颜色`0x1014`；选择器只返回连续队伍前缀中的槽位。

`BattleRenderer::render_character_selection`单一实现参数0..6的圆角框、Big5字节标题、NUL居中、数值宽度及选中色。`BattleSession`使用参数2战斗路径；`LegacyGameRuntime`在世界/场景菜单中使用参数0..6。医疗/解毒外层由`GameMenuController`过滤能力`>=10`的施术者，再进入本函数等价目标状态；目标Escape返回外层原施术者光标。装备、修炼、消耗品与离队均由选择器返回队伍槽，真实状态写集分别留在`sub_2A86C`和`sub_25BBA`的现代映射中。

回归覆盖参数0/1的世界整帧、参数0场景两层选择及框外背景逐像素保持、参数2世界三帧和战斗独立资产oracle；参数3/4/5含物品名的目标帧FNV64分别为`0xa32562c2475b1d86`、`0x6ca2ec3987565f66`、`0x90778a5b1b2be465`，参数6离队选择帧为`0x0ae903ebb645f691`，并覆盖目标槽回传、主角提示及角色1真实脚本离队。上下回绕、Escape与三确认键仍由共享controller覆盖。实现审计曾发现并修正参数0/1数值缺少`%3d`前导空格。本切片通过Linux/Windows `core/app × Debug/Release`及Linux app ASan+UBSan门禁。当前实现状态为`implemented_pending_review`；最终双向逐基本块REVIEW仍为`not_started`。
