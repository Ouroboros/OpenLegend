# 函数证据：`sub_2DE03` `0x2DE03..0x2DE2C`

状态：`implemented_pending_review`

映射：`openlegend::scene::SceneSession`、`openlegend::app::LegacyGameRuntime::start_battle`、`openlegend::app::LegacyGameRuntime::finish_battle_if_ready`。

`tmp/Z_DAT.entry-result.txt`由当前`Z_DAT`数据库的`tmp/`副本通过`idat.exe -A`导出。逐指令合同为：把battle id与get-exp参数压栈，调用`sub_31C75`；`cmp eax,1`，仅相等时返回true offset参数，否则返回false offset参数。该wrapper不解释其他值，也没有独立Escape分支。

`sub_31C75`由raw结果1/2减1，只返回0失败或1胜利。现代runtime因此仅将`BattleStepResult::victory`恢复为`SceneResponse::battle_victory`，`BattleStepResult::defeat`恢复为`battle_defeat`；AI动作11不离开battle。Linux Debug完整BUILD 14/14；最终完整汇编↔C++双向REVIEW尚未开始。
