# 函数证据：`sub_39DA3` `0x39DA3..0x39E88`

状态：`implemented_pending_review`

映射：`BattleSetup::apply_detox_value`。

解毒值先按signed `detoxification/3` 向零截断，再严格依次调用两次 `LegacyRandom::bounded(10)`，计算 `quotient+first-second` 并按signed 16位夹到0..99。若目标毒值严格大于 `detoxification+20`，结果归零；随后再受目标当前毒值限制，并以16位减法写回。写回后毒值<0夹0，严格>100才夹99，因此恰等于100会保留，这是原机器码边界。

固定detoxification80、目标poison90、seed1：RNG输出 `[8,8]`、state2524885223，raw26、实际26、目标64；detoxification20且poison41触发严格门槛得到0；高能力最多解99；poison100保持100而101写成99。Linux Debug 14/14通过。现代实现只对坏combatant/role索引安全拒绝；最终仍需完整汇编↔C++ REVIEW。
