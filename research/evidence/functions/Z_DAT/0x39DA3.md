# 函数证据：`sub_39DA3` `0x39DA3..0x39E88`

状态：`platform_adapted / converged_no_new_differences`

映射：`openlegend::battle::apply_role_detox_value`、`BattleSetup::apply_detox_value`；菜单caller由`LegacyGameRuntime`按party role id调用共享核心，战斗caller由`BattleSetup::apply_detox_target`调用。

## 机器身份与owner边界

fresh临时数据库`tmp/Z_DAT-b8-order63.i64`仅由`/mnt/d/Dev/Crack/IDA/idat.exe -A`配合`tmp/ida_dump_b8_order63.py`分析。owned范围固定为229 bytes、54条连续指令、13个基本块、6个条件分支、0个无条件跳转、10处绝对数据fixup、3次direct call、2个入口xref和1个本地RET。raw SHA256为`d5eca3b1cc278ca4475476773089aaec09af21f540eb9901b161ffeb2d54589c`，IDA loaded SHA256为`8043f993618a3195701a6048029d0b3d1431b8699c8d75e475ffbfe6a83b91ea`。

10处fixup相对偏移为`0x019,0x063,0x075,0x090,0x099,0x0AB,0x0B2,0x0BC,0x0D0,0x0DA`；每处loaded DWORD均为raw `+0x20000`，逐处反变换后与原始229字节完全一致。3次调用依次为`0x39DA8->sub_3ED1E(12)`栈探测，以及`0x39DD0/0x39DDC->sub_3D612(10)`。入口xref为菜单`sub_21AC0:0x21C65`和战斗动作`sub_39B8E:0x39D2A`，没有外部xref进入内部基本块。

四个栈参数中只读取第三个signed actor role id与第四个signed target role id；前两个参数完全不读。菜单caller传`(-1,-1,actor_role,target_role)`，保存完整EAX供`%3d`结果框显示；战斗caller传`(actor_slot,target_slot,actor_role,target_role)`，将返回EAX复制到EDX后以DX写目标combatant damage word。两个caller、栈探测和RNG helper均为独立owner，不随本owner传播closure。

## 完整汇编合同

入口先按signed role id读取actor `detoxification` word，以`SAR EDX,31; IDIV 3`执行向零截断。随后无条件严格依次调用两次`sub_3D612(10)`，以32位顺序计算`quotient + first - second`；两次RNG发生在全部夹值、阈值、target cap和写回之前。

初始amount先以signed低16位执行`BX > 99`上限，再执行`BX < 0`下限。之后重新读取actor detoxification和target poison，以32位signed严格比较；仅当`poison > detoxification + 20`时清零完整EBX，等号保留amount。再重读target poison，以signed `BX > poison`比较；成立时只把poison word复制到BX。前序合法值保证EBX高16位为0，最终仍由`MOVSX EAX,BX`明确返回signed amount。

写回重读target role id后直接执行`SUB word_90174[eax],BX`，因此减法在16位word上回绕。紧接着重读signed poison，负值写0；随后再次按target role id重读signed poison，仅严格大于100时写99，恰等于100保持。最终返回cap后的signed BX，不按末次poison夹值反算实际delta。特别地，负初始poison会使target cap把该负word复制到BX，word减自身写成0，同时返回原负poison。

## 汇编→C++ REVIEW

从`0x39DA3`入口逐块对照`apply_role_detox_value`与两个现代caller。合法角色域的signed字段、向零`/3`、两次`bounded(10)`次序、加减、signed 0..99夹值、strict阈值、target cap、16位减法、两次写回后夹值及signed返回全部一致。现代在初始夹值前显式`wrapping_i16`；signed16 detoxification除3再加减两个0..9结果只落在`[-10931,10931]`，所以与机器第一次观察BX等价。

same-role alias在首次poison写入前已完成全部detoxification读取，现代引用别名不改变顺序。菜单路径直接以role id调用共享核心，保持机器两个dummy slot参数不读；战斗路径从合法combatant重读相同role id后调用核心。战斗只消费返回低16位，菜单把int16结果提升为int32，均与机器caller一致。

完整入口REVIEW未发现合法域产品差异，无需修改产品源码。现代对非法combatant slot或role id提前拒绝且不消费RNG，替代机器越界角色记录访问，归类`platform_adapted`。

## 回归、Golden与结论

独立向量覆盖正常值、strict阈值等号/上界、99上限、负能力、target cap、poison `0/-1/-32768/100/101/32767`、same-role alias和两次RNG状态，SHA256为`d7abe430906b406543b643d76ba19ea840790d7bbff7d20d4742708e99efc3ea`。固定detoxification80、poison90、seed1产生RNG`[8,8]`、state2524885223、返回26并写poison64；detoxification20与poison40保留amount6，poison41触发strict门槛返回0；poison-1/-32768分别返回-1/-32768并写0。

C++回归新增上述strict门槛、seed2 target cap、signed极值、负poison、poison100/101/32767、same-role alias及非法索引不消费RNG。正式Golden新增`battle_detox_value_machine`；两次临时生成及正式第三生成逐字节一致，历史56键逐值不变，正式文件SHA256为`4b5f54db596da762efb3a2f763f358b0f6c2e135ee1b97ffbbdc6a664fe08ea3`。统一Linux app Debug 14/14通过。

原程序动态差分继续登记为`blocked_runtime_oracle`；本owner结论为`platform_adapted / converged_no_new_differences`。
