# 函数证据：`sub_22A59` `0x22A59..0x2399E`

状态：`implemented_pending_review`

函数在每页入口按`word_C0BF2`调用`sub_2558B`、`sub_29D2D`或`sub_3AA85`重画世界、场景或战斗背景，再绘制圆角面板`(55,0,210,200)`、HDGRP头像`(78,68)`和角色名，随后连续显示两页；每页由`sub_20C32`读取任意非零键后才继续。

第一页显示等级、生命/上限、内力/上限、体力/100、经验、升级阈值，以及攻击、防御、轻功、医疗、用毒、解毒、拳掌、御剑、耍刀、特殊兵器、暗器。当前生命按内伤0/1..33/34..66/>66分色，最大生命按中毒0/1..49/>=50分色；内力类型0/1/2使用三组固定色，其他值复用前一中毒色。攻击、防御、轻功加上两件有效装备的对应属性；等级>=30显示等号，否则显示机器表阈值。

第二页显示两件装备次名称、修炼物次名称、当前修炼经验、所需经验或满级等号，以及最多十项已学武功名称和`unsigned(熟练度)/100+1`等级。修炼物无武功时保留`2*(7-IQ/15)*need_experience`分母；有武功且等级<9时为`(level+1)*(7-IQ/15)*need_experience`，否则显示满级等号。

`BattleRenderer::render_character_status`是角色状态的单一精确renderer。`BattleSession`在战斗背景上调用，`LegacyGameRuntime`在世界或场景背景重画后调用；旧`BasicUiRenderer`简化状态页及其重复标签/公式已删除。三类调用都保留第一页present后任意非零键翻页、第二页present后任意非零键返回的同步边界。

逐基本块回审曾发现并修正最大生命中毒色、修炼所需经验普通色和分母系数读取资质而非修炼经验三处差异。正式`research/tools/generate_b8_player_status_golden.py`独立读取WARFLD、WDX/WMP、HDGRP、FONT和palette资产复算，输出`research/evidence/battle-player-status-golden.json`；与C++整帧FNV64分别一致为`0x1c5e879ce61d5b34`和`0x9592da33a3c151d4`。世界路径固定独立运行态选择框和两页回归hash；场景路径确认中央面板外逐像素保持场景背景。

世界、场景和战斗调用上下文均已实现，状态提升为`implemented_pending_review`；最终双向逐基本块REVIEW仍为`not_started`。
