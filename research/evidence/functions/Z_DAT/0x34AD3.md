# 函数证据：`sub_34AD3` `0x34AD3..0x34AEC`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::rest_actor`及`BattleSession::finish_ai_handler`的休息接线。

## 机器身份

- 25 bytes、7条指令、0条分支、0处重定位；raw/loaded SHA256均为`2aad66b5c996733006f51f3887c3d1eff17c7e7b8324c0207851533ce2453ca3`。
- 六个direct caller分别位于AI动作0/7 dispatch、逃跑/物品重定位尾、攻击回退、用毒回退、医疗回退和解毒回退。
- 两个direct call依次为Watcom栈检查`sub_3ED1E`和休息状态核心`sub_3A8A4`；两个callee均保留各自owner，不随本项传播closure。
- 原程序动态差分仍登记为`blocked_runtime_oracle`；本项结论来自原始`Z.DAT`完整机器码、caller上下文和独立原资产golden。

## 汇编合同与现代映射

函数执行`push 8; call sub_3ED1E`作8字节Watcom栈空间检查，随后从`[esp+4]`读取actor slot并以signed int16扩展，原样压栈调用一次`sub_3A8A4`。返回后只回收该参数并立即返回，EAX不再改写，故原样返回休息核心结果。函数自身没有条件、循环、全局读写、RNG、绘制、present、等待、音频或第二个业务出口。

现代C++省略编译器栈检查，并将该wrapper与`sub_3A8A4`合并映射到受检`BattleSetup::rest_actor`。AI动作none/wait、逃跑结束、攻击/用毒/医疗/解毒回退均通过`finish_ai_handler(..., true)`先调用`rest_actor`，再调用`finish_ai_turn`；这保留机器休息核心先写action-done、caller或统一尾再次写入的顺序。逃跑/物品重定位的原始参数0/非0由语义化`rest_after_move`正向表示，逃跑为true、物品重定位为false。非法slot/role安全失败属于合法域外平台适配。

`rest_actor`内部体力、HP、MP、三次RNG和夹取规则归属`sub_3A8A4`（B8 order69），不得由本wrapper关闭；既有ready/tired向量仅证明委托路径可运行。

## REVIEW与验证

完整汇编→C++ REVIEW覆盖全部7条指令、0分支、0重定位、两个call、六个caller及唯一出口；最终从入口复核零新增差异，产品代码无需修正。

独立生成器直接从原始`Z.DAT`固定25字节、两个调用目标及六个caller，双生成逐字节一致；正式battle golden SHA256为`d5ed45e73b16ac4c47d99b4db73d0ef79d21271142dc508f651f8178fd0fc555`。机器、静态/golden、closure、reverse framework与根Linux Debug 14/14均通过。
