# 函数证据：`sub_34AD3` `0x34AD3..0x34AEC`

状态：`implemented_pending_review`

映射：`BattleSetup::rest_actor`。

函数仅执行8字节原栈清理后调用`sub_3A8A4(actor_slot)`并原样返回。现代C++没有对应的遗留栈窗口，行为全部由已恢复的`rest_actor`覆盖：先写action_done，再按round value、速度、体力和三次短路RNG恢复体力/HP/MP。因此该25字节wrapper不需要额外状态。

既有休息oracle、状态测试和Linux Debug 14/14通过；最终仍需完整汇编↔C++ REVIEW。
