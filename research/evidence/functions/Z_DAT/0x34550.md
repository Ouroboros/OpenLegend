# 函数证据：`sub_34550` `0x34550..0x34AD3`

状态：`implemented_pending_review`

映射：`BattleSetup::choose_ai_offensive_action`。

函数使用全部combatant的`HP+attack`按side以int16逐次回绕累计，不排除隐藏或已死亡槽。仅当`(敌方总值/敌数)/2 > actor(HP+attack)`且己方总值严格大于敌方总值两倍时进入援助：actor医术和体力均不少于20/50时选择缺HP最多的未隐藏同伴写动作5；否则仅在解毒和体力均不少于20/50时选择中毒值最大的未隐藏同伴写动作4；同值不替换早槽。

无援助后固定先消费`bounded(50)`；`use_poison-attack`严格大于结果时再消费`bounded(150)`，结果严格小于use_poison才写动作3。之后按side扫描物品：队伍暗器负`add_hp`绝对值须严格大于`3*attack/2`且`bounded(hidden_weapon)>20`，正`add_poison`同门槛且`bounded(10)<3`；敌方随身物品门槛改为attack，负HP分支`bounded(10)<6`、正毒分支`<3`。首个命中写动作10与物品来源/槽位。

无暗器时体力<=10返回0；否则扫描10武功槽中非零ID的最小need_mp，当前MP不少于该值才返回动作2。动作2只返回、不写word10；动作3/4/5/10按原写入。独立向量覆盖最大缺HP目标、用毒RNG`[38,58]`、队伍暗器RNG`[38,58]`和仅返回攻击；Linux Debug 14/14通过。最终仍需完整汇编↔C++ REVIEW。
