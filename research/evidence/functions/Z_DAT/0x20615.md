# 函数证据：`sub_20615` `0x20615..0x20663`

状态：`implemented_pending_review`

机器码从 `base + y*320 + x` 开始，逐行读取16个 Big5 glyph word；每个置位 bit 以小端 word 写入“前景、右侧阴影”两个相邻字节。每行处理16像素后地址加304，总步长320。函数不检查 x/y 或屏幕边界，因此横向超过319时按线性 framebuffer 地址落到后续扫描线。

现代场景专用 `draw_dialogue_text` 保留该线性地址语义和双字节写序；通用 UI glyph renderer 的安全边界不改变。真实 talk1841 的344像素行独立帧 hash 为 `0xf420561dcb42e981`。最终双向 REVIEW 尚未执行。
