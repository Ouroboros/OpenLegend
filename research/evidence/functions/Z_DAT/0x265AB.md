# 函数证据：`sub_265AB` `0x265AB..0x269AB`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；审计包 `tmp/b9-review/0x265AB.txt`

## 1. 独立机器恢复

成功保存的机器顺序固定为：

1. 将活动scene-map工作记录写入编号 `S*`；
2. 将活动scene-event工作记录写入编号 `D*`；
3. 把运行态header、六名队员和200组背包值写回ranger缓冲；
4. 将ranger记录写入编号 `R*`。

world边界同步主地图坐标、方向、船坐标、船图和原始 `in_ship`；scene边界保留scene方向。`in_sub_map`不在该同步块被额外改写，编号槽IDX不参与。

## 2. 汇编 → C++ 映射

- S/D先写：`persistence::write_numbered_slot_scene_archives`。
- 运行态header/team/inventory同步：`world::WorldSession::sync_persistent_state`、`model::GameState::export_snapshot`。
- R后写：`persistence::write_numbered_slot_ranger`。
- 三者编排：`app::LegacyGameRuntime::perform_pending_io`。
- 直接完整槽写：`persistence::write_numbered_slot`保持同样S→D→R运输顺序。

## 3. 差异与整轮重启

实现阶段已将原先过早写header的路径改为保存边界同步，并拆分writer以保证S/D完成后才同步运行态并写R。最终轮从 `0x265AB` 入口独立恢复，确认S→D→运行态写回→R、world/scene字段边界、原始 `in_ship` 和IDX不参与，零新增差异。

## 4. 平台适配

- 会话期 `ALLSINBK/ALLDEFBK` 工作副本由内存snapshot代替。
- 现代writer使用受检完整文件写与错误报告，不复现DOS部分写损坏副作用。
- 合法存档域的记录内容、字段宽度和提交顺序保留。

## 5. 验证与结论

- Linux app Debug：14/14。
- persistence/UI回归覆盖S/D阶段R不变、保存边界header同步、完整往返及写失败报告。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
