# 函数证据：`sub_3AA17` `0x3AA17..0x3AA4B`

状态：`implemented_pending_review`

映射：`BattleSetup::defer_turn_to_end`。

输入为当前combatant slot。函数从该slot开始，依次以`sub_32B78(current,current+1)`交换相邻槽，直到当前角色被移动到`combatant_count-1`，返回最终slot。交换完整沿用已恢复的14-word布局、word8跳过暂存、两次occupancy写入和sprite重算顺序；函数本身不写`action_done`。

真实battle3在完成队伍选择后顺序`[0,101,102,103,104]`，从slot1等待后得到`[0,102,103,104,101]`并返回4。Linux Debug完整测试通过；最终仍需完整汇编↔C++ REVIEW。
