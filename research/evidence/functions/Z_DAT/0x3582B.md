# 函数证据：`sub_3582B` `0x3582B..0x3598C`

状态：`implemented_pending_review`

部分映射：`BattleSetup::begin_ai_throwing_weapon_plan`、`resume_ai_throwing_weapon_after_move`。

函数先调用`sub_3505B`重选攻击目标，再将actor hidden_weapon signed除15加1作为暗器射程。首次targeting距离在射程内立即调用`sub_3598C(actor,1)`；超距且round value>0时请求movement mode1，随后只复检原目标，不重选；仍超距回退`sub_34C47`自动攻击。round value<=0时跳过移动但真实重复第二次targeting检查。

固定hidden_weapon80得射程6；最近目标slot3距离6立即使用且无RNG。morality75、seed9输出2并选attack更高slot4，距离8时round3请求移动；未改变位置后第二次检查回退攻击，移动到`(13,23)`后距离2则使用。最高攻击策略门槛命中但attack均为0时不写word11，原版继续使用既有stale slot；现代保留合法stale slot，slot无效时安全拒绝原线性越界。

`BattleSession`保持原目标执行第一次射程检查；超距且行动值为正时按mode1逐格执行状态、render/present与两次BIOS tick变化，移动后复检同一目标。入射程则进入AI独立owner的暗器前奏/EFT/damage同步链并延后扣减来源槽；仍超距则不消费暗器，调用完整自动攻击入口。真实WARFLD回归覆盖直接、移动后命中及移动后二次超距攻击finish，固定移动后首EFT像素hash为`0x16a8f10ce319622b`。Linux Debug完整BUILD全部14项测试通过；最终完整汇编↔C++ REVIEW仍待执行。
