# 函数证据：`sub_2B483` `0x2B483..0x2BD8B`

状态：`platform_adapted`

现代映射：共享核心`battle::apply_role_item_effect`，调用者为`BattleSetup::apply_ai_item_effect`、`apply_player_item_effect`及`LegacyGameRuntime::handle_menu_item_result`；显示由`BattleSession`和`BattleRenderer::render_item_effect`承接。

参数依次为actor角色、target角色、item；函数建立23项效果数组。正add_hp先消费`bounded(10)`，仅当`add_hp-target.hurt/2+rng<0`时再消费`bounded(5)`并改为`rng+5`；负add_hp先算`add_hp+50-target.hurt/2-bounded(10)`，仅当结果严格大于0时再消费`bounded(5)`并改为`-5-rng`，最后减`3*actor.hidden_weapon`后除3。两分支按各自公式改hurt，HP写signed word后夹0..maximum_hp，数组记录实际HP差。

最大HP/MP写signed word后夹0..999并回夹当前值；负add_poison固定消费两次`bounded(5)`，正add_poison使用`((actor.hidden_weapon+add_poison)/2-target.anti_poison)/2`且anti_poison>=100或中间负值时归零；体力夹0..100；MP夹0..maximum_mp；change_mp_type仅值2生效。attack到knowledge共13项直接signed相加。原版虽把add_morality和add_attack_twice写入显示数组并计为效果，却不写角色morality或attack_twice；add_attack_with_poison正常写角色字段。

真实item19在seed1下得到HP100→200、hurt40→0、poison50→0、体力30→100、MP10→100，效果数组非零项为`[100,-50,70,90]`，三次RNG后终态662824084。真实item91仅产生add_attack_twice显示值1，角色attack_twice保持4且不消费RNG；真实item40把MP类型写2并把最大MP100→115。

有非零效果时，battle context先重绘战场，再绘制`(70,18,148,20*effect_count+30)`面板、item名`(75,25)`和逐项行，并present；玩家共享选择器随后扣库存并以`sub_20C32`等待任意键。现代玩家type3路径已实际执行该面板、present、库存提交和任意键continuation，单项HP效果整帧FNV64为`0xd518fb664f3e0e3c`。23项全零时函数不画面板且返回0；玩家选择器仍以入口结果1结束actor，但不扣库存、不等待第二键，现代回归已锁定这一边界。玩家适配层还保留入口battle effect数组，不引入AI target标记。

AI调用者`sub_3598C`在共享函数后仍无条件调用`sub_3DB83(340)`，即等待`340/40+1=9`次tick变化，再按side扣队伍库存或敌方携带槽；该AI面板/present/tick continuation与队伍库存/敌方携带槽提交已由现有battle session覆盖。非战斗菜单仅在`has_effect`时提交库存和进入任意键效果页，保持参数5外层边界。## 最终独立REVIEW

fresh IDA从只读原始`Z.DAT`重建`0x2B483..0x2BD8B`：2312 bytes、474条指令、85个基本块、47条分支、21个call、单一`RETN`，loaded SHA256=`275f0bc18a222c7e03976826e89183474b53b6ba28bab1be2fb7f908c26a8e76`。四个caller严格为`0x2B1A7/0x2B1F8`（`sub_2A86C`）和`0x35A27/0x35A8E`（`sub_3598C`），没有用caller closure替代本函数审计。

六个RNG callsite严格位于正/负HP和负poison条件路径；后续调用顺序依次覆盖world/scene/battle重绘、面板、item名/数值格式化、完整palette/frame present。逐项正向对照`apply_role_item_effect`确认23个字段、signed word回绕、夹值、RNG次数/次序和两个只显示不写回字段均一致；反向从现代每个分支回到机器入口，确认显示、等待、9 tick延迟和库存/携带槽扣除均仍由各自caller continuation独立承接。

合法资产/状态域未发现新差异。现代对非法role/item索引的安全拒绝、SDL present失败不提交以及DOS/VGA显示时序替代属于`platform_adapted`。最终全入口重审状态为`converged_no_new_differences`。
