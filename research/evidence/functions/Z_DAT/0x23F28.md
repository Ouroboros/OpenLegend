# 函数证据：`sub_23F28` `0x23F28..0x241DC`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；Y轴世界移动入口。

独立汇编恢复：结构与X轴入口对应，方向0/3改变Y，步行frame offset按2推进并在`>12`回2，船offset在`>6`回2。候选Y在0..479内，入口探测先于碰撞提交；返回后继续陆地/船碰撞。成功路径先完成坐标/船状态与cache提交，再按Y轴公式平移天气；失败保留位置但提交方向/动画。

现代映射：`WorldSession::move/complete_move`以`delta_y!=0`选择Y轴cache重载和天气公式；typed continuation同时覆盖Y轴。成功移动内部顺序经机器审计固定为50次party衰减在动画推进前、cache重载在天气平移前。

向量覆盖up/down、0/479边界、11/98 cache-local阈值、船移动/下船及blocked。最终双向逐基本块REVIEW为`not_started`。