# 函数证据：`sub_31945` `0x31945..0x31C2F`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；KDEF opcode65，仅 script939。

## 汇编独立合同

先按当前 scene 隐藏商人事件，fields0..7=`0,0,-1,-1,-1,-1,-1,-1`，picture_delay/x/y 不变：

```text
scene0: none
scene1: 16,17,18
scene3: 14,15,16
scene40: 20,21,22
scene60: 16,17,18
scene61: 9,10,11,12
```

再无条件消费一次 `sub_3D612(5)`，按0..4选择 `(1,16),(3,14),(40,20),(60,16),(61,9)`，把目标 fields0..7 写为 `1,1,938,-1,-1,8256,8256,8256`。

旧实现错误地隐藏 scene0/event16、scene1 只隐藏 event16，且 hide 只写 fields0/1，遗留旧 scripts/pictures。

## 测试

对六个 scene 预置 fields0..7=777；seed1 固定 random value3、state `0x41c67ea6`，因此激活 scene60/event16。核对 scene0 完全不隐藏、其余范围完整清字段、scene60/event16 先隐藏后激活。独立 golden 固定所有范围与字段。Linux Debug scene/ui 通过；最终双向 REVIEW 尚未执行。
