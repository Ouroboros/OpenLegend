# 函数证据：`sub_39B1F` `0x39B1F..0x39B8E`

状态：`platform_adapted`

映射：`BattleSetup::detox_targeting_range`与`BattleSession`玩家解毒目标选择wrapper。

## 机器身份、入口与owner边界

- fresh临时IDB导出完整`111` bytes、`36`条指令、`3`个基本块、`1`个条件分支、`0`个无条件跳转、`2`处32位重定位、`3`次direct call、`1`个caller和`2`个本地RET。
- 原文件偏移`0x3351F`；raw SHA256为`349ab646a10b3be27c00affb26813a08c15e8454ec05bcd06f1864b6cfe9ca57`，加载重定位后SHA256为`b5814edf073c37c1db1d1b2e7507103140891cceec07247d7ded61d8aab2d24d`。fixup位于函数偏移`0x20/0x2D`，加载值均比原文件大`0x20000`，逐处逆变换后完整111字节与只读`Z.DAT`一致。
- 三个call依次为`0x39B24 -> sub_3ED1E`的28-byte栈探测、`0x39B65 -> sub_36AF7`同步目标光标、`0x39B7F -> sub_39B8E`解毒动作。
- 唯一caller为`sub_32E59:0x3338E`动作菜单case3；caller无条件跳到`0x333DB`公共参数回收，随后从actor重新计算战斗员地址并检查action-done，因此完全忽略wrapper的EAX。
- `sub_36AF7`光标循环已由B8 order38的battle owner独立关闭；`sub_39B8E`状态/动画/提交属于B8 order62；栈探测和caller同样不传播本行closure。

## 完整机器合同

1. 入口把四字节local初始化为0，并把actor参数低16位sign-extend。以`actor*28`读取战斗员signed role word，再以`role*182+172`读取signed `detoxification` word。
2. `IDIV 15`按signed向零截断，商加1后执行`CWDE`。完整signed skill域结果为`trunc(detoxification/15)+1`，范围`-2183..2185`；正常`0..100`域为`1..7`。
3. 参数按`actor, range, mode=1, &local`传给`sub_36AF7`。本体直接RNG消费为0。
4. selector返回后仅比较local低16位是否**恰等于1**：等于1从`0x39B7D`返回EAX=-1，不调用解毒动作；所有其他低字值进入确认边，调用`sub_39B8E(actor)`，忽略callee返回并从`0x39B8D`返回EAX=0。
5. 正常`sub_36AF7`入口先把取消word清0，Escape写1，合法确认保持0；所以合法域只有上述取消/确认两条出口。异常的其他local值不属于同步callee正常合同。

## 汇编到现代实现逐块对照

- `BattleSetup::detox_targeting_range`逐项保持actor→role→`detoxification`、signed C++向零除15、加1与16位结果；`BattleSession::begin_player_targeting`只在玩家动作case3调用该射程，并固定进入`BattleCursorSelectionMode::targeting`。
- 已关闭的`begin_cursor_selection/apply_cursor_selection`保持机器mode1光标合同、入口两次present以及每次后续输入前一次present。Escape产生cancelled，Session清path/cursor后经`finish_player_action_call`回到同一菜单ordinal；action-done仍为0，与唯一机器caller忽略-1后检查word7的路径一致。
- 确认产生selected并立即把当前cursor传入`begin_player_target_effect(detoxification)`；其首个战斗状态调用为`apply_detox_target`，对应机器立即调用`sub_39B8E`。现代将同步callee拆成可呈现continuation；`sub_39B8E`内部状态、RNG、动画、音频与action-done必须在order62独立审计。
- 从入口逐块覆盖全部36条指令、唯一分支、两个RET、三次call与唯一caller，合法域零产品差异；新增判别性回归后重新核对完整入口合同，仍无新增差异。现代对非法actor或role安全返回`nullopt`，替代机器越界读取，归类平台适配。

## 判别性回归与验证

- 新增signed射程向量：`-32768→-2183`、`-15→0`、`-14/0/14→1`、`15→2`、`89→6`、`90→7`、`32767→2185`；非法actor与负role安全拒绝。
- 既有真实battle4 Session回归锁定detoxification30进入mode1选择，入口present门和Escape回原菜单ordinal1；完整玩家support effect回归锁定确认后立即扣毒，继而执行9帧magic、10帧damage、抑制闪烁、音频与动作完成。
- 正式Golden由两次独立原资产生成逐字节一致后更新，再以第三次生成核对；formula vector SHA256为`e91e030c5e87ada68a90af4c50f221dc8f73c2579a6531bcf97e3f0933bb0d0f`，正式文件SHA256为`86bf13f29fa1f1cb6f0a26c85010d73a65757390026d1a89da5401fed6050ff3`。
- 正式证据与Golden闭包后，根`./build.sh app --config Debug`以Clang 23精确通过`100% tests passed, 0 tests failed out of 14`。
- 最终归类：`platform_adapted / converged_no_new_differences`。
