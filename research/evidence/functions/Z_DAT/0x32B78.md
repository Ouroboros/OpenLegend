# 函数证据：`sub_32B78` `0x32B78..0x32E2F`

状态：`implemented_pending_review`

映射：`openlegend::battle::BattleSetup::swap_combatants`

输入为两个 signed 16位 slot。函数先暂存第一个槽的 word0..7、9..13，明确不暂存 word8；再按同一字段集合把第二槽复制到第一槽，立即按新第一槽的 word2/3 坐标更新 occupancy：word5非0写-1，否则写第一 slot。随后把暂存字段写入第二槽，并以相同规则立即更新第二 occupancy。最后分别调用 `sub_3B1E6` 重算两槽 word8。

因此不能把它简化为只交换 role id，也不能在交换前统一重建 occupancy；同格或 hidden 槽时两次写入的先后可观察。C++ 保留“第一槽复制→第一占位→第二槽复制→第二占位→两次 sprite 重算”顺序。测试覆盖 active/hidden 交换后 party cell=slot0、enemy cell=-1 与两个 sprite 值；Linux Debug 14/14。
