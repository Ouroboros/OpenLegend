# 函数证据：`sub_32B78` `0x32B78..0x32E2F`

状态：`platform_adapted / converged_no_new_differences`

映射：`openlegend::battle::BattleSetup::swap_combatants`。

## 1. 机器identity

695字节、131条指令、4个跳转、68项`raw+0x20000`重定位。raw SHA256为`d2dc4bd1aa1cedeef1cd85a5a4d15385d2ba4b52986a639901ebfe5eb511c8b9`，loaded为`9b65a53377fc8a303ca17e1d22ffab6bc2132106bda434a63b27ef7a9c6f1032`。caller为排序的`0x32B52`和等待动作逐槽后移的`0x3AA30`；直接调用为栈检查和两次`sub_3B1E6`。

## 2. 独立字段与顺序合同

两个参数只取低16位signed槽号，各乘28得到记录。先暂存第一槽的word0..7、9..13，共13个word，明确不保存word8；word13保留在CX，其余通过栈局部保存低16位。

随后严格执行：

1. 把第二槽上述13个word按序写第一槽。
2. 读取新第一槽的word5；为0时向`occupancy[y*64+x]`写第一槽号，否则写-1。x/y均signed扩展，字节寻址为`y*128+x*2`。
3. 将暂存的13个word写第二槽，再按新第二槽word5执行同样occupancy写入。
4. 先调用`sub_3B1E6(first)`并将AX写第一槽word8，再调用`sub_3B1E6(second)`并将AX写第二槽word8。

同格占位取第二次写值，即第二槽visible写second，hidden写-1；不能交换整条记录、保留旧word8、按坐标去重或重建占位表。等待callee的后移循环只证明此函数另一个调用场景，不随本行关闭。

## 3. 映射与验证

C++按原13字段集合执行第一复制/第一占位/第二复制/第二占位/两次精灵重算。现代槽位由受检排序和延后动作提供，原资产坐标及槽号均在有效范围；容器所有权和外围错误处理归类`platform_adapted`，本函数合法调用顺序无新增差异。

新增回归逐一比较全部13字段，证明word8不是旧精灵交换而是重新计算，并覆盖同格visible/hidden后写分别为1和-1。原独立向量保留异格active/hidden交换与5118/5132精灵；新增golden读取原角色10/20的头像及Z.DAT初始精灵常量，得到交换后5272/5190，作为callee调用结果的独立样例，不提前关闭其全部内部语义。

机器identity/重定位/两caller/三call、独立golden双生成和C++静态门通过；Linux app Debug 14/14（`proc_0899`）。从入口、全部字段读写、四个占位分支及两个callee顺序重新审计，零新增差异。`sub_3B1E6`、`sub_3AA17`仍保留独立closure；原程序动态差分为`blocked_runtime_oracle`。
