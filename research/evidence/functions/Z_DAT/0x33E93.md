# 函数证据：`sub_33E93` `0x33E93..0x340D9`

状态：`implemented_pending_review`

映射：`BattleSetup::choose_ai_poisoned_action`。

解毒严格门槛为：解毒能力>20、解毒能力>自身中毒值-30、体力>50，命中时选择自身并写动作4。否则队伍方扫描200库存槽，但原版检查的是item word56 `add_use_poison<0`；敌方扫描4个随身物品槽并检查item word47 `add_poison<0`。该side分支字段不一致原样保留，首个命中写动作6。仍无物品时按槽序找同side未隐藏角色，要求其解毒能力>20且>actor中毒值-30，写请求解毒动作9。

测试明确验证仅word47为负时队伍方不选、word56为负才选，而敌方随身物品使用word47；独立oracle登记同一差异。Linux Debug 14/14通过；最终仍需完整汇编↔C++ REVIEW。
