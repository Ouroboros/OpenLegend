# 函数证据：`sub_33E93` `0x33E93..0x340D9`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::choose_ai_poisoned_action`。

当前原始`Z.DAT`物理范围为582 bytes、118条连续指令、25条分支、48处重定位和1个direct call；raw SHA256为`a8e9fb5b86bdb21ca0299383ee5392db200bfec3e1592b0b2d2a4ab81d157357`，IDA加载后SHA256为`5e0980bfc12ad034b1c6d3ab632793a370c2fc2c3cf371896f29cdc2c0d2e502`。唯一caller为AI入口命中中毒门后的调用点；唯一直接调用是栈探测，不属于游戏逻辑。动作4/6/9与无动作分别借用前一函数的成功/清零共享返回尾；该尾仅作为本函数返回行为证据，不传播前一owner的关闭结论。

按原顺序选择中毒响应。首先要求自身解毒能力严格大于20、严格大于自身中毒值减30，且体力严格大于50；中毒值与解毒能力均先符号扩展到32位，减30不发生16位回绕。命中时保存actor自身坐标与目标槽，写动作4并经共享成功尾返回4。

自解毒失败后按side分流：side恰为0时按槽序扫描200个队伍inventory，检查item word56 `add_use_poison<0`；任意非零side时扫描actor四个carried item槽，检查item word47 `add_poison<0`。该字段分歧原样保留。两条路径均完全不读取数量字段；首个命中保存来源槽、actor目标槽与坐标，写动作6并返回6。现代对超出item表的非法ID安全失败，属于合法域外平台适配。

物品未命中后按combatant槽序寻找同side、非自身、未隐藏，且解毒能力严格大于20并严格大于actor中毒值减30的角色；首个命中保存该角色坐标与目标槽，给actor写请求解毒动作9并返回9。全部未命中经共享清零尾返回0，不写actor动作，也不清理机器中此前残留的目标共享状态；现代typed返回用空目标表达该不可用状态，动作0调用方不读取目标。

定向回归覆盖：普通自解毒；三项strict门最低通过；能力20、相对门恰等于及体力50分别拒绝；队伍inventory slot1只认word56负值且quantity=0仍选；actor side=-1证明任意非零side走carried路径、只认word47负值且quantity=0仍选；普通求援选择slot1，slot1 hidden或不同side时跳过并选择slot2；求援能力20与相对门恰等于分别拒绝；两条无动作路径保留既有动作值。正式battle golden由独立Python两次生成逐字节一致，只新增十一项中毒边界，SHA256为`ea25ce663fa44d759d5b067850ca75c68362985433d550c7d08a1ea6abddcbfa`。

首次完整机器推导后逐块对照未发现产品实现差异，只发现边界覆盖不足；补测试与独立golden后再次从入口覆盖全部118条指令、25分支、48重定位、唯一caller、三类命中副作用和无动作共享尾，零新增差异或未决项。机器门、静态/golden门、closure门、逆向框架及根`./build.sh app --config Debug` 14/14均通过，归类`platform_adapted / converged_no_new_differences`。
