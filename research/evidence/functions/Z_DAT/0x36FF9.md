# 函数证据：`sub_36FF9` `0x36FF9..0x37070`

状态：`implemented_pending_review`

映射：`BattlePathing::initialize(BattlePathMode::targeting)`。

64×64逐格只检查 WARFLD upper layer：非0写555，否则254；不检查 occupancy 和 ground tile 区间。由 `sub_36E7F` 用于攻击/目标路径距离。
