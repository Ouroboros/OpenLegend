# 函数证据：`sub_3B1E6` `0x3B1E6..0x3B238`

状态：`implemented_pending_review`

映射：`openlegend::battle::BattleSetup::sprite_word`

参数是参战者 slot。机器码以 `slot*0x1C` 读取参战者 word0 角色 ID，再以 `role_id*0xB6` 读取182字节角色记录的 word1，最后返回 signed 16位：

```text
int16(8 * role.word1 + word_556D4 + 2 * word_556CC + 2 * combatant.word4)
```

headless IDA 全库 data xref 与初值读取证明 `word_556CC=0`、`word_556D4=5106`，两者在当前程序中没有写入；角色表基址前182字节位置的 word1 为-1。`sub_31EB9` 初始化空槽时已经把角色 ID 写成-1，仍立即调用本函数，所以空槽 word8 严格为 `5106 + 8*(-1) = 5098`；活跃队伍和敌方插入时再以实际 role word1 与 word4 覆盖。
