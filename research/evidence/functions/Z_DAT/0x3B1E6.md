# 函数证据：`sub_3B1E6` `0x3B1E6..0x3B238`

状态：`platform_adapted / converged_no_new_differences`

映射：`openlegend::battle::BattleSetup::sprite_word` 与空槽初始化。

## 机器身份与闭包边界

- 物理范围`0x3B1E6..0x3B238`，82 bytes、20条连续指令、1个IDA flow block、0个条件分支、0个无条件跳转、5处32位重定位、1次direct call（`sub_3ED1E`栈检查）、唯一局部`RET @ 0x3B237`。
- raw偏移`0x34BE6`；raw SHA256=`985aa8b7a31a9cb2162f8bb491d66c7679aef3faaa3adf7802cd0081f81838f6`，loaded SHA256=`7128954c4011c4368e7a7da78881964187e8819414265ed5e071aa68307abb86`。5处差异全部是`+0x20000`重定位，归一化后82 bytes逐字节相同。
- 13个call site为`0x31F7E,0x32031,0x32100,0x325BE,0x326CB,0x32DFC,0x32E16,0x37449,0x382EB,0x399E2,0x39D82,0x3A0EB,0x3A87F`，来自9个caller owner。每处续行均把返回AX写入参战者word8；caller、栈检查和相邻owner不传播closure。

## 算术合同

参数是signed低16位参战者slot。机器按`slot*28`读取参战者word0角色ID和word4初始模式，再按`role_id*182`读取角色记录word1头图ID；最终返回：

```text
int16(8 * role.head_id + 2 * word_556CC + word_556D4 + 2 * combatant.initial_mode)
```

`CWDE`将最终AX符号扩展到EAX。完整data xref证明初值`word_556CC=0`、`word_556D4=5106`且无写入。现代`wrapping_i16`保持每个结果的低16位行为；`head_id=32767/-32768`和`initial_mode=32767/-32768`边界均与机器公式一致。

## 空槽合法别名与修正

`sub_31EB9`先把26个槽的role ID写成-1，随后仍逐槽调用本函数。机器因此从`role0_base-182+2`读取“角色word1”；该地址实际别名Ranger header byte offset656 / word328 / inventory slot155的item ID，而不是抽象常量-1。

四套原始资产`RANGER.GRP/R1.GRP/R2.GRP/R3.GRP`在该槽均为-1，所以原始库存资产的空槽sprite仍为5098；但合法非默认存档若slot155 item ID为7，机器结果是5162。首轮C++对照发现现代初始化把空槽固定成5098，丢失该合法共享布局行为；最小修正为初始化时读取`header.inventory_item(155).value`并按signed 16-bit公式计算。活跃角色的`BattleSetup::sprite_word`公式未改。

修正后从受影响`sub_31EB9`入口重审：26槽14-word初始化顺序、word8计算、count清零、mandatory/fixed/preset/manual队伍路径、敌方追加、occupancy与所有出口均保持既有机器合同；其余8个caller owner仅在交换、移动、用毒、解毒、医疗、暗器或休息后重算sprite，无需修正。

## 独立Golden与回归

- `battle_sprite_word_machine`完全从原始`Z.DAT`和四套Ranger资产生成；向量SHA256=`887993d4e883fec6a1c5f07e859f0c09443a6e17f52c12138a1de776fab4d3b9`。
- 两次临时生成逐字节一致，并以第三次正式生成核对；67键Golden SHA256=`03ed0086cd490bc790a1e26e342207c2f7aa6ae9dc725c46e3c62a78dcb0fde2`。
- C++回归覆盖slot155 item ID=7得到5162、零初始化合成快照得到5106、库存原始值-1得到5098，以及battle4的`head_id=32767/-32768`分别得到5102/5108。
- Linux App Debug统一门14/14通过。真正非法slot或role索引由现代安全拒绝，归类平台适配；合法role=-1初始化别名必须保留。

`final_review = converged_no_new_differences`
