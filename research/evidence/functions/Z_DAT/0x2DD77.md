# 函数证据：`sub_2DD77` `0x2DD77..0x2DE03`

状态：`implemented_pending_review`

opcode5 清当前键，复制原 Big5“是否與之過招（Ｙ／Ｎ）”，在 `(61,40,187,27)` 绘混色圆角 panel、于 `(71,45)` 以颜色 `0x0705` 绘字，present 后阻塞至任意键。只有键值大写 `Y` 返回 true offset，Enter、N、Esc及其他键均返回 false offset；函数不在按键后重绘裸场景。现代 question overlay 与 app 原始键路由保持该合同。

独立 scene70 frame hash `0x5d8fc752d48d9a98`；synthetic opcode5 固定 Y/non-Y 后续分支。最终 REVIEW 仍需审计委托 panel/text/present/key helper。
