# 函数证据：`sub_2E536` `0x2E536..0x2E571`

状态：`implemented_pending_review`

KDEF opcode22。固定循环六个队伍槽：slot0 无条件以其 role ID 清 mp；slot1..5 仅当 signed role ID `>0` 时清 mp。后续槽中的 role ID0 与负哨兵均跳过；函数不在首洞停止。

旧 C++ 对所有 `>=0` 槽处理，会把后续 role0 也清零。现显式保留 slot0 特例及 `>0` 条件。真实 script20 走 dialogue→图片动画→fade_to_black→opcode22：slot0=role1 的 mp88 清0，slot1=role0 的 mp77 保持。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
