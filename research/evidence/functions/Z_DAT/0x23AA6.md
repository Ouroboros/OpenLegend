# 函数证据：`sub_23AA6` `0x23AA6..0x23B3E`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码。

独立汇编恢复：每次调用先递增delay；`delay>3`时清delay并把动画step加2。step `<=12`时frame=`direction_base + direction_idle_offset + step`；step `>12`时清step、待机计数、active和delay，并恢复方向基帧。比较均为16位有符号状态，正常域形成2..12的六帧序列。

现代映射：`WorldSession::idle_animation_tick`和`player_frame`，方向专属offset为54/52/50/48。runtime固定在`idle_tick -> periodic_tick -> idle_animation_tick`顺序调用，scene返回continuation帧也补执行同一外层尾部。

向量覆盖第3/4次delay、step12/14、四方向基帧和完整动画结束。最终双向逐基本块REVIEW为`not_started`。