# 函数证据：`sub_29D2D` `0x29D2D..0x2A0D9`

状态：`implemented_pending_review`

机器码清屏后以28×28视口执行两遍 x 外层、y 内层扫描。第一遍只在 building-height层为0时画earth；第二遍依次处理：非零height格的earth、非零且不等15000的building、事件word7正图片、非0且非-86的玩家图、非零decoration及其独立height。锚点为 `screen_x=18*(local_x-local_y)+145`、`screen_y=9*(local_x+local_y)-81`，building/event/player减building height，decoration减decoration height；末尾再进入天气链。

现代 `SceneSession::render_map/draw_sprite/draw_weather_particle` 保持两遍次序、哨兵与坐标公式。本轮修正事件绘制从历史命名的 word5 `current_picture` 改为机器实际 word7 `begin_picture`；独立 direct-render synthetic scene70 帧固定 FNV1a64 `0x606eaa33da727537`。按 `sub_296E6→sub_29B3C→sub_29D2D` 的入口顺序，counter0扫描先令单格word7由102到104，对应入口帧 `0x3051f24e11c08dc5`；既有真实资产帧因初始word5/word7相同而未变化。最终单向汇编→C++ REVIEW 尚未执行。
