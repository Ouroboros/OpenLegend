# 函数证据：`sub_2F6C2` `0x2F6C2..0x2F6E3`

状态：`implemented_pending_review`

KDEF opcode36。机器码比较 role0 record word14（`sexual`）与参数，相等返回 true offset，否则返回 false offset。C++ `SceneSession::run_event(case36)` 以相同 signed int16 比较交给 `conditional`。

真实 script328 `36,2,6,0` 已固定两条分支：sexual=2 到 TALK1123，sexual=1 到 TALK1122。独立 B7 oracle 同步输出该向量；Linux Debug `openlegend.scene` 通过。最终双向 REVIEW 尚未执行。
