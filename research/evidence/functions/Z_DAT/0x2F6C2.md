# 函数证据：`sub_2F6C2` `0x2F6C2..0x2F6E3`

状态：`platform_adapted`；最终 REVIEW：`converged_no_new_differences`

来源：当前 `Z.DAT` 机器码与原始文件字节；headless IDA完整导出；当前 `KDEF.IDX/GRP`；`research/ida/reports/Z_DAT.b7_scene_xrefs.txt`。

## 1. 机器身份与caller

- 函数物理范围 `0x2F6C2..0x2F6E3`，33 bytes，9条指令。
- loaded SHA256 `93d83ed447bbbc2997a71ca28c17627b9be5ab4c5cf03f96b4b8bc77384eb2ac`；原始文件 `Z.DAT[0x290C2:0x290E3]` SHA256 `7299d2cdd8b2cd581a4bf8735a582601cc2e7033f7f5a7bfaeb3d52098a628ca`。
- 唯一差异字节是角色0性别word地址raw加加载基址 `0x20000` 的一项重定位；规范化后与loaded逐字节相同，未解释差异为零。
- 唯一物理代码caller是 `sub_2C319:0x2C8C3..0x2C8E8` 的opcode36；`0x5564C`只是函数地址表数据引用。
- caller把三个KDEF word sign-extend为 `(expected_sexual,true_offset,false_offset)`，调用后清理12字节栈，先把PC增加4 words，再叠加callee返回的32位signed偏移。
- 入口 `sub_3ED1E(4)` 仅为Watcom栈探测合同。

## 2. 不看C++的条件合同

1. 读取并sign-extend角色0记录word14 `sexual`。
2. 与sign-extend后的expected参数做32位精确相等比较。
3. 相等时原样返回sign-extend后的true offset；不等时原样返回false offset。
4. 函数不写任何状态，不产生画面、输入或音频边界。
5. caller最终PC公式为 `old_PC + 4 + returned_offset`。

## 3. 汇编→C++ REVIEW

C++ case36读取同一角色0 sexual word，以signed int16精确相等结果交给 `conditional(value,4,true,false)`；该lambda按ptrdiff先加4再加所选signed offset。合法快照域与机器逐项一致，本轮没有产品代码差异。

机器固定假设角色0记录存在；现代在损坏/空角色集合时稳定用-1比较，替代原机无效内存读取，归类 `platform_adapted`。随后从入口重新逐条复核9条指令、一项重定位、signed读取与比较、两项原样返回、caller清栈、固定宽度与偏移叠加，零新增差异。

## 4. 全KDEF与分支oracle

当前1,018条KDEF只有一次opcode36：script328 PC0 `(expected=2,true=6,false=0)`。按little-endian `<IIhhh>` 编码 `(script,PC,expected,true,false)` 的完整参数流SHA256为 `d81b351b35d2e14a14e3ba8a39497d27f8fba32a7d0c356e7d230df9f332d03a`。

- sexual2选偏移6并到达TALK1123；sexual1选偏移0并到达TALK1122。
- 合成script50以expected -32768、true4、false0固定signed边界：sexual -32768到达TALK1123，sexual32767到达TALK1122。
- 双次独立生成字节一致；正式 `scene-goldens.json` SHA256 `68101cc9ca307fd8f2fd093c5836c734b8a838492524ef433d4b733d379cdff8`。

## 5. 验证与结论

- Linux app Debug根构建：14/14 tests通过（`proc_238a`）。
- 原程序动态差分：`blocked_runtime_oracle`。

```text
closure_status = platform_adapted
final_review = converged_no_new_differences
remaining =
```
