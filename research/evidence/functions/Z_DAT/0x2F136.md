# 函数证据：`sub_2F136` `0x2F136..0x2F15F`

状态：`implemented_pending_review`

KDEF opcode29。参数表虽含 `(role,minimum,unused,true_offset,false_offset)`，函数只读取角色 word43 attack 并判断 signed `attack < minimum`；第三个 KDEF 数值不读取。caller 宽度6。

C++ case29 仅判断 `attack >= argument2`，明确忽略 argument3。真实 scene14/script655 使用 `(role0,90,1000,6,0)`；测试把 attack 设2000仍进入 opcode57 全35帧成功路径，证明1000不是上限。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
