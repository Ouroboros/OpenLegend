# 函数证据：`sub_295D0` `0x295D0..0x296E6`

状态：`implemented_pending_review`

机器码依次以 cache tag4/5 打开 `ALLSINBK.IDX/ALLDEFBK.IDX`，按同一场景号 offset 将当前地图49,152字节与事件4,400字节写回两个工作副本 GRP；每段写入后关闭，IDX 失败分别打印原错误并终止。

现代 `GameSnapshot::scene_maps/scene_events` 是工作副本的内存所有者，场景与事件 mutator 直接写目标固定 span；保存槽再按原物理顺序输出，因此会话离开时无需额外 flush。正确 WIDTHS 扫描确认当前 KDEF 有311条外部事件修改；真实 script436 覆盖外部 scene7/event6 在首个同步 dialogue 前写回且 scene70 会话不切换；全部存档 round-trip 属 delegated persistence 证据，最终 REVIEW 尚未执行。
