# 函数证据：`sub_24EA2` `0x24EA2..0x24F17`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；`research/ida/reports/Z_DAT.b6_world_xrefs.txt`；当前原版`BUILDX.002`。

## 1. 独立机器恢复

- 物理范围`0x24EA2..0x24F17`，117 bytes、39条指令、3个基本块；唯一caller为总加载`sub_24CE8`。
- EDI/ESI保存origin X/Y，EBX严格遍历0..127；每行以BUILDX句柄`word_C0BEE`绝对seek到`960*(origin_y+row)+2*origin_x`。
- 每次读256 bytes到`word_62FFC + row*256`，得到128个int16 owner-X；row127结果返回后由BUILDY调用覆盖。
- 本函数只复制原word，包括0值，不解释owner语义；无RNG或其他状态写入。

## 2. 汇编 → C++ 与适配

- `WorldCache::reload`的`WorldLayer::build_x`轮次使用相同480-word源步长、128-word目标步长和128-word行长，并在SURFACE之后、BUILDY之前执行。
- owner配对/去重属于后续深度重建，不属于本入口；现代在这里同样只复制。预载与输入检查属于平台适配。

## 3. 验证与结论

- origin `(293,171)`、`(0,0)`、`(352,352)`三份BUILDX完整缓存哈希逐项命中；两端独立oracle为`proc_087a`，覆盖owner-X中全部0与非0原值。
- Linux app Debug：14/14通过，受管进程`proc_c005`。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
