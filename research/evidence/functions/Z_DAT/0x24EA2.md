# 函数证据：`sub_24EA2` `0x24EA2..0x24F17`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；BUILDX owner cache loader。

独立汇编恢复：固定128行seek/read，偏移公式与其他层相同，目标为BUILDX 128×128 int16 owner-X表。

现代映射：`WorldCache::reload(WorldLayer::build_x)`；深度重建读取该层并与BUILDY配对去重，不把0误作owner。

真实cache/depth测试覆盖初始和跨阈值重载。最终双向逐基本块REVIEW为`not_started`。