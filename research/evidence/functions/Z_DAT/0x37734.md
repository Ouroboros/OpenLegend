# 函数证据：`sub_37734` `0x37734..0x3859E`

状态：`implemented_pending_review`

映射：`BattleSetup::learned_magic_count`、`automatic_magic_slot`、`attack_profile`、`attack_special_bonus`、`clear_attack_effects`、`apply_attack_area`、`apply_line_attack_area`、`refresh_combatant_sprites`、`commit_attack_iteration`、`finish_attack`与`BattleSession`攻击continuation。

入口先统计十个 role magic id 槽中严格大于0的数量。手动攻击只有一个已学槽时直接把 selected slot写0，而不是寻找唯一非零槽；因此仅 slot2=magic5 时仍会读取 slot0/magic0。自动选择调用 `sub_3D612(count)` 后也直接把结果当槽下标。等级下标严格为对应熟练度 unsigned word `/100`；从 magic record 读取 select distance word28+level、attack distance word38+level、area type word15、hurt type word14、need mp word16。角色 attack_twice word58严格等于1时执行两轮，否则一轮。

area type 0/3以目标格为中心扫描方形，type1按方向构造直线，type2按上/下/左/右十字扫描；HP伤害调用 `sub_39188`，MP伤害调用 `sub_395EC`。每轮伤害和 `sub_3859E/sub_38910` 动画完成后，原函数依次写 actor word7=1、word13按16位加2、熟练度加 `sub_3D612(2)+1` 并按 unsigned 999封顶、按新旧 `/100+1` 判升级，再扣 `(word_E6EC2/2)*need_mp` 内力并将 signed负值夹0。全部轮次后 physical_power按16位减3并将负值夹0。

现代实现固定了 slot0 BUG、profile字段、seed1→state1103527590、熟练度299→300升级、999封顶、cost scale3下 mp3→0、action counter0→2及体力2→0。方形严格按x外/y内扫描并更新朝向，十字逐距离按上→下→左→右扫描；友军占位格不标记，空格仅标记，敌方格写damage word9。type2即使hurt_type=1仍强制调用HP伤害；type0/3按hurt_type分派HP/MP，MP命中不改上次HP cost scale。固定effect hashes为方形 `0xe5f47b0a810ce2bd`、十字 `0x3144c415023d9464`、单格MP `0xab559939923b4f74`。

`BattleSession`现从单武功slot0 BUG、`sub_38DAC`多武功确认或AI automatic flag1进入完整攻击continuation：area type0/3复用mode1目标光标并支持Escape返回原动作ordinal，type1绘制“選擇攻擊方向”圆角框且按上/右/左/下写方向0/1/2/3，type2直接执行十字。确认后每轮清effect、提交HP/MP伤害、加载对应FIGHT包，按`sub_3859E`逐帧执行actor/EFT、双bank音频、present与wait17，再按`sub_38910`执行10帧damage、前四帧flash和wait1；随后重算全部sprite、重画/present、wait17，才写action word7、word13、熟练度和MP。跨百时额外绘制Big5升级框、present并wait500，然后才开始第二击。全部轮次后体力减3并进入共享胜负/下一actor尾部。AI路径按目标差自动写方向，直接命中或移动后命中均执行同一完整链，使用独立AI相位记录每个present/wait边界；固定直接向量10帧FIGHT、10帧damage、17tick提交、13tick升级等待，目标HP5000→4936、word13为0→12→14、seed1终态3655513600。

原函数在进入双击循环前缓存select distance、attack distance、area type、hurt type和轮数；现代`PlayerAttackState`同样保留初始profile供两轮area覆盖，即使首击升级也不扩大第二击范围。伤害公式仍在每次命中时按更新后的熟练度重新求level/cost scale，因此固定跨百双击向量第二轮在等级3的select distance仍为0时继续命中初始十字范围，同时cost scale由3变4、两轮内力20→5。四种area type均由真实battle4 Session锁定，type1方向提示整帧FNV64为`0x5e46c805f42677b0`，升级框为`0x1f0048d1945a4948`；双击两轮各10个magic帧与10个damage帧，升级等待13次BIOS tick变化。Linux Debug 14/14；本函数推进为`implemented_pending_review`，最终仍需全B8汇编↔C++双向REVIEW。
