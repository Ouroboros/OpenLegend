# 函数证据：`sub_2A74C` `0x2A74C..0x2A7E8`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSession::render_player_item_selection` 的下滚动箭头。

函数用color99依次绘制 `(267,175,2,1)`、`(266,174,4,1)`、`(265,173,6,1)`、`(264,172,8,1)` 与 `(266,161,4,11)` 五个实心矩形。现代battle物品菜单仅在item数严格大于 `5*(page+3)` 时执行同组像素；16项page0整帧回归已覆盖。

fresh IDA独立重建`0x2A74C..0x2A7E8`为156 bytes、43条指令、2个基本块，loaded SHA256=`76fdcf45808a37d6e654d8e7a200e3f4ace9d2b3e7998208a2493e5973b765a7`。从入口逐次对照五个矩形参数、共享填充callee和显示条件，最后一轮未发现新差异；非法framebuffer边界由现代安全拒绝，故为`platform_adapted`。
