# 函数证据：`sub_2A74C` `0x2A74C..0x2A7E8`

状态：`implemented_pending_review`

映射：`BattleSession::render_player_item_selection` 的下滚动箭头。

函数用color99依次绘制 `(267,175,2,1)`、`(266,174,4,1)`、`(265,173,6,1)`、`(264,172,8,1)` 与 `(266,161,4,11)` 五个实心矩形。现代battle物品菜单仅在item数严格大于 `5*(page+3)` 时执行同组像素；16项page0整帧回归已覆盖。最终双向REVIEW尚未执行，故为 `implemented_pending_review`。
