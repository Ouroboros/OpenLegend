# 函数证据：`sub_2A86C` `0x2A86C..0x2B227`

状态：`platform_adapted / converged_no_new_differences`

来源：当前`Z.DAT`机器码；独立IDAT副本`tmp/Z_DAT-input-order20.i64`；完整导出`tmp/input-order20-machine-all.txt`；共享尾导出`tmp/input-order20-shared-tail.txt`；机器门`tmp/input-order20-audit.py`。

本次仅关闭`input-font-closure.tsv`的`xref_last_key_code` owner。同址`ui-closure.tsv`的`game_menu_items_select`仍为`implemented_pending_review`；绘制、物品资格/属性、库存、事件与共享尾owner不得由本项传播closure。

## 1. 机器身份

- 装载范围`0x2A86C..0x2B227`，2491 bytes、548条指令、137个基本块、77个条件分支、35个无条件跳转（34个本地、1个共享尾出口）、47次call；原文件范围`Z.DAT[0x2426C:0x24C27]`。
- raw SHA256：`7494f3fde6bf4003b02830c6fb99e3899e2332bb3ccf94367f4eaf254347ae9f`。
- loaded SHA256：`813c4e3369973a4ed502e1c2193d4574d8f64340aadc0623769a07d092b4fd98`。
- 167项装载重定位均使little-endian dword增加`0x20000`；逐项还原后逐字节等于raw。
- 唯一两个入口xref为`sub_2A0D9:0x2A106`和`sub_3A29C:0x3A2D4`，没有外部内部入口。
- 函数没有本地`RET`。全部出口汇聚到`0x2B218`，最后由`0x2B222`跳往共享清理尾`0x2A0D1..0x2A0D9`：`add esp,4; pop ebp; pop edi; pop esi; pop ebx; retn`。该尾归属`sub_29D2D`，另有`0x2A0CE`、`0x2A055`、`0x2A0BC`三个入口，本项不关闭共享尾owner；尾字节SHA256为`b389950002ba37dbf8ee72e175c575064b3a60d86d656fbc1a0aebbdf240f405`。

两个直接caller分别独立固定：

- `sub_2A0D9 @ 0x2A0D9..0x2A10F`为54 bytes/16 instructions，raw/loaded SHA256分别为`1fd36ede5cb17507e24e83bc883074f6205a911ee34359aad6514b6280a2f383`与`ed5269e0ae12171d13068702b67735cd621e9984a90560a9da73e0bf1949bc2e`；传参`(0,-1)`，返回值被主菜单caller忽略。
- `sub_3A29C @ 0x3A29C..0x3A30B`为111 bytes/33 instructions，raw/loaded SHA256分别为`eb358abcceab4d2b8c735da754e73ff00bc611237a8cf23087e2475bfffad5c7`与`428460ab48a5ebbbab01f06e88eea25e01ed6d2e8b36ade0862210db547e18d3`；传参`(4,actor_role_id)`，返回4进入暗器目标选择，返回1把当前combatant `action_done`写1，其余返回不提交行动。

## 2. input机器合同

入口把page/row/column和返回值清0，把输入循环标志设1，并在`0x2A88B`清last-key。`0x2A8EA`每轮读取last-key。精确接受键集合为：

- Enter `0x0D`、Space `0x20`：确认；
- Escape `0x1B`：返回0；
- Down `0x98`：row小于2时row加1，否则page小于37时page加1；
- PageDown `0x99`：page小于35时加3；
- Left `0x9A`：column在0..4向左回绕；
- Right `0x9C`：column在0..4向右回绕；
- Up `0x9E`：row大于0时row减1，否则page大于0时page减1；
- PageUp `0x9F`：page大于2时减3。

keypad Insert `0x96`不确认物品：它沿`0x2A8B2 cmp al,98h`失败路径返回等待循环。其他未知键同样不改变选择、不退出。机器在未知键后留在同步等待循环；现代事件宿主返回并清last-key，但world/scene与battle物品选择均不推进游戏状态，下一合法键观察到相同选择状态，属于不可重入SDL事件边界适配。

每个合法方向/翻页先清last-key，再调用`sub_2A186(page,row,column)`重画。确认在`0x2A980`先清last-key，按`2 * (5 * (page + row) + column)`读取`word_D27C0`映射槽，并严格要求对应item的`show_introduction == 1`；不满足则保持选择继续等待，满足时写`word_54B74`并返回1。Escape在`0x2A9D3`清last-key并返回0。

last-key共有17个直接引用：14次写0、1次选择循环读取、2次大写Y比较。14个清零地址为`0x2A88B,0x2A8C6,0x2A90F,0x2A924,0x2A93B,0x2A950,0x2A968,0x2A980,0x2A9D3,0x2A9E1,0x2ADC0,0x2AFE5,0x2B1DC,0x2B218`；Y读取地址为`0x2ADCC`和`0x2AFF1`，均严格比较`0x59`。

退出选择循环后，`0x2A9E1`再次清last-key，再按调用环境恢复world/scene/battle底图并present。练功换人和挥剑自宫两个询问均为“显示提示→清旧键→`sub_20C32`等待任意新键→只接受大写Y”；消耗品效果等待同样在新键到达后清键。所有路径在`0x2B218`再无条件清一次last-key。

## 3. 首轮差异与最小修正

首轮逐块对照发现现代共享`confirms()`把keypad Insert `0x96`也用于物品确认：

- `GameMenuController::handle_key`会在world/scene物品选择中确认当前物品；
- `BattleSession::handle_player_item_key`会在battle物品选择中使用当前物品。

这与机器精确分派不符。最小修正只在两种物品选择上下文把确认集合收窄为Enter/Space；主菜单、选人、系统、存取档、战斗动作与其他既有Insert确认保持不变。

回归分别固定：world/scene物品选择收到Insert时返回`none`、screen仍为items且selection不变；battle收到Insert时返回`ignored`、phase仍为`player_item_selection`、HP与库存不变，随后Space仍可正常使用物品。

## 4. 修正后入口重审与边界

修正后废弃首轮结论，从`0x2A86C`重新覆盖全部548条指令、137个基本块、77个条件分支、35个无条件跳转、47次call、167项重定位、两个caller、全部出口及共享尾边界，零新增input owner差异：

1. prologue、返回值/光标初始化和入口清键一致；
2. 九键精确分派一致，Insert及未知键均不确认；
3. 左右回绕、上下row/page、三页翻页边界和每次识别后的清键一致；
4. 严格`show_introduction == 1`、隐藏物品保持选择和Escape返回0一致；
5. 选择循环后清键、两处Y/N提示和效果面板任意键等待一致；
6. 主菜单caller忽略返回、battle caller只按word返回4/1派生目标选择/行动完成一致；
7. 全部出口最终清键并跳共享尾一致。

本函数中的type0事件物品、type1装备、type2练功、type3消耗品、type4暗器逻辑只作为输入等待和返回上下文核对。`sub_2A186`绘制/present、`sub_22090`选人、`sub_2BD8B`资格、`sub_2B483`效果、库存提交及同址UI owner继续按各自证据独立关闭。

## 5. Golden与验证

`generate_b5_ui_goldens.py`直接读取只读`Z.DAT`，独立固定raw/loaded身份、167项重定位、47次call、17个last-key引用、两个caller和共享尾，并生成20组键位/边界状态向量：

- 合同SHA256：`32629e0bed2b0284822887666be436db69ac8e37f75dcb3770533ca7e40ab4c1`；
- 向量SHA256：`f80f863c607a8d9aea5b9a678e7a9bb644608cc6c57d838d014479df2f49f09a`；
- 两份临时Golden及正式第三次生成逐字节一致；正式`title-menu-new-game-goldens.json` SHA256为`33f8bd403715655e0a49b3f287e0c4ff9c2d8ca2cf0a2b9239f8c6a96e469d04`，既有11个顶层节点逐值不变，仅新增`game_menu_item_selector_machine`。

修正后统一Linux app Debug `proc_7746`为14/14。机器门、静态门、Golden门、closure计数门和577-function逆向框架门见本阶段最终验证。

原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
