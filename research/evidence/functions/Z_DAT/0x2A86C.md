# 函数证据：`sub_2A86C` `0x2A86C..0x2B227`

状态：`implemented_pending_review`

现代映射：`GameMenuController`、`LegacyGameRuntime::handle_menu_item_result/handle_menu_item_confirmation`、`BattleRenderer::render_item_effect`、`BattleSession::handle_player_item_key`以及`battle::apply_role_item_effect`。

选择循环的输入边界为：左右在column 0..4回绕；下键先把row推进到2，已在row2且page<37时page加1；上键对称；PageDown仅page<35时加3，PageUp仅page>2时减3；Escape返回0；Enter、Space与keypad Insert共享确认。确认只接受 `show_introduction==1` 的有效slot。

参数4时，type3调用 `sub_2B483(actor,actor,item)`；有非零显示delta时重画battle、present效果面板，随后扣库存并调用 `sub_20C32` 阻塞到任意键，最终返回1。若23项delta全零，`sub_2B483`返回0：不画面板、不扣库存、也不读第二个键，但选择器入口结果仍为1，因此battle wrapper仍结束actor行动。type4直接返回4，由 `sub_3A29C` 进入暗器目标选择。

参数0..3的菜单路径现已接入：type0在场景菜单中继续交给事件物品匹配；type1调用`sub_22090(3)`等价选择器，通过`sub_2BD8B`门禁后按装备类型解除旧双方绑定并建立`role.equipment/item.user`双向关系；type2若已有修炼者先询问是否换人，再调用`sub_22090(4)`，依次执行已学武功检查、十武功上限、完整门禁、物品78/93的挥剑自宫确认，随后解除旧绑定并清零修炼经验；type3调用`sub_22090(5)`并以actor=target执行共享23项效果，只在存在非零显示delta时扣库存、显示效果面板并等待任意键。

23项效果已抽为`battle::apply_role_item_effect`，战斗玩家、战斗AI和非战斗菜单共用同一signed word、夹值及RNG消费实现；库存压缩也由`battle::consume_inventory_item_slot`共享。现代battle回归覆盖5×3分页、四方向、PageUp/PageDown、Escape、三确认键、不可用空格、数量显示、type3面板present后任意键、零效果不扣库存但结束行动，以及type4目标取消/空格/命中。菜单回归另覆盖5×3控制器的左右列回绕、上下行/页推进、PageUp/PageDown、`show_introduction!=1`保持物品页，以及装备绑定、已有修炼者Y/N换绑、十武功上限、资格失败、挥剑自宫Y/N、消耗品效果与库存提交。本切片通过Linux/Windows `core/app × Debug/Release`及Linux app ASan+UBSan门禁。当前状态为`implemented_pending_review`；最终双向逐基本块REVIEW仍为`not_started`。
