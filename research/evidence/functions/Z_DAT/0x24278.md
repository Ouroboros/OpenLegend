# 函数证据：`sub_24278` `0x24278..0x24417`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；成功移动后的viewport origin/local坐标与cache提交。

独立汇编恢复：根据轴向参数计算`clamp(world-64,0,352)`；当对应local坐标`>98`或`<11`且候选origin不同于现origin时，同时更新X/Y origin，按EARTH→BUILDING→SURFACE→BUILDX→BUILDY重载128×128 cache，再重算X/Y local坐标。未跨阈值或候选origin相同时立即返回，不读取层文件。该函数不写RANGER header；world X/Y、方向、in_ship与船字段只在`sub_265AB`保存槽边界复制到header。

现代映射：`WorldSession::reload_cache_if_needed`；`sync_persistent_state`仅由现代`sub_265AB`保存适配边界调用。审计修正了两项差异：五层枚举循环曾把SURFACE提前；现代曾在每次成功/阻挡移动提前写RANGER header。现构造加载和cache复制均用显式机器顺序，运行时world状态保持在`WorldSession`，保存时才运输。原文件seek/read由已加载只读层的`copy_n`替代，真实资产字节和行序一致。

向量覆盖local10/11/98/99、origin不变/变化、35格轨迹、移动后header保持旧值及显式保存同步。最终双向逐基本块REVIEW为`not_started`。