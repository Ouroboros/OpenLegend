# 函数证据：`sub_2F34C` `0x2F34C..0x2F39C`

状态：`implemented_pending_review`

KDEF opcode31。线性扫描200个库存槽，遇到第一个 item ID=174 时读取该槽 count、设 found 并立即结束扫描；`found && first_count >= required` 返回 true_offset，否则 false_offset。不得汇总重复 item174 槽。

C++ case31 使用 `first_inventory_count(174)`。真实 script234 反例：slot0=(174,5)、slot1=(174,10)、required10，必须 false→dialogue790；slot0 count10 才 true→dialogue791。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
