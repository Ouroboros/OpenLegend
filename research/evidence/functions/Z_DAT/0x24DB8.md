# 函数证据：`sub_24DB8` `0x24DB8..0x24E2D`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；BUILDING cache loader。

独立汇编恢复：与EARTH loader相同的128行seek/read循环，句柄和目标切换为BUILDING；每行文件偏移`960*(origin_y+row)+2*origin_x`、长度256。

现代映射：`WorldCache::reload`的`WorldLayer::building`分支，在EARTH之后、SURFACE之前执行。真实初始/重载cache hash固定。

无RNG或状态写入；最终双向逐基本块REVIEW为`not_started`。