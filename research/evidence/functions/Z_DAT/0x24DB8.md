# 函数证据：`sub_24DB8` `0x24DB8..0x24E2D`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；`research/ida/reports/Z_DAT.b6_world_xrefs.txt`；当前原版`BUILDING.002`。

## 1. 独立机器恢复

- 物理范围`0x24DB8..0x24E2D`，117 bytes、39条指令、3个基本块；唯一caller为总加载`sub_24CE8`。
- EDI=origin X、ESI=origin Y、EBX严格遍历0..127；每行以BUILDING句柄`word_C0BF4`绝对seek到`960*(origin_y+row)+2*origin_x`。
- 每次把读长写为256，并把128个int16读到`word_87E2C + row*256`；row127 read结果返回后由SURFACE调用覆盖。
- 无分支、提前退出、RNG或游戏状态写入。

## 2. 汇编 → C++ 与适配

- `WorldCache::reload`在EARTH之后、SURFACE之前处理`WorldLayer::building`，每行从`(origin_y+row)*480+origin_x`复制128 words到`row*128`。
- `WorldMapData`的BUILDING层索引和源文件名一一对应；预载后的内存复制、整体合法性检查和bool返回属于平台适配，合法原资产结果不变。

## 3. 验证与结论

- origin `(293,171)`、`(0,0)`、`(352,352)`三份BUILDING完整缓存哈希逐项命中；两端独立资产oracle为`proc_087a`，同时锁定行宽、边界和层归属。
- Linux app Debug：14/14通过，受管进程`proc_c005`。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
