# 函数证据：`sub_241DC` `0x241DC..0x2422A`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码。

独立汇编恢复：把X增量加到玩家world X，随后夹到0..479；以`world_x-cache_origin_x`重算cache-local X。该薄边界不读取Y、不做碰撞或资源加载，返回更新后的X。

现代映射：`WorldSession::complete_move`的X坐标提交及`cache_x()`派生值。现代不持久保存可导出的local全局，而由world-origin实时计算；合法域结果一致。

向量覆盖-1/0/479/480和origin 0/352。最终双向逐基本块REVIEW为`not_started`。