# 函数证据：`sub_24F17` `0x24F17..0x24F8C`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；BUILDY owner cache loader。

独立汇编恢复：固定128行seek/read，偏移`960*(origin_y+row)+2*origin_x`，每行256字节写BUILDY cache；返回最后一次read结果。

现代映射：`WorldCache::reload(WorldLayer::build_y)`，五层机器顺序的最后一层；与BUILDX共同为深度列表提供owner world坐标。

真实cache/depth测试覆盖。最终双向逐基本块REVIEW为`not_started`。