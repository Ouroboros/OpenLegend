# 函数证据：`sub_24F17` `0x24F17..0x24F8C`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；`research/ida/reports/Z_DAT.b6_world_xrefs.txt`；当前原版`BUILDY.002`。

## 1. 独立机器恢复

- 物理范围`0x24F17..0x24F8C`，117 bytes、39条指令、3个基本块；唯一caller为总加载`sub_24CE8`。
- EDI/ESI保存origin X/Y，EBX严格遍历0..127；每行以BUILDY句柄`word_C0BE6`绝对seek到`960*(origin_y+row)+2*origin_x`。
- 每次读256 bytes到`word_6AFFC + row*256`，得到128个int16 owner-Y；row127 read结果作为本入口及总入口结果返回。
- 三个总入口caller均立即覆盖EAX，故最后read返回不控制初始化、移动或scene返回流程；本函数不解释owner值且不消费RNG。

## 2. 汇编 → C++ 与适配

- `WorldCache::reload`最后处理`WorldLayer::build_y`，使用相同480-word源步长、128-word目标步长与128-word行长；与BUILDX仅在后续深度重建中配对。
- 现代把未消费的机器read结果适配为整体reload bool，且从预载只读层复制；合法原资产与合法origin均返回成功并得到相同缓存。

## 3. 验证与结论

- origin `(293,171)`、`(0,0)`、`(352,352)`三份BUILDY完整缓存哈希逐项命中；两端独立oracle为`proc_087a`，覆盖五层最后一个目标和最大源坐标479。
- Linux app Debug：14/14通过，受管进程`proc_c005`。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
