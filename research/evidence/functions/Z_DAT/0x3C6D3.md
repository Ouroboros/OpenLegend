# 函数证据：`sub_3C6D3` `0x3C6D3..0x3CBE3`

状态：`implemented_pending_review`

部分映射：`BattleSetup::status_panel_plan`。

参战者side非0时横向偏移220，否则0；面板原点`(220-offset,19)`、尺寸100×140，头像`(242-offset,82)`。角色名按格式化缓冲区byte1..8找到首个NUL，横坐标为`270-4*index-offset`；前8字节无NUL时不绘名称。typed计划保留原角色名10字节、头像ID、体力、HP/最大HP、MP/最大MP及所有坐标来源。

HP当前值颜色按hurt为0..33→1797、34..66→3600、67+→5142；最大HP值颜色按poison为0→8993、非零且<50→12338、>=50→13623。MP三值颜色按内力类型0→20558、1→1797、2→26211；其他类型保留原寄存器残值BUG，复用此前poison颜色。party/enemy面板原点、名称byte2 NUL坐标262、三类颜色和非法内力类型回退均有固定向量。

`BattleRenderer::render_status_panel`现已执行原`sub_2CEBF`圆角半透明面板、离散白色边框、`HDGRP`头像、原Big5标签字节、名称与右对齐数值的实际indexed像素；颜色直接使用typed计划保留的packed word。真实battle4固定队员面板叠加战场后的FNV64为`0x630a82d57e1d8715`，独立Python oracle直接读取`WDX/WMP`、`CLOUD`、`HDGRP`、`FONT.X16`、`FONT.C16`和`MMAP.COL`复算一致。

IDA代码xref证明本函数仅有三个调用点：`sub_32E59`内两处玩家菜单重绘和`sub_33599`内一处AI prelude。玩家菜单两处由同一`player_action`重绘相位覆盖；AI prelude及其300延迟保持帧现也在战场上叠加当前actor状态面板。真实AI攻击prelude整帧FNV64固定为`0xb02104139829a80d`，原独立面板oracle仍为`0x630a82d57e1d8715`。Linux Debug完整BUILD 14/14，因此推进为`implemented_pending_review`；最终完整双向REVIEW尚未开始。
