# 函数证据：`sub_3C6D3` `0x3C6D3..0x3CBE3`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::status_panel_plan`、`BattleRenderer::render_status_panel`、`BattleSession::render_player_action_menu`及AI prelude相位。

## 机器身份与owner边界

- 原始范围1296 bytes、338条指令、39个IDA flow block、17个条件分支、14个无条件跳转、22次direct call、87处HIGHLOW重定位及唯一`RETN @ 0x3CBE2`。
- raw/loaded SHA256分别为`7b7e43180089ca6cf33a5db5cab51a0bcdbc019d98bee609104363948a7862c2`与`bf86a5a30d9179987631eb70fd8e4cc88005313fd8901ccb8aa60f7493762e1a`；87处loaded DWORD均为raw值加`0x20000`，逆变换后逐字节等于原资产。
- 三个入口xref为`sub_32E59:0x33140`、`sub_32E59:0x33502`和`sub_33599:0x33668`，分别对应玩家动作菜单初次完整重绘、动作handler返回后的完整重绘，以及AI prelude状态叠加。
- `0x3CBDB..0x3CBE3`是共享epilogue；另有12个其他owner直接跳入`0x3CBDB`，本owner不向这些入口传播closure。

## 面板与名称合同

机器先sign-extend actor slot并按28-byte参战者记录读取side；side恰0时横向偏移0，任意非零signed word时偏移220。面板以style3在`(220-offset,19)`绘100×140框，头像由signed role ID查head ID后绘于`(242-offset,82)`。

角色名先经`"%s"`写入全局scratch，再按byte1→byte8顺序找首个NUL。首个NUL位于index `n`时以`x=270-4*n-offset,y=84,colors=0x0705`绘制；byte1..8均非零时不发名称draw call，byte9的NUL不参与。现代固定十字节名称、私有格式化存储与确定性空名处理防止原机器越界读取或scratch残留，但合法数据的首NUL、坐标及可见像素不变。

## 三行文字、signed格式与颜色

调用顺序固定为框、头像、可选名称，然后体力、生命、内力三行；最大名称路径为6次格式化和13次文字绘制。所有角色数值均sign-extend后使用`%3d`，宽度3只是最小宽度，`1000`、`32767`、`-1234`与`-32768`不得截断。

- 体力行`y=101`：`体力 `在`x=225-offset`颜色`0x2321`；当前值在262颜色`0x0705`；斜杠在285颜色`0x6663`；固定`100`在292颜色`0x2321`。
- 生命行`y=118`：当前HP颜色按signed hurt为`<=33 -> 0x0705(1797)`、`34..66 -> 0x0E10(3600)`、`>=67 -> 0x1416(5142)`；最大HP颜色按signed poison为`==0 -> 0x2321(8993)`、非零且`<50 -> 0x3032(12338)`、`>=50 -> 0x3537(13623)`，所以负poison进入`0x3032`。斜杠固定`0x6663`。
- 内力行`y=135`：label固定`0x2321`；MP type 0/1/2分别选择`0x504E(20558)`、`0x0705(1797)`、`0x6663(26211)`。其他signed值不写EBX，精确保留上一段最大HP的poison颜色；当前MP、斜杠和最大MP三者都使用该选择值或寄存器残值。

owner本身无RNG、输入、present、wait或battle状态写入。最终text helper的EAX残值被三个caller全部丢弃；现代bool只用于合法资产失败传播。

## caller时序与现代对照

两个玩家caller都先完整重画战场、动作框与全部label，再调用状态面板并present。现代`player_action_initial_present`和`player_action_return_present`执行同一完整路径；之后轮询严格从`player_action_frame_`恢复上一完整帧并只覆写label，不重读战场或角色状态，与机器不再经过两处完整重绘call site一致。

AI caller先计算双方态势，叠加当前actor状态，present后执行300参数等待。现代AI prelude在状态叠加前重建未变化的同一战场；独立完整像素oracle证明结果一致，进入`ai_wait`后不再重绘。等待结束才消费selector RNG。

逐基本块对照`BattleSetup::status_panel_plan`、`BattleRenderer::render_status_panel`与上述三个Session路径，合法caller域未发现产品实现差异。现代对非法slot/role/portrait/资产的安全拒绝、私有scratch和显式present/wait相位归类平台适配；产品源码无需修改。

## 独立oracle与回归

`battle_status_panel_machine`锁定完整机器身份、六段行为切片、三段caller切片、原字符串字节、side/name/signed格式和全部颜色边界。独立Python仅读取`Z.DAT`、`HDGRP`、`FONT3.E16`、`FONT3.C16`与`MMAP.COL`，从非均匀保留帧构造左右两侧状态面板；party与enemy FNV64分别为`0xcf0f77700cd1e7a1`和`0xebce8d4d6d8c1f14`，组合向量SHA256为`8a01741539af2e73e7c8f9b601dd944583cb1c79b647c1e7ad9663a7e8b28370`。

`run_battle_status_panel_review_test`覆盖side `-32768/-1/0/1/32767`、名称NUL位置1..8及无NUL、hurt 33/34/66/67、poison 0/1/49/50与负值、MP type 0/1/2及非法残值、signed极值格式、非法slot/role安全边界与左右完整像素。input-font Order38改用启动链`FONT3.E16/FONT3.C16`后，两次临时生成和第三次正式生成逐字节一致；当前76键`battle-goldens.json` SHA256为`ab3b9a67ceec89430176a5469899e3e3ed318efc7d44a1a4947272115e77d6ab`，旧字体像素哈希已废弃，非字体合同保持；Linux/Windows `core/app × Debug/Release`模块关闭八项矩阵全部通过，core各13/13、app各14/14。
