# 函数证据：`sub_35217` `0x35217..0x35372`

状态：`implemented_pending_review`

映射：`BattleSetup::choose_ai_specialist_target`。

先扫描全部同side槽（不排除自身、隐藏或死亡）判断是否存在use_poison>20。若存在，以共享signed best=0选未隐藏敌方中detoxification最大者并记录是否达到20；未达到20才继续用同一best选择medicine最大者并记录是否达到20。最后只检查medicine达到flag：若为0，无论此前detox是否达到20，都调用最低攻击selector覆盖目标。这一独立flag顺序BUG原样保留。

固定盟友use_poison21、敌方detox`[30,0]`先暂选slot3，随后因medicine flag仍为0回退最低攻击并最终选slot4；无毒师且medicine`[30,10]`选slot3。Linux Debug 14/14和独立oracle通过；最终仍需完整汇编↔C++ REVIEW。
