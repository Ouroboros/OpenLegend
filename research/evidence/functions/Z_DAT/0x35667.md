# 函数证据：`sub_35667` `0x35667..0x3570F`

状态：`implemented_pending_review`

映射：`BattleSetup::choose_ai_strongest_poison_target`。

按combatant槽序扫描不同side且未隐藏目标；要求目标poison严格小于95、target anti_poison严格小于actor use_poison。以signed best attack=0起步，仅strict更大时写actor word12，因此同值保留早槽，所有合格目标attack<=0时返回-1且不写。Linux Debug 14/14和独立oracle通过；最终仍需完整汇编↔C++ REVIEW。
