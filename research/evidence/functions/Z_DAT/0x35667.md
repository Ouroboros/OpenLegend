# 函数证据：`sub_35667` `0x35667..0x3570F`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::choose_ai_strongest_poison_target`。

当前原始`Z.DAT`物理范围为168字节、46条连续指令、8个显式跳转、11处加载时fixup和1次direct call；raw SHA256为`a8d1da0042ee44038243a0bb88e57f997936834ebfdc210d6bce7efe90996b93`，IDA加载后SHA256为`12572b96cfa2b9999aee3f02ba635490c26c9cf6268d83fe2a2ce04a34d9912d`。11处加载重定位相对raw均增加`0x20000`，归一化后逐字节相等。唯一caller为`sub_355FF`的`0x3563F`；唯一call是入口栈探测`sub_3ED1E`，本函数不消费RNG。

SI最佳攻击初始化为signed int16零，BX从零开始按signed int16与combatant count比较并扫描全部槽。候选side必须与actor不同，hidden必须恰等于0，任何正负非零hidden都跳过；函数不读取HP。候选poison按signed word严格小于95，candidate anti_poison按signed word严格小于actor use_poison；等号均拒绝。candidate attack按signed word严格大于当前best才更新，因此attack等于0或负数永不选择，同值保留首个槽。每次strict improvement都把候选槽写入actor word12并继续扫描，后续更大值可再次覆盖。

循环后只test SI：best仍为0时返回32位-1且保留旧word12；非零返回32位1。返回值不是目标槽，caller只据1/-1决定是否进入陈旧距离fallback。机器合同完成后才读取现代实现；`choose_ai_strongest_poison_target`的int16 best、过滤顺序、strict比较、逐次word12写入、无HP读取及bool false/true控制效果均逐块一致。现代提前安全验证actor/candidate role及异常count，替代原版线性越界，归类平台适配。

既有回归固定攻击`[30,50]`写slot4与`[0,0]`不写并回退。新增HP`[0,100]`且攻击`[50,50]`选择dead首槽3，锁定无HP过滤与strict tie；hidden`[-1,0]`且攻击`[100,50]`选择slot4，锁定负hidden也跳过；actor use_poison=-10、候选poison均-1、anti_poison`[-20,-10]`且攻击`[30,50]`选择slot3，锁定signed负值与anti等号拒绝。三组均由上层IQ61/seed9固定roll2、终态1341714958进入本selector。

首轮从入口覆盖全部46条指令、8个跳转、11处fixup、唯一caller、唯一call、全部过滤/更新路径和两个出口，未发现产品差异或未决项。正式battle golden由原始只读资产独立双生成逐字节一致，SHA256为`4b2490ea98be72e6b863709fe3dc8479a18896f5f1fa89f82b2ac6c5b7844dbe`。Linux app Debug根BUILD `proc_1449`全部14项测试（含battle、reverse framework与SDL smoke）通过。caller与陈旧距离fallback保留order25/27独立owner，不传播closure。故本owner归类`platform_adapted / converged_no_new_differences`。
