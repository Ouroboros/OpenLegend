# 函数证据：`sub_36A98` `0x36A98..0x36AF7`

状态：`pending_implementation`

部分映射：`BattleSetup::begin_player_movement_selection`、`finish_player_movement_selection`与`advance_player_movement`。

`0x36AA2..0x36AC3`先建立本地取消word并以`actor slot`、该参战者`round_value`、mode 0和取消word地址调用`sub_36AF7`。返回后`0x36ACB..0x36ADB`仅在取消word等于1时返回`-1`；未取消则`0x36ADC..0x36AED`先调用`sub_37245`按当前光标目的格反向标记最短路，再以`actor,0,0,0`调用`sub_37355`同步执行玩家移动，最后返回0。

现代实现保留该边界：玩家选择以当前行动值作为可选path上限；只有已确认且未取消的movement选择才能复制选择期路径图并标记最短路；逐格continuation复用原版写入顺序，直到目的格或行动值耗尽。battle4真实向量从`(26,24)`选择`(26,25)`，首步消费旧path、搬移occupancy、写方向和sprite、体力10→9、行动值2→1，并返回render、present与40 tick typed参数。

当前尚未由`BattleSession`实际读取键盘、重画战场、present并等待40 tick，因此函数保持`pending_implementation`。完整汇编静态REVIEW与Linux Debug真实资产测试覆盖取消、确认、最短路和首步状态。
