# 函数证据：`sub_3C672` `0x3C672..0x3C6D3`

状态：`implemented_pending_review`

映射：`BattleSetup::clear_hidden_ai_targets`。

逐参战者依次读取word11 `ai_target`与word12 `ai_poison_target`，当所指参战者word5 `occupancy_hidden`严格等于1时，把对应目标写-1；标志2等其他非零值不清除。现代实现保留攻击目标先于用毒目标的写入顺序。

原函数对-1或大于25的目标仍以`target*28`寻址，属于26槽参战者数组外内存读取；现代实现对0..25槽均执行原检查，包括当前活动数之外的槽，仅负值与大于25的值保持原值且不读取数组外。battle4向量覆盖hidden 1清除两字段、hidden 2不清除、活动数之外但26槽内目标清除及-1保留。函数无绘制、呈现、输入或等待，本体状态核心完整，现代安全边界留待最终REVIEW确认，当前标`implemented_pending_review`。
