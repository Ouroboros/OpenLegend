# 函数证据：`sub_3C672` `0x3C672..0x3C6D3`

状态：`platform_adapted`

映射：`BattleSetup::clear_hidden_ai_targets`。

## 机器身份

fresh临时IDB导出固定本体为97 bytes、24条连续指令、8个IDA flow block、3个条件分支、1个无条件跳转、7处fixup、1次栈探测call和本地`RET @ 0x3C6D2`。唯一入口xref为`sub_3271E:0x32A0D`，没有共享尾或内部外部跳入；唯一call为`0x3C677 -> sub_3ED1E`。

raw SHA256=`743c6fe530a8498dcffdf53dcebd1fbaf639b99f8c4b227f31be2ab6a6d2a2a5`，loaded SHA256=`d94dd9860aca3416096d717258f6aba2a1e38a323ec6fd80560137e800d91291`。7处加载值均为原始值`+0x20000`，逐项逆重定位后与只读`Z.DAT`逐字节一致。

## 状态合同

1. `EDX=0`，以signed低字`DX < word_556D2`为循环条件，活动源槽按升序扫描。
2. 每个活动源槽固定先读取word11 `ai_target`，再读取word12 `ai_poison_target`。
3. 每项目标word均sign-extend后乘28；查找范围不受当前活动参战者数限制，0..25固定槽均可引用，包括inactive目标槽。
4. 只有目标槽word5 `occupancy_hidden`严格等于1时才把对应目标字段写`-1`；hidden为0、2、负值或其他值都保留。目标HP、side、role及源槽自身hidden均不参与。
5. 当前活动数之外的源槽不扫描，即使其字段指向hidden1目标也保持不变。
6. 机器返回EAX为过程残值，唯一caller不读；无RNG、渲染、present、输入、音频或等待。

## C++与caller对照

逐基本块对照`BattleSetup::clear_hidden_ai_targets`，合法caller域零产品差异。现代lambda保留攻击目标先于用毒目标和活动源槽升序；清除计数是宿主测试观测，四个Session调用点均只检查optional成功。

机器唯一调用序列为`sub_3B238 -> sub_3C672 -> action_done/actor索引续行`，全部actor完成后才调用`sub_3C563`回合状态扣血。现代正常动作、wait同槽、hidden槽跳过和outcome异步结算四类路径均维持该可观察顺序：先结果检查，ongoing或战后消息完成后执行cleanup，再推进/保持actor或执行回合状态并进入round_wait。

原机器对任意signed目标都执行`DC736 + target*28`读取；`-1`最终仍为`-1`，但`<-1`与`>25`可能读取26槽数组外内存。现代对0..25均执行原检查，对其他值安全保留且不越界；实际初始化与selector合法写域仅为`-1`或活动槽号，归类平台安全适配。非法setup提前拒绝亦为平台边界。

## 验证

直接回归覆盖多活动源、攻击/用毒独立、同目标与自目标、hidden 0/1/2/负值、slot0/slot25、inactive target、inactive source、`-1/-2/26/32767`安全边界及无效role不参与。Session回归覆盖hidden-skip时全部活动源目标在推进前清除，以及outcome战后消息确认后cleanup先于回合状态扣血。

独立向量SHA256=`f596fa71372d63877c7acbd25eb4afce8a877d59b327144858399669ea06e084`；Golden三生成逐字节一致且历史73键逐值不变，74键SHA256=`2c2cd200fd7a5558f11b8bc515f80c3b69aef2d3fffacedf4351d05292ad3734`；Linux app Debug 14/14通过。

本owner归类`platform_adapted / converged_no_new_differences`。栈探测、唯一round-loop caller、结果/结算、actor索引续行和回合状态扣血保持独立owner。
