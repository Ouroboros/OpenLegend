# 函数证据：`sub_25911` `0x25911..0x25AB7`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；世界候选格同步场景入口。

独立汇编恢复：先从slot1开始找首个`role_id<=0`，未找到则party前缀长度6；固定扫描scene 0..83，两组入口坐标任一命中后处理condition。condition0直接允许；condition2仅扫描上述连续party前缀，任一角色IQ>=70允许；condition1及其他值拒绝。首个允许scene先render世界、present、执行64级fade-to-black，再同步调用scene。返回后按进入前方向反转0↔3、1↔2，清walk offset并恢复方向基帧；若scene返回world则以仍存活的世界cache/weather/idle/50步计数/palette render、present并执行65级fade-from-black，fade完成后才返回原移动调用者继续碰撞/坐标/cache/weather。

现代映射：`entrance_at`、`WorldMoveContinuation`、`LegacyGameRuntime`的world双侧预present/fade状态、保留的`WorldSession`、`start_scene`和return_world恢复。审计修正五项差异：condition2曾越过首空槽；scene返回曾丢弃移动后半段；现代曾直接切scene而缺入口世界present/fade；返回世界曾缺present/fade-from-black；scene往返曾重建world并清空全部瞬态。

回归覆盖IQ69/70、空槽后高IQ不触发、真实scene70返回后阻挡stay、合成可行入口moved、反向朝向、输入/世界palette周期在fade期间冻结、已旋转palette跨scene不变，以及第49次入口后第50次移动继续触发party衰减。原程序动态差分为`blocked_runtime_oracle`。最终双向逐基本块REVIEW为`not_started`。