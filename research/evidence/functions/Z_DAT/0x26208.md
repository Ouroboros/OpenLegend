# 函数证据：`sub_26208` `0x26208..0x265AB`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；审计包 `tmp/b9-review/0x26208.txt`

## 1. 独立机器恢复

- 按所选编号构造三类槽文件名。
- 成功域固定读取顺序为 scene-map `S*`、scene-event `D*`、ranger `R*`；编号槽IDX不参与运输。
- 三类固定记录读取完成后才将ranger/header/team/inventory等状态导入运行态。
- I/O成功后保留调用前画面执行64级淡黑。
- 导入完成后重建world运行态；在全黑palette下连续present两次相同裸world像素。
- 随后执行65级淡入。
- title/scene调用者进入world；系统页调用者由外层 `sub_25D0E` 恢复系统覆盖。

## 2. 汇编 → C++ 映射

- S/D/R固定记录读取：`persistence::load_numbered_slot`。
- 原子导入：`model::GameState::import_snapshot`。
- 调用者目的状态、64+2+65视觉continuation：`app::LegacyGameRuntime::perform_pending_io`、`activate_pending_load`、`advance_scene_effect`、`render`。
- world重建：`LegacyGameRuntime::start_world`。

## 3. 差异与整轮重启

实现阶段已修复系统调用者错误地直接返回world、scene载入失败无法返回槽位页、以及缺失的统一64+2+65尾链。最终轮从 `0x26208` 入口独立恢复，逐块确认 S→D→R、旧帧淡黑、两次全黑裸world、65级淡入和调用方返回时序，零新增差异。

## 4. 平台适配

- 原会话期BK文件由内存 `GameSnapshot` 代替；不复现BK磁盘副作用。
- 共享IDX常驻缓存与RAII关闭代替DOS句柄。
- 损坏/缺失输入采用无部分提交的失败处理；合法存档域结果与顺序不变。

## 5. 验证与结论

- Linux app Debug：14/14。
- persistence/UI回归覆盖完整snapshot恢复、调用者目的页面、两次黑屏像素相同及65级淡入。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
