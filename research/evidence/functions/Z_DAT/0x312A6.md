# 函数证据：`sub_312A6` `0x312A6..0x3192E`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；KDEF opcode64，仅 script938。

## 汇编独立合同

scene→shop：`0/1→0, 3→1, 40→2, 60→3, 61→4`。dialogue2974 后列出 stock>0 的槽；确认选择时用 `sub_2F34C` 只读取第一个 item174 槽的 count 判价，成功再由 `sub_2F39C` 只扣首 money 槽、stock--、`sub_2E571` 增加目标 item、dialogue2976，失败 dialogue2975；Esc 直接退出。不得汇总重复 money 槽。

退出后按本场景选择商人事件：scene0无；scene1 events17/18；scene3 15/16；scene40 21/22；scene60 17/18；scene61 10/11/12。每条记录通过 `sub_2D841` 把 fields0..7 完整写为 `0,0,-1,-1,939,-1,-1,-1`，仅 picture_delay/x/y 保持；不是只写 event_3。购买反馈 dialogue/render 完成后才写回。

## 修复差异

- 旧 `resume()` 先清 pending 再读 `pending_.shop_id`，任何购买都不会执行；现保存 previous shop_id。
- case64 曾在开场 dialogue 前提前设置 shop continuation，确认 dialogue 会被误当取消；现只由 `emit_queued()` 真正发出 shop result 时设置。
- 新增 `shop_feedback` continuation，确保成功/失败反馈 dismiss 后再写商人记录；取消则立即写回。
- `sub_2D841` helper 终审发现旧 `close_shop_events()` 只写 event_3=939，遗漏 cannot_walk/index/event_1/event_2 及三图片字段复位；现改为同一13参数完整修改。

## 测试

- 六个 scene 的 shop_id、cancel 和精确退出事件范围；所有待关闭记录先把 fields0..7 置777，再核对完整结果 `0,0,-1,-1,939,-1,-1,-1`。
- scene3/shop1：item42 stock2 price7，item174=10；购买后 stock1、货币3、item42=1，反馈前 event_3=-1、反馈后939。
- 重复 money 反例：首槽 item174=5、第二槽=100，price7 仍失败2975，stock/两 money 槽/目标物品均不变。
- `scene-goldens.json:kdef.opcode_64_script_938` 固定映射/范围/对话/货币。
- Linux Debug scene/ui 通过；最终双向 REVIEW 尚未执行。
