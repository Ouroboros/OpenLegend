# 函数证据：`sub_2E278` `0x2E278..0x2E28A`

状态：`platform_adapted`
最终REVIEW：`converged_no_new_differences`

## 1. 物理边界与调用者

- 入口/出口：`0x2E278..0x2E28A`，18字节，5条指令。
- 当前IDA loaded bytes与原始`Z.DAT`文件偏移`0x27C78`起18字节完全相同，SHA256均为`b2c3f06340020f016c1ea1bdc785c29984c9380a13df8ffcf3b03a3aa56edf24`；函数内两个call均为相对位移，无重定位差异。
- 共有4个物理callsite、3个owner：
  - `sub_2C319:0x2C5BB`：opcode13，无参数，返回后走共享PC+1路径。
  - `sub_302E0:0x30381`：武林大会每轮三胜后的轮间恢复完成后淡入，再显示下一轮对话。
  - `sub_302E0:0x3044A`：武林大会终局禁用49个场景事件并present后淡入，再显示终局对话。
  - `sub_30480:0x304DC`：每场大会战斗胜利并present后淡入，再显示胜利对话。
- 四个caller均不解释返回值。

## 2. 入口到出口机器行为

wrapper只执行栈帧保护、无参数调用`sub_3CD17`、把EAX清零并返回。`sub_3CD17`在返回前同步完成从黑到当前调色板的淡入；淡入算法本身属于独立UI closure，本项只关闭调用选择、无参数、先后顺序和返回边界。

因此机器中每个caller的淡入完成先于下一条对话、事件或PC指令；没有额外状态写入、分支或错误返回。

## 3. 当前原资产与非解释器caller域

独立Python按68槽真实宽度扫描全部1,018条KDEF：

- opcode13共346次，首条`(script2,PC20)`，末条`(script1016,PC128)`。
- 完整`<II:script_id,pc>`位置流SHA256为`719e01b8842f5d467f82cd3160e1cfcdbbf35e7ba602ca2148f6aa6920dd935e`。
- synthetic script4为`[13,14,-1]`，固定淡入回调完成后才产生淡出边界，再完成事件。
- 真实script931在休息恢复前含opcode14、恢复后含opcode13，固定两种回调与同步opcode12之间的顺序。
- 武林大会全胜路径固定15次战后淡入、4次轮间淡入和1次终局淡入，共20次，分别覆盖三个非解释器callsite。

上述资产域、4个物理callsite与大会20次分解写入`scene-goldens.json:kdef.opcode_13_fade_from_black`；oracle文件SHA256为`e2252a1718834e1f64a4a8254d9aa0adf8f96c096be136140bf41baf7a804693`。

## 4. 汇编→C++收敛

暂不参考现有C++完成上述机器推导后，逐caller对照：

- `SceneSession::run_event(case13)`先固定PC+1，再返回`fade_from_black`；`resume`只在宿主完成该效果后继续下一word，与同步机器调用的可观察顺序一致。
- `advance_tournament_trial`在战斗胜利、轮间恢复及终局事件禁用三个阶段按原caller顺序排入`fade_from_black`，后续对话均位于该队列项之后。
- `LegacyGameRuntime::handle_scene_result`和世界菜单事件结果处理都把该输出交给宿主效果链，完成后再resume原SceneSession。
- 委托`sub_3CD17`的调色板步进仍由其`ui-closure.tsv`行独立终审，本项不提前关闭。

首轮完整wrapper及4个caller对照未发现产品差异；仅新增全资产opcode13位置流和大会20次淡入计数。最终复核零新增差异或未决项。

现代宿主逐帧continuation替代DOS函数内同步调色板循环，调用顺序和完成边界一致，归类`platform_adapted`。

## 5. 验证

- headless IDA从正式数据库副本重新导出5条指令、4个callsite、caller上下文和委托callee body；原资产与正式IDB保持只读。
- 独立oracle覆盖346次opcode13、4个物理callsite和大会20次淡入分解；二次生成逐字节一致（`proc_9829`）。
- Linux app Debug根BUILD脚本14/14通过（`proc_234d`）；order37静态门及全局closure计数通过（`proc_853e`）；reverse framework 577行有效、scene-event剩61项（`proc_0aa4`）。
- 动态原程序差分仍按统一策略记为`blocked_runtime_oracle`，不替代静态机器码与独立资产oracle。
