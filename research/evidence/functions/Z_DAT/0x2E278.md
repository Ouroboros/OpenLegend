# 函数证据：`sub_2E278` `0x2E278..0x2E28A`

状态：`implemented_pending_review`

机器码仅执行 stack guard、调用 `sub_3CD17`（从黑淡入）并返回0；opcode13 固定宽度1。现代 case13 先推进 PC，再返回 `fade_from_black` 同步边界；下一次 `resume` 才继续解释后续 word。synthetic `[13,14,-1]` 固定淡入→淡出→stay 的三个独立边界；最终 REVIEW 仍需审计委托平台淡入链和 tournament 调用者。
