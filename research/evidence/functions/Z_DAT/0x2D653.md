# 函数证据：`sub_2D653` `0x2D653..0x2D678`

状态：`implemented_pending_review`

机器码顺序固定为调用 `sub_29D2D` 重绘场景，再把当前 framebuffer 交给 `sub_3D6D1` 呈现，返回0。KDEF opcode0 直接调用本函数后 PC 加1。

现代 `SceneSession::run_event(case0)` 在 PC 加1后返回同步 `SceneStepKind::present`；宿主实际呈现后才恢复同一事件，`render_map/draw_overlay` 提供该帧。真实 scripts274、931及多条对话间 opcode0 已覆盖 continuation；最终双向 REVIEW 尚未执行。
