# 函数证据：`sub_29B3C` `0x29B3C..0x29C36`

状态：`implemented_pending_review`

机器码按 x 外层、y 内层扫描当前场景完整64×64事件层，而不是遍历200条事件记录。每个非 `-1` 格取对应11-word事件：word5为动画首图，word6为末图，word7为当前显示图，word8为延迟。word5 `<=0` 跳过；word7 `>=word6` 时先重置到word5；word7 `>word5`、计数 `%4==0` 且未到末图时加2；延迟 `<=计数%100`、word7等于word5且未到末图时再加2。同一事件若被多个格引用会在一次调用中按出现次数重复推进。

现代 `SceneSession::idle_tick` 的 delegated scan 已恢复逐格顺序并只写 word7（枚举历史名 `begin_picture`），word5继续作为首图/条件字段；主循环先将计数 `+1 mod1000` 再进入本函数。独立 oracle 固定 synthetic 双格在计数4从102推进到106、word5保持100；全100份原资产扫描确认当前无重复事件格。`sub_28E40` 场景入口还有一次不增计数的调用，保留在该 caller 的待映射范围，最终双向 REVIEW 尚未执行。
