# 函数证据：`sub_36E06` `0x36E06..0x36E7F`

状态：`implemented_pending_review`

映射：`openlegend::battle::BattlePathing::build(..., BattlePathMode::movement)`。

函数先调用 `sub_36EF8` 建移动阻挡图，把 source cell强制写0，再初始化255槽环形队列为 sentinel `(0,-1)`、source、读下标0、写下标2、distance0；反复调用 `sub_37070`，直到其返回-1。真实 battle0/93 空 occupancy 与单格占位 FNV-1a 已由独立 oracle 固定，Linux Debug 14/14。最终 REVIEW 前不标 `assembly_exact`。
