# 函数证据：`sub_22066` `0x22066..0x22090`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；审计包 `tmp/shared-ui-review/0x22066.txt`

## 1. 独立机器恢复

- `sub_3ED1E(8)`仅为编译器栈前导。函数压入参数2并调用共享队伍选择器 `sub_22090`。
- 若返回值为负，直接保留该返回值退出；当前合法取消域为 `-1`。
- 若返回值非负，则以该值作为party slot，从 `word_C0B78[slot]` 有符号扩展角色ID，压入后调用状态页函数 `sub_22A59`；除此之外不修改角色、队伍或选择状态。
- 机器调用点只有游戏主菜单状态项 `0x21446` 和战斗动作7 `0x333BC`。两处都复用同一个参数2选择器与后续状态页，只由全局上下文决定world/scene/battle背景。

## 2. 汇编 → C++ 映射

- world/scene包装链：`GameMenuController` 的status命令、direct party-select与selected party slot，`LegacyGameRuntime::render/handle_game_menu_result`，以及后续 `BattleRenderer::render_character_status`。
- battle包装链：`BattleSession::begin_player_status_selection/handle_player_status_selection_key/render_player_status_selection`；确认时通过party slot读取角色ID，进入两页状态continuation。
- 机器同步调用/返回拆为事件驱动状态，但选择器确认前不进入状态页，取消后回到原调用菜单，角色ID映射边界不变。

## 3. 差异、修正与重启

- 首轮包装函数本身的42字节控制流已闭合，但其callee `sub_22090` 尚未完成三上下文终审，因此不提前登记终态。
- `sub_22090`终审发现world/scene选择页遗漏主菜单层、battle选择页遗漏键态清零回报；修正后本包装函数旧结论同步废弃。
- 从 `0x22066` 入口重新覆盖参数2调用、负值短路、party-slot映射、`sub_22A59`调用和唯一出口，并沿两处调用点核对现代world/scene/battle状态机；最终轮零新增差异。

## 4. 平台适配

- 原函数的EAX返回值仅被其调用方忽略；现代以菜单/战斗phase表达取消或进入状态页，不暴露数值返回ABI。
- 角色ID越界时现代安全拒绝进入状态页；当前原始RANGER合法路径与机器映射一致。

## 5. 验证与结论

- `tests/unit/ui/title_menu_test.cpp`固定状态选择页、两页状态与world/scene背景。
- `tests/unit/battle/battle_data_test.cpp`固定battle动作7的取消、party-slot映射、三确认键、两页状态hash及动作未消耗边界。
- Linux app Debug `proc_0f3c`：14/14。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
