# 函数证据：`sub_22066` `0x22066..0x22090`

状态：`pending_implementation`

该函数以参数2调用`sub_22090`。返回值非负时，先经`word_C0B78`队伍表把party slot映射为角色ID，再调用`sub_22A59`连续显示两页角色状态；取消时直接返回。

战斗调用点`sub_32E59`现映射为`BattleSession::begin_player_status_selection`与后续两页相位：队伍选择Escape取消返回原动作ordinal，确认后映射角色并显示两页，每页各等待任意非零键，第二页后回到动作菜单。该路径不写参战者action-done、不推进actor、不消费RNG。

battle4固定两人队伍覆盖取消、上下回绕、Enter/Space/keypad Insert三确认键、第一页任意键翻页和第二页任意键返回；关键日志与三帧独立原资产像素hash均由回归固定。共享非战斗菜单调用点尚未执行最终双向REVIEW，因此保持`pending_implementation`。
