# 函数证据：`sub_22066` `0x22066..0x22090`

状态：`implemented_pending_review`

该函数以参数2调用`sub_22090`。返回值非负时，先经`word_C0B78`队伍表把party slot映射为角色ID，再调用`sub_22A59`连续显示两页角色状态；取消时直接返回。

战斗调用点`sub_32E59`现映射为`BattleSession::begin_player_status_selection`与后续两页相位：队伍选择Escape取消返回原动作ordinal，确认后映射角色并显示两页，每页各等待任意非零键，第二页后回到动作菜单。该路径不写参战者action-done、不推进actor、不消费RNG。

battle4固定两人队伍覆盖取消、上下回绕、Enter/Space/keypad Insert三确认键、第一页任意键翻页和第二页任意键返回；关键日志与三帧独立原资产像素hash均由回归固定。

世界和场景游戏菜单现由`GameMenuController`执行同一参数2输入状态，`LegacyGameRuntime`先重画各自背景，再调用共享`BattleRenderer::render_character_status_selection`和`render_character_status`。世界路径锁定选择框及两页整帧hash；场景路径逐像素确认选择框矩形及中央状态面板之外保持场景背景。旧`BasicUiRenderer`简化状态页已删除，角色状态只有一个精确renderer实现。

实现映射已覆盖世界、场景和战斗三个调用上下文，状态提升为`implemented_pending_review`；最终双向逐基本块REVIEW仍为`not_started`。
