# 函数证据：`sub_212C0` `0x212C0..0x21496`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；审计包 `tmp/shared-ui-review/0x212C0.txt`

## 1. 独立机器恢复

- `sub_3ED1E(16)`仅为编译器栈前导。入口按有符号16位方向索引 `word_544E2`，把对应基础人物图号写入 `word_C0BF8`；随后把主菜单选择 `dword_C0820`清零、循环标志 `word_544E0`置1并清last-key `byte_51B6B`。
- `word_C0BF2==0`时菜单项数为6并调用world renderer `sub_2558B`；非零时项数为4，但只有等于1才调用scene renderer `sub_29D2D`。每轮严格执行背景render→`sub_269AB`菜单绘制→`sub_3D6D1` present，再读取无符号last-key。
- 仅 `Enter=0x0D`、`Space=0x20`、keypad Insert=`0x96`进入确认分支；先清last-key及三枚确认键态，再按当前选择调用：0 `sub_21496`医疗、1 `sub_21AC0`解毒、2 `sub_2A0D9`物品、3 `sub_22066`状态、4 `sub_25BBA`离队、5 `sub_25D0E`系统。scene的4项边界使4、5不可选。
- `Down=0x98`在末项回绕到0，否则递增；`Up=0x9E`在0回绕到末项，否则递减。
- 原BUG：Down和Up两个分支都清 `byte_51C05`，即translated `0x98`状态；Up分支不清自身 `0x9E`状态。两分支均清last-key。
- `Esc=0x1B`清last-key、完整Esc状态并把循环标志清零；函数从唯一循环出口返回。其他键不改选择或循环状态。
- 子菜单返回后继续使用原主菜单选择并从背景render开始下一轮；被调函数可通过共享状态改变后续控制流，其内部行为分别留给对应函数终审。

## 2. 汇编 → C++ 映射

- 主菜单状态、world六项/scene四项、环绕与精确确认键：`ui::GameMenuController::show_main/handle_key/confirm_main`。
- world/scene入口基础人物帧写入：`world::WorldSession::prepare_game_menu_frame`与`scene::SceneSession::open_ui`；world以独立显示override保留机器“改当前图号但不改步行动画计数”的写集。
- last-key及键态清理：`LegacyGameRuntime::handle_key`的`main_game_menu_key_state_reset`、`LegacyKeyStateReset::down_translated`，以及SDL主循环的`LegacyKeyboard::clear_last_key/clear_state/clear_confirmation_states`。
- 背景→菜单→present：`LegacyGameRuntime::render`的game-menu分支、`BasicUiRenderer::render_game_menu`及平台present；主菜单命令由`handle_game_menu_result`路由至对应现代continuation。

## 3. 差异、修正与重启

- 首轮发现主菜单层未返回任何键态清理请求；确认键、Esc、Down/Up可能把宿主键态带出菜单。该轮结论废弃。
- 继续恢复机器写集后发现入口基础人物图号未承接，菜单背景会保留最近步行/idle图；并确认Up分支原样误清Down状态。新增world/scene基础帧承接、主菜单专用reset及`down_translated`平台分支。
- 初次Linux门禁因旧framebuffer hashes及“背景保持”测试仍把菜单前步行人物帧当作不变量而失败；更新为基础人物帧golden，并仅在局部保持断言中排除机器明确改写的人物精灵区域。
- 上述每次修正后均废弃当轮，从`0x212C0`入口重新覆盖初始化、context、render/present、无符号键分派、全部状态写、环绕、六个调用和唯一出口。
- 后续审计 `sub_269AB/sub_2CEBF/sub_2050A` 发现主菜单primitive应为五级圆角style3混色，而非现代旧直角框；初次圆角修正又误作style4的4/8+4/8。读取 `unk_54294` 原始权重表后改为palette0的3/8加目标5/8，两轮旧可视结论均废弃。最终再次从`0x212C0`入口覆盖world/scene背景、主菜单调用参数、present与六项分派，零新增差异。

## 4. 平台适配

- DOS同步嵌套轮询改为SDL事件与宿主帧continuation；每个实际呈现帧仍保持背景→菜单→present，确认前最后一帧及子菜单返回重绘边界保持。
- 非法 `word_C0BF2` 值在机器中会跳过背景render但继续绘菜单；现代context是封闭枚举，只允许world/scene合法域。
- 原BUG没有修正为对称行为：Up仍请求清translated Down `0x98`，保留原写集。

## 5. 验证与结论

- `tests/unit/world/world_map_test.cpp`固定步行帧5032进入菜单后变为方向基础帧5030；`tests/unit/scene/scene_test.cpp`固定入口override 6890进入菜单后变为方向基础帧5016。
- `tests/unit/ui/title_menu_test.cpp`固定主菜单Up返回`down_translated`、确认组三键reset，并更新world/scene菜单各子页的真实资产framebuffer hashes。
- Linux app Debug `proc_765f`：14/14。
- `git diff --check`通过。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
