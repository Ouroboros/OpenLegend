# 函数证据：`sub_29819` `0x29819..0x299A1`

状态：`implemented_pending_review`

机器码实现水平单步：无条件清临时动画、frame offset加2且大于12回到2，按 step符号选择 right/left 基础图；target x先 clamp `[0,63]`。仅当 building=0、target height-current height `<10`、事件为空或 cannot_walk=0、earth不落九组阻挡范围时更新 x；成功后视口 x按 `x-11` clamp `[0,36]`，阻挡时仍保留朝向与步行动画推进。

现代 `move/apply_scripted_walk_step/target_is_walkable/update_view_origin/player_frame` 分解保持上述顺序与条件。真实正常移动、阻挡、边界及 opcode30 水平脚本逐帧 hash 已覆盖合法域；`sub_2F171` 证据的 delegated subset 在此登记，最终双向 REVIEW 尚未执行。
