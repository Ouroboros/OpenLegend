# 函数证据：`sub_363AC` `0x363AC..0x3650E`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::begin_ai_support_plan`、`resume_ai_support_after_move`与`BattleSession`动作4解毒continuation。

## 机器身份与调用边界

当前原始函数固定为354 bytes、79条连续指令、7个分支、28处fixup和7次direct call。raw SHA256为`31cc8176adbb3df2976334035fa8a1d10adf44ecdb639d389f1b541c659c0acd`，IDA loaded SHA256为`892ef6cd06154bd14bb479526d94cccfab365ec97a2061d23ca5e3dfbdcc08f4`；28处DWORD均严格增加`0x20000`，逐处反变换后与原始354字节完全一致。

唯一caller为`sub_33599:0x33BED`动作4case：按signed低16位压入actor，调用后不读取EAX，跳到统一动作尾并最终写actor combatant word13 `action_done=1`。

7次调用依次为栈探测`sub_3ED1E`一次、targeting `sub_36E7F`两次、解毒`sub_39B8E`一次、移动`sub_3650E`一次、自动攻击`sub_34C47`一次和休息`sub_34AD3`一次。与`sub_36210`医疗owner不同，本函数没有任何`sub_3F50B`绝对值调用。所有callee及外部共享尾`loc_39A3E`独立关闭，不随本owner传播closure。

## 完整汇编合同

入口按signed actor取得role detoxification word，执行signed IDIV 15向零截断再加1；商保存在signed DI，EDX余数以后不再读取。actor当前x/y写入`word_556D6/word_556D8`作为source，调用`sub_36E7F`建targeting图；随后按signed `word_E6EE0`重读目标当前x/y并取得signed距离word。`DI >= distance`时立即调用`sub_39B8E(actor)`解毒。

首次超距时，actor signed行动值word6严格大于0才调用`sub_3650E(actor,mode1,range)`；零或负值跳过，移动EAX被忽略。两路汇合后重新读取同一全局目标槽及其当前x/y，写`word_556DA/word_556DC`，再重读actor当前x/y到source并第二次调用`sub_36E7F`建图。第二次取得signed目标距离后，`DI >= rebuilt distance`回跳解毒调用。

二检仍超距才进入回退：现场重读actor role signed attack并以32位加倍，同时读取AI回合入口冻结在signed word中的己方总值`word_E6EDC`及人数`word_E6ECC`，执行signed `(2*total)/count`。只有`2*actor_attack`严格大于平均值才调用完整自动攻击，相等或更小调用休息。

解毒、攻击和休息三路都尾跳到外部`loc_39A3E`；该共享epilogue丢弃delegated actor参数、依次恢复EDI/ESI/EBX并直接RET回`sub_33599`。EAX由各callee透传但caller忽略。owner自身只在两次建图前写source x/y、第二次建图前写target x/y；不直接写action、action_done、combatant状态或RNG状态，也不直接消费RNG。

## 汇编→C++ REVIEW

先冻结机器合同后再从`0x363AC`逐条覆盖79条指令及三种出口。`detox_targeting_range`的signed除法、加1和signed16返回与DI一致；共享support plan按actor/target当前坐标建图，以signed距离方向判断，严格按mode1/range传递移动参数；正行动值进入移动，零/负值立即执行第二次建图。移动continuation保留同一目标槽并在二检前重读当前目标坐标。

计划携带`BattleAiTurnPrelude`在`sub_33599`入口等价阶段冻结的wrapped己方总值/人数，并跨移动保持；fallback只现场重读actor attack，不重扫队友。signed 32位倍增、signed除法与strict greater门完全一致。该入口直接解毒时选择既有`apply_detox_target`及动画链；完成时`finish_detox_action`提交delegated action_done/counter/体力，再由`finish_ai_turn`重复外层action_done赋值，与`sub_39B8E`返回后caller统一尾一致。攻击与休息分支进入各自独立handler再执行外层完成。

动作选择到直接执行之间无额外帧或RNG，移动前后也不新增owner直接RNG。现代对非法actor/role/target/count和坐标安全拒绝，替代唯一合法caller域外的原版线性访问或除零，归类平台适配。首轮入口REVIEW未发现产品差异，因此本owner无需修改产品源码。

## 回归、golden与结论

动作4聚焦plan回归覆盖range1/distance1直接解毒、正行动值mode1移动、目标坐标改变后第二次测距重读、行动值0和-1均跳移动但执行两次测距、signed detoxification `-30 -> range -1`、严格相等休息、wrapped己方总值、live actor attack、队友突变仍使用入口冻结total/count，以及非法target/actor/role拒绝。既有完整Session回归锁定直接动作4的poison100→62、体力100→98、counter递增、外层action_done、9帧magic+10帧damage、attack/effect音频、首帧hash及最终RNG；共享support移动continuation另有真实四步mode1 Session向量。

正式battle golden加入本函数机器身份、28处重定位、7次call、7个分支及signed/frozen独立向量；两次原始资产独立生成逐字节一致，SHA256为`7358979c860b6dc41acde03c5213306e5bba6c821a38db385e7dc63f31758041`。根BUILD `proc_8d98`完成Linux app Debug 14/14，battle、reverse framework及其余组合测试全部通过。

本owner归类`platform_adapted / converged_no_new_differences`；原程序动态差分继续登记为`blocked_runtime_oracle`。
