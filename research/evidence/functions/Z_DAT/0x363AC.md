# 函数证据：`sub_363AC` `0x363AC..0x3650E`

状态：`pending_implementation`

部分映射：`BattleSetup::begin_ai_support_plan`与`resume_ai_support_after_move`。

该入口处理AI解毒动作4，与`sub_36210`结构相同，但射程字段改为actor detoxification，命中后调用`sub_39B8E`。射程为signed除15向零截断再加1；targeting距离不大于射程直接解毒；超距且round value严格大于0时调用`sub_3650E(actor, mode=1, value=range)`，随后恢复同一目标并重检。

移动后或跳过移动后的第二次重检仍超距时，按`2*actor.attack` strict greater于`2*wrapped_allied_total/allied_count`选择自动攻击，否则休息。尾跳`0x39A3E`只是原调用帧共用退出路径。固定typed验证覆盖range1直接解毒及round0超距两次建图后休息；handler自身不提前消费RNG或写action_done。

`BattleSession`现实际执行mode1/value=range逐格状态、render/present和两次BIOS tick变化，移动后恢复同一目标并调用typed重检；重检为rest时也实际休息并完成外层actor continuation。解毒动画/呈现、攻击及命中后的外层continuation仍未接线，故保持部分实现。Linux/Windows Debug完整BUILD、全部测试、独立oracle与逆向框架门禁通过。
