# 函数证据：`sub_363AC` `0x363AC..0x3650E`

状态：`implemented_pending_review`

映射：`BattleSetup::begin_ai_support_plan`与`resume_ai_support_after_move`。

该入口处理AI解毒动作4，与`sub_36210`结构相同，但射程字段改为actor detoxification，命中后调用`sub_39B8E`。射程为signed除15向零截断再加1；targeting距离不大于射程直接解毒；超距且round value严格大于0时调用`sub_3650E(actor, mode=1, value=range)`，随后恢复同一目标并重检。

移动后或跳过移动后的第二次重检仍超距时，按`2*actor.attack` strict greater于`2*wrapped_allied_total/allied_count`选择自动攻击，否则休息。尾跳`0x39A3E`只是原调用帧共用退出路径。固定typed验证覆盖range1直接解毒及round0超距两次建图后休息；handler自身不提前消费RNG或写action_done。

`BattleSession`直接命中时调用共享解毒状态核心，再执行9帧effect36、attack/effect双bank音频、10帧无flash damage、sprite刷新及AI外层完成；真实battle4向量锁定目标poison100→62、体力100→98、首magic/damage整帧hash`0xae0f13fbbc4c8083`/`0xa9cb56cb87bcbf4d`、最终RNG662824084。mode1移动、同目标恢复和二次重检复用医疗入口的实际逐格render/present/tick continuation；重检为attack/rest时分别进入完整共享自动攻击或休息及外层actor continuation。Linux Debug完整BUILD全部14项测试通过；本函数推进为`implemented_pending_review`，最终汇编↔C++ REVIEW仍待执行。
