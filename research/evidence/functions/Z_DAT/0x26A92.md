# 函数证据：`sub_26A92` `0x26A92..0x26B5E`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；每50次方向移动尝试的party衰减。

独立汇编恢复：32位计数递增，只有恰好50执行并清0。扫描6个slot：slot0无条件，后续仅role id>0。门禁保留机器原BUG：hurt读取真实`roles[role_id]`，poison却读取`roles[slot]`；任一>50时，对真实role的HP、MP、physical power分别在`>1`时减1。三项顺序固定且不修改hurt/poison。

现代映射：`WorldSession::move`入口的`role_recovery_counter_`与bug-compatible slot poison读取；该调用发生在方向/动画推进和scene入口探测之前。新增slot1→role10、poison取role1的错位回归，确认衰减作用于role10。

blocked和enter_scene也计入移动尝试；无RNG。最终双向逐基本块REVIEW为`not_started`。