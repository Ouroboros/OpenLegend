# 函数证据：`sub_2E28A` `0x2E28A..0x2E29C`

状态：`implemented_pending_review`

机器码仅执行 stack guard、调用 `sub_3CC97`（淡出至黑）并返回0；opcode14 固定宽度1。现代 case14 先推进 PC，再返回 `fade_to_black` 同步边界；下一次 `resume` 才继续解释后续 word。synthetic `[13,14,-1]` 固定淡入→淡出→stay 的三个独立边界；最终 REVIEW 仍需审计委托平台淡出链和 tournament 调用者。
