# 函数证据：`sub_2E28A` `0x2E28A..0x2E29C`

状态：`platform_adapted`
最终REVIEW：`converged_no_new_differences`

## 1. 物理边界与调用者

- 入口/出口：`0x2E28A..0x2E29C`，18字节，5条指令。
- 当前IDA loaded bytes与原始`Z.DAT`文件偏移`0x27C8A`起18字节完全相同，SHA256均为`9676e06789973719b49261b6257f5fd6a124ed06284db962d5a4b1785a848cc4`；两个call均为相对位移，无重定位差异。
- 共有3个物理callsite、2个owner：
  - `sub_2C319:0x2C5C5`：opcode14，无参数，返回后走共享PC+1路径。
  - `sub_302E0:0x3036A`：武林大会每轮三胜后的对话和present之后淡出至黑；随后另执行300计数延迟、恢复主角、再从黑淡入。
  - `sub_302E0:0x30414`：武林大会全部五轮胜利的五段对话和present之后淡出至黑；随后禁用49个场景事件。
- 三个caller均不解释返回值。

## 2. 入口到出口机器行为

wrapper只执行栈帧保护、无参数调用`sub_3CC97`、把EAX清零并返回。`sub_3CC97`在返回前同步完成当前调色板至黑的64帧淡出；算法本身属于独立UI closure，本项只关闭调用选择、无参数、先后顺序和返回边界。

因此每个caller只在淡出完成、画面已黑后执行下一条PC指令或大会逻辑；wrapper没有额外状态写入、分支或错误返回。

## 3. 当前原资产与非解释器caller域

独立Python按68槽真实宽度扫描全部1,018条KDEF：

- opcode14共171次，首条`(script10,PC129)`，末条`(script1016,PC70)`。
- 完整`<II:script_id,pc>`位置流SHA256为`f40aad4e13d6cf6bf668883a8d3243002c7a5e01d4a1664a779d4542ae9d9575`。
- synthetic script4为`[13,14,-1]`，固定先淡入完成，再淡出完成，之后事件才结束。
- 真实script931固定opcode14淡出完成后执行opcode12休息恢复，再由opcode13淡入。
- 武林大会全胜路径固定4次轮间和1次终局淡出，共5次，覆盖两个非解释器callsite。轮间淡出后的机器300计数延迟换算为`legacy_delay_ticks(300)=8`；现代结果写`wait_ticks=9`，其中基准1不产生额外停留，其余8 ticks只在64帧淡出全部完成后于黑屏消耗，不会延长每一帧。

上述资产域、3个callsite、大会5次淡出及黑屏停留边界写入`scene-goldens.json:kdef.opcode_14_fade_to_black`；oracle文件SHA256为`6d0c629e6ab40d49614740412e29ea0c836c9ae9fa17b87d16a8d7ae227dfc77`。

## 4. 汇编→C++收敛

暂不参考现有C++完成上述机器推导后，逐caller对照：

- `SceneSession::run_event(case14)`先固定PC+1，再返回`fade_to_black`；宿主效果完成后的`resume`才解释下一word。
- `advance_tournament_trial`在4次轮间对话后返回`fade_to_black(wait_ticks=9)`；`LegacyGameRuntime::advance_scene_effect`先逐一呈现全部淡出调色板帧，只有最后一帧完成后才消耗额外8 ticks（`legacy_delay_ticks(300)=8`），等价保留机器淡出后的独立300计数黑屏延迟。
- 终局对话后的`fade_to_black(wait_ticks=1)`完成后才进入事件禁用阶段。
- `LegacyGameRuntime::handle_scene_result`和世界菜单事件处理均在宿主效果完成后resume对应SceneSession。
- 委托`sub_3CC97`调色板步进继续由其UI closure独立终审，本项不提前关闭。

首轮完整wrapper及3个caller对照未发现产品差异；仅新增全资产opcode14位置流和大会5次淡出计数。最终复核零新增差异或未决项。

现代宿主逐帧continuation替代DOS函数内同步调色板循环，调用顺序、完成边界及轮间黑屏停留一致，归类`platform_adapted`。

## 5. 验证

- headless IDA从正式数据库副本导出5条指令、3个callsite、caller上下文及`sub_3CC97`完整128字节body；正式IDB与原资产保持只读。
- 独立oracle覆盖171次opcode14、3个物理callsite、大会5次淡出及轮间停留分解；双次生成逐字节一致（`proc_871f`）。
- Linux app Debug根BUILD脚本14/14通过（`proc_9b68`）；order38静态门及全局closure计数通过（`proc_71aa`）；reverse framework 577行有效、scene-event剩60项（`proc_fe9c`）。
- 动态原程序差分仍按统一策略记为`blocked_runtime_oracle`，不替代静态机器码与独立资产oracle。
