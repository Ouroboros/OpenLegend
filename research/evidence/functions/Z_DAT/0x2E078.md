# 函数证据：`sub_2E078` `0x2E078..0x2E155`

状态：`platform_adapted`
最终REVIEW：`converged_no_new_differences`

## 1. 物理边界与调用者

- 入口/出口：`0x2E078..0x2E155`，221字节，45条指令。
- 当前IDA loaded bytes SHA256：`69caaca765139c6fb1333c9bddbce324f325f8583dde02dd6f8a6fc39e474cdc`。
- 原始`Z.DAT`文件偏移`0x27A78`起221字节SHA256：`83a294457130b648e49416cd3883755e376ef4de2a1153a98302e1d1c956901f`。
- loaded/raw共有14个差异字节；每个均为绝对地址operand高字节从raw增加`0x02`，完整解释为DOS加载基址`+0x20000`重定位。
- 两个物理caller：
  - `sub_2C319:0x2C6AA`：opcode21有符号压入role word，返回后进入共享`0x2C530`路径回收4字节并固定PC+2；helper返回值不解释。
  - `sub_30559:0x3057E`：武林大会收尾从物理index6递减到1读取队伍相邻words，仅当读取值signed `>0`时调用；返回值不解释。

## 2. 入口到出口机器行为

| 基本块 | 机器行为 |
|---|---|
| `0x2E087..0x2E0C4` | 从team slot1开始，以movsx后的word和role ID精确比较。第一个命中槽开始把后续槽向左移动到slot4，slot5写`-1`，然后退出扫描。slot0不参与；未命中则六个team槽均不写。 |
| `0x2E0C4..0x2E0E5` | 读取角色word23；值恰为`-1`时跳过，否则以`item_id*190`定位item record并把word38 `user`写`-1`。 |
| `0x2E0E5..0x2E106` | 对角色word24重复同一item.user解绑。 |
| `0x2E106..0x2E127` | 对角色word61修炼物重复同一item.user解绑。 |
| `0x2E127..0x2E151` | 无条件把角色word23、24、61、62依次写为`-1,-1,-1,0`，即清两件装备引用、修炼物引用和修炼经验。 |
| `0x2E151..0x2E155` | 返回0。 |

队伍是否命中不控制个人物品清理；因此“不在队伍中的合法role ID”仍会解绑三件物品并清角色字段。重复role ID只移除扫描遇到的第一份，后续重复值随其他槽一起左移而保留。

机器只把item ID `-1`当空；其他负值、item越界及role越界都会形成原机越界寻址。现代对这些当前资产不可达输入稳定拒绝，不把未定义内存破坏引入容器。

## 3. 第二caller的物理别名

`sub_30559`把循环index初始化为6，而队伍实际只有slots0..5。机器读取`word_C0B78[index*2]`时，index6恰好落到紧邻的header word18，即inventory item0；随后才按5、4、3、2、1读取队伍槽。每次调用本helper后，下一次读取看到的是已压缩的最新team状态。

现代`SceneSession::run_event(case59)`显式把index6映射为`inventory_item(0)`，其余映射为`team_member(index)`，并只对`role_id>0`调用`remove_team_role`。这是对原物理越界别名的确定性表示，不提前关闭`sub_30559`后续36条事件禁用逻辑。

## 4. 当前原资产域

独立Python按68槽真实宽度扫描全部1,018条KDEF：

- opcode21共35次，完整`<IIh:script_id,pc,role_id>`参数流SHA256为`d08335eb84f4c585ab7ed59750f0593730f09d05e5ed2eaf2a5796f709de97f2`；首条`(320,358,26)`，末条`(998,4,76)`。
- 共26个role ID：`1,2,9,16,17,25,26,28,29,35,36,37,38,44,45,47,48,49,51,53,54,58,59,61,63,76`，范围1..76，全部落在320条角色记录内。
- 唯一opcode59为script932 PC38。
- 这26个基准角色共有78个装备/修炼物槽；仅role44 word24=item123、role58 word23=item106非空，两件物品的基准user均为`-1`，其余槽均为`-1`。
- 真实script950为21 words，在PC4执行`(21,1)`后继续opcode3事件写入；回归设置team `[0,1,2,-1,...]`及角色1的装备4/5、修炼物6，验证队伍左移、尾槽`-1`、三条item.user解绑和四个角色字段清理。
- script932回归把inventory item0设为role6，验证index6别名在role6不属于team时仍完成个人物品清理；slots1..5则验证逐次压缩读取直至仅保留slot0。

完整参数频率、基准个人物品域、script950和opcode59 caller向量写入`scene-goldens.json:kdef.opcode_21_leave_role`；oracle文件SHA256为`e840f6444b464fb8cab9c23a9aacc1cfe3533e3986d1d9af390ed1220034df92`。

## 5. 汇编→C++收敛

暂不参考现有C++完成上述机器推导后，逐基本块对照：

- `remove_team_role`从slot1开始，首命中后逐槽读取next并把slot5置`-1`；未命中不写team但仍进入清理。
- `clear_role_personal_items`按角色word23、24、61顺序写合法item的user=-1并清角色引用，最后清word62。
- opcode21固定PC+2；opcode59显式保持index6物理别名、signed `>0`门和调用后的动态队伍读取。
- 合法当前资产不触发现代role/item范围保护。

首轮完整入口到出口对照未发现产品差异；仅补充真实script950三件item.user均已解绑的直接断言。无需修改产品实现，最终复核零新增差异或未决项。

现代安全边界替代原机非法role/item的越界寻址，合法状态与调用顺序一致，归类`platform_adapted`。

## 6. 验证

- headless IDA从正式数据库副本重新导出45条指令、两个caller、全部data refs和短callee body；原资产与正式IDB保持只读。
- 独立oracle覆盖35次opcode21、唯一opcode59及26个真实角色的基准个人物品域；二次生成逐字节一致（`proc_f20d`）。
- Linux app Debug根BUILD脚本：14/14通过（`proc_e4a9`）。
- order34静态门通过（`proc_9d28`）；reverse framework 577行有效、scene-event剩64项（`proc_e89d`）。
- 动态原程序差分仍按统一策略记为`blocked_runtime_oracle`，不替代静态机器码与独立资产oracle。
