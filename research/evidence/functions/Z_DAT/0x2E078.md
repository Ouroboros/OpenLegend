# 函数证据：`sub_2E078` `0x2E078..0x2E155`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；`research/ida/reports/Z_DAT.b7_scene_xrefs.txt`

直接调用者：KDEF opcode21 handler 与 `sub_30559`。

## 汇编独立合同

1. 在队伍槽 1..5 查找给定 role ID；命中后左移后续槽，槽5写 `-1`。槽0不参与。
2. 无论是否在队伍命中，读取角色两件装备和修炼物品；每个非 `-1` item 的 `user` 字段写 `-1`。
3. 角色两装备、修炼物品写 `-1`；物品经验写 `0`。
4. 返回 `0`。

## C++ 映射

- `SceneSession::remove_team_role`：槽1..5搜索、压缩和尾槽 `-1`。
- `SceneSession::clear_role_personal_items`：两装备/修炼物品解绑、角色字段清空和物品经验归零。
- case21 与 case59 共用同一 helper，避免两份语义漂移。

## 验证

- case21 既有离队测试。
- scene70/script932 opcode59 反例覆盖 role1..5 队伍移除，并用原 `sub_30559` i=6 越界读取首库存 item ID 的路径对“不在队伍中的 role6”执行完整个人物品解绑。
- Linux Debug `openlegend.scene` 通过。
- 最终双向 REVIEW 尚未执行；状态不得提升为 `assembly_exact`。
