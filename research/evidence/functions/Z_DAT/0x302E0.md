# 函数证据：`sub_302E0` `0x302E0..0x30480`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；直接调用者为 KDEF opcode58（仅 script936 PC54）。

## 汇编独立合同

- 栈上 6×6 word 标记表先清零；外层 group=0..4，每组从 0..5 随机抽 3 个不重复 opponent。每次抽取都调用 `sub_3D612(6)`，重复值仍消费 RNG。
- 每个 cell=`group*6+opponent` 调 `sub_30480(cell)`；任一返回0立即返回0，不得预抽后续对手。
- 每组前三胜后，group0..3 依次：dialogue2891(head70,style0)→render→fade_to_black→`sub_3DB83(300)`→`sub_30510`→fade_from_black→dialogue2892(head0,style1)→render。
- 五组全胜后依次输出 dialogue2884..2888（每条后 render），fade_to_black；事件24..72 的 fields0..7 写 `0,0,-1,-1,-1,-1,-1,-1`，picture_delay/x/y 不变；render→fade_from_black→dialogue2889→render→item143+1，返回1。
- `sub_3DB83(ms)` 循环次数为 signed `ms/40 + 1`；300 对应8 ticks。现代 fade effect 的 `wait_ticks=9` 表示最终 fade frame 自身一 tick 后再保持8 ticks 黑屏。

## C++ 映射

`TournamentTrialState` 按 `choose_opponent / awaiting_battle / after_victory / interround / finale / reward` 同步推进。每次只在需要下一个对手时消费 RNG；所有 dialogue、present、fade、battle 和 notice 均通过原调用点的 continuation 输出。失败立即 quit，并停止后续 RNG。

## 独立向量与验证

- seed1 全胜 selected indices：`2,4,3,7,11,6,15,12,13,23,18,21,27,24,26`；battle IDs：`104,106,105,109,113,108,117,114,115,125,120,123,129,126,128`；最终 RNG `0xe95678e2`。
- 首场失败只消费一次 RNG，state=`0x41c67ea6`，battle104 后同步 quit。
- `scene-goldens.json:kdef.opcode_58_script_936` 固定脚本 PC、talk/head/battle、RNG、8 tick、事件范围和奖励。
- 真实 scene25/script936 全胜/失败测试与 Linux Debug `openlegend.scene`、`openlegend.ui` 通过。

## 未关闭边界

`sub_30480` 的失败分支调用完整死亡 UI `sub_2E659`；当前只保留同步 quit，`0x2E659` closure 仍 pending。正向/反向最终 REVIEW 尚未执行。
