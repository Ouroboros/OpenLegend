# 函数证据：`sub_2F890` `0x2F890..0x2F8AB`

状态：`implemented_pending_review`

KDEF opcode39。机器码按 `scene_id*52` 定位 RANGER scene metadata，并清零 record word0（`entrance_condition`）。C++ `SceneSession::run_event(case39)` 写入同一84条 metadata 模型字段。

当前 KDEF 中 opcode39 共3处。真实 script420 已固定 scene75 `entrance_condition:777->0`，独立 B7 oracle 同步输出该向量。Linux Debug `openlegend.scene` 通过；最终双向 REVIEW 尚未执行。
