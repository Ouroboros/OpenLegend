# 函数证据：`sub_2F890` `0x2F890..0x2F8AB`

状态：`platform_adapted`；最终 REVIEW：`converged_no_new_differences`

来源：当前 `Z.DAT` 机器码与原始文件字节；headless IDA完整导出；当前 `KDEF.IDX/GRP`；RANGER场景metadata物理布局。

## 1. 机器身份与caller

- 函数物理范围 `0x2F890..0x2F8AB`，27 bytes，6条指令。
- loaded SHA256 `07f126747fc2b60453cb90dc9a5c14ff1369789d7d38b427803138b6fe699f17`；原始文件 `Z.DAT[0x29290:0x292AB]` SHA256 `d302409b626ae24dc3d082a7591ffe8204131cc8364dc4857122dec278a10b10`。
- 唯一差异字节来自场景metadata基址raw加加载基址 `0x20000` 的一项重定位；规范化后与loaded逐字节相同，未解释差异为零。
- 唯一物理代码caller是 `sub_2C319:0x2C929..0x2C93C` 的opcode39；`0x55658`只是函数地址表数据引用。
- caller把场景参数sign-extend后传入，忽略返回0，经共享尾块清理4字节栈并把PC增加2 words。
- 入口 `sub_3ED1E(4)` 仅为Watcom栈探测合同。

## 2. 不看C++的清零合同

1. 以32位参数乘52，得到RANGER场景metadata记录字节偏移。
2. 在场景metadata基址加该偏移处写一个16位0。
3. 该地址正是84条、每条52字节记录的word0 `entrance_condition`。
4. 不读取旧值，无条件覆盖；其他25个word不变。
5. 返回0；无提示、画面、输入或音频边界。

## 3. 汇编→C++ REVIEW

C++ case39对合法场景索引选择同一metadata记录，把同一 `entrance_condition` word覆盖为0，随后PC增加2。合法84场景域与机器逐项一致，本轮没有产品代码差异。

机器不检查参数范围，负数或不小于84会别名/越界写相邻内存；现代稳定拒绝此类非法索引，归类 `platform_adapted`。随后从入口重新逐条复核6条指令、唯一重定位、52字节寻址、word0写0、return0和caller，零新增差异。

## 4. 全KDEF与边界oracle

当前1,018条KDEF共有3次opcode39。按little-endian `<IIh>` 编码 `(script,PC,scene)` 的完整参数流SHA256为 `12e3351df86fb83d3bf39ab24eea4ec4ca1eebef1ffabc6aa8d4185a2e9850d7`：script409 PC320 scene80、script420 PC24 scene75、script423 PC24 scene75；全部真实场景合法。

- 真实script420固定scene75 `777 -> 0`。
- 合成合法边界固定scene0 `-32768 -> 0`、scene83 `32767 -> 0`。
- 合成非法scene -1和84保持scene0/83哨兵值不变，固定现代边界保护。
- 双次独立生成字节一致；正式 `scene-goldens.json` SHA256 `35aae03b0a4582fc3dbbdbdaf4f3b991ffe20249ed1f5fbd4e6e56ee6ce743ef`。

## 5. 验证与结论

- Linux app Debug根构建：14/14 tests通过（`proc_9875`）。
- 原程序动态差分：`blocked_runtime_oracle`。

```text
closure_status = platform_adapted
final_review = converged_no_new_differences
remaining =
```
