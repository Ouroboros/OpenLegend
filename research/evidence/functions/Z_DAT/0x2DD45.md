# 函数证据：`sub_2DD45` `0x2DD45..0x2DD77`

状态：`implemented_pending_review`

opcode4 把当前物品菜单槽对应的 item ID 与参数1比较；不等返回 false offset，等于时先写瞬时 `word_54B7A=1` 再返回 true offset。dispatcher 固定推进4 words 后叠加返回值。现代 `EventContext::item_id` 与 case4 `conditional` 保留可观察分支；headless IDA 对 `0x54B7A` 的全局 data-xref 查询证明全程序只有本函数写入、无读取，因此该瞬时死写不产生可观察后续行为。

synthetic `[4,123,0,3,2,200,1,-1]` 分别用 item123/124 固定命中执行后续加物品和未命中直接终止；最终 REVIEW 仍需反查原物品菜单槽表与 dispatcher 调用边界。
