# 函数证据：`sub_21496` `0x21496..0x21AC0`

状态：`platform_adapted`

## 独立机器恢复

本轮先仅依据当前 `Z.DAT` 的 `0x21496..0x21ABF` 指令和原始字符串字节，从入口独立恢复行为，再读取现代实现。入口先把六项临时槽初始化为 `-1`；从队伍槽1开始扫描 `word_C0B78`，首个 `<=0` 的槽号作为连续队伍长度，没有哨兵则长度为6。随后按槽序筛选角色记录步长 `0xB6`、医疗能力字段 `word_901A8 >= 10` 的施术者，并在临时数组中保存原party slot。

无合格施术者时，函数不调用世界或场景背景重绘，而是在调用前保留的主菜单帧上绘制 `(70,18,154,26)` 提示框、“隊員中無人醫術夠格”，present 后调用 `sub_20C32` 等待任意非零键并返回。存在合格施术者时，每轮按 `word_C0BF2` 的0/1值重画世界或场景背景，再调用 `sub_269AB` 绘主菜单，叠加 `(70,18,108,26)` 标题框、`(70,45,90,20*count+30)` 列表框、“誰要使用醫術”和“醫療能力”。姓名经 `%s` 写入临时缓冲后，只检查字节1..6中的首个NUL，X依次为95/91/87/83/79/75；未命中则不绘姓名。能力值以 `%2d` 绘于 `x=130`，普通项颜色 `0x2321`，当前项重绘为 `0x6663`。

施术者列表的Down `0x98` 和Up `0x9E` 在过滤数组内回绕，并分别清自身translated state与last-key。Enter、Space、keypad Insert共同确认，先清三项确认键态与last-key，再以参数0调用 `sub_22090`。目标Escape返回 `-1` 后保持原施术者光标继续列表；确认目标后分别经队伍表把目标和过滤后的施术者party slot映射为角色ID，再调用 `sub_3A10C(-1,-1,actor,target)`。外层Escape清last-key及Esc状态后直接返回主菜单。

医疗完成后按当前上下文重画世界或场景背景，绘制 `(112,47,96,26)` 结果框、“恢復生命”和 `%3d` 返回值，present 后由 `sub_20C32` 等待任意非零键；返回值0仍显示结果框。函数所有正常出口均返回EAX=0。

## C++ 映射与差异处理

- `LegacyGameRuntime::update_menu_counts` 保留从槽1扫描首个 `<=0` 哨兵和最多六人的连续队伍前缀；`GameMenuController::set_ability_party_options` 按signed医疗能力 `>=10` 保存原party slot。
- `GameMenuController` 的source/target两级状态保留过滤列表光标、目标Escape回到原施术者、三确认键和上下回绕；runtime按party slot取得角色ID并调用 `apply_role_medicine_value`。
- `BattleRenderer::render_party_ability_selection`、`render_character_selection` 和 `render_party_action_notice` 保留框坐标、原Big5字节、姓名NUL居中、`%2d/%3d`前导空格、颜色及结果值0的显示边界。
- 无合格提示的现代帧先从未推进的世界/场景状态重建同一主菜单帧，再叠加提示；同步自旋由SDL事件续体承接。无效资源、角色ID和宿主绘制边界的安全拒绝属于平台适配，不改变合法原始数据路径。

首次单向逐基本块对照发现 `LegacyGameRuntime::handle_key` 仅对主菜单和系统菜单返回键态清理，遗漏本函数在施术者列表明确写零的Down、Up、Esc与确认组三项状态。该轮结论立即废弃；实现已令 `party_select` 使用 `menu_key_state_reset`，真实医疗路径新增Down/Up/Escape及Enter确认组回归。修正后从 `0x21496` 入口重新恢复，逐块复核队伍边界、signed阈值、临时过滤槽、全部输入分支、调用参数、世界/场景绘制分支、列表像素、present、阻塞等待与返回写集，未发现新增差异。

后续终审共享callee `sub_22090` 时发现world/scene目标选择页遗漏 `sub_269AB` 主菜单层；该差异会改变本函数确认施术者后的目标帧，因此本函数旧终审结论同步废弃。补回主菜单层后再次从 `0x21496` 入口重启，确认施术者列表原本已有的主菜单层、参数0目标选择层、Escape回程、效果结果与无合格尾链均零新增差异。

## 验证

- Linux app Debug：14/14，`proc_0f3c`。
- 医疗真实资产整帧：world施术者 `0xE8012B081084965B`、world目标 `0x1427485A30154B25`、scene施术者 `0x13F1A3B8F19BE319`、scene目标 `0x83E99495AE4D5CC1`、结果 `0x70CE82FBA86ECED6`、无合格提示 `0xC9828D8AE6422CEF`。
- 固定医疗结果：HP `20 -> 80`、内伤 `40 -> 0`、施术者体力 `100 -> 98`。
- 原程序动态差分因本机无DOS运行器登记为 `blocked_runtime_oracle`；当前机器码静态闭环、真实资产像素与独立状态oracle不受影响。

```text
final_review = converged_no_new_differences
remaining =
```
