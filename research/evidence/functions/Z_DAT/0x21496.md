# 函数证据：`sub_21496` `0x21496..0x21AC0`

状态：`implemented_pending_review`

该函数先按`word_C0B78`从slot 0开始取连续队伍前缀，再筛选医疗能力`>=10`的施术者party slot。没有合格施术者时，不重画世界/场景背景，而是在调用前已显示的游戏主菜单上叠加`(70,18,154,26)`提示框、“隊員中無人醫術夠格”，present后由`sub_20C32`等待任意非零键返回主菜单。

存在合格施术者时，每轮按`word_C0BF2`重画世界或场景背景，再绘制施术者标题框`(70,18,108,26)`、列表框`(70,45,90,20*count+30)`、“誰要使用醫術”、“醫療能力”、过滤后的姓名和`%2d`能力值。上下键在过滤列表内回绕；Enter、Space和keypad Insert确认；Escape直接返回主菜单。

确认施术者后以参数0调用`sub_22090`选择全部队员中的治疗目标。目标选择Escape返回原施术者光标，不退出游戏菜单；确认后经队伍表把施术者和目标party slot分别映射为角色ID，再调用`sub_3A10C(-1,-1,actor,target)`。现代实现复用`apply_role_medicine_value`，保持既有16位回绕、伤势分段、一次`bounded(5)`、生命夹值、伤势下降及施术者体力减2的原顺序。

效果完成后再次按当前上下文重画背景，绘制`(112,47,96,26)`结果框、“恢復生命”和`%3d`结果值，present后等待任意非零键再返回主菜单。结果值0仍显示结果框，不能与“无合格施术者”混淆。

现代映射由`GameMenuController`保存施术者/目标两层选择状态和过滤slot映射，`LegacyGameRuntime`负责party slot到角色ID映射及状态提交，`BattleRenderer`负责能力列表、参数0目标列表和结果/无合格提示像素。外部每次打开游戏菜单由`show_main()`按入口机器码把主光标重置为0；子页面返回主菜单则保留当前主项。

定向回归覆盖能力阈值、过滤slot映射、上下回绕、三确认键、目标Escape返回施术者、无合格提示、结果任意键边界、生命/伤势/体力写入、世界整帧hash及场景框外背景逐像素保持。逐指令实现审计发现并修正数字字段缺少`%2d/%3d`前导空格，以及无合格提示错误重画纯背景而漏掉主菜单两项确定差异。

Linux app Debug、Windows app Debug及Linux app ASan+UBSan均14/14通过；sanitizer后普通Linux Debug cache已恢复并复验。独立B5 golden与tracked证据一致，五项原版资产摘要未变化。功能映射状态提升为`implemented_pending_review`；最终双向逐基本块REVIEW仍为`not_started`。
