# 函数证据：`sub_3650E` `0x3650E..0x36A98`

状态：`pending_implementation`

部分映射：`BattleSetup::begin_ai_movement_plan`与`advance_ai_movement`。

入口先以请求目标为source建立targeting图，计算`target_distance-actor.round_value<=range`。mode2且条件成立时，以目标为source建立movement图，从range层向下搜索严格同x或同y的格，按x外层、y内层扫描并以strict最小Manhattan距离选首格；mode3执行相同层级搜索但不要求轴向对齐。mode2条件不成立及mode0/1走通用分支：以actor为source建立movement图，从目标周围按上、右、左、下先找与actor同x/y且path<128的首格，再找任意path<128首格；均失败时按x优先、再y逐格把候选退向actor。

目的格与source不同后，保留原版两次严格`path<128`可达性检查：第一次读取分支当前图，第二次强制重建actor movement图；两次均通过才调用最短路标记。battle4真实向量actor `(26,24)`、目标 `(26,26)`：mode0/1目的格均`(26,25)`且movement图重建3次；mode2 range3选择层3的`(23,26)`；mode3 range3选择层3的`(25,24)`。三种路径均通过双检查并按上、右、左、下标记，首步分别为`(26,25)`、`(25,24)`、`(25,24)`。

`advance_ai_movement`每次只推进一个原版同步格：消费旧path为255、搬移occupancy、写坐标/方向/sprite、条件扣体力、最后扣行动值，并返回新中心、夹0..32视图偏移、render/present和40 tick参数。AI mode0/3在目的格或行动值耗尽时停；mode1还在Manhattan不大于range时停；mode2额外要求同x或同y。round1的mode0固定首步后round归0并立即结束。

负range进入mode2/3层级搜索在合法资产中不存在；现代安全拒绝该异常输入，登记最终REVIEW。实际逐格battle render、present和40 tick等待尚未接线，故保持`pending_implementation`。Linux Debug 14/14、独立WARFLD/occupancy oracle与完整汇编双向REVIEW通过。
