# 函数证据：`sub_37166` `0x37166..0x371AE`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattlePathing::build` 内的queue dequeue lambda。

## 机器身份

- raw范围：`Z.DAT raw+0x30B66..+0x30BAE`；loaded范围：`0x37166..0x371AE`。
- 72 bytes，21条指令，0个跳转，2处32位fixup；raw SHA256 `1fab3fba7ceae1e002dce08eebdcf2e7b48ce91dc7ccc61ef494e40a27ccdf9b`，loaded SHA256 `3bd60c28edc86229bb6609c2dfac3809e6b1a6d4077a2e5af4295a0ddc7d2e6a`。两项fixup均由`raw+0x20000`得到loaded image，反向归一化逐字节一致。
- 唯一direct call为`0x3716B -> sub_3ED1E`，栈探测值8 bytes；函数以自有`pop ebx; ret`退出。
- 两个入口xref均来自独立owner `sub_37070`：`0x3708F`为每step首次出队，`0x370D5`为sentinel后的第二次出队。

## 机器合同

1. 第三个参数是signed int16读下标指针；第一和第二参数分别是x、y的16位输出指针。
2. x读取前signed扩展当前读下标，从`0xE6ABE..0xE6CBC`的255-word x队列取word，并先写x输出。
3. y读取前再次从第三参数signed扩展下标，从`0xE6CBC..0xE6EBA`的255-word y队列取word，并写y输出。
4. 更新前第三次从第三参数signed扩展下标，执行32-bit `index+1`及signed `idiv 255`；DX余数写回第三参数，EAX商作为返回值。
5. 两个wrapper把读下标初始化0，合法状态保持0..254。下标0..253时返回商0并写回1..254；下标254时返回商1并写回0。
6. 两个真实caller均传入互异的x local、y local与共享`word_E6ED4`指针，不存在输出参数/读下标别名。
7. 两个caller返回后均先清栈并比较刚写出的signed y；不读取或分支使用EAX商。
8. 直接RNG消费为0。

## 现代逐块对照

- `BattlePathing::build`的dequeue lambda从同一255-slot逻辑队列按`read_index`读取`BattlePathCoord{x,y}`，再以`increment_mod(index,255)`更新下标并返回坐标。
- 机器使用两个平行255-word数组，现代使用255个交错`BattlePathCoord`；每槽逻辑x/y、signed word值与访问下标相同，队列存储不被wrapper外观察。
- 机器在x写出、y写出、下标更新前各重读一次第三参数；两个真实caller三指针互异且函数体无回调，现代一次读取完整coordinate再更新的顺序等价。
- 合法0..254域中，现代`increment_mod`与机器signed `idiv 255`余数相同。机器254→0时EAX商1、其他下标商0，均被caller忽略；现代省略该返回副产物不可观察。
- 机器对负下标或大于254没有数组边界门；wrapper初始化和本函数模更新保证合法caller不进入该域，现代私有index没有外部破坏入口。
- 从入口重新覆盖全部21条指令、RET和两个caller参数/续接，零产品差异。平行数组改交错结构、共享状态私有化、无观察点的读取/写出顺序及省略商返回归类平台适配。

## 判别性验证

- 原资产Golden固定72-byte identity、2处fixup、唯一call、两个caller、255-slot双数组地址、写出顺序、signed更新和EAX商忽略合同。
- synthetic queue从read index253连续三次出队：`(1234,-1234)`后写254/返0，`(32767,-32768)`后写0/返1，`(-32768,32767)`后写1/返0；锁定signed word及254→0环绕。
- battle0/93真实targeting图回溯前分别含457/822个距离值0..127的可达格，均超过255；完整图hash不变并实际覆盖读下标环绕。
- 独立原资产Golden双生成逐字节一致，SHA256 `94cb55ff4a200ddb35d840435805c45dbaafe0768998063acdfff59b912caa35`；Linux app Debug 14/14通过。
