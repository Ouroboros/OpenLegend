# 函数证据：`sub_37166` `0x37166..0x371AE`

状态：`implemented_pending_review`

映射：`BattlePathing::build` 的 dequeue。

从两个255-word队列的 signed 读下标取 x/y，再把读下标写为 `(index+1)%255`。C++ 以255个 `BattlePathCoord` 保留同一环绕。
