# 函数证据：`sub_32A51` `0x32A51..0x32B78`

状态：`platform_adapted / converged_no_new_differences`

映射：`openlegend::battle::BattleSetup::sort_by_effective_speed`。

## 1. 机器identity

295字节、69条指令、9个跳转、19项`raw+0x20000`重定位。raw SHA256为`db96219a6c8da281549570eaa5368b919b5b5c5b66df755ff9f4c8196d6cb125`，loaded为`da296c26c4afc941329f205877efa581e6507aa9f548f683d67057d678f4f74a`。两个caller为主循环首次排序`0x3273E`及轮首排序`0x327EF`；直接调用仅栈检查和`0x32B52→sub_32B78`。

## 2. 独立循环与位宽合同

DI保存signed16 count，ESI自0递增至count-2。每一外层先把ESI复制给EBX，跳到内层尾递增EBX，再进入比较，故内层为`j=i+1..count-1`。比较中的第一槽始终由SI寻址，第二槽由BX寻址；不是相邻冒泡，也没有缩短末端或提前停止。

每个比较值从当前槽读取角色号并signed扩展乘182，基础为角色word44；装备word23/24各自ID>=0时signed扩展乘190，再加item word53。每次ADD只写16位，最终`CMP DX,CX`后`JGE`跳过交换；只有first<second调用swap(i,j)。下一个比较重新读取已交换槽的角色，不缓存旧速度。

等速不直接交换，**不代表稳定排序**：`[A10,B10,C20]`只交换slot0/2，得到`[C20,B10,A10]`。隐藏状态不参与比较。函数不消费RNG。

## 3. 映射与验证

C++保持同样的跨槽双循环、两件装备16位回绕、signed严格小于及交换后重读。现有循环本身无需修正；审计中曾误写的稳定冒泡修改已完整撤回。角色/装备ID越出受检记录时现代返回稳定错误，区别于原裸内存寻址；合法资产域内比较和交换一致，归类`platform_adapted`。

独立golden从原机器字节恢复两个caller/两次call及重定位，向量固定非稳定交换轨迹`[[0,2]]`。C++覆盖40/40无直接交换、41/40交换、三角色间接反序、`32760+30`回绕为负数、hidden参战者仍排序及`prepare_round()`不隐式排序。Linux app Debug 14/14（`proc_0899`）、机器与静态门通过。最终从入口、四次装备条件、比较及两个循环回边重新审计，零新增差异。

`sub_32B78`在同包独立登记；其余callee/同址owner不传播关闭。原程序动态差分为`blocked_runtime_oracle`。
