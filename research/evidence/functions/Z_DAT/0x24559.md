# 函数证据：`sub_24559` `0x24559..0x24667`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；乘船水域/海岸通行探测。

独立汇编恢复：要求候选moved coordinate严格`>10 && <459`，BUILDING与BUILDX/BUILDY的成对空格条件通过；再检查current或target EARTH是否落在六对海岸区间。机器表保留重复`458..464`，其余为358..362、374..380、506..610、1016..1022。命中允许船移动，否则转下船探测。

现代映射：`WorldSession::target_is_ship_water`，区间按机器顺序保存在`kShipCoastRanges`，重复项未清理。边界10/11/458/459及固定船轨迹有定向测试。

损坏cache坐标在现代返回0属于安全适配。最终双向逐基本块REVIEW为`not_started`。