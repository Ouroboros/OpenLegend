# 函数证据：`sub_340D9` `0x340D9..0x341F6`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::choose_ai_low_mp_action`。

当前原始`Z.DAT`物理范围为285 bytes、68条连续指令、11条分支、22处重定位和1个direct call；raw SHA256为`d6d38f652055207d5fcb4836a59547e0839d6d427858bd6d23a14ada9e5475c2`，IDA加载后SHA256为`749b9b86c369ed7b9cc387fee5265012185f6fd54cf0d5351d9d31cb8a61ced4`。唯一caller为AI入口命中低内力门后的调用点；唯一直接调用是栈探测，不属于游戏逻辑。

本函数自身不读取actor当前MP或最大MP，也没有自身恢复能力门或同伴求援路径；低内力条件属于caller owner。函数直接按side分流：side恰为0时按槽序扫描200个队伍inventory，任意非零side时扫描actor四个carried item槽。两条路径都跳过signed item id负值，并要求item word50 `add_mp`严格大于0；零值和负值均跳过，首个正值命中。

两种物品路径均完全不读取数量字段。命中时保存来源槽、actor目标槽与actor坐标，写动作6并返回6。全部未命中返回0，不写actor动作，也不清理机器中此前残留的目标共享状态；现代typed返回用空目标表达该不可用状态，动作0调用方不读取目标。现代对越界actor、role或item id安全失败，属于合法域外平台适配。

定向回归覆盖：`add_mp=0`拒绝并保留既有动作；actor当前/最大MP均为100时selector仍不增加MP门，队伍slot1 `add_mp=-1`跳过、slot3 `add_mp=1`且quantity=0命中；slot1与slot3均为正时选首个slot1；actor side=-1证明任意非零side走carried路径，slot2 `add_mp=1`且quantity=0命中。正式battle golden由独立Python两次生成逐字节一致，只新增四项低内力边界，SHA256为`8b3eb38f18950159579790ea408097766452a9b8353bc1340e047ac6bd42a5af`。

首次完整机器推导后逐块对照未发现产品实现差异，只发现边界覆盖不足；补测试与独立golden后再次从入口覆盖全部68条指令、11分支、22重定位、唯一caller、两类命中副作用和无动作尾，零新增差异或未决项。机器门、静态/golden门、closure门、逆向框架及根`./build.sh app --config Debug` 14/14均通过，归类`platform_adapted / converged_no_new_differences`。
