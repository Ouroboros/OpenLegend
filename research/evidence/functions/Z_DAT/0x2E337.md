# 函数证据：`sub_2E337` `0x2E337..0x2E46B`

状态：`implemented_pending_review`

opcode17 固定宽度6 words，参数顺序为场景、层、x、y、值。目标场景为 `-2` 时直接写当前场景工作缓冲；显式场景先 flush 当前场景，按 `ALLSIN.IDX/GRP` 载入目标场景，在 word offset `4096*layer + 64*y + x` 写16位值，回写目标场景后重新载入当前场景。因此当前场景编号与工作状态不随外部写入改变。

现代全100场景常驻 `GameSnapshot` 以同一线性索引直接写目标场景，省略无可观察差异的归档换入换出。当前资产共127次 opcode17，场景参数为 `[-2,11,18,21,49,52,53,55]`；真实 script274 固定当前场景两次写入后的 present，synthetic opcode17 固定 scene69 写入且 scene70 同格及当前会话不变。最终 REVIEW 仍需复核 archive I/O 平台适配和全部合法坐标调用者。
