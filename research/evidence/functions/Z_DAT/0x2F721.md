# 函数证据：`sub_2F721` `0x2F721..0x2F890`

状态：`platform_adapted`；最终 REVIEW：`converged_no_new_differences`

来源：当前 `Z.DAT` 机器码与原始文件字节；headless IDA完整导出；当前 `KDEF.IDX/GRP`；既有 `0x25AB7`、`0x29391`、`0x294AD`、`0x29537` 场景工作副本证据。

## 1. 机器身份与caller

- 函数物理范围 `0x2F721..0x2F890`，367 bytes，105条指令；两个控制流出口继续进入共享epilogue `0x2DA71..0x2DA7D`。
- loaded SHA256 `c258353fe70f264c52117e2530a2f9c0abb888ba70d1223c970734951e1b1a09`；原始文件 `Z.DAT[0x29121:0x29290]` SHA256 `56c22fdf3d5fd0d937b6104a050bab331f8c90aa45d5945f84a40e71571855ee`。
- 全部17个差异字节均分别由绝对地址raw加加载基址 `0x20000` 的17项重定位解释；规范化后与loaded逐字节相同，未解释差异为零。
- 唯一物理代码caller是 `sub_2C319:0x2C8FB..0x2C929` 的opcode38；`0x55654`只是函数地址表数据引用。
- caller把四个KDEF word sign-extend为 `(target_scene,layer,old_value,new_value)`，忽略返回0，经共享尾块清理16字节栈并把PC增加5 words。
- 入口 `sub_3ED1E(0x24)` 仅为Watcom栈探测合同。

## 2. 不看C++的批量替换合同

### target_scene精确等于-2

1. 在当前49,152字节场景地图缓冲中定位 `layer*8192` 字节的层起点。
2. 两层64次循环遍历该层全部4,096个word。机器的内层按128字节步进、外层按2字节步进，联合覆盖每个word恰好一次；遍历顺序不改变最终状态。
3. 每个word先sign-extend并与old_value比较；仅精确相等时写new_value低16位。重复值全部替换，非匹配值保持。
4. 直接进入共享epilogue返回0。

### 其他target_scene

1. 先用当前场景号把当前49,152字节地图缓冲flush到会话工作副本。
2. 确保`allsin.idx`偏移表已载入，随后以读写方式打开`allsinbk.grp`。
3. 按target_scene对应offset seek，读取49,152字节到同一缓冲，执行完全相同的4,096-word层扫描和替换。
4. seek回同一offset，写回全部49,152字节并关闭归档。
5. 按当前场景号重新载入原场景地图缓冲，进入共享epilogue返回0。

函数无提示、present、输入或音频边界。归档I/O、seek/read/write/close与当前场景flush/reload均属被委托callee职责，本closure只确认调用顺序和参数，不传播callee closure。

## 3. 汇编→C++ REVIEW

C++ case38把精确值-2解析为当前场景，以固定64×64循环读取目标层，并对所有等于old_value的word写new_value；`GameSnapshot::scene_maps`以100个连续49,152字节span持有原版会话期`ALLSINBK`工作副本，因此外部场景直接目标span读改写与机器的flush→读目标→改写→写回→reload当前在可观察工作状态上等价，且当前场景span保持不变。本轮没有产品代码差异。

现代对越界scene/layer和损坏map尺寸稳定拒绝，而机器会越界索引/访问，归类 `platform_adapted`；工作副本由内存而非游戏目录中的可写BK文件持有也是既有平台适配。随后从入口重新逐条复核105条指令、17项重定位、两路控制流、两组4096-word循环、外部归档调用顺序、共享epilogue和caller，零新增差异。

## 4. 全KDEF与场景oracle

当前1,018条KDEF共有3次opcode38，按little-endian `<IIhhhh>` 编码 `(script,PC,scene,layer,old,new)` 的完整参数流SHA256为 `543f15968ca9914ad3068245bf950fe5e3983cc27df7299e5b5cc4aae46e9e48`：

- script416 PC11：`(-2,0,994,990)`；
- script432 PC84：`(-2,0,990,994)`；
- script434 PC0：`(-2,0,990,994)`。

全部真实调用均为当前场景、层0。真实script434固定索引0/63/4095全部从990改994、非匹配索引64保持123。合成script57固定外部scene7末层：索引0/63/64/4095全部从123写为-32768，索引65的124不变，当前会话scene70同层匹配值123不变。

双次独立生成字节一致；正式 `scene-goldens.json` SHA256 `bccb77c43001539eb526353df32d54664d69b735e62ea491980b2551437200e1`。

## 5. 验证与结论

- Linux app Debug根构建：14/14 tests通过（`proc_7166`）。
- 原程序动态差分：`blocked_runtime_oracle`。

```text
closure_status = platform_adapted
final_review = converged_no_new_differences
remaining =
```
