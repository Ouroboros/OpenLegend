# 函数证据：`sub_2F721` `0x2F721..0x2F890`

状态：`implemented_pending_review`

KDEF opcode38。参数依次为目标场景、六层地图 layer、旧值、新值。目标场景为 `-2` 时，机器码直接按 `layer*4096 + y*64 + x` 扫描当前 64×64 地图并替换全部匹配 word；外部场景路径会关闭当前归档、从 ALLSINBK 载入49152字节、执行同一替换、写回并恢复当前场景归档。

C++ `SceneSession::run_event(case38)` 保留 `-2` 解析为当前场景、固定64×64全扫描、只替换等值 word；`GameSnapshot` 同时持有全部场景，因此外部路径以同步内存写回实现同一持久副作用，不暴露原归档切换细节。当前 KDEF opcode38 共3处且均为 `scene=-2, layer=0`。真实 script434 `(990->994)` 已固定索引0/63/4095全部替换、非匹配索引64保持123；独立 B7 oracle 同步输出该向量。Linux Debug `openlegend.scene` 通过；最终双向 REVIEW 尚未执行。
