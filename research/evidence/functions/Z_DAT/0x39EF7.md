# 函数证据：`sub_39EF7` `0x39EF7..0x3A10C`

状态：`implemented_pending_review`

已映射状态边界：`BattleSetup::apply_medicine_target`、`BattleSetup::finish_medicine_action`。

函数沿用解毒的方向与清effect顺序；合法目标格若为敌方则完全跳过，空格写effect1，友方格写effect1、effect kind4、调用 `sub_3A10C` 并把返回治疗值低16位写combatant damage word9。原边界判断重复检查x<64而未检查y<64；现代实现对y≥64安全拒绝，登记待最终REVIEW。

状态结算固定调用magic type0/effect0动画，damage display kind4并抑制flash，重算全部combatant sprite，然后跳入用毒/解毒共享尾部：actor action_done置1、word13加1、体力再减2并夹0。固定向量actor `(26,24)`、友方target `(26,26)`：方向3，单格hash `0xab559939923b4f74`，治疗63，effect kind4；公式内体力51→49，共享尾部49→47，counter0→1。空格标记、敌方不标记。

`BattleSession`现于目标确认后先提交状态，再载入actor FIGHT包并执行10帧effect0，每帧render/present及一次BIOS tick变化；首帧分派attack sample7与effect sample0。随后执行10帧damage kind4且全部抑制flash，最后重算sprite、提交共享尾部并推进下一actor。独立Session向量锁定同格HP50→77、hurt20→0、体力100→96及完整可读日志；Linux与Windows Debug全部14项测试通过，推进为`implemented_pending_review`。
