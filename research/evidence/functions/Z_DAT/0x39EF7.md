# 函数证据：`sub_39EF7` `0x39EF7..0x3A10C`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::apply_medicine_target`、`BattleSetup::finish_medicine_action`与`BattleSession`玩家/AI医疗效果continuation。

## 机器身份、入口与owner边界

fresh临时数据库`tmp/Z_DAT-b8-order65.i64`仅由`/mnt/d/Dev/Crack/IDA/idat.exe -A`配合`tmp/ida_dump_b8_order65.py`分析。owned范围固定为533 bytes、132条连续指令、28个IDA flow block（含外部尾终止块）、15个条件分支、7个无条件跳转、35处绝对数据fixup、7次direct call、2个入口xref且没有本地RET。raw SHA256为`0b47a665b7ce2cb420ed0ee46f9f070c6f38dc9f8f1360089d363e16da376ad2`，IDA loaded SHA256为`9eea49d981caba4f24cc52a013c3a72ebdab71a7a3a6b57b89f360d5702d5f85`；35处DWORD均严格增加`0x20000`，逐处反变换后与原始533字节完全一致。

两个入口xref分别是AI action5 `sub_36210:0x36290`和玩家医疗wrapper `sub_39E88:0x39EE8`；没有外部xref进入内部基本块。7次调用依次为栈探测`sub_3ED1E`、两次绝对值`sub_3F50B`、医疗值kernel `sub_3A10C`、效果动画`sub_3859E`、damage动画`sub_38910`和循环内sprite helper `sub_3B1E6`。`0x3A107`尾跳到owned范围外的`0x399FE`用毒共享行动提交尾，再经`0x39A3E`公共epilogue返回；callee、两名caller及外部共享尾均按owner独立关闭，不随本owner传播closure。

## 完整汇编合同

入口把参数作为signed低16位actor槽，经28-byte combatant记录读取actor signed x/y；目标全局x/y同样按signed读取。32位delta依次调用两次绝对值helper。同格不改方向；否则严格`abs(y)>abs(x)`选纵向，相等选横向，正delta选右1/下3，非正选左2/上0。

随后两个signed `0..63`循环在任何目标检查前清零全部4,096个effect word。原边界顺序为`x>=0`、`x<64`、`y>=0`、再次`x<64`，因此遗漏`y<64`。通过后按`y*64+x`读取signed occupancy槽，并在判断-1前先按该槽读取combatant side：空格因此短暂读取`combatants[-1].side`。非空敌方不标记；空格与友方都写effect1。空格不调用医疗kernel；友方读取actor/target role id，调用`sub_3A10C(actor,target,actor_role,target_role)`，返回EAX低字写目标combatant damage word9。

所有分支——包括越界拒绝、敌方和空格——随后都调用`sub_3859E(actor,magic_type=0,effect=0)`，写damage kind4，并以低字1调用`sub_38910`抑制flash。接着signed槽循环`0..<combatant_count`逐项调用`sub_3B1E6`，将返回低字写sprite。最后跳`0x399FE`：actor action-done置1，attack counter执行signed16回绕加1，actor role physical power执行signed16回绕减2，仅当回绕结果为负才夹0；公共epilogue返回EAX=0。本owner自身无直接RNG调用，只有友方目标委托的医疗值kernel可能消费RNG。

## 汇编到现代实现逐块对照

从入口重新覆盖全部132条指令和22条分支/跳转，对照`BattleSetup::apply_medicine_target`、`finish_medicine_action`及玩家/AI `BattleSession`效果链。合法actor/坐标上的signed delta、绝对值比较、同格保持、纵向strict greater、横向tie、方向号和完整4,096格先清零逐项一致。空格、友方、敌方的最终effect、kernel调用次数、返回低字写damage及kind4一致；`sub_3A10C`不修改坐标/occupancy，因此机器调用后重读target槽与现代保留已解析槽在合法域等价。

Session先提交医疗状态，再固定建立type0/effect0动画，按既有跨战武功槽预载attack/effect双bank并保持同帧bank1先于bank2的启动顺序；每帧状态在render/present前写入，随后等待delay17的tick变化。动画结束后固定执行10帧damage kind4且全部抑制flash；最终帧delay后才清kind、刷新全部sprite并进入共享行动提交。玩家随后推进下一actor，AI进入独立finish handler。动画、damage和共享尾已分别独立关闭，本行只锁定调用参数、顺序和宿主屏障。

合法域首轮完整入口REVIEW零产品差异，无需修改产品源码。

## 平台适配、回归与验证

原版重复检查x而接受`y>=64`，并会线性读取64×64网格外数据；现代完整坐标验证安全拒绝。原版空格先读`combatants[-1].side`，现代避开该无效读取但保持effect1且不调用kernel。现代也拒绝非法actor、非法非空occupancy槽和非法role id，替代原版signed越数组访问。以上只影响机器不安全域，归类`platform_adapted`。

新增C++回归覆盖同格保向、四方向、纵向strict与横向tie、空/友/敌、完整effect清零、x/y边界含`y=64`、非法actor/role/occupancy无RNG推进、全部sprite刷新及counter/体力回绕。既有完整玩家与AI Session回归继续锁定状态先提交、effect0帧、双bank音频、10帧无flash damage、sprite/共享尾与下一continuation。

独立机器向量SHA256为`1958639fc75bb68858498f3eeaf860f0589ed6fd170095ba86a59141e7329b9a`。正式Golden由两次独立原资产生成逐字节一致后更新，再以第三次生成核对；文件SHA256为`ebfc177795f318f87b3e6535a9325a131b48348376e82b1ccafd9edaab37caaa`，历史58键逐值不变。根`./build.sh app --config Debug`精确通过`100% tests passed, 0 tests failed out of 14`。

原程序动态差分继续登记为`blocked_runtime_oracle`；最终结论为`platform_adapted / converged_no_new_differences`。
