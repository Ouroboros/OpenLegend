# 函数证据：`sub_301D1` `0x301D1..0x302E0`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；`research/ida/reports/Z_DAT.b7_scene_xrefs.txt`

函数物理范围：`0x301D1..0x302E0`

直接调用者：`sub_2C319` jump-table case 57

调用约定与参数：opcode 57 宽度 1，无 KDEF 参数。

返回合同：完成两阶段动画后 `eax=0`；caller 不使用返回值并把 PC 增加 1 word。

## 1. 范围与非范围

- 本单元负责：固定玩家引导动画、三事件同步图片动画、条件冻结、render 和 2 tick delay。
- 明确转交：通用事件修改/场景绘制/tick delay helpers。

## 2. 不看 C++ 的独立汇编恢复

阶段 0：

```text
for picture=7664; picture<=7674; picture+=2:
  capture tick
  word_C0BF8=picture
  render
  delay(50) = 2 ticks
  wait pre-frame tick change
```

阶段 1：

```text
for j=0; j<=56; j+=2:
  capture tick
  if word_C0BF8 < 7688: word_C0BF8=j+7676
  event2 current/end/begin = j+7690
  event3 current/end/begin = j+7748
  event4 current/end/begin = j+7806
  render
  delay(50) = 2 ticks
  wait pre-frame tick change
return 0
```

阶段 0 共 6 帧；阶段 1 共 29 帧。玩家图片在阶段 1 的 j=12 达到 7688，此后因严格 `<7688` 条件保持不变。最终事件图片为 7746/7804/7862。

## 3. 汇编独立测试向量

- KDEF 中 opcode57 仅出现于 script655 PC47。
- scene14 是唯一同时包含 7664..7862 所需全部非空 sprite 的场景；event2/3/4 位于 `(33,16)/(33,15)/(33,14)`。
- 设置 role0 attack=100 且 inventory item106>0，script655 走成功分支：dialogue2706→opcode0→opcode57。
- 独立 Python oracle 以玩家 `(32,15)` 生成全部 35 帧 player/event pictures 和 framebuffer FNV-1a64；见 `scene-goldens.json:kdef.opcode_57_script_655`。

## 4. 现有 C++ 映射

```text
case57 / PC+=1              -> ThreeStatueAnimationState
7664..7674 inclusive +2     -> phase 0
j=0..56 inclusive +2       -> phase 1
word_C0BF8<7688             -> player_frame() 条件 + player_frame_override_
event2/3/4 sub_2D841       -> set_animated_picture（current/end/begin）
render + sub_3DB83(50)      -> present + wait_ticks=2
```

## 5. LST/机器码 → C++ 正向追溯

- [ ] 最终 REVIEW 尚未执行。

## 6. C++ → LST/机器码反向追溯

- [ ] 最终 REVIEW 尚未执行。

## 7. 差异记录与重启 REVIEW

旧 case57 只把 event2/3/4 的 current_picture 静态设为最终值；缺失三字段同步、6+29 中间帧、玩家帧及 7688 冻结、render 和 delay。修正后最终 REVIEW 必须从入口重做。

## 8. 验证

- Linux Debug `openlegend.scene` 通过。
- 真实 scene14/script655 全 35 帧字段与独立 framebuffer hash 通过。
- oracle 可重复重生成；framework validator 在提交前执行。
- Windows/sanitizer 留到 B7 收口。
- 原程序动态差分：`blocked_runtime_oracle`。

## 9. 最终结论

```text
final_review = not_started
remaining = B7 全实现后完成双向 REVIEW，并审计 sub_2D841/sub_2D653/sub_3DB83 转交边界
```
