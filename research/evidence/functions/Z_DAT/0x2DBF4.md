# 函数证据：`sub_2DBF4` `0x2DBF4..0x2DD45`

状态：`implemented_pending_review`

opcode26 固定宽度6 words：目标场景 `-2` 使用当前场景，目标事件 `-2` 使用当前事件；随后把三个有符号参数分别加到事件 words 2/3/4，存回时按16位回绕。显式外部场景路径临时载入 `ALLDEF.IDX/GRP`、修改、回写并恢复当前会话；现代常驻 `GameSnapshot` 直接写目标场景，保持可观察结果。

当前资产共121次 opcode26，事件参数为 `[0,1,2,4,5,6]`，未实际使用 `-2` 当前事件哨兵；synthetic opcode26 专门以事件 `-2` 和 `32767+1→-32768`、`-2+2→0`、`3+3→6` 固定未覆盖边界，并以 scene69/event5 的 `10/20/30 + -1/-2/-3 → 9/18/27` 固定外部写入且 scene70 同事件和当前会话不变。现代 case26 使用共享 `wrapping_add`，不依赖实现定义的窄化转换。最终 REVIEW 仍需复核当前/外部两条归档路径和 dispatcher PC 边界。
