# 函数证据：`sub_2E155` `0x2E155..0x2E1E8`

状态：`implemented_pending_review`

opcode11 清当前键，复制原 Big5“是否住宿過夜（Ｙ／Ｎ）”，在 `(61,40,187,27)` 绘混色圆角 panel、于 `(71,45)` 以颜色 `0x0705` 绘字并 present；阻塞读取前再次清键。只有大写 `Y` 返回 true offset，其他任意键返回 false offset，按键后不额外重绘裸场景。

独立 scene70 frame hash `0xd070227a07492883`；synthetic opcode11 固定 Y 分支且没有 join 专属 present。最终 REVIEW 仍需审计委托 panel/text/present/key helper。
