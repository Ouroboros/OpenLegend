# 函数证据：`sub_3D950` `0x3D950..0x3DA40`

状态：`implemented_pending_review`

输入仍为 TALK 的 XOR `0xFF` 字节。机器码逐字节取反：取反后255（原零终止字节）返回0；ASCII `'*'` 只换行并令 y 加17，第三次换行后返回本次已消费字节；其他 ASCII 直接画8像素 glyph，Big5 成对解码并画16像素 glyph。函数没有按218宽面板软换行，也不裁剪横向越界。

现代 `paginate_dialogue` 只在完整 Big5 token 与显式 `'*'` 边界运输页面，每三个换行结束一页，并无条件保留终止符所在的最终调用页；`draw_dialogue_text` 从 `(panel_x+13,panel_y+3)` 以17像素行距和颜色 `0x1715` 写线性 framebuffer。全2,977条 TALK 的最大显式行宽为344像素（talk1841），talk14仍为5页。最终双向 REVIEW 尚未执行。
