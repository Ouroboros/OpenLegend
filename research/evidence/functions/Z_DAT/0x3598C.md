# 函数证据：`sub_3598C` `0x3598C..0x36133`

状态：`pending_implementation`

部分映射：`BattleSetup::apply_ai_throwing_weapon_target`，并复用`remove_carried_item_slot`。

函数入口清空64×64 effect图并标记全局目标格。mode0按actor side从队伍inventory或敌方taking-item槽取item，调用尚未恢复的共享`sub_2B483`物品效果与`sub_3DB83`显示，然后signed数量减一并在不大于0时移除槽。该分支仍未实现。

mode1按同一side来源取暗器，播放item effect后从目标格occupancy定位角色。HP负增量先按target hurt的0、1..33、34..66、>66四档除4/3/2/1并消费一次`bounded(5)`，再算`(base-2*actor.hidden_weapon)/3`；hurt按`-hp_delta/4`、HP按signed word写回后夹边界，damage写绝对HP差。add_poison非负时直接累加；负值时仅算`(add_poison-actor.hidden_weapon)/2`，两分支都不追加RNG且完全不读取target anti_poison。这与手动暗器`sub_3A30B`的正毒抗性公式及非正两次额外RNG不同。随后队伍库存或敌方携带数量signed减一并按各自左移函数清槽。

现代AI mode1状态核心保留真实item102向量：seed1伤害21、hurt40→45、HP100→79、poison10→50，终态1103527590；真实无毒item96在seed2下只消费一次RNG，poison10保持10、终态2207042835；队伍`[[102,1],[97,2]]`耗尽后左移为`[[97,2],[-1,0]]`，敌方携带槽同样左移。AI分支不改变actor方向且不提前写action_done，外层`sub_33599`负责完成标记。

实际`sub_3884A/sub_38910` effect/damage render、sample/present/wait和mode0 `sub_2B483/sub_3DB83`物品效果尚未接线；空目标及非法slot原线性越界采用现代安全拒绝，故主函数保持部分实现。Linux Debug 14/14和独立oracle通过。
