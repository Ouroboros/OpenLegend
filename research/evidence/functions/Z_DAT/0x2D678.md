# 函数证据：`sub_2D678` `0x2D678..0x2D841`

状态：`implemented_pending_review`

KDEF opcode2 的带 UI 物品增减。扫描全部200库存槽，所有匹配 item ID 的 count 都按 int16 加参数；若无匹配，首个 ID=-1 槽改为目标 ID，并在该槽原 count 上相加。函数不合并、不排序、不因 count<=0 删除。随后显示物品 notice。

fame>=200 时再次只按 ID presence 扫描：item189 ID 存在即阻止；ID144..157全存在且189不存在才把 scene70/event11 改为 script932/picture7968，count均不读取。

C++ `add_inventory` 和 `update_book_event_if_ready` 已按上述拆分。真实 script149 重复 item109 两槽都+1且邻槽不移动；script497 首空槽残留 count9 加1成为10；script36 以十四书 count0 触发，而 item189 count0 阻止。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
