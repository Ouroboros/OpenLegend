# 函数证据：`sub_38910` `0x38910..0x38988`

状态：`implemented_pending_review`

已映射状态边界：`BattleSetup::damage_animation_frames`。

函数把damage display phase置0，固定循环10帧；每帧先令flash为 `frame<4 && !suppress_flash`，再render→present，随后phase加1并wait1。循环后把damage display kind清0。

普通向量phase 0..9、wait均为1、前4帧flash，完整hash `0x364953a2c8f42144`；suppress flag非零时10帧均不flash，hash `0xec7a73890ce825c4`。

玩家用毒、解毒、医疗现已实际写入damage kind2/3/4，逐帧render→present→一次BIOS tick变化，并在第10帧后清damage kind与highlight。真实battle4 Session锁定用毒flash序列4真6假，解毒/医疗均10假；三条路径均完成actor尾部并推进下一槽。

玩家攻击现按HP/MP结果写damage kind1/5，每击严格执行10帧、前4帧flash、每帧present后wait1；末帧后清kind/highlight，并在提交熟练度与内力前先恢复全部combatant sprite、重画、present和wait17。固定双击向量完整执行两组10帧。玩家暗器也已按原 `sub_38910(0)` 执行正常flash的10帧damage，首帧整帧FNV64为`0xd41fa068222d444a`，结束后才扣库存、完成行动并刷新sprite。AI自动攻击与AI用毒均执行10帧damage、前四帧flash与每帧wait1，首帧整帧hash分别为`0x04c528de57fbffa0`、`0xd76de7fa195a1ac3`；AI医疗/解毒也执行10帧无flash damage，首帧分别为`0xa962994ff58c7064`/`0xa9cb56cb87bcbf4d`。AI暗器现同样执行十帧damage，直接首帧hash为`0x335fd35ea7e3f367`，并在最后一帧后才扣AI来源槽、完成AI外层。最终完整汇编↔C++ REVIEW仍待执行。
