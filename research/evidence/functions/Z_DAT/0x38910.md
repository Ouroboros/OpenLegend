# 函数证据：`sub_38910` `0x38910..0x38988`

状态：`pending_implementation`

已映射状态边界：`BattleSetup::damage_animation_frames`。

函数把damage display phase置0，固定循环10帧；每帧先令flash为 `frame<4 && !suppress_flash`，再render→present，随后phase加1并wait1。循环后把damage display kind清0。

普通向量phase 0..9、wait均为1、前4帧flash，完整hash `0x364953a2c8f42144`；suppress flag非零时10帧均不flash，hash `0xec7a73890ce825c4`。typed帧序列已实现并由Linux Debug 14/14验证；damage kind、render/present/wait与结束清理尚未接入BattleSession，因此不能标为完整实现。
