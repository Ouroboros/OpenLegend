# 函数证据：`sub_24417` `0x24417..0x24496`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码。

独立汇编恢复：天气inactive立即返回；active时三个粒子按相机反向等角平移。Y移动`delta_y`使`x += 18*delta_y`、`y -= 9*delta_y`；X移动`delta_x`使`x -= 18*delta_x`、`y -= 9*delta_x`。所有粒子字段写回16位，循环固定3次。

现代映射：`WorldSession::complete_move`成功、cache重载之后的`weather_`循环。blocked和scene尚未完成continuation时不平移；scene返回后若移动成功只执行一次。

向量覆盖四方向、active/inactive和16位负粒子坐标；300 tick oracle覆盖组合结果。最终双向逐基本块REVIEW为`not_started`。