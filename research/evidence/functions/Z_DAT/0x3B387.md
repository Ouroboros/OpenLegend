# 函数证据：`sub_3B387` `0x3B387..0x3B6BE`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::prepare_battle_settlement`、`BattleSetup::apply_post_battle_experience`、`BattleSetup::settle_battle`、`BattleSession`战后消息continuation，以及`LegacyGameRuntime`的raw get-exp边界。

## 1. 物理边界与二进制身份

- fresh临时IDA库：`tmp/Z_DAT-b8-order75.i64`；导出：`tmp/b8-order75-machine-all.txt`。
- 地址范围`0x3B387..0x3B6BE`，823 bytes、194条指令、44个IDA flow block、23个条件分支、6个无条件跳转。
- 61处四字节重定位全部满足loaded=raw+`0x20000`；逆变换后823 bytes逐字节匹配。
- raw SHA256：`bc96bb312a2beb8f0aa52bb8a229d36fc94ea03d272d9c53bb0cc1f9c776a164`。
- loaded SHA256：`e147e1633843da05bed92c0cb77700634664971f8cf287ee21fb7391a6fa0414`。
- 10次direct call依次为`0x3ED1E,0x3AA85,0x3EF4A,0x2CEBF,0x3D832,0x3D6D1,0x20C32,0x3B6BE,0x3BA85,0x3C2AC`。
- 唯一caller为`sub_3B238:0x3B37E`；唯一`RETN @ 0x3B6BD`。

## 2. 汇编独立合同

1. 第一循环按signed low16运行时参战者数量扫描全部slot。side恰1时把角色HP/MP恢复到各自上限、体力写100、内伤/中毒写0；任何非1 side角色HP signed严格大于0时计入均分人数。故side=-1进入分母，但不按敌方恢复。
2. 仅raw结果word恰2时分配WAR记录word7总经验。分母0先写1；总经验sign-extend后执行signed `idiv`并向零截断。共享值只加到side恰0且HP signed大于0者的参战者word13，按low16回绕；side=-1不领取。
3. 第二循环只处理side恰0。最大HP signed除5并向零截断；存活者HP低于该值才补齐，死亡者无条件写该值，且体力signed低于10时写10。非0 side不执行该下限。
4. 第三循环不分side，把每个slot的word13加入对应角色经验。角色经验、练功经验、制造经验均先写low16，再按unsigned比较；结果大于60000才写60000。
5. 训练经验不是普通signed八成：word13先sign-extend到32位，`shl eax,3`保留32位回绕，清EDX后执行unsigned `div 10`。例如reward=-1时，角色经验从0先成为0xFFFF并封顶60000；训练商`0xFFFFFFF8/10`写入low16为39320。
6. 消息门严格为side恰0且（get-exp word恰1或结果word恰2）。通过后固定顺序为战场重画→`%s 獲得經驗點數%5d`格式化→`(60,30,200,27)`框→`(63,35,0x0705,16)`文字→present→清旧last-key并等待新非零键。随后等级signed小于30时调用`sub_3B6BE`；practice item不等于-1时依次调用`sub_3BA85`与`sub_3C2AC`。
7. 所有角色均先提交三个经验字段，再决定是否显示消息或调用delegated callee；因此side1和side=-1同样提交自身word13，但永不显示经验消息。

## 3. 汇编→C++ REVIEW与修正

- `prepare_battle_settlement`的敌方恢复、非side1存活分母、胜利共享、side0发放、HP/体力下限与三次slot顺序完全一致。
- `apply_post_battle_experience`用`uint16`执行“加法后low16写回，再unsigned封顶”，并以`uint32(int32(reward)*8)/10`恢复负reward的32位左移与unsigned除法；完整int16输入域一致。
- `BattleSession`按slot保持经验先提交，再进入战场/框/文本/present/new-key，随后等级→练功→制造。复制角色/RNG的预演只用于把DOS同步消息拆成宿主phase，不改变共享状态或最终RNG。
- 首轮发现一处产品差异：`LegacyGameRuntime`以`battle_get_exp != 0`压成bool，而机器只接受word恰1。原资产145个opcode6调用虽仅含0/1，但完整signed-word脚本域不等价。已最小修正为`battle_get_exp == 1`，synthetic值9现在保持false。
- 修正后从`0x3B387`入口重新审计全部194条指令、44块、29分支、10次调用、61处重定位、唯一caller和唯一RET，零剩余合法word域差异。
- 现代非法slot/role拒绝、typed outcome及render/present/input分相属于`platform_adapted`；delegated等级/练功/制造、renderer、present/input和caller均保持独立owner。

## 4. 回归与独立Golden

- C++回归新增：side=-1计入分母但不领奖/不恢复；negative reward=-1得到角色经验60000、两项训练经验39320；65000加1000先word回绕为464且训练为264；59000加2000/1600后unsigned封顶60000；side1仍提交经验但不显示；运行时raw get-exp=9不启用开关。
- 新增`battle_settlement_machine`八个独立向量，覆盖negative total signed share、分母0→1、死亡side0、get-exp=1/9、negative reward、回绕先于封顶、unsigned封顶和negative maximum HP signed floor。
- 向量SHA256：`499fd7109920c8f9eee8f31015e158e1af76d7eb984da2be29ab9073c2bcabcd`。
- 两次临时生成逐字节一致，历史68键逐值不变；第三次正式生成一致。69键正式Golden SHA256：`a343bfe7d579a8b5de486a63d5654ea3851ade75ea19f0826b94bf7313179c41`。
- Linux `./build.sh app --config Debug`通过，14/14 tests passed。
- 原程序动态执行仍登记`blocked_runtime_oracle`；独立资产oracle不冒充原程序运行输出。
