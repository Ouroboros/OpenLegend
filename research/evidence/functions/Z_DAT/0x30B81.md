# 函数证据：`sub_30B81` `0x30B81..0x30C38`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；`research/ida/reports/Z_DAT.b7_scene_xrefs.txt`

函数物理范围：`0x30B81..0x30C38`

直接调用者：`sub_2C319` jump-table case 62

调用约定与参数：6 个 sign-extended KDEF words：`(first_event, first_start, first_end, second_event, second_start, second_end)`；第六参数由 caller 压栈但函数不读取。

返回合同：本函数不返回；逐帧动画结束后 `call sub_30C3D` 进入结局序列。现代边界在动画完成后同步启动 `SceneSession::start_ending`，`sub_30C3D` 由独立 closure 与 evidence 所有。

## 1. 范围与非范围

- 本单元负责：case62 到 `sub_30B81` 的 38 帧前置动画、玩家隐藏、两事件图片同步、render/delay 和结局转交边界。
- 明确转交：`sub_30C3D` 完整结局/字幕序列（独立 closure `0x30C3D`，当前为 `implemented_pending_review`）。

## 2. 不看 C++ 的独立汇编恢复

```text
first_picture = first_start
second_picture = second_start
word_C0BF8 = -86
for counter=first_start; counter<=first_end; counter+=2:
  capture pre-frame BIOS tick
  if first_event != -1:
    event[first_event].current/end/begin = first_picture
  if second_event != -1:
    event[second_event].current/end/begin = second_picture
  first_picture += 2
  second_picture += 2
  render
  delay(50) = 2 ticks
  wait until current tick differs from pre-frame tick
call sub_30C3D  // noreturn
```

此函数的 `-1` 事件语义是“跳过”，与 `sub_2F9F2` 的“更新玩家图片”不同。范围只由第一序列 end 控制，第二 end 参数不读取。

## 3. 汇编独立测试向量

- opcode62 仅出现于 script1017 PC5：`62,0,8054,8128,1,8130,8204`，共 38 帧。
- script1017 真实流：dialogue2958→opcode0→opcode62。
- scene83 event0/1 位于 `(23,42)/(23,41)`；测试玩家 `(22,41)`，view `(11,30)`。
- 独立 Python oracle 用真实 SDX083/SMP083、ALLSIN/ALLDEF 生成全部 38 帧字段和 framebuffer FNV-1a64；明确把 `-86` 作为不绘制玩家的哨兵。

## 4. 现有 C++ 映射

```text
case62 / PC += 7                 -> player_frame_override_=-86 + DualPictureAnimationState
first<=end inclusive +2          -> advance_dual_picture_animation_frame
-1 event skips                   -> skip_negative_events=true
三字段同步                       -> set_animated_picture
render + sub_3DB83(50)           -> present + wait_ticks=2
call sub_30C3D noreturn boundary -> quit_after=true -> start_ending()
```

## 5. LST/机器码 → C++ 正向追溯

- [ ] 最终 REVIEW 尚未执行。

## 6. C++ → LST/机器码反向追溯

- [ ] 最终 REVIEW 尚未执行。

## 7. 差异记录与重启 REVIEW

旧 case62 直接返回 quit，缺失玩家隐藏、38 帧图片序列、三字段写入、render/delay。当前转交已接入完整 `sub_30C3D` continuation；最终 REVIEW 仍必须从入口重做并审计转交边界。

## 8. 验证

- Linux Debug `openlegend.scene` 通过。
- 真实 scene83/script1017 全 38 帧字段、`-86` 隐藏和独立 framebuffer hash 通过；后继完整结局序列亦由独立 oracle 验证。
- oracle 可重复重生成；framework validator 在提交前执行。
- Windows/sanitizer 留到 B7 收口。
- 原程序动态差分：`blocked_runtime_oracle`。

## 9. 最终结论

```text
final_review = not_started
remaining = B7 全实现后重做 0x30B81 双向 REVIEW，并审计到 0x30C3D 的无返回转交
```
