# 函数证据：`sub_247DD` `0x247DD..0x24A02`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；世界资源和RANGER状态装载入口。

独立汇编恢复：依次读CLOUD.GRP、CLOUD.IDX并建立5个累计帧指针；读MMAP.COL；RANGER.IDX只在全局未初始化时读一次并以0为首offset，失败打印并等待键；随后打开RANGER.GRP，按IDX六段依次读取header、200角色、物品、场景等持久区，最后关闭句柄。读段顺序和长度均由相邻IDX差给出。

现代映射：`persistence::load_baseline/load_numbered_slot`与`model::GameSnapshot`拥有RANGER六段；`WorldSession`按需打开CLOUD、MMAP和palette；`PackedArchive`由IDX累计offset提供span。所有字节仍来自同一原资产，owner从裸全局缓冲区转为typed model/resource。

真实RANGER/云/palette资产摘要与完整快照往返有固定测试。文件错误改为typed error view，不复刻阻塞CRT错误路径，属于平台适配。最终双向逐基本块REVIEW为`not_started`。