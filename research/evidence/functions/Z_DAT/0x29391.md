# 函数证据：`sub_29391` `0x29391..0x294AD`

状态：`implemented_pending_review`

机器码以场景号索引 `ALLSIN.IDX/ALLDEF.IDX`，从工作副本 `ALLSINBK.GRP/ALLDEFBK.GRP` 分别读取固定 49,152 与 4,400 字节到当前场景地图/事件缓冲；IDX 打开失败打印原错误后进入终止链，IDX cache tag 分别为2/3，两个 GRP 每次打开、seek、read，地图 GRP 随后关闭，事件 GRP 通过共享尾块关闭。

现代实现由 persistence 在 snapshot 导入时一次解码全部100场景，`SceneSession` 通过 `scene_value/event_value` 读取当前场景同一原始字节；所有权变化不改变场景号寻址、固定记录尺寸或错误返回。真实 scene70 入口/像素哈希、全部归档尺寸及 scene mutation 测试覆盖合法资产；最终双向 REVIEW 仍需审计 delegated packed archive、错误路径与会话调用顺序。
