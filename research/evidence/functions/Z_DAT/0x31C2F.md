# 函数证据：`sub_31C2F` `0x31C2F..0x31C4A`

状态：`implemented_pending_review`

KDEF opcode66；仅 script531 PC26，参数 music ID。函数调用 `sub_3E1B2(id)` 后跳回解释器默认 continuation；caller 宽度2。

C++ case66 原样追加 `SceneAudioCommand::music(argument1)` 并 PC+=2。真实 script531 用 item_context183 驱动 `fade_to_black→opcode66(9)→fade_from_black`，核对 music9 在 fade_from_black 返回前已入队。Linux Debug scene 通过。

底层 music stop/load/play 和平台命令序列转交 runtime-audio closure；最终双向 REVIEW 尚未执行。
