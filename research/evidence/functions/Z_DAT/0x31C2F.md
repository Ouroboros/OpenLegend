# 函数证据：`sub_31C2F` `0x31C2F..0x31C4A`

状态：`platform_adapted`；最终 REVIEW：`converged_no_new_differences`

来源：当前 `Z.DAT` 完整机器码与原始文件字节；IDAT完整指令、caller、共享返回尾部；当前1,018条KDEF；已关闭runtime-audio边界。

## 1. 机器身份与caller

- 物理范围 `0x31C2F..0x31C4A`，27 bytes，6条指令。
- 原始 `Z.DAT[0x2B62F:0x2B64A]` 与loaded函数逐字节相同，SHA256均为 `a089c3d1d8b6a2d7513d1916723bff9856ae0bffa043afcffe37188b616081ca`；函数内无重定位。
- 唯一可执行caller为事件解释器jump-table case66；另一xref只是jump table函数指针。caller将一个KDEF word以signed int16扩展为dword后压栈。
- 函数尾跳到共享 `0x2DE4D`，该块返回常量1；caller不读取返回值，回收4 bytes并执行 `PC += 2`。
- 当前KDEF仅script531 PC26调用一次，参数music9；调用流按 `little_endian_<IIh:script,pc,music_id>` 编码，SHA256为 `0ef94f5d19631b8f5b643be16a63bd609b9a5b2d3e8d7c5a3f248b33177e1769`；script531完整word流SHA256为 `6e203fe9ed597f8fc2c89a3f52f760f114a59574ae829caf258b4a28dea04ff3`。

## 2. 不看C++的机器合同

函数先以参数8调用栈探测helper，然后把唯一实参原样传给 `sub_3E1B2(music_id)`，待其同步返回后跳入解释器共享成功尾部。入口本身不读取音乐状态、不比较当前曲目、不验证ID，也不直接修改事件、地图、角色、库存、随机、画面、输入或时间状态。

唯一真实序列在script531中为 `fade_to_black -> music(9) -> fade_from_black`；音乐调用完成后解释器才继续执行淡入。

## 3. 汇编→C++ REVIEW

C++ case66追加 `{music, argument(1), force=true}` 并将PC加2。平台在同帧消费命令；`force=true`使相同曲目也无条件进入 `play_music`，对应机器每次都调用callee。已关闭的runtime-audio边界负责淡出、结束旧音乐、读取目标XMI及启动播放，本项不传播其closure。

现代实现把同步平台调用运输为一条场景音频命令，并对负ID或超出24首范围稳定拒绝，替代机器callee的外部资源/平台失败行为，归类 `platform_adapted`；唯一真实music9处于合法域，调用、强制重播及后续淡入顺序一致。

第一遍单向对照未发现合法域产品差异。第二遍从入口重新覆盖6条指令、8-byte栈探测、signed参数运输、无条件callee调用、共享返回1、caller回收和PC推进，零新增差异。

## 4. 验证

- 独立golden固定函数27字节、唯一script531调用、参数9、调用流及完整script哈希；双次生成逐字节一致；正式 `scene-goldens.json` SHA256为 `cd32a223b9f8936bc63cb17e137f363f09ea494f575152f232f39b406f4b65af`。
- 真实script531由item context183驱动至淡出，确认music9在淡入结果返回前已入队，且命令force位为true。
- runtime-audio closure已关闭；底层音乐stop/load/play不在本入口重复关闭。
- Linux app Debug根构建14/14通过（`proc_fb77`）。
- 原程序动态差分：`blocked_runtime_oracle`。

```text
closure_status = platform_adapted
final_review = converged_no_new_differences
remaining =
```
