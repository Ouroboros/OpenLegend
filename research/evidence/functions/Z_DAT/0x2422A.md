# 函数证据：`sub_2422A` `0x2422A..0x24278`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码。

独立汇编恢复：把Y增量加到玩家world Y，随后夹到0..479；以`world_y-cache_origin_y`重算cache-local Y。无碰撞、无RNG、无资源调用。

现代映射：`WorldSession::complete_move`的Y坐标提交及`cache_y()`派生值；局部坐标不重复存储，避免双owner但不改变结果。

向量覆盖-1/0/479/480和origin 0/352。最终双向逐基本块REVIEW为`not_started`。