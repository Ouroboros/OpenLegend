# 函数证据：`sub_3A29C` `0x3A29C..0x3A30B`

状态：`platform_adapted / converged_after_timing_fix`

映射：`BattleSetup::begin_item_selection`、`apply_player_item_effect`、`finish_player_item_action`、`BattleSession::begin_player_item_selection`、`handle_player_item_key`与`render_player_item_selection`。

## 机器身份、入口与owner边界

fresh临时数据库`tmp/Z_DAT-b8-order67.i64`仅由`/mnt/d/Dev/Crack/IDA/idat.exe -A`配合`tmp/ida_dump_b8_order67.py`分析。owned范围固定为111 bytes、33条连续指令、5个基本块、2个条件分支、0个无条件跳转、2处绝对数据fixup、5次direct call、1个入口xref和1个本地RET。raw SHA256为`eb358abcceab4d2b8c735da754e73ff00bc611237a8cf23087e2475bfffad5c7`，IDA loaded SHA256为`428460ab48a5ebbbab01f06e88eea25e01ed6d2e8b36ade0862210db547e18d3`；两个DWORD从`0xBC72C/0xBC73A`严格重定位为`0xDC72C/0xDC73A`，逐处减`0x20000`后与原始111字节完全一致。

唯一入口xref为战斗行动jump table owner `sub_32E59:0x333A8`。5次调用严格依次为24-byte栈探测`sub_3ED1E`、物品过滤`sub_2A10F`、物品网格绘制`sub_2A186`、物品选择器`sub_2A86C`和暗器目标wrapper `sub_3A30B`。栈探测、过滤、renderer/present、selector、type3效果、暗器目标、暗器动作及caller均保持独立owner，不因本wrapper关闭而传播closure。

## 完整汇编合同

入口先固定调用`sub_2A10F(4)`。该共享callee按库存slot 0..199顺序收录合法item ID中type4及兼容type3，不检查数量，因此count为0的type3/type4仍会进入列表。随后固定调用`sub_2A186(0,0,0)`绘制初始page0/row0/column0的5×3物品网格；其机器尾`0x2A734..0x2A74C`调用`sub_3D6D1`，在进入selector之前完整present一次。

机器把actor参数按signed low word装入EBX，以`actor*28`从combatant记录读取signed role ID，再调用`sub_2A86C(4, role)`。返回完整EAX先写本地DWORD，但两处分派都只比较低16位：

1. AX恰为4时调用`sub_3A30B(actor)`；wrapper自身不写`action_done`，暗器目标确认或取消由callee独立决定。
2. 保存结果的低word恰为1时，以actor slot重新定位combatant并写`action_done=1`。
3. 其他所有低word值均不调用暗器wrapper，也不改`action_done`。

因此type3非零效果由selector在效果present、库存提交及任意键确认全部完成后返回1；23项效果全零时不显示、不扣库存、不读确认键，但仍返回1并结束行动。Escape返回0且不完成行动。type4返回4后是否完成行动只取决于`sub_3A30B`及其动作continuation。

本函数没有定义供caller消费的稳定返回合同：低word1路径末尾EAX已变为actor记录偏移，低word4路径可保留callee结果，其他路径可保留selector结果。唯一caller在`0x333A8`调用后无条件清栈并直接检查actor `action_done!=0`，完全忽略EAX。

## 汇编到现代实现逐块对照与修正

首轮从入口覆盖33条指令、5个基本块及两个分支，对照`BattleSetup`和`BattleSession`。过滤参数、type3/type4并集、库存slot顺序、count0收录、初始坐标、actor角色解析、AX低字分派、type3完成/取消、type4 continuation和caller只读`action_done`均一致。

首轮同时发现一处合法宿主时序差异：机器在selector可读首键前，已经由`sub_2A186 -> sub_3D6D1`完成一次present；selector每个已识别的方向/PageUp/PageDown路径也先重绘并present，再读取下一键。现代SDL主循环会在一次frame中先排空多个host event、之后才render/present；原实现进入`player_item_selection`后可立即接受同批下一键，也可在一次方向键后未呈现新光标就接受下一键。

最小修正为在`BattleSession`加入物品选择present门：开始选择时计数置1；`finish_presented_tick`只在对应frame实际呈现后递减；计数非零时拒绝输入；每个已识别的导航键后重新置1；取消、type3完成或转入type4目标时清零。该修正不改变页面、光标、物品、库存、效果或行动结果，只恢复机器的render→present→下一输入顺序。

修正后废弃首轮结论并从`0x3A29C`重新覆盖全部33条指令、5个基本块、两个分支、全部5次调用、唯一RET与caller出口。合法域没有剩余产品差异。

## 平台适配、回归与验证

原机器以任意signed actor slot计算combatant地址，并可继续读取非法role或item；现代Session只从有效当前actor进入，并安全拒绝非法combatant、role和item索引。该差异仅替代机器不安全域，归类`platform_adapted`。

回归新增并锁定：初始物品frame呈现前方向键被忽略且光标不动；首次present后Escape返回行动菜单且不置`action_done`；重新进入后每个左右、上下、PageUp/PageDown导航都要求对应frame present后才接受下一键。既有回归继续覆盖16项分页、type3/count0收录、数量显示、三种确认键、不可用slot、菜单frame hash、type3非零效果的面板/库存/确认顺序、全零效果不消费库存但结束行动，以及type4目标取消、空格与命中continuation。

独立机器向量覆盖取消、type3非零/零效果、type4目标取消/确认、EAX高位非零但AX为4或1、其他结果保留既有`action_done`及count0过滤，SHA256为`9657c3c342df26368b3e944dcff5923946ea64026a02bdab8fb0d61ceea270a5`。两次独立原资产Golden生成逐字节一致，正式文件再由第三次生成并核对；新增唯一键`battle_player_item_wrapper_machine`，历史60键逐值不变，正式SHA256为`bbafb835c53f40faaa872d1052ceff7b63930ac2949420e43723003af41ceccd`。

根`./build.sh app --config Debug`精确通过`100% tests passed, 0 tests failed out of 14`。原程序动态差分继续登记为`blocked_runtime_oracle`；最终结论为`platform_adapted / converged_after_timing_fix`。
