# 函数证据：`sub_3A29C` `0x3A29C..0x3A30B`

状态：`pending_implementation`

已映射状态边界：`BattleSetup::begin_item_selection`。

函数依次调用共享物品过滤、5×3物品选择和角色物品处理界面。过滤参数固定4；共享 `sub_2A10F` 因此把库存中 item type为4或3的槽写入选择表，扫描全部200槽，只检查item ID非负，不检查库存数量。选择器返回4时调用 `sub_3A30B` 进入暗器目标；返回1时直接把actor `action_done`置1；其他返回值不写行动完成。

现代typed列表保留type3/type4并集、原库存槽序和“数量为0仍列入”边界。共享物品图标/详情界面、type3物品使用、选择返回值与目标continuation尚未接入BattleSession，因此本函数保持 `pending_implementation`。
