# 函数证据：`sub_3A29C` `0x3A29C..0x3A30B`

状态：`implemented_pending_review`

映射：`BattleSetup::begin_item_selection`、`apply_player_item_effect`、`finish_player_item_action`、`BattleSession`玩家物品相位。

函数依次调用共享物品过滤、5×3物品绘制和选择循环。过滤参数固定4；`sub_2A10F`因此按库存slot顺序收集item type4以及兼容的type3，不检查数量。选择器返回4才调用 `sub_3A30B` 进入暗器目标；返回1时把actor `action_done`置1；取消返回0不完成行动。

现代实现已实际执行battle战场重画、三个共享面板、5×3格、MMAP图标、名称/简介、数量、上下滚动箭头、左右/上下/PageUp/PageDown、Escape与Enter/Space/keypad Insert。16项且首项count2菜单FNV64为`0x68c3b70dfec20bba`，同帧count1为`0x17b6845a718a6cf8`。

type3固定以当前actor同时作为actor/target，复用23项共享效果状态；非零效果实际重绘battle、present面板、扣库存并等待任意键后结束行动，单项HP面板FNV64为`0xd518fb664f3e0e3c`。全零效果保留原边界：不画面板、不扣库存、不消费RNG，但仍按选择结果1结束行动。type4已进入 `sub_3A30B` 的完整目标与动画continuation。Linux与Windows Debug完整BUILD均14/14；最终逐基本块双向REVIEW尚未执行，故为 `implemented_pending_review`。
