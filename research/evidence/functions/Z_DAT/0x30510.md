# 函数证据：`sub_30510` `0x30510..0x30559`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；直接调用者 `sub_302E0` 轮间恢复。

## 汇编独立合同

仅当主角 `hurt < 50 && poison == 0`：

```text
hurt=0
physical_power=100
mp=maximum_mp
hp=maximum_hp
return maximum_hp
```

否则不修改角色。严格 `<50`，不是既有休息 helper 的 `<33`；中毒角色即使轻伤也不恢复。

## C++ 映射与验证

`TournamentTrialState::interround_fade` 在 fade_to_black 和 8 tick 黑屏完成后、fade_from_black 之前执行上述四字段写入。真实 scene25/script936 设置 hurt49/poison0/hp1/mp2/power3，首轮后核对恢复为 0/100/max/max；完整全胜路径通过 Linux Debug scene/ui。

最终正向/反向逐基本块 REVIEW 尚未执行。
