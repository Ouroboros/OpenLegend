# 函数证据：`sub_20899` `0x20899..0x208A9`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码。

独立汇编恢复：该函数保存EAX/EBX/EDX/ECX，以x86 `loop`按入口ECX精确重复调用`sub_208A9`；每次调用由被调函数各推进一个source与destination字节，计数结束后逆序恢复寄存器并返回。合法调用域ECX为当前RLE可见run长度，不存在额外判零分支。

现代映射：`WorldSession::draw_weather_particle`中`for (source_index : run.pixels)`的可见像素内循环；每次迭代恰好读取一个源palette index、一个目标index并写回一个目标字节，随后destination_x加1。run裁剪在外层保持与原入口ECX等价。

独立向量覆盖run长度1、多像素run及左右裁剪后的部分run。该边界没有独立状态owner；函数内联是批准的结构适配。最终双向逐基本块REVIEW为`not_started`。