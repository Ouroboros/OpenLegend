# 函数证据：`sub_20899` `0x20899..0x208A9`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码。

独立汇编恢复：函数依次保存EAX、EBX、EDX、ECX，以x86 `loop`按入口ECX精确重复调用`sub_208A9`；被调函数每次各推进一个source与destination字节。计数结束后逆序恢复寄存器并返回。机器无ECX判零分支，但全部合法caller只在可见run长度大于0时进入。

现代映射：world与scene天气循环、battle的`draw_cursor_overlay`像素循环分别内联该run helper；每个可见像素读取一个source index和一个destination index，调用等价混色并写回一个destination字节。透明区域不进入循环，左右边缘只缩短实际迭代区间，与机器入口ECX相同。

差异与适配：现代空span自然零次迭代，避免机器在非法ECX=0时`loop`回绕；真实CLOUD所有调用run均为正长度。函数内联不改变合法调用顺序。登记修正后已从`0x20899`入口重启最终单向汇编→C++ REVIEW，逐指令确认寄存器保存、首次无条件callee、ECX恢复、`loop`回边和逆序恢复；三份内联像素循环无新增可达差异，终态为`platform_adapted`。