# 函数证据：`sub_294AD` `0x294AD..0x29537`

状态：`implemented_pending_review`

机器码确保 `ALLSIN.IDX` cache tag=2，按场景号 offset 从 `ALLSINBK.GRP` 读取固定49,152字节，恢复当前地图缓冲。该函数由外部场景修改链在临时切换结束后调用；IDX 缺失走原打印与终止链。

现代 snapshot 同时常驻100场景地图，外部场景修改按 scene id 直接访问目标 span，不覆盖当前 scene span，因此不需要物理 reload，却保留该 helper 的可观察恢复结果。当前 KDEF 中 opcode38 唯一 occurrence 使用当前场景 `-2`，非当前地图 swap 分支无资产可达调用；实现保留任意合法 scene id 的直接寻址，最终 REVIEW 尚未执行。
