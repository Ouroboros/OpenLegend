# 函数证据：`sub_300FF` `0x300FF..0x301D1`

状态：`implemented_pending_review`

KDEF opcode56。机器码对 role0 record word59（`fame`）执行 16-bit 回绕加法；结果 signed `>=200` 时扫描全部200个库存槽，只按 item ID（不看 count）标记十四书144..157和书信189。没有189且十四书齐全时，把 scene70 event11 改为 event_1=932、三图片7968。

C++ `SceneSession::run_event(case56)` 使用显式 `wrapping_add`，随后调用共享 `update_book_event_if_ready`。当前 KDEF 中 opcode56 共105处；真实 script2 固定 fame199→200 时十四本零数量书触发 event_1=932，以及 fame32767+1→-32768 的无触发回绕反例。独立 B7 oracle 同步输出两向量，既有缺书/书信反例继续覆盖共享触发。Linux Debug `openlegend.scene` 通过；最终双向 REVIEW 尚未执行。
