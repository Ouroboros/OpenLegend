# 函数证据：`sub_2F9B5` `0x2F9B5..0x2F9F2`

状态：`implemented_pending_review`

KDEF opcode43。线性扫描全部200个库存槽；只要任一 slot.item_id 等于参数就置 found=1。函数不读取 count、不短路（后续槽仍扫描）。found 返回 true_offset，否则 false_offset；caller 宽度4。

旧 C++ 用 `inventory_count(item)>0`，错误排除 count=0/负数并受重复槽求和影响。现 case43 使用 `inventory_contains_id`。真实 script636：slot0=(item110,count0) 仍 true→dialogue2381；ID改-1则 false→dialogue2380。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
