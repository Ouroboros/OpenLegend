# 函数证据：`sub_351A7` `0x351A7..0x35217`

状态：`platform_adapted / converged_no_new_differences`

映射：`openlegend::battle::BattleSetup::choose_ai_weakest_attack_target`。

## 1. 机器身份与调用边界

- 范围`0x351A7..0x35217`，raw file offset `0x2EBA7`，共112字节、32条指令、5个显式跳转、7处加载时fixup。
- raw SHA256：`56611048416b38e1eca2c1cf738e488175de001df5cc2b851ef947fa85ec398f`。
- loaded SHA256：`76408936111ba57a30049c0dd11814af62191e1b81797757cee29e8f48488b46`。
- 七个fixup位于函数内偏移`0x29,0x30,0x39,0x43,0x50,0x5E,0x66`，每个加载值相对raw精确增加`0x20000`；归一化后112字节逐字节等于原始`Z.DAT`切片。
- 两个caller为`sub_3505B`在`0x350D8`的最低攻击策略分支和`sub_35217`在`0x35363`的专长策略尾。前者调用后直接置策略标记，后者调用后直接进入函数尾；两者均不检查EAX。
- 唯一direct call为入口栈检查`sub_3ED1E`；本函数不消费RNG，也不调用其他业务callee。

## 2. 完整机器合同

1. 入口将actor保存到EDI；EBX为槽循环计数器且清零，ECX以1000初始化为当前best。
2. 循环尾以`cmp bx, word_556D2`和`jl`实现signed 16位`slot < combatant_count`，正常battle域按槽序扫描全部combatant。
3. 每个候选先读取signed side，与actor side相等时跳过；再读取隐藏/占用word，任意非零值均跳过。机器流没有HP、死亡或额外self过滤。
4. 剩余候选按signed role id乘182字节定位角色记录，读取signed attack。`cmp ax,cx`后以`jge`跳过，因此仅`candidate_attack < best`更新。
5. best初值1000，所以小于1000的attack（包括负值）可写；attack等于或大于1000不写。严格小于使相同attack保留最早槽。
6. 更新时把candidate attack写入best低16位，并把candidate槽写入actor word11；更新不提前退出，继续扫描后续全部槽。
7. 无合格attack严格小于1000时保留调用前word11不变。函数无独立布尔返回合同，两个caller均不消费EAX；typed写入结果仅为现代宿主元数据。

## 3. 汇编→C++逐块对照

- `choose_ai_weakest_attack_target`按`slot=0..combatant_count-1`顺序遍历，并以`side == actor_side || occupancy_hidden != 0`跳过，与两个机器过滤分支完全一致。
- 角色attack由`int16_t`读取，`best_attack`为signed `int16_t`且初值1000；C++严格`attack < best_attack`后立即写actor `ai_target`并继续循环，保持1000边界、signed负值和first-tie。
- 固定边界回归：两个attack均30且早槽HP为0时仍选slot3；slot4 attack为-100但hidden为`-1`时跳过并选30的slot3；attack `[-1,-2]`选slot4；attack `[1000,1001]`不写并保留stale slot4；同side默认attack10不会参与。
- 现代对候选负role id或越界role id返回`nullopt`，替代原版线性越界读取；正常可达输入未发现行为差异，因此归类`platform_adapted`。
- 差异审计后从入口重新覆盖全部32条指令、5个跳转、7个fixup与两个caller，零新增差异。

## 4. 验证与结论

- 独立机器审计：`proc_3745`，112/32/5/7身份、两个caller和唯一direct call全部通过；IDA caller导出为`proc_7a30`。
- 原始只读资产双生成：`proc_d30a`与`proc_a7b7`逐字节一致；正式battle golden SHA256为`86772ef8670b75ef913c55dc83268d316a73116f595fa9536c4865327e27df47`。
- Linux app Debug根BUILD：`proc_49a8`，14/14通过，包含新增battle边界回归与reverse framework。
- 本owner结论：`platform_adapted / converged_no_new_differences`。`sub_35217/sub_35372`继续保持各自closure状态，不由本函数传播关闭。
