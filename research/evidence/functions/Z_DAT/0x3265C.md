# 函数证据：`sub_3265C` `0x3265C..0x3271E`

状态：`platform_adapted / converged_no_new_differences`

映射：`openlegend::battle::BattleSetup::append_enemies`。

## 最终机器边界

- 地址范围：`0x3265C..0x3271E`，194字节，42条指令，1条循环分支。
- Z.DAT raw偏移：`0x2C05C`。
- raw SHA256：`ea791c1d11fb53632d54ca0fbb75f232fb47d894bbb75344e1b219cf1c7bf351`。
- IDA loaded SHA256：`dd7d372cd20ca5f93ab279c9f30d6ec874aa1e695bb2b367c4112c11a6c8de3a`。
- 17项绝对地址重定位全部满足 `loaded=raw+0x20000`；归一化raw逐字节等于IDA loaded字节。
- 唯一caller为`sub_31C75`的`0x31CB4`；两个直接调用依次为`sub_3ED1E(16)`与`sub_3B1E6(current_count)`。callee closure不传播关闭。

## 敌方追加合同

函数顺序扫描WAR words33..52的20个signed敌方角色ID；只把精确的-1视为空项。每个非-1项严格按以下顺序追加到当前全局count指向的28字节参战者槽：

1. word0=敌方角色ID；
2. word1=1；
3. word2/3=WAR words53..72/73..92同下标x/y；
4. word4=1；
5. word8=`sub_3B1E6(current_count)`；
6. occupancy `[y*64+x]=current_count`；
7. current_count按16位word递增。

机器没有角色范围、坐标范围、重复occupancy、26槽容量或count溢出检查。现代`append_enemies`保持20项顺序、-1唯一哨兵、字段值和后写occupancy覆盖；对非法角色、坐标和容量的稳定拒绝属于安全适配。

## 独立资产与回归证据

`research/tools/generate_b8_battle_goldens.py`的`battle_setup_machine.enemy`直接从原始Z.DAT固定本函数raw切片、caller、两个call site/target、20项字段范围与写入顺序；`battle_setup.records`独立解析全部140条WAR记录。当前资产敌方最多20人，固定/预置静态队伍加敌方最多21人，全部静态坐标位于0..63。

battle93的敌方源下标8与10都位于`(13,34)`。队伍预置角色先占slot0，两个敌方随后为slot9与slot11；原机与现代均不去重，occupancy最终由后写slot11覆盖。`tests/unit/battle/battle_data_test.cpp`固定该向量及battle0/battle4字段、count与occupancy结果。独立golden双生成逐字节一致并与正式文件逐字节一致；Linux app Debug全门禁通过。

结论：合法当前资产域内，敌方20项扫描、-1哨兵、14-word槽字段、sprite计算、16位count及后写occupancy覆盖均一致；额外受检拒绝归类`platform_adapted`。最终状态为`platform_adapted / converged_no_new_differences`。
