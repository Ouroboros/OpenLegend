# 函数证据：`sub_3505B` `0x3505B..0x3513A`

状态：`implemented_pending_review`

映射：`BattleSetup::choose_ai_attack_target`。

按原短路顺序：morality>=75时消费`bounded(10)`，结果<7调用最高攻击selector；尚未选择且morality<=25时同门槛调用最低攻击selector；仍未选择且IQ>=70时同门槛调用专长selector；否则无RNG调用最近目标selector。一旦某策略门槛命中即停止，不因该selector未写目标而回退。

seed9首个输出2并选对应策略，终态1341714958；morality75、IQ70、seed1两次失败输出`[8,8]`后回退最近目标，终态2524885223。Linux Debug 14/14和独立oracle通过；最终仍需完整汇编↔C++ REVIEW。
