# 函数证据：`sub_3505B` `0x3505B..0x3513A`

状态：`platform_adapted / converged_no_new_differences`

映射：`openlegend::battle::BattleSetup::choose_ai_attack_target`。

## 1. 机器身份与调用边界

- 范围`0x3505B..0x3513A`，raw file offset `0x2EA5B`，共223字节、63条指令、9个显式跳转、6处加载时fixup。
- raw SHA256：`10afcc5cd8f924d80d1749c1fe5f2e906fa60b9a191767fb15436d50e6bae992`。
- loaded SHA256：`bbbf59bbdc65946d1c1e0d2e093cb5ccb4d46d831002dd285ae13720f8f9bbdf`。
- 六个fixup位于函数内偏移`0x19,0x26,0x59,0x66,0x99,0xA6`，每个加载值相对raw精确增加`0x20000`；归一化后223字节逐字节等于原始`Z.DAT`切片。
- 两个调用点为`0x34D61`攻击handler和`0x3583E`暗器handler，均把signed actor槽作为唯一参数压栈；调用后立即恢复栈且不检查EAX。
- 八次direct call依次为`0x3ED1E,0x3D612,0x3513A,0x3D612,0x351A7,0x3D612,0x35217,0x35372`；后三个策略selector与最近目标selector按各自owner独立审计。

## 2. 完整机器合同

1. 入口将32位`ebx`清零作为“策略已选择”标记；三个属性段都重新从栈按signed 16位读取actor，经28字节combatant记录取得signed role id。
2. 第一段读取角色morality并作signed比较。值小于75时不消费RNG；值大于等于75时调用一次`bounded(10)`，结果严格小于7才调用最高攻击selector `sub_3513A(actor)`，随后无条件将选择标记写1。
3. 仅在第一段未选择时进入第二段。morality大于25时不消费RNG；值小于等于25时调用一次`bounded(10)`，结果严格小于7才调用最低攻击selector `sub_351A7(actor)`并无条件置选择标记。
4. 仅在前两段未选择时进入第三段。IQ小于70时不消费RNG；值大于等于70时调用一次`bounded(10)`，结果严格小于7才调用专长selector `sub_35217(actor)`并无条件置选择标记。
5. 仅在三段均未选择时，无RNG调用最近目标selector `sub_35372(actor)`。
6. selector返回值和是否写入word11均不控制顶层回退：属性策略一旦命中便停止；selector未写时保留调用前word11。三个RNG调用点中，高低morality条件不能同时成立，因此单次调用实际只可消费0、1或2次RNG。
7. 函数返回寄存器保留最后一个callee的EAX，但两个caller均不消费；机器函数自身不写目标字段，全部目标写入委托给四个selector。

## 3. 汇编→C++逐块对照

- `choose_ai_attack_target`按完全相同顺序使用signed `morality >= 75`、`morality <= 25`、`iq >= 70`，且C++短路求值保证仅在属性门槛成立时消费`bounded(10)`。
- 三段均使用严格`< 7`；roll失败继续后续段，命中则立即返回对应策略。最后仅在全部未命中时调用nearest，RNG消费与机器流一致。
- `selection`读取当前`ai_target`并保留`target_written=false`，不会因delegated selector未写而调用下一策略；order18集成回归进一步锁定合法stale target继续由攻击handler使用。
- seed3首轮输出恰7并回退nearest、终态`3310558080`；signed morality最小值`-32768`在seed6首轮输出6时进入最低攻击策略、终态`2326136519`。既有seed9命中、seed1两次失败和未写目标向量继续覆盖0/1/2次RNG与不回退合同。
- 现代实现对非法actor槽、非法role id及delegated selector内部索引错误返回`nullopt`，替代原版线性越界访问；正常可达输入未发现行为差异，因此归类`platform_adapted`。
- 差异审计后从入口重新覆盖全部63条指令、9个跳转、6个fixup及两个caller，零新增差异。

## 4. 验证与结论

- 独立机器审计：`proc_f681`，223/63/9/6身份、两个caller和八次direct call全部通过。
- 原始只读资产双生成：`proc_e467`与`proc_372e`逐字节一致；正式battle golden SHA256为`296c8c9a0f8f790a165d701ca2b4a9b506657e9654d48a3bb67dbe5b2a3240c3`。
- Linux app Debug根BUILD：`proc_43e3`，14/14通过，包含battle回归与reverse framework。
- 本owner结论：`platform_adapted / converged_no_new_differences`。`sub_3513A/sub_351A7/sub_35217/sub_35372`继续保持各自closure状态，不由本函数传播关闭。
