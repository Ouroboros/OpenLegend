# 函数证据：`sub_2E1E8` `0x2E1E8..0x2E278`

状态：`implemented_pending_review`

KDEF opcode12。先扫描队伍槽1..5，找到首个 signed `<=0` 槽作为 party_end；若无则6。只遍历槽 `[0,party_end)`，不越过队伍洞，也不会处理作为空哨兵的 role ID0。对每个角色仅当 `hurt<33 && poison==0` 时写 hurt=0、physical_power=100、mp=maximum_mp、hp=maximum_hp。

旧 C++ 遍历全部六槽，可能跨洞恢复后续角色。现先计算连续前缀。真实 script931：slot0=role0、slot1=role1、slot2=-1、slot3=role2；role0恢复，poisoned role1不恢复，洞后的可恢复 role2亦保持原值。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
