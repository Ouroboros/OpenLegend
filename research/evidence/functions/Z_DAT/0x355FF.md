# 函数证据：`sub_355FF` `0x355FF..0x35667`

状态：`implemented_pending_review`

映射：`BattleSetup::choose_ai_poison_target`。

入口先将actor word12写为-1。actor IQ严格大于60时消费一次`bounded(10)`；结果<7先调用最高攻击用毒目标selector。该selector返回-1或门槛未命中时，再调用陈旧距离selector。IQ恰60不消费RNG。seed9输出2、终态1341714958；最高攻击`[30,50]`选slot4，`[0,0]`回退并选首个合格slot3。Linux Debug 14/14和独立oracle通过；最终仍需完整汇编↔C++ REVIEW。
