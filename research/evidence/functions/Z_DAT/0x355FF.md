# 函数证据：`sub_355FF` `0x355FF..0x35667`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::choose_ai_poison_target`。

当前原始`Z.DAT`物理范围为104字节、32条连续指令、3个显式条件跳转、2处加载时fixup和4次direct call；raw SHA256为`7821ab41dc1724bec3b199715ed04e2cd819aa105b5b69e59810f583e088afe7`，IDA加载后SHA256为`940a59f0582883b27b20b1f91cdc27c3326670fbe0053281a1cfae35e2d038cc`。两处加载重定位相对raw均增加`0x20000`，归一化后逐字节相等。唯一caller为`sub_3540E`的`0x35421`；四个call依次为栈探测、RNG、最高攻击用毒目标selector和陈旧距离selector。caller把返回值sign-extend后与-1比较，仅非-1时继续读取actor word12。

函数只把局部EBX初始化为-1，不在入口或任何基本块直接写actor word12；word12只可能由两个delegated selector写入。actor IQ按signed word严格大于60才调用一次`bounded(10)`；IQ等于60或更小均不消费RNG。RNG结果按signed 32位严格小于7才调用最高攻击selector；结果等于7或更大直接回退陈旧距离selector。最高攻击callee返回先写EBX，再经BX低16位截断并sign-extend与-1比较；非-1直接返回，恰为-1才回退。fallback返回同样先截为BX再sign-extend。单次调用直接消费0或1次RNG。

首次汇编→C++ REVIEW发现现代入口错误地先把actor word12写-1，把机器的局部结果初始化误当成共享状态初始化；这会让无目标路径错误清掉旧目标。删除该写入后作废首轮结论，并从入口重审全部32条指令、3个条件跳转、2处fixup、四次call、唯一caller及全部0/1次RNG出口，未再发现差异。现代typed结果在合法槽域等价于机器int16返回；非法actor/role由现代安全拒绝，替代原版线性越界，归类平台适配。

新增seed3回归固定IQ61、`bounded(10)=7`、终态3310558080、跳过最高攻击并由fallback写slot3；既有seed9固定输出2、终态1341714958并由最高攻击写slot4；最高攻击全0固定callee -1后fallback；IQ60固定不消费RNG。无合格目标将actor word12预置99，selector返回-1且word12原样保留；完整`BattleSession`无目标→自动攻击路径也固定word12保持1。陈旧共享目标的具体来源和两个selector内部循环属于order26/27独立owner，本项不传播其closure。

正式battle golden由原始只读资产独立双生成逐字节一致，SHA256为`8953f9289c39431a59f777cb84a09da4140f7d28011ef9b81853f77093ff07b6`。Linux app Debug根BUILD `proc_9800`全部14项测试（含battle、reverse framework与SDL smoke）通过。故本owner归类`platform_adapted / converged_no_new_differences`。
