# 函数证据：`sub_2E639` `0x2E639..0x2E659`

状态：`platform_adapted`
最终REVIEW：`converged_no_new_differences`

## 1. 物理边界与调用者

- 入口/出口：`0x2E639..0x2E659`，32字节，7条指令。
- loaded bytes SHA256为`b3b2507c471dd0ca376f333d960f71852b018c789f0d5583aab3f8bb5d6c9709`；原始`Z.DAT`文件偏移`0x28039`起32字节SHA256为`b65a65a395a3aa0f7223295061cebe037d1eeca0b4f0272f0a10352cecbccaf8`。
- 两份字节仅offset27一个字节不同，位于角色0的`use_poison`字段绝对地址dword第三字节；loaded `0x901AA`为raw `0x701AA`加`0x20000`，归一化后无差异。
- 唯一caller为`sub_2C319:0x2C6D0`的opcode23分派。caller先压signed value、再压signed role ID；返回后公共路径回收8字节并把PC固定推进3 words。
- 除统一栈检查`sub_3ED1E`外无业务callee；helper显式返回0，caller不解释该值。

## 2. 入口到出口机器行为

机器把第一个参数乘182得到角色记录字节偏移，再把第二个参数的低16位直接写入该记录word47，即`use_poison`：

- 不读取旧值，不做加法、钳位或符号条件。
- 只写一个word；相邻word46 `medicine`与word48 `detoxification`不变。
- role ID没有范围检查；负值或越过角色数组的值形成未定义的数组外写。
- 对caller提供的signed int16参数，写入值完整保留其16位位型。

## 3. 当前原资产域

独立Python按68槽真实宽度扫描全部1,018条KDEF：opcode23仅一次，完整`<IIhh:script,pc,role,value>`参数流为`(28,65,4,99)`，SHA256为`0d97f2cc103d8b9cfb264e1a7b843f3deefb5084b0dd973c226f61d75eccd22f`。

真实script28在PC65把role4 `use_poison`从任意旧值覆盖为99，随后继续到battle边界。role4合法落在当前320条角色记录内。

## 4. 汇编→C++收敛

暂不参考现有C++完成上述机器推导后，对照`SceneSession::run_event(case23)`：

- 合法role ID按同一角色索引，仅写`role_word::use_poison=argument(2)`。
- int16脚本参数准确保留机器的低16位覆盖语义；不修改相邻字段。
- PC固定推进3，并继续执行后续opcode。
- 现代先检查role ID非负且小于角色数组大小，替代原机非法ID的未定义数组外写；当前唯一资产参数role4不触发该保护。

合法域状态和时序一致；非法role范围由现代安全边界适配，归类`platform_adapted`。从入口到出口重审未发现新增产品差异或未决项。

## 5. 验证

- headless IDA从正式数据库副本导出7条指令、唯一caller及完整上下文；正式IDB与原资产保持只读。
- 独立oracle覆盖唯一opcode23位置、参数、字段和PC合同；双次生成逐字节一致（`proc_9b08`），oracle SHA256为`da1d1f35d6c504f6081765b12ebf687e290860a8830dfb78851ea703c8475cdf`。
- 真实script28回归把role4旧`use_poison=-32768`覆盖为99，并验证相邻medicine77和detoxification88保持不变，随后到达battle边界。Linux app Debug根BUILD脚本14/14通过（`proc_a603`）。
- order46静态门、全局closure计数及reverse framework 577行有效/scene-event剩52项通过（`proc_cba9`）。
- 动态原程序差分仍按统一策略记为`blocked_runtime_oracle`，不替代静态机器码与独立资产oracle。
