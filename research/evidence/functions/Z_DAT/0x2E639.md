# 函数证据：`sub_2E639` `0x2E639..0x2E659`

状态：`implemented_pending_review`

KDEF opcode23。除编译器栈探针外，机器码只按 `role_id*182` 定位角色记录，并把参数写入 record word47（`use_poison`）。C++ `SceneSession::run_event(case23)` 写入同一字段。

当前 KDEF 中 opcode23 仅真实 script28 一处 `(role4,99)`；测试推进到随后的 battle 边界并确认 role4 `use_poison=99`，独立 B7 oracle 同步输出该向量。运行时保护只覆盖当前脚本不可达的越界角色编号。Linux Debug `openlegend.scene` 通过；最终双向 REVIEW 尚未执行。
