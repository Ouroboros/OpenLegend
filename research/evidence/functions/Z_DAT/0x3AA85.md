# 函数证据：`sub_3AA85` `0x3AA85..0x3B1E6`

状态：`pending_implementation`

已映射typed边界：`BattleSetup::battle_render_plan`。

函数严格执行两个32×32 pass，均以local x为外层、local y为内层，并以相机偏移取得64×64战场格：

1. 第一pass逐格调用共享sprite wrapper绘制WARFLD layer0。锚点为`x=145+18*(local_x-local_y)`、`y=-81+9*(local_x+local_y)`。
2. 第二pass逐格按以下顺序执行：
   - 仅当path range严格大于0时：path值严格大于range且不等于555，以primary overlay、mode3绘制范围格；当前cursor也只在该分支内以primary/mode2或alternate/mode3绘制；
   - 可见secondary cursor以alternate/mode3绘制；
   - WARFLD layer1不为0且不为15000时绘制；
   - occupancy非负时绘制combatant。高亮条件为高亮flag、effect格为1且角色HP非负；mode1/2/3分别使用调色值255/47/78，其他mode在该分支不绘制角色；
   - effect flag且effect格为1时绘制sprite `2*effect_id+frame_offset`；
   - damage kind大于0、occupancy非负且effect格为1时绘制damage文字。

overlay和damage文字横坐标比普通sprite左移18。damage纵坐标为`-141+9*(local_x+local_y)-2*offset`。kind1..5依次使用packed颜色`0x1014,0x3032,0x9193,0x0705,0x5053`，符号依次为`-, -, +, +, -`；其他正kind不绘制文字。

独立oracle以真实battle4 WARFLD、定义布局和非对称view/cursor/effect状态生成1,157条命令，FNV64为`0xb9f8a428699b3712`；C++测试逐字段复算，并验证非对称path/主副cursor投影及目标格base、高亮角色、effect、damage的顺序。额外零range向量验证主cursor随range分支一并抑制，而secondary cursor仍由独立flag控制。

当前只恢复原调用顺序和参数的typed命令计划；尚未把命令执行为RLE sprite、overlay、高亮和字体像素，也未接BattleSession present。因此原函数仍保持 `pending_implementation`。
