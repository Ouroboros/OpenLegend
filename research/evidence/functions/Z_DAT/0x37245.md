# 函数证据：`sub_37245` `0x37245..0x37355`

状态：`implemented_pending_review`

映射：`BattlePathing::mark_shortest_path`。

读取 target distance 后先把 target写250；直到回到 source，每轮令 distance=`(distance+127)%128`，按上、右、左、下找严格位于0..63且值等于前一层的首格，写250并继续。battle0/93 标记后完整 hash与 source首步分别固定为 `(31,20)`、`(33,29)`。现代仅在不可达输入时返回false，避免原版无前驱死循环。
