# 函数证据：`sub_3C563` `0x3C563..0x3C672`

状态：`implemented_pending_review`

映射：`BattleSetup::apply_round_status_damage`。

逐参战者条件严格为`hurt>0 || (poison>0 && hp>0 && physical_power>0 && occupancy_hidden==0)`；进入分支后无论由哪一项触发，均先从HP减`hurt/20`，再减`poison/10`，两次均为有符号向零截断及word写回。体力严格小于0时写1，HP严格小于0时写1；等于0不夹回1。`hurt>0`分支不受死亡、体力或隐藏标志限制。

battle4合成向量覆盖：隐藏角色HP0、hurt20、体力-1仍执行，HP经-1后夹1、体力夹1；可见角色HP100、poison20减至98。函数无绘制、呈现、输入或等待，本体状态核心完整，进入最终双向REVIEW前标`implemented_pending_review`。
