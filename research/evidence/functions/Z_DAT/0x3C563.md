# 函数证据：`sub_3C563` `0x3C563..0x3C672`

状态：`platform_adapted`

映射：`BattleSetup::apply_round_status_damage`、`BattleSession`。

fresh临时IDB锁定本owner为271 bytes、58条指令、13个基本块、8个条件分支、1个无条件跳转、19处加载时fixup、1次direct call与本地`RET @ 0x3C671`；raw/loaded SHA256分别为`e35f7e568ef22c53843f9d8404e08447a4b300b4d3f10fe49c15b6a4208dcc35`和`a6505c9625d9804448ddbb6818d8d8f9f63e0fea8594b48490745b4cdf8252`。全部fixup均为`+0x20000`且归一后逐字节等于原`Z.DAT`；唯一入口xref为`sub_3271E:0x32A38`，唯一call为`sub_3ED1E`栈探测，无共享尾。

机器以signed低字`CX`从0递增并与combatant count作signed比较。逐槽资格短路严格为`signed hurt>0 || (signed poison>0 && signed hp>0 && signed physical_power>0 && occupancy_hidden==0)`；`hurt>0`完全绕过死亡、体力和hidden门。进入提交后无论由哪项触发，均先以signed向零`hurt/20`从当前HP相减并写回word，再以signed向零`poison/10`从刚写回的HP相减并再次写回word；负商会反向加HP，两次写入各自保留16位回绕。随后体力严格`<0`才写1，再重取role并令HP严格`<0`才写1；等于0保持。重复combatant role ID按slot顺序观察并累积前一槽写回。

唯一caller完成每个actor的结果/结算`sub_3B238`和隐藏目标清理`sub_3C672`，全部slot结束后恰调用本owner一次，再等待BIOS tick变化并进入下一轮。现代`BattleSession`以`round_wait`phase替代同步自旋，但正常轮末、战果结算后和全hidden轮末都在等待前提交一次状态，顺序一致。

逐块对照`BattleSetup::apply_round_status_damage`和三个Session入口未发现合法caller域产品差异。直接回归覆盖hurt绕门、poison四门、严格零边界、负hurt/poison商、两次HP word回绕、体力/HP夹1、重复role累积和非法role安全拒绝；正常Session锁定轮末HP `100→99`且tick等待期间不重复，既有战果Session锁定同步结算后hidden hurt `100→99`。独立向量SHA256为`fda21d81b051395e7b2cac5d012d0181658ca5aaf4588d6aebf337861df37eda`；Golden三生成一致、旧72键逐值不变，73键SHA256为`158e6c36bc46f9f1052934502b47a13272112310b345e360b87bd613489418c4`；Linux app Debug 14/14通过。

本owner归类`platform_adapted / converged_no_new_differences`。现代以`int16`局部保存第一次回绕值并把机器两次无观察点的连续HP store合并为最终store，后续slot观察值不变；非法role提前拒绝也属于平台安全适配。栈探测、结果/结算、隐藏目标清理、caller轮循环与BIOS tick等待均保持独立owner。
