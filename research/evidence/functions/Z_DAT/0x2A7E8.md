# 函数证据：`sub_2A7E8` `0x2A7E8..0x2A86C`

状态：`implemented_pending_review`

映射：`BattleSession::render_player_item_selection` 的上滚动箭头。

函数用color99依次绘制 `(267,72,2,1)`、`(266,73,4,1)`、`(265,74,6,1)`、`(264,75,8,1)` 与 `(266,76,4,11)` 五个实心矩形。现代battle物品菜单仅在item数大于15且page大于0时执行同组像素；16项PageDown/PageUp回归已覆盖。最终双向REVIEW尚未执行，故为 `implemented_pending_review`。
