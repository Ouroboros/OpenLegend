# 函数证据：`sub_361AC` `0x361AC..0x36209`

状态：`implemented_pending_review`

部分映射：`BattleSetup::begin_ai_request_plan`与`resume_ai_request_after_move`。

该入口处理请求医疗动作8。仅当actor combatant word6行动值严格大于0时，先同步调用`sub_3650E(actor, mode=0, value=0)`；零或负值完全跳过移动。移动返回后，或未移动时，均从全局请求目标槽`word_E6EE0`重新读取x/y写入目标坐标，再调用`sub_34C47(actor)`自动攻击。

固定typed向量：round value3返回move且movement mode/value均为0；round value0与-1直接进入automatic_attack；移动后恢复原请求target slot1坐标。handler自身不消费RNG、不写action_done，自动攻击内部RNG与外层`sub_33599`完成标志仍由后续continuation负责。

`BattleSession`对正行动值按mode0/value0实际逐格执行状态、render/present与两次BIOS tick变化，移动后调用`resume_ai_request_after_move`恢复原请求目标坐标；零行动值直接进入同一continuation。随后重新进入完整`sub_34C47`自动攻击，保留请求动作码，并在FIGHT/EFT、十帧damage、提交等待后由外层完成actor。真实battle2正行动值请求医疗向量锁定round value1、mode0移动一格、请求目标slot1恢复、攻击目标slot2、首magic整帧hash`0xcc6a249ebb919a23`、最终RNG3295386429、MP20→15、体力100→99→96及动作码8不被攻击覆盖。Linux Debug完整BUILD全部14项测试通过；本函数推进为`implemented_pending_review`，最终汇编↔C++ REVIEW仍待执行。
