# 函数证据：`sub_3A8A4` `0x3A8A4..0x3AA17`

状态：`platform_adapted`

映射：`BattleSetup::rest_actor`。

## 1. 最终机器身份

fresh临时数据库`tmp/Z_DAT-b8-order69.i64`由`/mnt/d/Dev/Crack/IDA/idat.exe -A`和单一脚本`tmp/ida_dump_b8_order69.py`导出。函数边界固定为`0x3A8A4..0x3AA17`，共371 bytes、83条连续指令、11个IDA基本块、5个条件分支、1个无条件跳转、24处绝对地址重定位、5次direct call和唯一`RETN @ 0x3AA16`。

原始文件偏移为`0x342A4`；raw SHA256为`3097774d3ce72a87fcb377ccafc49d93976447d67ad4f909412a30a44d2085ad`，IDA loaded SHA256为`70f7650bd3b925dd224e4f85a74b0ac0fcb6d866ca2efafa832c3b26cb18a7d2`。24处loaded DWORD均比raw增加`0x20000`，逐处减去该差值后完整371字节与只读`Z.DAT`一致。

三个独立机器切片为：

- 体力与阈值`0x3A8A4..0x3A93C`，152 bytes，SHA256=`713b01ed4b792d45312e58547dc4f88f3a50934a214ba7612100aea0968e4239`；
- 生命恢复`0x3A93C..0x3A9B8`，124 bytes，SHA256=`743a74c0c1561a3a97881bbdffea3401a63c54aa20d2cec0cf4962e8fe397edc`；
- 内力恢复与退出`0x3A9B8..0x3AA17`，95 bytes，SHA256=`9652d26b459a2ac27d337f94ea81d1989597b838270e3e919f816272f9ced244`。

两个入口xref分别是玩家动作8分派`sub_32E59:0x333C9`和AI休息wrapper `sub_34AD3:0x34AE3`。5次call依次为栈探测`sub_3ED1E`、体力相等路径`sub_3D612`、体力不等路径`sub_3D612`、生命`sub_3D612`、内力`sub_3D612`；中间两个调用点互斥，因此每次动作恰有一次必经`bounded(3)`。

## 2. 独立机器合同

1. signed actor参数先定位28-byte combatant并立即把word7 `action_done`写1。
2. role speed按signed word读取并以`IDIV 10`向零截断；signed round value与该商相等时，必经`bounded(3)`结果加3，否则加2。
3. 体力只执行低16位加法回绕；仅当回绕后的signed值严格大于100时夹100，不做下界夹取。
4. 更新后体力signed小于30时立即退出，不调用生命/内力两处RNG，也不写生命或内力。
5. 体力不少于30时计算signed `bound=physical_power/10-2`。机器在生命RNG前保存`physical_power/10`商，并在内力RNG前用同一商重建相同bound。
6. 生命严格先于内力：各调用一次`bounded(bound)`，结果加3后仅按低16位写回，再以signed严格`>`比较各自maximum；超过才写maximum，负回绕不被下限夹取。
7. `bound==1`时两处`sub_3D612`调用仍必经，但RNG helper返回0且不推进状态。因此调用次数与随机状态消费次数必须分开验证。
8. 正常完整路径退出时偶然EAX低字是最终MP，阈值短路路径则残留首个随机增量；玩家caller完全忽略EAX。`sub_34AD3`原样转发EAX，但其六个独立caller同样不消费。

## 3. 汇编→C++逐块对照

`BattleSetup::rest_actor`的signed `speed/10`、round相等分支、单次`bounded(3)`、`wrapping_i16`体力加法、strict `>100`夹值、signed `>=30`门、保存后的同一bound、生命后内力RNG顺序、两次低字加法与strict maximum夹值均逐块对应全部83条机器指令。现代结构化`BattleRestResult`只供caller判断成功，未把机器偶然EAX升级为产品合同。

玩家Session同步调用`rest_actor`后进入普通玩家动作完成；机器直接caller同样在清理参数后只检查actor `action_done`。AI none/wait及攻击、用毒、医疗、解毒、逃跑/物品重定位回退均经已独立关闭的`sub_34AD3`边界在统一AI尾之前休息；现代`finish_ai_handler(..., true)`保持相同状态与RNG顺序。函数内部没有render、present、音频或tick调用。

合法域完整对照零产品差异。现代对越界actor和非法role记录安全拒绝，并以optional结构返回代替无人消费的偶然EAX，归类平台适配；栈探测、RNG、玩家分派、AI wrapper与统一动作尾均保持独立owner，不传播本owner closure。

## 4. 判别性向量与验证

独立原资产Golden键`battle_rest_action_machine`锁定全部机器身份、三个切片和5个边界向量，向量SHA256为`ae98253e95a49d58796c472bda36e0a8d4d0b3f74965034e74a5cff4448c8ac8`：

- ready：seed1、speed60、round6、体力50、HP95/100、MP48/50，RNG `[2,1,0]`，结果55/99/50，终态662824084；
- tired：round5、体力25，只消费首个2并得体力29，HP/MP不变，终态1103527590；
- negative-speed threshold：speed -19向零除10为-1，与round -1相等，体力25得到30；bound1两次返回0但不推进状态，HP10→13、MP20→23；
- physical-power wrap：体力32767在不等路径低字回绕为-32765并跳过恢复；
- HP/MP wrap：体力100夹回100后，RNG `[2,6,1]`使HP32767→-32760、MP32766→-32766，signed负结果不被maximum100覆盖。

C++回归另锁定共享role别名只写当前combatant `action_done`，以及非法actor/role拒绝时RNG不推进且不提前写action-done。Golden两次临时生成和第三次正式生成逐字节一致；正式文件SHA256为`2fe0a9e0eb676fa9df0bad9c735df4935caeeca90e039d117bb22b7a8cf20a23`，原62个键逐值不变且仅新增本函数键。Linux app Debug统一构建14/14通过。

原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
