# 函数证据：`sub_3A8A4` `0x3A8A4..0x3AA17`

状态：`implemented_pending_review`

映射：`BattleSetup::rest_actor`。

函数先把actor `action_done`置1。体力固定先消费一次`bounded(3)`：若combatant round value等于signed `role.speed/10`，增加随机值+3，否则增加随机值+2；只对大于100的结果夹100，不做下界夹取。

更新后体力不少于30时，令bound为signed `physical_power/10-2`，严格依次消费两次`bounded(bound)`；第一次+3加到HP，第二次+3加到MP，分别只在严格大于maximum时封顶。bound等于1时沿用随机helper的无状态消费边界。固定speed60、round6、体力50、HP95/100、MP48/50、seed1得到RNG `[2,1,0]`、体力55、HP99、MP50；round5、体力25时只消费首个RNG并得到体力29，HP/MP不变。

独立oracle、状态单测和Linux Debug完整测试通过。最终仍需完整汇编↔C++ REVIEW。
