# 函数证据：`sub_30A5A` `0x30A5A..0x30B45`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；KDEF opcode60。

参数：`(scene,event,expected,true_offset,false_offset)`，caller PC 宽度6。

## 汇编独立合同

- scene=`-2`：读取当前 scene event record 的 `word_C569A[11*event]`，即 field5 `current_picture`。
- 其他 scene：按 ALLDEF.IDX/ALLDEF.GRP 加载指定场景 4400 bytes，读取 `word_C456A[11*event]`，仍只比较 field5 `current_picture`，随后释放临时资源。
- 等于 expected 返回 true_offset，否则 false_offset。
- 不读取/比较 end_picture 或 begin_picture。

## C++ 映射与测试

case60 把 `-2` 解析为当前 scene，读取唯一 `SceneEventField::current_picture` 并按宽度6跳转。

真实资产反例：

- script491：当前 scene52 event2，current=100、end=begin=6298 必须 false→stay；current=6298、end=begin=100 必须 true→dialogue1742。
- script990：外部 scene80 event1，current=100、end=begin=6068 必须 false→dialogue2807；current=6068 必须 true→dialogue2968。

Linux Debug `openlegend.scene` 通过。最终正向/反向逐基本块 REVIEW 尚未执行。
