# 函数证据：`sub_31C4A` `0x31C4A..0x31C75`

状态：`implemented_pending_review`

KDEF opcode67；参数 wave ID。函数依次调用 `sub_3E2E2(1,id)` 与 `sub_3E288(1,id)` 后跳回解释器默认 continuation；caller 宽度2。

C++ case67 原样追加一个同步抽象 `SceneAudioCommand::wave(argument1)` 并 PC+=2，由 audio boundary 保持底层 load/play 顺序。真实 script389 从 PC0 驱动，核对 wave22 在 dialogue1255 返回前入队；script1001 true 分支另覆盖 wave23→dialogue2914。Linux Debug scene 通过。

底层 wave 装载/播放转交 runtime-audio closure；最终双向 REVIEW 尚未执行。
