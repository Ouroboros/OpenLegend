# 函数证据：`sub_2A186` `0x2A186..0x2A74C`

状态：`implemented_pending_review`

battle context部分映射：`BattleSession::render_player_item_selection`、`BattleRenderer::draw_item_icon`。

函数在context=2时先调用 `sub_3AA85` 重绘战场，再绘制三个圆角框 `(45,2,230,23)`、`(45,27,230,23)`、`(45,52,230,145)`。5×3格固定从 `(55,62)` 开始、横纵步长42、每格40×40；每格先以color0画边框，再从MMAP按item ID绘制图标，当前格以color255重画边框。

页状态使用 `list_index=5*(page+row)+column`。当item数严格大于 `5*(page+3)` 时调用 `sub_2A74C` 画下箭头；item数大于15且page大于0时调用 `sub_2A7E8` 画上箭头。选中详情在show-introduction为1时使用secondary name和introduction；type3/4名称以 `160-4*byte_length` 居中。数量严格大于1才绘制 `X` 与 `%2d`，数量1不绘制标记。

现代battle Session已执行战场重绘、三框、MMAP图标、方向/PageUp/PageDown分页、上下箭头、名称/简介/数量及三确认键。16项、首项count2菜单整帧FNV64为 `0x68c3b70dfec20bba`；同帧count1为 `0x17b6845a718a6cf8`。`BasicUiRenderer::render_items`现覆盖世界/场景菜单背景、同一5×3网格、上下箭头、secondary name、简介及数量；`GameMenuController`共享列回绕、行/页切换、PageUp/PageDown、Escape和三确认键。Linux/Windows `core/app × Debug/Release`及Linux app ASan+UBSan全部通过；当前保持`implemented_pending_review`，最终双向逐基本块REVIEW仍为`not_started`。
