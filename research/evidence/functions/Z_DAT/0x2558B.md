# 函数证据：`sub_2558B` `0x2558B..0x258FE`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；审计包 `tmp/startup-review/0x2558B.txt`

## 1. 独立机器恢复

- 第一遍以cache-local `x=center_x-11..center_x+20`为外循环、`y=center_y-11..center_y+20`为内循环，无条件绘制32×32 EARTH。锚点为 `x=18*dx-18*dy+145`、`y=9*dx+9*dy-81`。
- 第二遍保持相同扫描和投影，仅绘制非零SURFACE。
- 随后按`word_54506`给出的depth列表顺序绘制：
  - sprite `5000`在`in_ship==0`时于`(145,117)`绘主角帧，在`in_ship==1`时于同址绘船帧，其他非零值跳过该中心节点；
  - sprite `6000`在`in_ship==1`时同样走中心船分支，否则按owner world坐标投影船帧；
  - 其他非零sprite按owner world坐标投影绘制。
- 投影基准为cache origin与`center-10`，屏幕锚点 `x=18*dx-18*dy+145`、`y=9*dx+9*dy-63`。
- 天气活动字等于1时按槽0→2固定顺序处理；仅`y>-1000`的粒子进入8级indexed半透明绘制。
- 末尾共享阴影块在状态大于0时每32 tick重算随机偏移，状态1应用`3_shadow.msk`零偏移，状态2应用`4_shadow.msk`随机偏移。

## 2. 汇编 → C++ 映射

- EARTH/SURFACE两遍扫描与投影：`world::WorldSession::render`、`render::legacy_world_tile_screen`。
- depth列表及owner投影：`render::build_legacy_world_depth_list`、`render::project_isometric`。
- `5000/6000`与raw `in_ship`分支：`WorldSession::rendered_player_frame`及`WorldSession::render`的船节点分支。
- 普通RLE覆盖：`WorldSession::draw_sprite`、`render::draw_rle_sprite`。
- 天气顺序、阈值及indexed混合：`WorldSession::draw_weather_particle`。
- 阴影原语：`render::apply_legacy_shadow_mask`。当前全部world-context调用方在调用本函数时阴影状态恒为0；非零状态只在11个特殊scene的`sub_29D2D`场景绘制路径可达，由场景closure独立终审，不在world会话复制第二份状态。

## 3. 差异与整轮重启

- 从`0x2558B`入口独立恢复后逐块核对两遍tile循环、depth三类节点、raw `in_ship`三分支、天气三槽及阴影尾块。
- 既有实现已在早期审计中把`in_ship`从bool恢复为raw 16位；值2会跳过中心玩家节点，但仍让sprite6000在world位置绘船，和机器分支一致。
- 本轮确认现代`framebuffer.clear(0)`是机器函数没有的显式操作。独立原资产oracle分别以0和255填充旧buffer后执行机器绘制顺序，64,000像素结果完全相同，SHA256均为`8b925f9bb4d8378cd9a134965e0ae95e56e85360036a38f64387e073a486bc2a`；因此当前资产域不可观察。
- 从入口重启最终轮，确认正常world调用域零新增差异。

## 4. 平台与安全适配

- 固定全局cache/depth/framebuffer改为typed span、vector与owned indexed framebuffer；扫描顺序和像素覆盖不变。
- 损坏sprite、越界owner、非法weather kind/weight改为checked failure，不复刻宿主内存越界。
- 现代显式清屏在当前原资产EARTH完整覆盖下不可观察；不据此宣称损坏资源域assembly exact。

## 5. 验证与结论

- Linux app Debug `proc_96cf`：14/14。
- 独立B6原资产oracle：初始framebuffer FNV1a64 `0x6f6cf22b7c8cb4b8`；seed1的300tick天气framebuffer FNV1a64 `0xdff4c0d05bd3426b`。
- 清屏可观察性oracle `proc_5574`：0填充与255填充结果差异像素为0。
- 回归覆盖四方向28个主角帧、连续35格cache重载、船中心/owner分支、raw `in_ship==2`、depth玩家可见性与三槽天气。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
