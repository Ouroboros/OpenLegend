# 函数证据：`sub_2558B` `0x2558B..0x258FE`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；世界整帧render。

独立汇编恢复：先按32×32、X外Y内以`x=18*dx-18*dy+145,y=9*dx+9*dy-81`绘EARTH；第二遍同坐标绘非零SURFACE；再按depth顺序绘建筑/玩家/船。sprite5000：`in_ship==0`绘陆地frame于(145,117)，`==1`绘船frame，其他非零跳过；sprite6000在`in_ship==1`时也走中心船分支，否则按world owner投影。最后依次绘y>`-1000`的三个天气粒子，再处理消息层。

现代映射：`WorldSession::render`、`project_isometric`、`draw_sprite`、depth list和天气绘制。审计把`in_ship`从bool恢复raw int16，并由`rendered_player_frame()`复用中心玩家分支；值2回归确认不产生中心帧且header不归一化。

初始/移动/天气framebuffer hash与28个角色帧可见性覆盖正常域。消息层转交B5 UI；损坏sprite返回false是安全适配。最终双向逐基本块REVIEW为`not_started`。