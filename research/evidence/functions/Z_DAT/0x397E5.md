# 函数证据：`sub_397E5` `0x397E5..0x39A45`

状态：`implemented_pending_review`

已映射状态边界：`BattleSetup::apply_poison_target`、`BattleSetup::finish_poison_action`。

函数按目标与actor的绝对x/y差定朝向；y差严格大于x差时选上0/下3，否则选左2/右1；同格不改方向。随后清4,096格effect。合法目标格若为友军则完全跳过，空格写effect1，敌方格写effect1、effect kind2、调用 `sub_39A45` 并把返回毒值写combatant damage word9。原边界判断误将x<64检查两次而未检查y<64；现代实现对y≥64安全拒绝，登记待最终REVIEW。

状态结算后固定调用magic type0/effect30动画，damage display kind2，重算全部combatant sprite；actor action_done置1、word13加1、体力减2夹0。固定向量actor `(26,24)`、target `(26,26)`：方向3，单格hash `0xab559939923b4f74`，加毒9，effect kind2；体力1→0、counter0→1。空格标记、友军不标记。同格不改方向。

`BattleSession`现于目标确认后先提交状态，再载入actor FIGHT包并执行11帧effect30，每帧render/present及一次BIOS tick变化；首帧分派attack sample7与effect sample30。随后执行10帧damage kind2，严格前4帧flash，最后重算sprite、提交action_done/word13/体力并推进下一actor。独立Session向量锁定目标poison0→7、体力100→98及完整可读日志；Linux与Windows Debug全部14项测试通过，推进为`implemented_pending_review`。
