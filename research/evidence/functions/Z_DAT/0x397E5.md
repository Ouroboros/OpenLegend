# 函数证据：`sub_397E5` `0x397E5..0x39A45`

状态：`platform_adapted`（`converged_no_new_differences`）

fresh临时数据库锁定本owner为608 bytes、148条指令、16个条件分支、6个无条件跳转、42处重定位、7次direct call、2个正常caller和1个本地RET。raw/loaded SHA256分别为`b24698b7078ab75be072d4a388f4fb3f0567bacfc5d5a08c1fb1190530e85937`与`8a812a50dbaeb92cd56dd31e9d9f88cbf1c13612744cbb91e7fa44047c1ba81e`；42个loaded dword均为raw值加`0x20000`，反归一化后608字节逐字节相同。calls依次为`sub_3ED1E(36)`、两次`sub_3F50B`、`sub_39A45`、`sub_3859E`、`sub_38910`、逐槽`sub_3B1E6`；正常caller为AI `sub_3540E:0x3551B`和玩家wrapper `sub_39776:0x397D6`，均不观察EAX。

机器先以target-actor的signed差和两次abs选择方向；同格保留，`abs(y)>abs(x)`才选垂直，否则含tie选水平，正差为右1/下3，负差为左2/上0。随后按signed双循环清4096个effect word。边界判断严格为`x>=0、x<64、y>=0、x<64`，末项错误重复x而不检查`y<64`；现代对`y>=64`安全拒绝，替代原机越出64×64数组的非法访问。合法格为空时虽机器短暂读取`combatants[-1].side`，最终仍标effect1；现代省略该不可安全读取。同方非空格完全跳过；敌方写effect1、kind2，调用独立owner `sub_39A45`并把返回低16位（含负数）写damage word。

越界、友军、空格和敌军路径均继续固定`sub_3859E(actor,0,30)`、damage kind2、`sub_38910(0)`，再对全部combatant调用`sub_3B1E6`并写回低16位sprite。共享尾写action_done1、actor word13按int16加1、角色体力按int16减2后只在signed负数时夹0，最终EAX为0。`0x399FE`另有`0x39D9E/0x3A107`外部owner进入，`0x39A3E`另有七个外部owner进入；共享尾、毒值、动画、damage、sprite、栈探测、abs和两个caller均保持独立closure。

`BattleSetup::apply_poison_target`与`finish_poison_action`逐块保留合法域方向、清图、空/友/敌效果、毒值低字、全部sprite、action_done、counter和体力回绕合同。无效actor/role/occupancy、空格负索引和upper-y越界由checked拒绝替代。`BattleSession`玩家及AI路径均先应用状态，执行11帧effect30与10帧kind2 damage（前四帧flash），最后刷新sprite和提交行动，再回各自外层caller；render/present与BIOS tick continuation拆分属于宿主时序适配。

独立原资产向量覆盖同格、四方向、tie、4096格清理、negative/high x、negative y、upper-y机器BUG、空/友/敌、负毒值低字、全部sprite、counter/体力16位回绕、非法actor和完整call顺序；向量SHA256为`30c5af17b805884d0d3f1588046fac237ae8f7403d8c12999ac1f8da8be19243`。首轮从入口覆盖全部148条指令、22个跳转、7次call、两个caller、两组共享入口和RET，合法域零产品差异；新增边界回归后第二轮完整入口审计仍零差异，无产品源码修改。Golden双生成与正式第三生成逐字节一致，SHA256为`45fd92b983c702d907797f6d45d70b47287033b2c486ab5e8545a64ae4a9619e`；Linux app Debug 14/14通过。
