# 函数证据：`sub_208A9` `0x208A9..0x2094A`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码。

独立汇编恢复：ESI `lodsb`读取source index，EDI读取destination index。两者分别经`3*index`取得当前RGB6 palette分量；source查`64*weight`表，destination查`64*(8-weight)`表。三个8位和按`red<<8 | green<<4 | blue`形成RGB4 cube索引，再查4096字节最近色表并由`stosb`写回。8位加法和分量表共同保证结果0..15；同距palette索引由建表时严格`<`保留首项。

现代映射：`WorldSession::draw_weather_particle`的source/destination palette读取、`component*weight/32`、`red*256+green*16+blue`及`rgb4_lookup_`写回。`sub_3D34A`的建表合同由同一构造器实现。

向量覆盖weight 6/7/8、palette端点、同距最近色及固定天气像素hash。无已知合法域差异；最终双向逐基本块REVIEW为`not_started`。