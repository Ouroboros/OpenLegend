# 函数证据：`sub_2F62F` `0x2F62F..0x2F6C2`

状态：`implemented_pending_review`

KDEF opcode35 `(role,slot,magic,level)`。slot==-1 时扫描十个 magic ID 槽，首个 ID==0 槽写入；无空槽则覆盖槽0。slot非-1时直接写指定槽。ID和level均直接写低16位，无 clamp/notice。

C++ case35 保留自动槽与满槽fallback。真实 script289 opcode35(36,0,60,100) 后 role36 slot0 ID60、level100；现有真实脚本均给合法显式槽。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
