# 函数证据：`sub_2E46B` `0x2E46B..0x2E536`

状态：`implemented_pending_review`

KDEF opcode19。机器码分别把两个参数写入场景坐标并 clamp 到 `0..63`，再以 `coordinate-11` 计算两个视口原点并 clamp 到 `0..36`。C++ `SceneSession::run_event(case19)`、`update_view_origin` 和 `commit_header` 保留该顺序与范围。

当前 KDEF 中 opcode19 共15处。真实 script235 的 `(14,14)` 已固定最终坐标、`(3,3)` 视口原点，并与紧随的 opcode40 共同验证；独立 B7 oracle 同步输出该向量。Linux Debug `openlegend.scene` 通过；最终双向 REVIEW 尚未执行。
