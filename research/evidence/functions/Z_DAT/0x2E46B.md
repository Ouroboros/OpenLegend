# 函数证据：`sub_2E46B` `0x2E46B..0x2E536`

状态：`assembly_exact`
最终REVIEW：`converged_no_new_differences`

## 1. 物理边界与调用者

- 入口/出口：`0x2E46B..0x2E536`，203字节，38条指令。
- loaded bytes SHA256为`eb441b79259ba6aa32de956c9810d7e941a8887dcc81ad9432402edcdbaa889f`；原始`Z.DAT`文件偏移`0x27E6B`起203字节SHA256为`eafef387d4fdbb2b4550bff9b8dc83d246fc0101c9fe7e12a683fe55f5f62ead`。
- 两份字节仅有19个单字节差异，offset为`18,28,43,52,62,70,79,91,100,110,119,129,138,148,156,165,177,186,196`；全部位于场景x/y和视口原点x/y四个绝对地址dword的第三字节，19个loaded地址均为raw值加`0x20000`。归一化后无差异。
- 唯一物理caller为`sub_2C319:0x2C67B` opcode19。caller按右到左压入y、x，调用后在公共路径把PC推进固定3 words。
- 除统一栈检查`sub_3ED1E`外没有业务callee；本函数不绘制或present场景。

## 2. 入口到出口机器行为

两个signed参数依次为`x,y`。机器先把参数低16位分别写入场景x、y全局word，再按以下次序归一化：

1. 场景x钳位到`0..63`。
2. 视口原点x取`scene_x-11`，再钳位到`0..36`。
3. 场景y钳位到`0..63`。
4. 视口原点y取`scene_y-11`，再钳位到`0..36`。
5. 返回0。

所有比较都是signed 16位语义，因此输入`-32768`落到0，输入`32767`落到63；位置63对应视口原点36。函数不修改朝向、动画帧、场景图层或事件状态。

## 3. 当前原资产域

独立Python按68槽真实宽度扫描全部1,018条KDEF：

- opcode19共15次，完整`<IIhh:script_id,pc,x,y>`参数流SHA256为`59063796bd864eb36610866d9bdca4e490bde722bb3bee802fa481bced4c2d0b`。
- 首条为`(script30,PC127,x35,y31)`，末条为`(script664,PC30,x38,y18)`。
- x输入范围14..38，y输入范围14..49；全部当前资产坐标已在`0..63`，但y=49会覆盖视口原点上限36。
- 真实script235在PC30使用`(14,14)`，得到场景坐标`(14,14)`和视口原点`(3,3)`；紧随opcode40设置朝向3，最终玩家帧5044。
- synthetic极值分别固定`(-32768,32767)→位置(0,63)/视口(0,36)`和`(32767,-32768)→位置(63,0)/视口(36,0)`。

## 4. 汇编→C++收敛

暂不参考现有C++完成上述机器推导后，对照`SceneSession::run_event(case19)`、`update_view_origin`和`commit_header`：

- case19以相同参数顺序分别用`std::clamp(...,0,63)`写`scene_x_`、`scene_y_`。
- `update_view_origin`对两个已钳位坐标执行`clamp(position-11,0,36)`。
- `commit_header`把最终位置同步到快照header，对应原机四个场景状态word；朝向保持既有值。
- PC固定推进3；不产生host输出、重绘或额外事件边界。

现代中间赋值次序先得到两个钳位位置再计算两个视口原点，而机器完成x及其原点后才处理y；期间没有调用、输出或可重入观察点，所有signed输入的最终四word完全一致。最终复核零新增差异或未决项，归类`assembly_exact`。

## 5. 验证

- headless IDA从正式数据库副本导出38条指令、唯一caller及完整上下文；正式IDB和原资产保持只读。
- 独立oracle覆盖15次opcode19、完整参数流、全部派生位置/视口、script235和两组signed极值；双次生成逐字节一致（`proc_9c56`），oracle SHA256为`818bede2e3ed85d023274f61166f9c517e7bc83344a53baf28f281130d4da6d4`。
- Linux app Debug根BUILD脚本14/14通过（`proc_2c94`）；order43静态门、全局closure计数及reverse framework 577行有效/scene-event剩55项通过（`proc_553b`）。
- 动态原程序差分仍按统一策略记为`blocked_runtime_oracle`，不替代静态机器码与独立资产oracle。
