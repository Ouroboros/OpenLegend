# 函数证据：`sub_28E40` `0x28E40..0x29391`

状态：`implemented_pending_review`

独立机器码推导确认该函数拥有完整场景会话循环。入口依次加载场景状态、推进一次 `sub_29B3C` 事件图片、重绘、淡入、调用 `sub_2C0BB` 场景标题、再次重绘呈现并检查脚下自动事件。稳定循环每 tick 先调用事件图片推进，再按左、上、下、右、交互、主菜单的固定优先级最多消费一个动作；动作完成后无条件重绘呈现，随后才执行五 tick 周期更新、自动事件、出口格和内部跳转检查。出口与内部跳转都先淡出；内部跳转加载目标后重新执行淡入、标题、裸场景呈现和自动事件，出口则返回唯一调用者。

现代 `SceneSession::tick/finish_tick_after_action/finish_tick_after_auto_event/complete_scene_jump` 用显式 continuation 保留这些同步边界；`LegacyGameRuntime` 聚合同 tick 请求并只按原优先级消费一个动作，菜单返回后继续未完成 tick。独立 oracle 固定入口、循环和跳转序列、首20 tick 的 `[1,6,11,16]` 周期点、84条 metadata 的10处内部跳转和252个出口格。Linux Debug BUILD 覆盖动作后独立 `present`、自动事件先于出口、内部跳转淡出/淡入/标题链以及 app 同 tick 方向优先级。

唯一调用者 `sub_25911` 在调用前保存世界方向；返回后按 `[3,2,1,0]` 反转方向、清移动状态并恢复对应世界玩家图。现代 app 已在 `handle_scene_result(return_world)` 保留该反向映射。最终双向逐基本块 REVIEW 仍未执行。
