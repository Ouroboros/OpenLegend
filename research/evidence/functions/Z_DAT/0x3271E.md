# 函数证据：`sub_3271E` `0x3271E..0x32A51`

状态：`platform_adapted / converged_no_new_differences`。

order8补导出的战场绘图读取分支确认，`E6EE2/E6EE4`对应`secondary_cursor`，不是`primary_cursor`；`556F0`为菜单次光标可见标记。C++已修正三个actor定位写点且保留继承的`primary_cursor`，菜单入口/确认/返回的可见生命周期亦按机器接线。本页随后从入口、全部166条指令及共享返回重新审计，旧错误关闭结论不再使用。

映射：`BattleSetup::prepare_round`；`BattleSession`的初始呈现、回合、actor、战果/结算及返回continuation；`LegacyGameRuntime`与SDL确认键状态运输。

## 1. 机器identity与出口

当前Z.DAT原始偏移`0x2C11E`，819字节、166条指令、27个跳转、69项绝对地址重定位；全部加载差为`+0x20000`。

- raw SHA256：`d8e0befe7bfaa838fb81780210ec2814c6f3898b005fc6f6342e4a64906997dc`。
- loaded SHA256：`03fb8619f4d161ca82d1c6b26f20c3b08f56b129855f65af27771a8fe24328b3`。
- 唯一caller：`0x31D39`，位于战斗入口。
- 13个直接调用依次为栈检查、排序、绘制、present、淡入、排序、绘制、present、玩家菜单、AI、胜负检查、隐藏目标清理、轮末异常状态；对应`3ED1E,32A51,3AA85,3D6D1,3CD17,32A51,3AA85,3D6D1,32E59,33599,3B238,3C672,3C563`。
- `0x327DD`条件出口跳到共享`0x35407..0x3540E`，字节`83c4045f5e5bc3`：释放4字节局部、恢复EDI/ESI/EBX、返回。不能把物理末尾的回边当作返回或漏审共享尾。

## 2. 从汇编独立恢复

1. 局部循环条件置1，结果word清0，首次排序。slot0的word2/3写次光标坐标，分别减11、按signed word钳位0..32后写视图原点；绘制、present、淡入。
2. 外层先比较循环条件，再读取并保存BIOS tick。每轮只排序一次，随后仅清`E6EE6/E6EDA/E6EBC`，分别为effect帧偏移、effect可见、highlight模式；不清路径范围。
3. 对所有slot计算word6：基础速度加两件非负ID装备的item word53，每次加法16位回绕；有效速度signed `/15`减hurt signed `/40`，两次除法向零取整，结果写word后负值钳0。hidden槽也参与排序和计算。
4. 每槽入口先采样`51B7A/51B8D/51C03`，相对键态基址`51B6D`为`0x0D/0x20/0x96`（Enter/Space/Keypad Insert）。任一非0则清全部三键并清automatic，再检查hidden word5；不是永久锁存keydown，也不是在present后采样。
5. 可见槽更新次光标和视图、绘制present，然后清word7/10；仅side==0且automatic==0调用玩家菜单，其余调用AI。返回AX==6时索引减1，抵消公共循环尾的递增。hidden槽不调用动作，但仍进入公共尾。
6. 公共尾先调用`sub_3B238`，再调用`sub_3C672`；结果word>=1时清循环条件并令actor循环结束。仍调用一次`sub_3C563`，在不重绘画面的情况下等待当前tick与轮首保存值不同，再从外层条件走共享返回；无结果则开始下一轮。

### 必须保持的callee同步边界

`sub_3B238 @ 0x3B238..0x3B387`的结果>0分支在内部调用战场绘制、战果面板/文字、`sub_3D6D1`、`sub_20C32`及`sub_3B387`。因此完整顺序是：**战果呈现→任意键→战后结算及全部消息→返回主循环→隐藏目标清理→轮末异常状态→tick等待→返回战斗入口**。不能先扣轮末状态再显示战果，也不能在最后一条结算消息完成时直接返回场景。这里只审计callee的调用/返回边界，不传播其奖励、升级、伤害等内部closure。

## 3. 历史映射与重开事项

- `prepare_round()`只计算word6，排序由轮首显式调用；保存tick、排序、三项清零、word6的先后与机器一致。
- 轮首和后续actor均从hidden检查开始；首槽hidden不被错误呈现/分派。机器要求更新次光标和相机，不额外清路径范围。三个定位点现均写`secondary_cursor`，路径选择/效果使用的`primary_cursor`保持继承值；战场读取分支与独立菜单像素oracle共同验证该所有权。
- Session仅镜像三确认键是否有非零当前状态，逐槽入口消费并发出清键请求；runtime转运，SDL在make/break、会话推进和present continuation之间同步，并清原键盘模型的全部三键。玩家菜单确认在其原清键点消费，选择自动不会由同一确认再次取消；槽入口之后才到达、又在下一槽前释放的键不取消自动。
- 胜负结果先进入战果与战后消息continuation；只有全部结算返回后才清隐藏目标、执行轮末状态并进入`round_wait`。等待不重绘，比较的仍为轮首tick，不要求相对最后动作再经过一个全新tick。tick不等后才发布typed Victory/Defeat供入口淡出与资源回收。
- 首轮的“稳定冒泡”推断被完整SI/BX循环否定，相关产品改动已撤回；跨槽排序见`0x32A51.md`。中间“先轮末后战果”的判断亦被完整callee序列否定并修正。原战后像素hash未从现代输出重新生成或改写。

现代事件采样、present回调和受检状态对象替代DOS IRQ、自旋及裸全局，归类`platform_adapted`。当前合法参战者/坐标/装备域内的位宽、顺序和状态副作用保持机器合同；没有借本行关闭输入专属同址owner或其他callee。

## 4. 最终验证

- `proc_bb3d`从当前原始Z.DAT与完整机器导出重跑identity、69项`+0x20000`重定位、唯一caller、13个直接调用和共享返回强断言，全部通过。
- 独立`battle_round_machine`与更新后的菜单/次光标golden均从原资产生成两次并逐字节一致；正式`battle-goldens.json` SHA256为`aa606cc4949dc4c5da8e39edf42ad9e9c32dfa9aec85aa43b6a4fecdc12dfc22`。
- 定向回归覆盖：每轮一次排序、word6不排序、非稳定等速案例、装备16位回绕、13-word交换、同格后写覆盖、首槽hidden、确认键保持/释放/清状态、三项清零与其他绘图状态保留、三个actor次光标写点、原主光标继承、菜单可见生命周期、战果后结算再轮末、hidden目标延后清理、一次状态伤害、同tick等待、BIOS日界回绕及等待期间像素保持。
- 最终联合静态/golden门`proc_84ad`、closure机械门`proc_daeb`及reverse framework `proc_99c7`通过；最新Linux `./build.sh app --config Debug`由`proc_ec73`完成，14/14、0失败。Windows矩阵留在B8完整模块关闭时执行。
- 原程序动态差分：`blocked_runtime_oracle`。

从入口、全部基本块、共享状态、全部出口、present/宿主接线重新对照后零新增差异；本owner最终归类`platform_adapted / converged_no_new_differences`。同址input-font owner、排序/交换、玩家菜单及各callee仍按各自closure独立关闭，不由本行传播。
