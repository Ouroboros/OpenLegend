# 函数证据：`sub_3271E` `0x3271E..0x32A51`

状态：`pending_implementation`

部分映射：`BattleSetup::prepare_round` 与 `BattleSetup::evaluate_outcome`。

## 主循环顺序

1. 结果 word清0；调用 `sub_32A51` 排序；以 slot0 坐标为视图中心，两个 origin 分别取 `clamp(coord-11,0,32)`；绘制、present、fade。
2. 每个外层 tick 保存 BIOS tick，重新排序，清三个 battle globals。
3. 顺序为每个 slot 计算 word6：`max(0, int16(effective_speed)/15 - role.hurt/40)`；division 是 signed truncation toward zero。
4. 顺序遍历 slot。任意确认 flag 命中时清三个 flag并清自动标志；word5非0的槽跳过行动分派。
5. 活跃槽先更新视图中心、绘制并 present，再清 word7与word10。side word非0或自动标志非0调用 `sub_33599`，否则调用玩家菜单 `sub_32E59`；菜单返回6时当前 slot index减1，使循环末尾递增后仍指向同槽。
6. 每槽行动后调用 `sub_3B238` 胜负检查与 `sub_3C672`；结果 word>=1时结束本轮和外层循环。
7. 一轮后调用 `sub_3C563`，忙等到 BIOS tick 与轮首值不同，再开始下一轮。

`BattleSession`现已在建队完成后执行首次稳定速度排序，以slot0坐标计算两个`clamp(coord-11,0,32)`视图原点，实际绘制并通过`finish_presented_tick`逐帧执行黑场淡入；随后按外层轮顺序再次排序、调用`prepare_round`计算word6，更新当前actor视图，实际呈现后进入player或AI动作边界。真实battle2初始视图为`(19,13)`，选择确认后的战场像素独立oracle与C++一致。

玩家等待/休息动作后，Session现按原顺序执行胜负检查、26槽隐藏目标清理，保持等待动作`--slot`抵消循环末递增的效果，或在普通完成动作后推进下一槽；隐藏槽逐个跳过且仍执行胜负/清理，下一actor重新居中并实际进入present。每个actor present后按原顺序清word7/word10。最后一槽完成后调用轮末异常状态，并比较本轮开始时捕获的BIOS tick；仅tick发生变化才排序并开始下一轮。自动动作保留重绘/present后才置flag并进入同actor AI相位。其余玩家动作、非休息AI handler、结果画面及战后提交仍未接入，因此本函数继续保持 `pending_implementation`。
