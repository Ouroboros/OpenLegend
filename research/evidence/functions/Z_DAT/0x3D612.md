# 函数证据：`sub_3D612` `0x3D612..0x3D643`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；bounded RNG包装。

独立汇编恢复：upper_bound `<=1`或`>30000`直接返回0且不调用底层RNG；其余调用一次`sub_3F98D`，以有符号`idiv upper`返回余数。底层当前实现产生0..32767，合法正上界因此余数0..upper-1。

现代映射：`LegacyRandom::bounded`完全相同的上界门禁与一次`next()%upper_bound`；`next`为`state=state*0x41C64E6D+0x3039`后取`(state>>16)&0x7fff`。

向量覆盖-1/0/1/2/30000/30001、状态不消费和固定seed序列；`legacy_runtime_test.cpp`固定`-1/1/30001`均为0、随后`2`的首个随机结果仍为0、`30000`为5758。weather/idle/UI及scene opcode51/58/65共享同一owner以保持跨模块消费顺序。

最终REVIEW仍需反查全部调用者及委托`sub_3F98D`/stack-guard边界；双向逐基本块状态为`not_started`。
