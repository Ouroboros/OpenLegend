# 函数证据：`sub_3D612` `0x3D612..0x3D643`

状态：`assembly_exact`

来源：当前 `Z.DAT` 机器码；`research/ida/reports/Z_DAT.b6_world_xrefs.txt`。

## 1. 独立机器恢复

- 入口先以参数 `4` 调用编译器栈检查 `sub_3ED1E`。
- `upper_bound` 按32位有符号数比较；`<=1`或`>30000`时直接清 `EDX/EAX` 并返回0，不调用底层RNG。
- 合法上界只调用一次 `sub_3F98D`。返回值复制到 `EDX`，以 `SAR EDX,31`形成有符号被除数高半部，再由 `IDIV upper_bound`取余数并返回 `EDX`。
- `sub_3F98D`的结果域为`0..32767`，所以合法正上界的余数为`0..upper_bound-1`；本函数没有重试、偏移或额外状态写入。

## 2. 汇编 → C++ 映射

- `random::LegacyRandom::bounded(std::int32_t)`以相同有符号边界执行`<=1 || >30000`提前返回，提前出口不调用`next()`。
- 合法路径只调用一次`next()`并执行正数 `% upper_bound`；`next()`已按`sub_3F98D`实现32位LCG回绕和15位结果，因此与机器的有符号`IDIV`余数一致。
- Watcom栈检查属于编译器运行时边界，现代C++由宿主ABI承担，不产生游戏可观察状态。

## 3. 收敛 REVIEW

本轮先仅依据入口机器指令独立记录栈检查、两次有符号比较、两个提前出口、唯一RNG调用、`IDIV`和返回寄存器，随后才对照现代实现与既有向量。逐基本块未发现新差异；委托的`sub_3F98D`状态更新与结果位宽已在RNG核心证据中终审。world、scene和runtime三张closure中的同址行统一传播为`assembly_exact`。

## 4. 验证与结论

- 固定向量覆盖`-1/0/1/2/30000/30001`、非法上界不消费状态、合法上界单次消费及固定seed序列。
- Linux app Debug：14/14通过，受管进程`proc_9d5b`。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
