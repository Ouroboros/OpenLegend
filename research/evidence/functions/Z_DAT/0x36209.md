# 函数证据：`sub_36209` `0x36209..0x36210`

状态：`pending_implementation`

部分映射：`BattleSetup::begin_ai_request_plan`与`resume_ai_request_after_move`。

函数仅执行`push 0x10`后跳到`sub_361AC+5`，与请求医疗handler共享完整函数体；该入口对应请求解毒动作9。其严格行动值门槛、`sub_3650E(actor,0,0)`移动参数、移动后恢复请求目标坐标、随后调用`sub_34C47`自动攻击及不自行写action_done的行为完全相同。

现代以`BattleAiRequestPlan::request_action`区分request_detox与request_medicine，同时共享相同同步步骤。`BattleSession`现实际执行mode0/value0逐格状态、render/present和两次BIOS tick变化，并在移动后恢复请求目标及automatic_attack计划；自动攻击和外层完成仍未接线，故保持部分实现。Linux/Windows Debug完整BUILD、全部测试、独立oracle与逆向框架门禁通过。
