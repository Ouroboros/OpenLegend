# 函数证据：`sub_36209` `0x36209..0x36210`

状态：`platform_adapted / converged_no_new_differences`

映射：请求解毒分派到共享的`BattleSetup::begin_ai_request_plan`、`resume_ai_request_after_move`与`BattleSession`请求continuation。

## 机器身份与wrapper owner

当前原始及IDA loaded字节完全相同：`68 10 00 00 00 EB A1`。本owner严格为7字节、2条连续指令、1个short无条件跳转、0处fixup、0次local call、0个local RET；raw/loaded SHA256均为`39c017dd2b811b83553060d2eafb40e83c07e19d3e6685c6382075fffa133f09`。`0x3620E`的signed短位移精确解析到`0x361B1`。

唯一caller为`sub_33599:0x33C19`动作9请求解毒case。caller在`0x33C15`将actor按signed低16位扩展，在`0x33C18`压栈，调用本owner后于`0x33C1E`跳到共享`0x33BB6`清理一个actor参数，不读取最终EAX；最终`0x33C3F`写该actor combatant word13 `action_done=1`。

## 完整汇编合同与栈等价

第一条指令压入立即数16，与`sub_361AC:0x361AC`的栈探测准备完全相同；第二条指令跳过对方的同一push，直接进入`sub_361AC+5 @ 0x361B1`的`sub_3ED1E`调用。因wrapper由CALL进入，actor及原返回地址仍位于相同栈位置；共享主体末尾`0x36208`的RET直接返回`sub_33599:0x33C1E`，栈形状与从`sub_361AC`正常入口执行一致。

委托主体`0x361B1..0x36209`已由order32独立冻结：signed actor行动值word6严格大于0时调用`sub_3650E(actor,mode0,value0)`并忽略EAX，零或负值跳过；两路汇合后重读signed请求目标槽及当前x/y坐标，无条件调用`sub_34C47(actor)`完整自动攻击，再RET。攻击EAX最终由`sub_33599`忽略。7字节owner自身没有call、状态写入或RNG消费。

本closure只关闭`sub_36209`两指令wrapper及其跳入/返回合同，不复制或改变已独立关闭的`sub_361AC`主体，也不传播到栈探测、移动或自动攻击callee。

## 汇编→C++ REVIEW

机器合同冻结后读取现代实现。AI dispatcher把动作9映射为`BattleAiHandler::request_detox`，但与动作8一样进入共享`begin_ai_request_plan`、实际移动、`resume_ai_request_after_move`和automatic attack路径；`BattleAiRequestPlan::request_action`保持动作9身份。

正signed行动值产生mode0/value0移动，零或负值跳过；resume按目标槽重读当前x/y，随后无条件进入完整自动攻击；外层在handler完成后写action_done。该共享typed分派正是两指令尾跳的结构化等价物，且不会把自动攻击的局部动作覆盖回combatant请求动作9。

首轮wrapper入口审计未发现产品差异。测试/golden修改后重新从`0x36209`覆盖两条指令、short位移、唯一caller、0 fixup、0 local call/RET、共享栈形状及最终caller返回，仍为零新增差异。现代共享typed主体对非法actor、请求动作或目标安全拒绝，替代原版委托主体的线性越界，归类平台适配。

## 回归、golden与结论

聚焦动作9请求解毒回归覆盖round value2的mode0/value0移动计划、目标坐标改变后resume重读、round value0和-1跳移动并进入自动攻击、request_action保持9、action_done仍由外层处理，以及非法目标和actor安全拒绝；既有选择测试锁定动作9分派到`request_detox`handler，共享完整Session链继续覆盖移动、render/present/tick、自动攻击及outer completion。

正式battle golden加入7字节wrapper机器身份、caller、short-jump目标、零fixup/call/RET及独立closure边界，并复用原始资产推导的正/零/负请求向量；两次独立生成逐字节一致，SHA256为`4849e78fac9f2808920b8d20adf386c20e6dc229c7754e15e28d074eaeacb0b5`。根BUILD `proc_321e`完成Linux app Debug 14/14，battle、reverse framework及其余组合测试全部通过。

本owner归类`platform_adapted / converged_no_new_differences`；原程序动态差分仍登记为`blocked_runtime_oracle`。
