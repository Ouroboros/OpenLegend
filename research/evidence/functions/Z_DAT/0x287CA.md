# 函数证据：`sub_287CA` `0x287CA..0x28975`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；`tmp/name-review/0x287CA.txt`；当前 `CFONT`、`FONT.X16`、`FONT.C16`

## 1. 独立机器恢复

- 参数是1-based候选起点和inclusive终点。入口清 `(0,160,320,40)`，以颜色 `0x1719` 在 `(3,161)` 重绘当前输入模式。
- 循环按 `candidate_base + 2*(index-1)` 读取两字节，用格式 `"%d%c%c"`产生页内编号1..8；坐标严格为 `(30*page_local_index,180)`，颜色 `0x1719`。
- 起点为1且终点小于总数时只在 `(300,180)`画后箭头；起点不为1且终点小于总数时在 `(270,180)`画“前/后”三字符；起点不为1且到达总数时在 `(300,180)`画前箭头。单页无箭头。
- 函数最后present，不修改候选内容。主循环为第一页错误地同时置前后可用flag，因此Shift+comma可传入起点-7、终点0并读取候选起点前16字节。

## 2. 汇编 → C++ 映射

- 有符号页码、可见索引和原始负索引读取：`NewGameNameEditor::candidate_page/visible_candidate`。
- 前后页条件：`has_previous_candidate_page/has_next_candidate_page`。
- 模式提示、页内编号、30-pixel步长、箭头和颜色：`BasicUiRenderer::render_name_entry`。

## 3. 差异、重启与平台适配

- 旧实现使用 `(4+38*i,181)`、默认色且无箭头；修正后废弃该轮并从入口重审清区、模式提示、循环上下界、三种箭头分支和present出口，零新增差异。
- 当前CFONT负页八组原始字节为 `7ef3 b7f3 eaf4 62f8 bcd5 5ad6 68ef ab1a`。原字体例程对其中非法/不完整Big5会越界访问，现代保留可稳定的编号、ASCII、合法Big5与分页BUG，非法字形确定性跳过；继续向CFONT头部前翻时执行边界停止。

## 4. 验证与结论

- 独立oracle：正常第一页 `b2e1b2b2b13b316c`；有符号负第一页及安全字形适配 `2ed4a5f661fa48a1`。
- UT覆盖20候选、首屏Shift+comma、负页Space回0、页内数字0/负/正选择语义。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
