# 函数证据：`sub_30B45` `0x30B45..0x30B81`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；KDEF opcode61。

参数：`(true_offset,false_offset)`，caller PC 宽度3。

## 汇编独立合同

```text
all = 1
for event=11; event<25; ++event:
  if current_scene.events[event].current_picture != 4664:
    all = 0
return all==1 ? true_offset : false_offset
```

严格检查 event11..24 共14项，仅 field5 current_picture；不短路（机器循环仍读完全部14项），不读取 begin/end。

## C++ 映射与测试

case61 按同范围累积 `all = all && current_picture==4664`，再按宽度3跳转。

真实 script1001（item_context=144）测试：

- event11 由脚本先改为4664，event12 current=100 而 begin/end=4664，其余 current=4664：opcode61 false→stay。
- event11..24 current 全4664，begin/end 均100：true→wave23→dialogue2914。

Linux Debug `openlegend.scene` 通过。最终正向/反向逐基本块 REVIEW 尚未执行。
