# 函数证据：`sub_39E88` `0x39E88..0x39EF7`

状态：`pending_implementation`

已映射状态边界：`BattleSetup::medicine_targeting_range`。

函数把actor role `medicine/15+1`（signed除法向零截断）作为 `sub_36AF7` 的选择射程，target mode固定1。目标选择out flag为1时返回-1；否则调用 `sub_39EF7` 并返回0。

固定medicine80得到射程6。`BattleSession`现按该射程实际进入targeting mode1，逐键执行四方向/Escape/三确认键并在每轮重画/present；取消返回同一菜单ordinal，确认保存目标坐标。`sub_39EF7`状态、动画与结束continuation尚未接入，因此保持`pending_implementation`。Linux与Windows Debug完整BUILD、全部14项测试、逆向validator及真实battle4 Session回归通过。
