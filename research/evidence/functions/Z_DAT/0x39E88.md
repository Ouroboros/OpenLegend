# 函数证据：`sub_39E88` `0x39E88..0x39EF7`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::medicine_targeting_range`与`BattleSession`玩家医疗目标选择wrapper。

## 机器身份、入口与owner边界

- fresh临时IDB导出完整`111` bytes、`36`条连续指令、`3`个基本块、`1`个条件分支、`0`个无条件跳转、`2`处32位重定位、`3`次direct call、`1`个caller和`2`个本地RET。
- 原文件偏移`0x33888`；raw SHA256为`9bf7f964882ca59aca5e0a35a7a330d6d803317aba46495820b099320452a480`，加载重定位后SHA256为`61ad50f584df27e27ed8fc8ba6b349a300b2aa9d44a60e6aeb4318d6ec0e29f8`。fixup位于函数偏移`0x20/0x2D`，加载值均比原文件大`0x20000`，逐处逆变换后完整111字节与只读`Z.DAT`一致。
- 三个call依次为`0x39E8D -> sub_3ED1E`的28-byte栈探测、`0x39ECE -> sub_36AF7`同步目标光标、`0x39EE8 -> sub_39EF7`医疗动作。
- 唯一caller为`sub_32E59:0x3339B`动作菜单case4；caller无条件跳到`0x333DB`公共参数回收，随后从actor重新计算战斗员地址并检查action-done，因此完全忽略wrapper的EAX。
- `sub_36AF7`光标循环已由B8 order38独立关闭；`sub_39EF7`状态/动画/提交属于B8 order65；栈探测和caller同样不传播本行closure。

## 完整机器合同

1. 入口把四字节local初始化为0，并把actor参数低16位sign-extend。以`actor*28`读取战斗员signed role word，再以`role*182+168`读取signed `medicine` word。
2. `IDIV 15`按signed向零截断，商加1后执行`CWDE`。完整signed skill域结果为`trunc(medicine/15)+1`，范围`-2183..2185`；正常`0..100`域为`1..7`。
3. 参数按`actor, range, mode=1, &local`传给`sub_36AF7`。本体直接RNG消费为0。
4. selector返回后仅比较local低16位是否**恰等于1**：等于1从`0x39EE6`返回EAX=-1，不调用医疗动作；所有其他低字值进入确认边，调用`sub_39EF7(actor)`，忽略callee返回并从`0x39EF6`返回EAX=0。
5. 正常`sub_36AF7`入口先把取消word清0，Escape写1，合法确认保持0；所以合法域只有上述取消/确认两条出口。异常的其他local值不属于同步callee正常合同。

## 汇编到现代实现逐块对照

- `BattleSetup::medicine_targeting_range`逐项保持actor→role→`medicine`、signed C++向零除15、加1与16位结果；`role_word::medicine=46`对应182-byte角色记录内byte offset92，和机器`word_901A8[role*182]`一致。
- `BattleSession::begin_player_targeting`只在玩家动作case4调用该射程，并固定进入`BattleCursorSelectionMode::targeting`；signed射程不做额外夹值。
- Escape产生cancelled，Session清path/cursor后经`finish_player_action_call`回到同一菜单ordinal；action-done仍为0，与唯一机器caller忽略-1后检查word7的路径一致。
- 确认产生selected并立即把当前cursor传入`begin_player_target_effect(medicine)`；其首个战斗状态调用为`apply_medicine_target`，对应机器立即调用`sub_39EF7`。现代将同步callee拆成可呈现continuation；`sub_39EF7`内部状态、RNG、动画、音频与action-done必须在order65独立审计。
- 从入口逐块覆盖全部36条指令、3个基本块、唯一分支、两个RET、三次call与唯一caller，完整合法域REVIEW未发现产品差异。现代对非法actor或role安全返回`nullopt`，替代机器越界读取且无RNG副作用，归类平台适配。

## 判别性回归与验证

- 新增signed射程向量：`-32768→-2183`、`-15→0`、`-14/0/14→1`、`15→2`、`89→6`、`90→7`、`32767→2185`；非法actor与负role安全拒绝。
- 既有真实battle4 Session回归锁定medicine30进入mode1选择，入口present门和Escape回原菜单ordinal2；完整玩家support effect回归锁定确认后立即进入医疗状态，继而执行10帧magic、10帧无flash damage、双bank音频与动作完成。
- 正式Golden由两次独立原资产生成逐字节一致后更新，再以第三次生成核对；formula vector SHA256为`3ebada112102eafaa27d00f9fc365f7dc2c4cb185609a0f764c158480149d039`，正式文件SHA256为`ef5c52444a0c3a0cd6643d8ffd4a42fa0955f53e4f48fee47e76b47b6721429a`，历史57键逐值不变。
- 正式证据与Golden闭包后，根`./build.sh app --config Debug`精确通过`100% tests passed, 0 tests failed out of 14`。
- 最终归类：`platform_adapted / converged_no_new_differences`。
