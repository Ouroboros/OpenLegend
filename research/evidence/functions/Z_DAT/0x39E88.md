# 函数证据：`sub_39E88` `0x39E88..0x39EF7`

状态：`pending_implementation`

已映射状态边界：`BattleSetup::medicine_targeting_range`。

函数把actor role `medicine/15+1`（signed除法向零截断）作为 `sub_36AF7` 的选择射程，target mode固定1。目标选择out flag为1时返回-1；否则调用 `sub_39EF7` 并返回0。

固定medicine80得到射程6。公式已由Linux Debug 14/14验证；`sub_36AF7`目标光标、input/render/present continuation及对 `sub_39EF7` 的同步接线尚未实现，因此保持 `pending_implementation`。
