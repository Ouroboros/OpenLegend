# 函数证据：`sub_39E88` `0x39E88..0x39EF7`

状态：`implemented_pending_review`

已映射状态边界：`BattleSetup::medicine_targeting_range`。

函数把actor role `medicine/15+1`（signed除法向零截断）作为 `sub_36AF7` 的选择射程，target mode固定1。目标选择out flag为1时返回-1；否则调用 `sub_39EF7` 并返回0。

固定medicine80得到射程6。`BattleSession`现按该射程实际进入targeting mode1，逐键执行四方向/Escape/三确认键并在每轮重画/present；取消返回同一菜单ordinal，确认保存目标坐标并立即进入`sub_39EF7`状态、动画与结束continuation。真实battle4同格确认后完成10帧effect0、10帧无flash damage、双bank音效与下一actor推进；Linux与Windows Debug完整BUILD、全部14项测试及逆向validator通过，因此推进为`implemented_pending_review`。
