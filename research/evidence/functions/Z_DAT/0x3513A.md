# 函数证据：`sub_3513A` `0x3513A..0x351A7`

状态：`platform_adapted / converged_no_new_differences`

映射：`openlegend::battle::BattleSetup::choose_ai_strongest_attack_target`。

## 1. 机器身份与调用边界

- 范围`0x3513A..0x351A7`，raw file offset `0x2EB3A`，共109字节、32条指令、5个显式跳转、7处加载时fixup。
- raw SHA256：`ab91c4c972a5a17b4db00ceea5a803d7d0e5f6d949c4bccd0b37c97fe7dbcb0c`。
- loaded SHA256：`786b1e5e58c1c76dae493c53978e0f2568c230ec67c48fd9a9a55a4c140188ea`。
- 七个fixup位于函数内偏移`0x26,0x2D,0x36,0x40,0x4D,0x5B,0x63`，每个加载值相对raw精确增加`0x20000`；归一化后109字节逐字节等于原始`Z.DAT`切片。
- 唯一caller为`sub_3505B`在`0x35098`的最高攻击策略分支；actor以32位栈参数传入，槽寻址使用signed低16位。caller调用后无条件置“策略已选择”标记，不检查EAX或目标是否写入。
- 唯一direct call为入口栈检查`sub_3ED1E`；本函数不消费RNG，也不调用其他业务callee。

## 2. 完整机器合同

1. 入口将actor保存到EDI；EBX为槽循环计数器且清零，ECX为当前best且清零。
2. 循环尾以`cmp bx, word_556D2`和`jl`实现signed 16位`slot < combatant_count`，正常battle域按槽序扫描全部combatant。
3. 每个候选先读取signed side，与actor side相等时跳过；再读取隐藏/占用word，任意非零值均跳过。机器流没有HP、死亡或额外self过滤。
4. 剩余候选按signed role id乘182字节定位角色记录，读取signed attack。`cmp ax,cx`后以`jle`跳过，因此仅`candidate_attack > best`更新。
5. best初值0，所以attack等于0或为负均不写目标；严格大于使相同attack保留最早槽。
6. 更新时把candidate attack写入best低16位，并把candidate槽写入actor word11；更新不提前退出，继续扫描后续槽。
7. 无合格正攻击目标时保留调用前word11不变。函数无独立布尔返回合同，唯一caller亦不消费EAX；typed写入结果仅为现代宿主元数据。

## 3. 汇编→C++逐块对照

- `choose_ai_strongest_attack_target`按`slot=0..combatant_count-1`顺序遍历，并以`side == actor_side || occupancy_hidden != 0`跳过，与两个机器分支完全一致。
- 角色attack由`int16_t`读取，`best_attack`为signed `int16_t`且初值0；C++严格`attack > best_attack`后立即写actor `ai_target`并继续循环，保持非正值不写和first-tie。
- 固定边界回归：两个attack均50且早槽HP为0时仍选slot3；slot4 attack100但hidden为`-1`时跳过并选attack30的slot3；attack `[0,-1]`时返回未写并保留stale slot4；同side默认attack10不会参与。
- 现代对候选负role id或越界role id返回`nullopt`，替代原版线性越界读取；正常可达输入未发现行为差异，因此归类`platform_adapted`。
- 差异审计后从入口重新覆盖全部32条指令、5个跳转、7个fixup与唯一caller，零新增差异。

## 4. 验证与结论

- 独立机器审计：`proc_5ec7`，109/32/5/7身份、唯一caller和唯一direct call全部通过。
- 原始只读资产双生成：`proc_6ddf`与`proc_cf0a`逐字节一致；正式battle golden SHA256为`bfe4f2a9d4e7dcbef19679d7cff681c5bf91fa61810104f2b6c63919adf40e6b`。
- Linux app Debug根BUILD：`proc_5a0f`，14/14通过，包含新增battle边界回归与reverse framework。
- 本owner结论：`platform_adapted / converged_no_new_differences`。`sub_351A7/sub_35217/sub_35372`继续保持各自closure状态，不由本函数传播关闭。
