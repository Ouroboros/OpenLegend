# 函数证据：`sub_3513A` `0x3513A..0x351A7`

状态：`implemented_pending_review`

映射：`BattleSetup::choose_ai_strongest_attack_target`。

按combatant槽序扫描不同side且未隐藏目标，以signed best=0起步；仅attack严格大于best时更新actor target word11，因此同值保留早槽，所有attack<=0时完全不写目标。固定攻击`[30,50]`选择slot4；`[0,0]`保持-1且顶层不回退。Linux Debug 14/14和独立oracle通过；最终仍需完整汇编↔C++ REVIEW。
