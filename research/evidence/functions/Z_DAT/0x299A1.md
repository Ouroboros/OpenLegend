# 函数证据：`sub_299A1` `0x299A1..0x29B3C`

状态：`implemented_pending_review`

机器码实现垂直脚本单步。入口唯一显式参数为 signed step；先清玩家临时图片，frame offset 加2且大于12回到2。step `>0` 写 direction0，step `<=0` 写 direction3，再将目标 y=`current_y+step` clamp 到 `[0,63]`。仅当 building=0、target height-current height `<10`、事件为空或 cannot_walk=0、earth 不落九组阻挡范围时更新 y；成功后视口 y 按 `y-11` clamp `[0,36]`。阻挡时仍保留朝向和步行动画推进。

`sub_2F171` 调用者确认目标 y 墑减时分别实传 `+1/-1`，因此这里的朝向不能套普通键盘 delta：真实 script343 从 `(28,24)` 走到 `(28,19)` 应保持 direction3、图片5046..5054，结束后基础图5044。现代 `apply_scripted_walk_step/target_is_walkable/update_view_origin/player_frame` 已按该顺序修正；独立 oracle 与逐帧 framebuffer hash 覆盖正常及阻挡路径。最终双向 REVIEW 尚未执行。
