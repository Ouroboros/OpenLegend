# 函数证据：`sub_30C3D` `0x30C3D..0x3123C`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；审计包 `tmp/b9-review/0x30C3D.txt`

## 1. 独立机器恢复

- 入口设置20项职员表初始Y：`210,386,551,698,950,1111,1255,1391,1557,1718,1772,1938,2087,2195,2335,2479,2642,2743,3055,3301`。
- 读取`ENDCOL.COL`到活动RGB6调色板，保留当前scene像素并64级淡黑；载入`ENDWORD.GRP/IDX`，清屏，在`(94,90)`绘legacy id0，65级淡入、present、`sub_3DB83(2000)`停留，再64级淡黑。
- 清屏，在`(44,210)`与`(44,313)`绘legacy id2/4，present并65级淡入。之后当第二项Y`>-130`时每帧清屏、按当前Y重绘两项、present、两项Y各减1、`sub_3DB83(100)`；严格产生443帧，位置从`(210,313)`到`(-232,-129)`。
- 滚动后清屏并执行64级淡黑，打开`KEND.GRP`；若全局IDX cache不是10则读取`KEND.IDX`并设cache=10。按IDX端点顺序读取0..220共221个恰为64000字节的raw indexed帧，每帧整帧覆盖并present；仅frame0后执行65级淡入。frame220后同步等待任意非零键，再64级淡黑并关闭KEND句柄。
- 清屏，先在X=60、各自初始Y绘legacy id6/8/10/12，present并65级淡入。随后当最后一项Y`>57`时，每帧清屏、绘20项偶数legacy id6..44、present、全部Y各减1、`sub_3DB83(100)`；严格产生3244帧，最后一项Y从3301到58。
- 20项X固定为前四项60，中间十三项115，legacy id40/42/44分别105/135/56。最终同步等待任意非零键、64级淡黑，然后尾调用`sub_31241`，本函数不返回。

## 2. 汇编 → C++ 映射

- ENDCOL、ENDWORD与KEND读取/验证：`SceneSession::load_ending_assets`、`load_ending_frames`。
- 偶数legacy ID `/2`与RLE绘制：`draw_ending_word`、`render::legacy_sprite_index`；raw KEND整帧：`set_ending_frame`。
- scene像素保留后换ENDCOL palette：`start_ending`先`render_map(ending_framebuffer_)`再设置`ending_palette_`。
- 标题、双字、KEND、职员表、两次等待及最终退出：`EndingState`十三阶段与`advance_ending`。
- fade/present/delay/key wait：`SceneStepKind::fade_to_black/fade_from_black/present/wait_key`及app `SceneEffectKind`；`legacy_delay_ticks(2000)=51`、`legacy_delay_ticks(100)=3`。
- `sub_31241` noreturn边界：`SceneStepKind::quit`的`ending_complete=true`，交由`LegacyGameRuntime::handle_scene_result`和SDL RAII shutdown。

## 3. 逐基本块复核

- 从`0x30C3D`入口独立读取后，逐块覆盖palette/资源顺序、初始标题、443帧条件与递减时点、KEND IDX cache及221次读取、首次frame淡入、两次任意键等待、20项X/Y表、3244帧条件与递减时点、最终淡黑和noreturn尾调用。
- 现代实现的每个阶段均有唯一机器来源；循环边界、第一帧重复绘制、frame0专用淡入、按键等待位置与资源切换顺序一致。本轮零差异，不需要实现修正或重启。

## 4. 平台适配

- DOS同步长循环与BIOS tick忙等改为SDL逐帧continuation；帧数、像素、palette相位、逻辑停留tick和输入门禁保留。
- 机器在KEND后显式关闭句柄；现代archive由session RAII持有到结束，文件内容已全量载入且后续无可观察文件操作。
- DOS尾调用关闭平台并进程退出改为typed quit后统一RAII shutdown；结局完成标志、最终淡黑和退出顺序保留。

## 5. 验证与结论

- Linux app Debug `proc_c33b`：14/14。
- 独立B7 oracle与`scene_test`共同固定：标题`0x20420db943fd8def`；443帧双字滚动6个选点；KEND frame0/1/220 `0xa2186a3321f0153a / 0x68bf029a91b5d73c / 0x42c2240f8d7700c7`；3244帧职员表7个选点；两次独立`wait_key`及最终`quit(ending_complete=true)`。
- 真实资产合同：ENDCOL 768字节；ENDWORD至少23帧；KEND 221帧且每帧64000字节。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
