# 函数证据：`sub_31C75` `0x31C75..0x31DA0`

状态：`implemented_pending_review`

映射：`openlegend::app::LegacyGameRuntime::start_battle`、`openlegend::battle::BattleSession`、`openlegend::app::LegacyGameRuntime::finish_battle_if_ready`。

当前正式`research/ida/reports/Z_DAT.targets.txt`与只在`tmp/Z_DAT.postbattle-review.i64`上生成的`tmp/Z_DAT.entry-result.txt`逐指令一致证明入口顺序：保存get-exp/battle id，写battle mode=2，加载battle定义与WARFLD、清occupancy、初始化参战者并完成固定/预置/手选建队；仅在手选确认后加载WDX/WMP与EFT，使用跨战保留的battle render globals重画并present首帧，随后按WAR word8调用music，再进入`sub_3271E`执行首次排序、重定位、fade与主循环。返回后清battle资源，重新加载当前scene SMP/SDX，按scene metadata word7恢复music，负值分支传0，最后写mode=1并返回`raw_result-1`。

`sub_3B238`仅产生raw result 1失败或2胜利，因此本函数只返回0失败或1胜利；没有第三种battle级Escape结果。AI逃跑属于`sub_33599`动作11，完成移动/休息后战斗继续。

现代runtime按同一所有权边界建立并持有`BattleSession`，保留上一场结束时的完整`BattleRenderState`供下一场首帧使用；队伍选择完成后才加载战斗专属WDX/WMP/EFT，首帧实际present后先排入battle music，再由Session执行首次排序、重定位、fade、玩家/AI回合、战果与战后逐消息同步链。完成后runtime保存最终battle render globals，先销毁battle资源并按scene `entrance_music`恢复音乐，再把typed胜利/失败送回scene。Linux Debug完整BUILD 14/14；当前仍为实现差异审计，B0–B9最终完整汇编↔C++双向REVIEW尚未开始。
