# 函数证据：`sub_31C75` `0x31C75..0x31DA0`

状态：`platform_adapted / converged_no_new_differences`

目标所有者：`scene` 的 battle-entry 生命周期边界。映射：`openlegend::app::LegacyGameRuntime::start_battle/begin_battle_transition_if_ready/advance_scene_effect/finish_presented_tick/finish_battle_if_ready/complete_battle_after_fade`、`openlegend::battle::BattleSession::prepare_initial_fade_to_black/finish_initial_fade_to_black`、`openlegend::battle::BattleRenderer::load_battlefield_assets/load_effect_assets`。

## 机器身份与入口合同

当前 `Z.DAT` 函数严格为299字节、72条指令，raw偏移`0x2B675`。raw SHA256为`8f3e6d11d5d85a9c7882511546962fdd2793c5c3a11c8d580fc62bbec2e113b6`，loaded SHA256为`634a5e34a676f54ebc829e443e2bafcced1d3117dbf73eaa0a71485d802bdb64`；24项绝对地址差异均为`raw+0x20000` DOS relocation，归一化后逐字节一致。直接IDAT批处理输出为`tmp/order98-machine-function.txt`，独立解析审计为`tmp/order98-audit.json`。

两个且仅两个直接caller已由`tmp/order98-machine-callers.txt`复核：

- `sub_2DE03`传入事件opcode6的有符号battle id与get-exp，只把返回值恰等于1映射到真偏移，其余值映射到假偏移；
- `sub_30480`传入`battle=102+cell`与`get_exp=0`，同样只把返回值恰等于1视为胜利，其余值进入死亡菜单。

入口保存get-exp、写运行模式2并保存battle id；尾部把运行模式写回1，返回有符号`word_E6ED2-1`。结果word的完整终局写域只有raw 1失败与raw 2胜利，故可观察返回只有0/1。AI动作11的逃跑仍是战斗内动作，不是第三种入口返回值。

## 完整调用与呈现顺序

14个直接调用严格为：栈探测，载入WAR记录/战场，建立瞬时战斗状态，建立/选择队伍，打开WDX/WMP，入口淡出，打开EFT，重画战场，present首帧，按WAR word8启动战斗音乐，进入战斗主循环，出口淡出，恢复当前场景SMP/SDX，按场景metadata word7恢复音乐（机器值-1改传0）。

入口与出口两次`sub_3CC97`均是64帧淡至黑色。队伍选择结束后必须先打开WDX/WMP，再在进入战斗前的场景framebuffer上执行入口淡出；EFT只能在该淡出完成后打开。首个战斗framebuffer以黑色palette实际present后才能发出战斗音乐。

`sub_3271E`入口的直接机器序列为首次排序、从当前actor坐标计算并钳位视图、`sub_3AA85`重画、`sub_3D6D1`present，再调用`sub_3CD17`。后者依次present原palette减64至减1的64帧，并最终present原palette，共65帧；因此首帧音乐之后的delegated初始序列严格为1个排序后的黑色present加65帧fade，共66次present。直接IDAT证据为`tmp/order98-machine-fade-sequence.txt`。

主循环返回后必须保留最终战斗framebuffer和战斗Session完成64帧出口淡出，之后才释放战斗资源、恢复场景音乐并把typed结果送回事件解释器。

## 现代运输与资产域

现代runtime以协作状态机运输同步机器调用：入口/出口淡出由宿主持有，以冻结正确的scene或battle像素并统一屏蔽输入；`BattleRenderer`把WDX/WMP与EFT加载拆成机器同序的两个阶段；`BattleSession`在宿主交接完成后才进入首帧present，并在音乐之后提供1+65帧delegated初始序列。恢复场景时复用仍由runtime持有的`SceneSession`与已加载场景资源，代替机器关闭并重开SMP/SDX，合法场景的像素、音乐命令、脚本结果与顺序等价，因此归类`platform_adapted`。

全量`WAR.STA`为140条×186字节，SHA256为`98e3f66912c5ba4a0be00aaeff3462eb8c99f4d591d92a754930070dde9649b6`。battlefield id域为0..25，全部对应WDX/WMP文件存在；音乐id仅5/6/7，计数49/65/26。独立scene golden记录机器身份、caller、调用顺序、返回编码、帧数及完整140场资产域；正式文件由同一只读资产独立生成两次并逐字节比较后覆盖，SHA256为`1095242e7cfeb92d3b8619cf0663054013400987e450dc9a662fff90276e24b8`。

真实runtime集成测试从scene70的script691 opcode6发出battle4，而非直接调用私有入口；覆盖65帧场景淡入后的自动battle请求、冻结场景像素的64帧入口淡出、黑色首帧、音乐发出点、1+65帧初始序列、真实胜利结算、冻结最终战斗像素的64帧出口淡出，以及场景音乐恢复。全部直接构造`BattleSession`的测试夹具显式执行新的宿主淡出交接。Linux app Debug根构建14/14通过（`proc_79fc`）；原程序动态差分仍登记为`blocked_runtime_oracle`。

本项仅关闭`scene-event-closure.tsv`中`target_owner=scene`的battle-entry合同；`sub_31DA0`、`sub_31EB9`、`sub_3265C`、`sub_3271E`、`sub_3AA85`及同址B8 owner仍按`battle-closure.tsv`各自终审，不传播closure。完整入口从机器首条指令重审后无新增差异。
