# 函数证据：`sub_2E659` `0x2E659..0x2EB49`

状态：`platform_adapted`；最终 REVIEW：`converged_no_new_differences`

来源：当前 `Z.DAT` 机器码与 `Z.DAT` 原始文件字节；`research/ida/reports/Z_DAT.b7_scene_xrefs.txt`；headless IDA 完整物理范围导出；当前 `KDEF.IDX/GRP`、`DEAD.BIG`、`MMAP.COL`、`FONT.X16`、`FONT.C16`、`RANGER.DAT`。

## 1. 机器身份与调用边界

- 物理范围固定为 `0x2E659..0x2EB49`，1264 bytes；IDA 识别330条代码指令。
- `0x2EA28..0x2EA30` 另有8字节 `83 c4 04 e9 0e fe ff ff`，是 `sub_3EE5F` 退出调用后的不可达 `add esp,4; jmp 0x2E83E`；完整物理范围审计包含该区域，不能用IDA code-head拼接代替顺序字节。
- loaded SHA256 `e9350b3775deca2ee7ec049f92d20f411f1f1a1a6c3b33f493bcaaec85ca9826`；原始文件 `Z.DAT[0x28059:0x28549]` SHA256 `832743bec15c92ed76da1680bce82e80485330dba141da442ba5634fb5b936d5`。
- loaded/raw共79个差异字节，组成79个32位绝对地址重定位；每项均满足 loaded=`raw+0x20000`，未解释差异为零。
- 两个物理caller：`sub_2C319:0x2C5D8` 的opcode15 dispatch把指令后第一个word原样传入；`sub_30480:0x30500` 的五轮试炼任一非胜利出口固定传入83。callee不读取该参数，且当前KDEF的全部opcode15参数也均为83。
- 旧证据“当前KDEF没有opcode15”错误。当前1,018条KDEF共114次opcode15，参数全部为83；首尾为 `(script20,PC138,83)`、`(script1015,PC691,83)`，按little-endian `<IIh` 编码的完整位置流SHA256为 `1dfc4c31f6ed8cedaa09a0f99ad21dc6f8a3e7fc091ba356b41c2fb5ec289d9d`。
- 三个读档分支最终跳入与 `sub_2D678` 共用的 `0x2D838` epilogue：`xor eax,eax; add esp,38h; pop edi; pop esi; pop ebx; ret`。这不是调用载入菜单helper。

## 2. 不看C++的完整汇编恢复

入口依次执行：

1. 复制四个11字节菜单文字（载入进度一/二/三、离开游戏）。
2. 取得本地年、月、日并按 `"  %4d/%2d/%2d  "` 格式化。
3. 读取恰好64000字节的 `DEAD.BIG` 作为320×200 indexed framebuffer，不切换当前palette。
4. 在 `(97,46)` 画RANGER role0姓名，在 `(190,8)` 画日期，并在 `(190,28/48/68)` 画三行固定Big5文字。
5. 以source palette index0和目标像素分别除8后相加，转RGB4并查4096项最近色表，按圆角条带暗化 `(205,90,101,90)` 菜单区域；边线写index255。
6. 画四项未选文字，再以选中色重画selection0；present后fade from black。

选择循环固定保持selection `0..3`：

- 每轮先画四项未选文字，再重画选中项并present；
- Down `0x98`：`(selection+1)%4`；Up `0x9E`：`(selection+3)%4`；
- Enter `0x0D`、Space `0x20`、keypad Enter `0x96`进入当前项；其他键不改变状态；
- selection0..2分别写 `word_D2954=0`、`word_544E0=0`、`word_D2950=selection`，然后清屏、present，并从共享epilogue返回；
- selection3先在 `(71,180,177,20)` 叠加退出确认框，在 `(75,182)` 画确认文字，present后等待一个键；
- 只有uppercase `Y` (`0x59`) 调用输入/视频shutdown并 `exit(0)`；包括lowercase `y` 在内的任意其他键均重新读取 `DEAD.BIG`、重建姓名/日期/固定文字/菜单底图，并回到selection3；
- load分支的黑帧present后不再等待按键；quit分支在确认帧后等待一次按键。

## 3. 独立资产与像素oracle

`research/tools/generate_b7_scene_goldens.py` 独立解析真实资产并固定日期 `1996/1/1`：

- `DEAD.BIG`：64000 bytes，SHA256 `c2fea09d332a38e5e84ebeb0d7054508021185a6ae8f3130ce8a87b2d7050caa`；
- `MMAP.COL` SHA256 `534ac556653edae875a34321baa9d0f63291291f101d346ef1ae75ab25e78f3a`；
- `FONT.X16` SHA256 `d821d564c90dd6b7dc78ba0cdf6c7052066a5311ffc4b6e308bc0b60af20ecec`；
- `FONT.C16` SHA256 `8d395791f14395b18f8afd98204856e3a836a1b4497de41e4bb701de2fc19c40`；
- role0十字节姓名字段为 `ae7da470ab4c00000000`；
- selection0..3 framebuffer FNV-1a64：`0x9da84526f6317a4a`、`0x5f72cf3141ce8b24`、`0xb2db9b6f5ea184de`、`0x11f91fd0e5becceb`；
- 退出确认帧：`0x4ba394e637cc051e`；读档前全黑帧：`0xdd14fcc6528cab25`；
- oracle同时固化三行固定文字、四项菜单与确认文字原字节、全部坐标、packed color拆分、两个panel矩形、按键表、wrap、0-based槽号、全局写入和输出时序；
- 首次与第二次生成逐字节一致，正式 `scene-goldens.json` SHA256为 `7319439b2b7242f6bdec4ed1d21f8f0106684c04f0f7f6f82dbd2113a96e4e79`。

## 4. 汇编→C++单向REVIEW

映射如下：

```text
DEAD.BIG/current palette             -> death_image_/death_framebuffer_/rgb4_lookup_
DOS/local date                       -> current_local_date + SceneDate测试注入
name/date/fixed captions/menu bytes  -> render_death_menu + legacy font renderer
RGB6/RGB4 dark blend                 -> blend_death_rectangle
sub_2CEBF rounded panel shape        -> draw_death_panel
selection/input/confirmation loop    -> DeathMenuState + advance_death_menu
slot globals + clear/present/return  -> present continuation + SceneStepKind::load_slot
outer load consumer                  -> LegacyGameRuntime PendingIo::load
shutdown + exit(0)                   -> SceneStepKind::quit + host event-loop shutdown
```

逐基本块从 `0x2E659` 重做到共享 `0x2D838` epilogue后，合法输入上的文字、坐标、颜色、混色、selection、按键、全局槽号、清屏/present/fade与退出顺序均未发现C++产品差异。现代实现每次重建最终death framebuffer，而机器在部分选择轮只覆盖菜单文字；两者在下一次present前没有可观察中间帧，像素oracle相同。

## 5. C++→汇编反向核对

对 `SceneSession::start_death_menu`、`advance_death_menu`、`render_death_menu` 及 `LegacyGameRuntime` battle/load/quit消费逐项反查：

- opcode15在PC增加2 words后进入菜单；114个真实资产occurrence均使用同一dispatch分支；
- script190 `PC10` 的真实opcode15经battle28失败进入fade/menu，独立覆盖解释器caller；
- script936五轮试炼battle104失败覆盖 `sub_30480` 直接caller；
- selection3用Space确认，lowercase `y`返回selection3，keypad Enter重新确认，uppercase `Y`才退出；
- selection2按Enter后先返回黑帧present，再产生0-based load slot2；载入失败的cancel resume恢复selection2及同一像素；
- runtime把battle victory/defeat、load cancel与quit同步送回同一scene session，没有提前执行事件后续PC。

反向核对未找到无机器来源的合法域业务分支。

## 6. 平台适配边界与委托callee

归类 `platform_adapted`，不是 `assembly_exact`：

- DOS日期查询、文件读取、输入/video shutdown和 `exit(0)` 由现代宿主API与step/result边界替代；
- 原机读档通过全局槽变量回到外层，现代通过 `SceneStepKind::load_slot` 与 `PendingIo::load`；
- 现代对缺失/错误 `DEAD.BIG` 返回稳定错误而非继续使用未定义缓冲区；
- 合法资产、合法键与成功I/O域内的可观察顺序和结果保持一致。

本closure只关闭 `scene-event-closure.tsv` 中 `sub_2E659` 的死亡菜单caller合同；同址 `input-font-closure.tsv audit_order=25` 的输入职责仍独立待审，因此本次不传播关闭且 `unique_all` 不增加。对callee也不传播关闭：`sub_2CEBF`、`sub_20C32`、`sub_24C8D`、`sub_3CF19` 已在各自owner收口；`sub_3CD17`、`sub_3CF45`、`sub_3D6D1` 仍按UI inventory独立待审；字体、清屏、日期格式化、present与shutdown helper继续服从各自target-owner证据。

## 7. 验证与结论

- Linux app Debug根构建：14/14 tests通过（`proc_4ce9`）。
- scene测试覆盖真实script190 opcode15、script936五轮试炼失败、四项方向wrap、Enter/Space/keypad Enter、非Y/lowercase y、uppercase Y、slot2黑帧与载入失败回收。
- 完整物理范围重定位、114次KDEF位置流、DEAD/font/palette/name/文字/几何/像素和runtime结果边界均有独立静态或真实资产证据。
- 原程序动态差分：`blocked_runtime_oracle`。

```text
closure_status = platform_adapted
final_review = converged_no_new_differences
remaining =
```
