# 函数证据：`sub_39B8E` `0x39B8E..0x39DA3`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::apply_detox_target`、`BattleSetup::finish_detox_action`、`BattleSetup::finish_poison_action`与`BattleSession`玩家/AI解毒效果continuation。

## 机器身份与边界

fresh临时数据库`tmp/Z_DAT-b8-order62.i64`仅由`/mnt/d/Dev/Crack/IDA/idat.exe -A`配合`tmp/ida_dump_b8_order62.py`分析。owned范围固定为533 bytes、132条连续指令、15个条件分支、7个无条件跳转、35处绝对数据fixup、7次direct call、2个入口xref且没有本地RET。raw SHA256为`9e6023ca65b3c68c1ff802ae357a070e6663cfe09d690e933abf95ff66f3d952`，IDA loaded SHA256为`78fdc57cddbb7d096e0de2173ca9cb6e98d4f0687411ea22538783e1fbf2718e`；35处DWORD均严格增加`0x20000`，逐处反变换后与原始533字节完全一致。

fixup相对偏移为`0x01D,0x024,0x03B,0x042,0x06C,0x073,0x080,0x08B,0x09C,0x0A3,0x0B0,0x0BB,0x0D6,0x0ED,0x0FB,0x109,0x117,0x125,0x12F,0x137,0x147,0x14E,0x15C,0x166,0x16E,0x178,0x187,0x195,0x1A9,0x1B3,0x1BB,0x1C5,0x1DC,0x202,0x20A`。

两个入口xref分别是AI action4 `sub_363AC:0x3642C`和玩家wrapper `sub_39B1F:0x39B7F`；没有外部xref进入内部基本块。7次调用依次为`sub_3ED1E`、两次`sub_3F50B`、`sub_39DA3`、`sub_3859E`、`sub_38910`和循环中的`sub_3B1E6`。`0x39D9E`尾跳到owned范围外的`0x399FE`用毒共享行动提交尾，再经`0x39A3E`公共epilogue返回；这些callee、两名caller及外部共享尾均按owner独立关闭，不随本owner传播closure。

## 完整汇编合同

入口将参数作为signed低16位actor槽，经28-byte combatant记录读取actor signed x/y；目标全局x/y同样按signed读取。32位计算delta后先后调用两次绝对值helper。同格不改方向；否则严格`abs(y)>abs(x)`选纵向，相等选横向，正delta选右1/下3，非正选左2/上0。

随后两个signed `0..63`循环在任何目标检查前清零全部4,096个effect word。原边界顺序为`x>=0`、`x<64`、`y>=0`、再次`x<64`，因此遗漏`y<64`。通过后按`y*64+x`读取signed occupancy槽，并在判断-1前先按该槽读取combatant side：空格因此短暂读取`combatants[-1].side`。非空敌方不标记；空格与友方都写effect1。空格不调用解毒kernel；友方读取actor/target role id，调用`sub_39DA3(actor,target,actor_role,target_role)`，返回EAX低字写目标combatant damage word9。

所有分支——包括越界拒绝、敌方和空格——随后都调用`sub_3859E(actor,magic_type=0,effect=36)`，写damage kind3，并以低字1调用`sub_38910`抑制flash。接着signed槽循环`0..<combatant_count`逐项调用`sub_3B1E6`，将返回低字写sprite。

最后跳`0x399FE`：actor action-done置1，attack counter执行signed16回绕加1，actor role physical power执行signed16回绕减2，仅当回绕结果为负才夹0；公共epilogue返回EAX=0。本owner自身无直接RNG调用；仅友方目标委托的解毒值kernel可能消费RNG。

## 汇编→C++ REVIEW

从`0x39B8E`入口重新覆盖全部132条指令和22条分支/跳转，对照`BattleSetup::apply_detox_target`、`finish_detox_action`、`finish_poison_action`及玩家/AI `BattleSession`效果链。合法actor/坐标上的signed delta、绝对值比较、同格保持、纵向strict greater、横向tie、方向号和完整4096格先清零逐项一致。空格、友方、敌方的最终effect、kernel调用次数、damage word和effect kind一致；`sub_39DA3`不修改坐标/occupancy，因此机器在调用后重读target槽与现代保留已解析槽等价。

Session在状态应用后固定创建effect36/type0动画，再执行damage kind3且全部抑制flash；零命中结果也不跳过该链。伤害动画结束后，`finish_detox_action`先经同一sprite helper合同刷新全部槽，再委托已经独立关闭的用毒共享行动提交。玩家随后推进下一actor，AI进入其独立finish handler。玩家wrapper清理参数后覆盖EAX为0；AI wrapper尾跳epilogue透传EAX，但其唯一action4 caller已独立证明不读取EAX而以action-done决定完成。

合法域首轮完整入口REVIEW零产品差异，无需修改产品源码。

## 平台适配与回归

原版重复检查x而接受`y>=64`，并会线性读取64×64网格外数据；现代完整坐标验证安全拒绝。原版空格先读`combatants[-1].side`，但两种比较结果最终都只产生effect1且不调用kernel；现代避开该无效读取。现代同时拒绝非法actor、非法非空occupancy槽和非法role id，替代原版signed越数组访问。以上仅影响机器不安全域，归类`platform_adapted`。

独立向量覆盖同格保持、四方向、纵向strict与横向tie、空/友/敌分派、4096格清零、固定effect36/damage3/suppress1、全sprite写回、正常体力夹0、counter回绕及physical-power减法回绕；错误域另锁定负/高x、负y、`y=64`漏检和空格负槽读取。向量SHA256为`1a876ce56e8bd4e7d96e3c66b147165a5227518aebbc56a29a6e136c8f143cd9`。

C++回归增加同格及四方向、两类tie、空/友/敌、x/y边界含`y=64`、非法actor/occupancy、无kernel路径不消费RNG、全部sprite刷新和共享尾正常/回绕结算。既有完整玩家与AI Session向量继续锁定状态先提交、9帧magic、10帧damage、音频与完成continuation。正式Golden键`battle_detox_action_machine`由只读原始资产三次独立生成逐字节一致，SHA256为`1d77356fefa8e722b6682fa129f9515cb14caea72accece5a0dc2b3405632afe`，历史55个键逐值不变。

原程序动态差分继续登记为`blocked_runtime_oracle`；本owner结论为`platform_adapted / converged_no_new_differences`。
