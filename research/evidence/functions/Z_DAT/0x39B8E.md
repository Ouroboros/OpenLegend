# 函数证据：`sub_39B8E` `0x39B8E..0x39DA3`

状态：`pending_implementation`

已映射状态边界：`BattleSetup::apply_detox_target`、`BattleSetup::finish_detox_action`。

函数按目标与actor的绝对x/y差定朝向；y差严格大于x差时选上0/下3，否则选左2/右1；同格不改方向。随后清4,096格effect。合法目标格若为敌方则完全跳过，空格写effect1，友方格写effect1、effect kind3、调用 `sub_39DA3` 并把返回解毒值写combatant damage word9。原边界判断误将x<64检查两次而未检查y<64；现代实现对y≥64安全拒绝，登记待最终REVIEW。

状态结算固定调用magic type0/effect36动画，damage display kind3并抑制flash，重算全部combatant sprite，然后跳入与用毒相同的共享尾部：actor action_done置1、word13加1、体力减2夹0。固定向量actor `(26,24)`、友方target `(26,26)`：方向3，单格hash `0xab559939923b4f74`，解毒26，effect kind3；体力1→0、counter0→1。空格标记、敌方不标记。状态前后边界已由Linux Debug 14/14验证；target globals、动画/render/present/wait continuation未接入，故保持 `pending_implementation`。
