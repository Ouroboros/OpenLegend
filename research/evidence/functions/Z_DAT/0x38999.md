# 函数证据：`sub_38999` `0x38999..0x38DAC`

状态：`implemented_pending_review`

映射：`BattleSetup::apply_line_attack_area`。

方向值0/1/2/3严格对应上/右/左/下。函数从距离1循环到武功select distance，越过64×64边界的格子跳过但不终止；友军占位格完全跳过，空格写effect=1，敌方格写effect=1、设置effect kind1、无条件调用`sub_39188` HP伤害并把返回值写combatant word9。命中敌方或友军都不截断后续距离；该函数不读取magic hurt_type。方向不在0..3时switch default直接返回，不改effect、RNG或角色状态。

固定向量使用actor `(26,24)`、方向3、range2、敌方 `(26,26)`：中间空格与敌方格hash `0xae7c1e4e161ac125`，damage29。将中间格设为友军后，该格跳过但仍命中第二格，hash `0xab559939923b4f74`；非法方向hash `0xb9d103fd6854a325`且seed1不消费。hurt_type固定为1仍执行HP伤害。Linux Debug 14/14。现代实现对坏occupancy安全拒绝；最终仍需完整汇编↔C++ REVIEW。
