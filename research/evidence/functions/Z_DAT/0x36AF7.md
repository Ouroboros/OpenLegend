# 函数证据：`sub_36AF7` `0x36AF7..0x36E06`

状态：`pending_implementation`

部分映射：`BattleSetup::begin_cursor_selection`与`apply_cursor_selection`。

真实四个栈参数依次为actor slot、path上限、mode和取消word地址。`0x36B0C..0x36B5A`清确认键状态，把actor坐标同时写入视图中心与主光标；mode 0在`0x36B60..0x36B65`建立movement图，mode 1在`0x36B6C..0x36B72`建立targeting图。随后取消word清0、path上限写入`word_556EE`，并在每次读取输入前严格执行战场重画与present。

方向优先级为下、右、左、上。每个方向先读取相邻格path值：不大于path上限时允许光标移动；大于上限时仅当同一线性格occupancy不等于-1仍允许悬停。该条件使movement光标可移到被占格，但其path值仍为阻挡值，不能确认；targeting图不以occupancy阻挡，因此可确认范围内参战者。Escape在`0x36D42..0x36D5D`把path上限清0、循环结束并写取消word=1。确认键在`0x36D67..0x36E01`先要求当前path不大于上限；mode 0还严格要求path大于0，禁止选择actor原格，mode 1允许path等于0。

现代typed状态保留选择期路径图、actor/source/cursor、path上限、mode、取消/确认结果，以及每轮render/present要求；线性索引兼容`y*64+x`别名，超出4096-word存储的真越界安全拒绝登记最终REVIEW。battle4测试覆盖movement光标从`(26,24)`经`(26,25)`悬停占用格`(26,26)`但拒绝确认、回到`(26,25)`确认，targeting range1确认占用格，以及Escape清零上限。

movement mode0现已由`BattleSession`实际接入四方向翻译键、Escape、三确认键及每轮路径范围/主光标战场重画和present；取消与确认均按机器分支清路径显示上限。battle4回归覆盖初始`(26,24)`、下移`(26,25)`、确认、取消及关键日志。targeting mode1仍未接入玩家攻击/用毒/解毒/医疗/暗器流程，因此函数保持`pending_implementation`；状态核心继续由独立oracle与真实资产测试覆盖。
