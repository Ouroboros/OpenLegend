# 函数证据：`sub_36AF7` `0x36AF7..0x36E06`

状态：`platform_adapted`

映射：`BattleSetup::begin_cursor_selection`、`apply_cursor_selection`与`BattleSession` movement/targeting选择相位。

## 机器身份、入口与owner边界

- 独立临时IDB导出完整`783` bytes、`157`条指令、`35`个分支、`72`处32位重定位和`7`次direct call。
- 原文件偏移`0x304F7`；raw SHA256为`880246a653a60137ff63b21d437f2722ffbfb14bb4a0dfa6f4b06d64d34215e9`，加载重定位后SHA256为`cb0b440727ac96038276f0de9a9b139c820184b9dba7a5772b45c4b4800ed9de`。全部72处fixup加载值均比原文件大`0x20000`，逐处逆变换后完整783字节与只读`Z.DAT`一致。
- 六个正常caller依次为`sub_36A98:0x36AC3`玩家移动、`sub_37734:0x37951`玩家攻击、`sub_39776:0x397BC`玩家用毒、`sub_39B1F:0x39B65`玩家解毒、`sub_39E88:0x39ECE`玩家医疗及`sub_3A30B:0x3A35A`玩家暗器。
- 四个栈参数依次为signed-low-word actor slot、signed-low-word path上限、signed-low-word mode和取消word地址。直接call仅覆盖栈探测、`sub_36E06/sub_36E7F`路径图及两组`sub_3AA85/sub_3D6D1`战场重画/present；直接RNG消费为0。
- 本函数没有独立RET。`0x36BA7`在循环sentinel不等于1时跳到order36内部的共享`0x3677E`，只执行pop EDI、pop ESI、pop EBX、RET。该共享尾、两个路径图callee及两个render/present callee保持各自owner，不传播本行closure。

## 完整机器合同

1. `0x36B0C..0x36B5A`清三个确认键状态，把actor signed坐标同时写入source/view和live cursor两组全局word。
2. `0x36B60..0x36B77`仅识别mode0与mode1：mode0调用`sub_36E06`建立movement图，mode1调用`sub_36E7F`建立targeting图；随后把取消word清0并发布signed path上限。
3. `0x36B86..0x36BC2`先在循环建立前执行一次战场重画与present，再在首轮输入扫描前执行第二次；此后所有循环回边均严格先重画/present一次，再扫描输入。
4. 输入优先级固定为下、右、左、上、Escape、三个确认键。每个方向清自身两种键状态，按signed `y*64+x`线性下标读取相邻word；方向分别使用`+64/+1/-1/-64`，没有独立x/y边界判断。
5. 相邻signed path不大于signed上限时允许移动；超出上限时，仅当同一线性别名occupancy不等于`-1`仍允许悬停。接受后只对对应坐标word加减1并回到重画/present。
6. Escape清自身状态、把全局path上限清0、将循环sentinel置0并把取消word写1；下一次循环头经共享尾返回。
7. 三个确认键先整体清零，再要求当前signed path不大于signed上限。mode0还严格要求path大于0，因此不能确认actor原格或阻挡格；mode1允许path等于0但拒绝负值。接受后清path上限并退出，取消word保持0。
8. 非法mode既不建立新图也不能确认；原版对真正超出4096-word存储的线性下标会非法访问。现代对非法mode/actor和真正存储越界安全拒绝，登记为平台适配；64列内的边缘线性别名行为保留。

## 汇编到现代实现逐块对照

- `BattleSetup::begin_cursor_selection`冻结actor/source/cursor、signed path上限及mode，并按mode建立movement或targeting路径图；六个正常caller的参数域与机器一致。
- `apply_cursor_selection`保持下/右/左/上方向、16位坐标更新、signed path比较、occupancy悬停例外、movement `path>0`确认及targeting `path>=0`确认。`legacy_cursor_index`只检查`y*64+x`总下标，保留边缘线性别名而不加入独立二维边界。
- movement由玩家移动wrapper传入当前signed行动值；targeting由攻击选择距离、用毒/解毒/医疗能力射程及暗器射程进入。取消和确认后的最短路、移动或效果continuation均属于各caller owner。
- 首轮逐基本块对照发现一项产品差异：旧Session进入选择相位后只完成一次present就接受首键，而机器在首个输入前严格完成两次，并在每个方向或拒绝确认后再完成一次。
- 最小修正仅位于`BattleSession`宿主时序：创建movement/targeting selection时计数置2；只有成功render后的`finish_presented_tick`才递减；moved或unchanged输入后置1；取消或确认清零。`BattleSetup`同步选择规则不变。
- 修正后从`0x36AF7`入口重新覆盖全部157条指令、所有35个分支、六个caller和两个出口，零剩余产品差异。

## 判别性回归与验证

- 原有真实battle4向量继续锁定source`(26,24)`、movement首格`(26,25)`、占用格`(26,26)`可悬停但不可确认、退回合法格确认、targeting占用格可确认，以及Escape清path上限与取消结果。
- Session新增明确时序回归：选择入口计数为2；第一次present前Escape被忽略；第一次present后计数1且Escape仍被忽略；第二次present后计数0才允许取消。重新进入后，在source拒绝确认把计数置1，同一present前立即发送方向仍被忽略，present后才移动。
- 正式golden由两次独立原资产生成逐字节一致后更新，SHA256为`36ff2d77f9602e6501a11a54dc89e19d8cfd87877df6f3bdb2a039a6ca5fc78b`。
- 根入口`./build.sh app --config Debug`以Clang 23完成构建并通过14/14测试；机器、static、closure和reverse framework门共同验证本owner。
