# 函数证据：`sub_30559` `0x30559..0x30A5A`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；`research/ida/reports/Z_DAT.b7_scene_xrefs.txt`

直接调用者：`sub_2C319` jump-table case59；opcode59 仅出现于 script932 PC38。

调用约定：无 KDEF 参数；返回 `1`，caller 不使用并把 PC 增加 1 word。

## 1. 汇编独立合同

### 1.1 队伍清理与原越界读

```text
for i=6; i>0; --i:
  role_id = word_C0B78[i]
  if role_id>0: sub_2E078(role_id)
```

`word_C0B78` 对应 6 个队伍 words（索引0..5）；因此 i=6 物理上读取紧邻的首库存 item ID。该原 BUG 必须保留。i=5..1 清理同伴；角色0不清理。`sub_2E078` 还解绑装备/修炼物品，不等价于直接把队伍槽写 `-1`。

### 1.2 跨场景事件禁用

按机器码顺序调用 36 次 `sub_2D841`，目标 `(scene,event)`：

```text
(0,0) (49,2) (4,1) (44,0) (44,1) (37,5) (30,0) (59,0)
(40,3) (56,1) (1,7) (1,8) (1,10) (40,7) (40,8) (77,0)
(54,0) (62,3) (62,4) (60,2) (60,15) (52,1) (61,0) (61,8)
(78,0) (18,0) (18,1) (69,0) (69,1) (45,0) (52,2) (42,6)
(42,7) (8,8) (7,6) (80,1)
```

每条记录前 9 fields 写为：

```text
cannot_walk=0, index=0,
event_1/event_2/event_3=-1,
current/end/begin_picture=-1,
picture_delay=0
```

x/y 传 `-2`，保持不变。

## 2. C++ 映射

- case59 保留 i=6..1 读取顺序；i=6 映射到 `inventory_item(0)`，i=5..1 映射到同下标队伍槽。
- `remove_team_role`/`clear_role_personal_items` 映射 `sub_2E078`。
- 固定 36 对 targets 和 13-word `modify_event` 参数精确映射跨场景写入。

## 3. 独立向量与测试

- `scene-goldens.json:kdef.opcode_59_script_932` 保存 PC、i 顺序、i=6 来源、9 fields 和 36 targets。
- UT 从真实 script932 PC0 驱动 dialogue2845→present→item189 notice→present→opcode59。
- 构造 team `[0,1,2,3,4,5]` 且首库存 item ID=6；核对 role1..6 的装备/修炼物品 user、角色字段和经验归零，验证 i=6 原 BUG。
- 36 个事件逐字段核对，且 x/y 必须保持原值。
- Linux Debug `openlegend.scene` 通过。

## 4. 最终 REVIEW

正向/反向逐基本块 REVIEW 均尚未执行；B7 全实现后从入口重做。`sub_2E078` 与 `sub_2D841` 作为转交 helper 各有独立 closure。
