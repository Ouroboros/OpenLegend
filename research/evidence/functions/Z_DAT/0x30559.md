# 函数证据：`sub_30559` `0x30559..0x30A5A`

状态：`platform_adapted`；最终 REVIEW：`converged_no_new_differences`

来源：当前 `Z.DAT` 完整机器码与原始文件字节；headless IDA完整指令、caller、callee和数据引用；当前KDEF script932。

## 1. 机器身份与caller

- 物理范围 `0x30559..0x30A5A`，1281 bytes，557条指令。
- loaded SHA256 `9948e5478b35dc14fcd01a5b3985eba7e0aaa6ca50149e3ea26b86a162254f26`；原始 `Z.DAT[0x29F59:0x2A45A]` SHA256 `2f1dd8e628e8e1bd0b1c510c72a0917cbb7e20f516da70003c2513af6c14eb2d`。
- loaded/raw仅有2个差异字节，分别属于两次队伍header基址绝对引用；两个dword均为`raw+0x20000`。覆盖全部差异后完整1281 bytes逐字节相同。
- 唯一可执行caller是事件解释器jump-table case59；另一xref只是jump table函数指针。当前1,018条KDEF中仅script932 PC38调用一次，无参数。函数返回EAX=1，caller不读取并进入共享PC+1尾部。
- `sub_3ED1E(60)`只是Watcom栈探测；业务callee只有一次动态 `sub_2E078` callsite和36次 `sub_2D841` callsite，两者各自由既有closure负责，本行不传播关闭。

## 2. 不看C++的机器合同

### 2.1 倒序离队与物理别名

EBX初始化为6，按6、5、4、3、2、1递减。每轮先从`team_base + index*2`读取当时最新的signed word；值小于等于0直接进入下一轮，严格大于0才立即调用离队callee，返回后再递减。

队伍只有index0..5，因此index6物理上别名为紧邻的首库存item ID；index5..1才是五个同伴槽，index0主角永不读取。每次读取发生在前一次离队及队伍左移完成后，不是预先复制六个值。离队callee负责从首个命中队伍槽左移、解绑两件装备和修炼物并清修炼经验；该内部语义已经独立终审。

### 2.2 三十六处事件禁用

循环结束后，机器严格按下列顺序执行36次事件修改：

```text
(0,0) (49,2) (4,1) (44,0) (44,1) (37,5) (30,0) (59,0)
(40,3) (56,1) (1,7) (1,8) (1,10) (40,7) (40,8) (77,0)
(54,0) (62,3) (62,4) (60,2) (60,15) (52,1) (61,0) (61,8)
(78,0) (18,0) (18,1) (69,0) (69,1) (45,0) (52,2) (42,6)
(42,7) (8,8) (7,6) (80,1)
```

每次13-word实参均为：

```text
scene, event, 0, 0, -1, -1, -1, -1, -1, -1, 0, -2, -2
```

即覆盖事件fields0..8为`0,0,-1,-1,-1,-1,-1,-1,0`，x/y用`-2`保持。36次完整参数流SHA256为 `c1fa8def58b9b65b2e437a93c3c465149e8e30d400dbdd03869004eb9f810fae`。随后返回1；函数无对话、render、音频、输入、随机或归档调用。

## 3. 汇编→C++ REVIEW

`run_event(case59)`同样在每一轮现场读取：index6显式取`inventory_item(0)`，其余取当前`team_member(index)`；以signed int16 `role_id > 0`判断并立即调用 `remove_team_role`。显式别名稳定表达机器合法线性越界，不把它误修为仅遍历五个队伍槽；主角仍不处理。

C++的36项常量表逐项、逐序匹配机器，并为每项构造完全相同的13-word参数后调用已终审的 `modify_event`。最后PC增加1；机器偶然返回值在caller不可观察。

现代role/item范围保护替代原机非法索引内存访问，全场景snapshot替代事件callee的外部归档即时I/O；当前原资产及本caller合法域结果不变，故归类 `platform_adapted`。

本轮第一遍单向对照未发现产品差异。第二遍从入口重新覆盖557条指令、两个循环分支、两项重定位、栈探测、动态离队callsite、全部36个事件调用、返回和dispatcher尾部，零新增差异。

## 4. 验证

- 独立oracle固定原始1281字节、script932完整word流、倒序source/物理别名、36次13-word参数流和程序计数器合同；双次生成逐字节一致；正式 `scene-goldens.json` SHA256 `98bb67d804180226b10e95cdcd977ee189d092b8f3aef221715ee12515867c18`。
- 真实script932测试预置36个目标的全部fields0..8为非目标值，验证禁用结果和x/y保持；另验证未列事件、主角装备/修炼物及经验完全不变。
- 队伍`[0,1,2,3,4,5]`与首库存ID6验证真实调用顺序对应的role1..6清理；synthetic单独opcode59以队伍slot3=0验证严格正值门不清主角。
- Linux app Debug根构建14/14通过（`proc_fef8`）。
- 原程序动态差分：`blocked_runtime_oracle`。

```text
closure_status = platform_adapted
final_review = converged_no_new_differences
remaining =
```
