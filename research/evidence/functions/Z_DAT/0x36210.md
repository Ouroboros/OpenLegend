# 函数证据：`sub_36210` `0x36210..0x363AC`

状态：`implemented_pending_review`

映射：`BattleSetup::begin_ai_support_plan`与`resume_ai_support_after_move`。

该入口处理AI医疗动作5。射程为actor medicine signed word除15向零截断后加1；同次IDIV产生的余数留在EDX，但完整`sub_36E7F`汇编只读目标图全局，不读入口寄存器，因此余数无行为效果。以actor为source建立targeting图，目标距离不大于射程时直接进入`sub_39EF7`；超距且round value严格大于0时调用`sub_3650E(actor, mode=1, value=range)`。

移动返回后或零/负行动值跳过移动后，均恢复同一目标槽坐标并第二次建图。机器码在第二次建图前调用两次`sub_3F50B`但丢弃返回值；callee完整汇编确认其仅返回32位绝对值且无副作用。重检命中则医疗；仍超距时比较`2*actor.attack`与`2*wrapped_allied_total/allied_count`，strict greater才自动攻击，否则休息。

固定typed验证覆盖range1/distance1直接医疗、超距先mode1/value1移动、移动失败后第二次重检并按actor attack300选择攻击，以及round0超距不移动但仍建图两次且相等时休息；30,000/30,000与10,000/10,000两角色attack/HP累计按int16回绕为14,464，双倍平均14,464而actor双倍攻击60,000。handler自身在医疗/攻击/休息前不消费RNG、不提前写action_done。

`BattleSession`现对直接命中调用共享医疗状态核心，再执行10帧effect0、attack/effect双bank音频、10帧无flash damage、sprite刷新及AI外层完成；真实battle4固定actor HP10→93、体力100→96、首magic/damage整帧hash`0xbec9ef2738ca79b4`/`0xa962994ff58c7064`、最终RNG1103527590。超距分支实际按mode1逐格render/present并每格等待两次BIOS tick变化，移动后恢复同一目标并重检；真实battle2距离6、射程2向量移动4格后医疗，目标HP10→28、首magic hash`0x2138cfdf8041c6bb`、最终RNG2900951131。重检为attack/rest时分别进入完整共享自动攻击或休息及外层actor continuation。Linux Debug完整BUILD全部14项测试通过；本函数推进为`implemented_pending_review`，最终汇编↔C++ REVIEW仍待执行。
