# 函数证据：`sub_36210` `0x36210..0x363AC`

状态：`platform_adapted / converged_no_new_differences`

映射：`BattleSetup::begin_ai_support_plan`、`resume_ai_support_after_move`与`BattleSession`动作5医疗continuation。

## 机器身份与调用边界

当前原始函数固定为412 bytes、97条连续指令、6个显式分支、33处fixup和9次direct call。raw SHA256为`170152b7fbff2346acf70a92693b0aabe6e58b50b5198769553c9e25bdf92bd3`，IDA loaded SHA256为`b37845bf6761f6a50de0f11a59e0ca389b2b836d51c50dcb059aee01da735197`；33处DWORD均严格增加`0x20000`，逐处反变换后与原始412字节完全一致。

唯一caller为`sub_33599:0x33BF8`动作5case：按signed低16位压入actor，调用后不读取EAX，跳到`0x33BB6`清理一个actor参数，并在统一尾`0x33C3F`写actor combatant word13 `action_done=1`。

9次调用依次为栈探测`sub_3ED1E`一次、targeting `sub_36E7F`两次、医疗`sub_39EF7`一次、移动`sub_3650E`一次、纯绝对值`sub_3F50B`两次、自动攻击`sub_34C47`一次和休息`sub_34AD3`一次。所有callee独立关闭，不随本owner传播closure。

## 完整汇编合同

入口按signed actor取得role medicine word，执行signed IDIV 15向零截断再加1；商保存在signed DI，EDX余数以后不再读取。actor当前x/y写入`word_556D6/word_556D8`作为source，调用`sub_36E7F`建targeting图；随后按signed `word_E6EE0`重读目标当前x/y并取得signed距离word。`DI >= distance`时立即调用`sub_39EF7(actor)`医疗并退出。

首次超距时，actor signed行动值word6严格大于0才调用`sub_3650E(actor,mode1,range)`；零或负值跳过，移动EAX被忽略。两路汇合后重新读取同一全局目标槽及其当前x/y，写`word_556DA/word_556DC`。机器码计算目标与actor的signed x/y差并各调用一次`sub_3F50B`，但两次EAX均被覆盖；该callee完整汇编仅执行signed 32位绝对值且无副作用，故调用结果无行为用途。

随后重新读取actor当前x/y到source，第二次调用`sub_36E7F`建图，再重读目标当前x/y及signed距离。`DI >= rebuilt distance`回跳医疗入口；仍超距才进入回退。回退现场重读actor role signed attack并以32位加倍，同时读取AI回合入口冻结在signed word中的己方总值`word_E6EDC`及人数`word_E6ECC`，执行signed `(2*total)/count`；只有`2*actor_attack`严格大于平均值才调用完整自动攻击，相等或更小调用休息。

医疗、攻击、休息三路EAX均经统一RET返回，但外层caller忽略。owner自身只在两次建图前写source x/y、第二次建图前写target x/y；不直接写action、action_done、combatant状态或RNG状态，也不直接消费RNG。

## 汇编→C++ REVIEW与修正

机器合同冻结后的首轮逐基本块对照发现一个产品差异：原版回退读取`sub_33599`在AI prelude呈现及动作选择前冻结的己方总值/人数；现代`update_ai_support_fallback`却在真正回退时重新扫描所有队友attack与HP，移动被拆成异步continuation后尤其可能观察到入口之后的状态变化。本轮完成结论作废。

修正后`begin_ai_support_plan`显式接收已经保存的`BattleAiTurnPrelude`，把signed wrapped allied total/count存入计划并跨移动保留；回退只现场重读actor attack，和机器一致。区分向量在入口冻结total300/count2，计划建立后把队友改成attack1000/HP1000：正确冻结平均仍为300，actor双攻600选择攻击；旧重扫会得到2300并错误休息。

修正后重新从`0x36210`入口覆盖97条指令及全部出口：signed医术除法与DI生命周期、两次source/target读取、两次targeting建图、signed距离方向、行动值正/零/负分支、mode1/range参数、移动EAX丢弃、目标坐标重读、两个纯abs返回丢弃、live actor attack、32位倍增、signed除法、strict攻击门、三种delegated出口、栈回收及唯一caller返回均无第二处产品差异。

现代对非法actor/role/target/count和坐标安全拒绝，替代唯一合法caller域外的原版线性访问或除零，归类平台适配。

## 回归、golden与结论

动作5聚焦回归覆盖range1/distance1直接医疗、正行动值mode1移动、目标坐标改变后第二次测距重读、行动值0和-1均跳移动但执行两次测距、signed medicine `-30 -> range -1`、严格相等休息、wrapped己方总值、live actor attack、队友突变仍使用入口冻结total/count，以及非法action/target/actor/role拒绝。既有完整Session回归继续锁定直接动作5的10帧magic+10帧damage、音频/状态/RNG/action完成，以及真实distance6/range2移动4个presented step后医疗。

正式battle golden加入本函数机器身份、33处重定位、9次call、6个分支及signed/frozen独立向量；两次原始资产独立生成逐字节一致，SHA256为`56dd722def3f65427014e232ada7b12d3345a12ec4bdf45dd9edfb472453fa6a`。根BUILD `proc_b1f5`完成Linux app Debug 14/14，battle、reverse framework及其余组合测试全部通过。

本owner归类`platform_adapted / converged_no_new_differences`；原程序动态差分继续登记为`blocked_runtime_oracle`。
