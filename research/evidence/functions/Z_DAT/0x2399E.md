# 函数证据：`sub_2399E` `0x2399E..0x23AA6`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码。

独立汇编恢复：仅当`in_ship==0`时递增步行复位计数，`>20`把walk offset与计数清零并恢复方向基帧；walk offset非零时清待机计数/延迟，否则递增待机计数。待机计数`>50`且动画未active时恰好调用一次`bounded(10)`，结果0启动动画、步进加2。每200 tick扫描6个party slot：slot0无条件，后续仅role id>0；physical power小于100时加1。

现代映射：`WorldSession::idle_tick`及`LegacyGameRuntime::advance`的无方向分支。审计发现现代曾在乘船时也递增20帧计数，已改为仅陆地递增；修正后从入口重做独立分支核对。party空槽与100相等边界由定向测试覆盖。

RNG向量固定`<=50`不消费、`>50`每tick一次直到启动；200 tick恢复不消费RNG。原程序异常role id会越界，现代跳过越界id为内存安全适配。最终双向逐基本块REVIEW为`not_started`。