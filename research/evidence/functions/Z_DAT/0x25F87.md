# 函数证据：`sub_25F87` `0x25F87..0x26208`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；审计包 `tmp/b9-review/0x25F87.txt`

## 1. 独立机器恢复

- 入口以参数区分 load/save，槽位选择初始化为 `0`。
- 每轮恢复调用方背景，绘制三槽框、三项槽位文字和当前高亮，然后 present。
- 上键：`0 -> 2`，其余减一；下键：`2 -> 0`，其余加一。处理后清对应单键态。
- Escape 清 Escape 单键态并返回，不执行I/O。
- Enter/Space/Insert 任一确认后同时清三确认键态。
- 确认后先绘制等待框并 present，再调用编号槽 load/save；等待文字为原字节“请稍候”，框 `(154,18,68,31)`，文字 `(158,25)`，颜色 `0x0705`。
- 被调 load/save 返回后退出本槽位函数；是否继续外层系统页由调用者 `sub_25D0E` 决定。

## 2. 汇编 → C++ 映射

- 三槽选择和回绕：`ui::GameMenuController::handle_key`。
- 等待框像素：`ui::BasicUiRenderer::render_io_wait`。
- present-before-I/O 门禁：`app::LegacyGameRuntime::finish_presented_tick`、`perform_pending_io`。
- 操作完成后的槽位页收束：`GameMenuController::complete_slot_operation`。
- 键态清理：`LegacyKeyStateReset` 与 `LegacyKeyboard`。

## 3. 差异与整轮重启

实现阶段曾缺少真实present门禁，已在本次终审前修正为等待帧present后才允许I/O。最终轮从 `0x25F87` 入口独立恢复并逐块对照，三槽回绕、Escape、确认键组、等待帧几何/文字、present-before-I/O和调用后出口均零新增差异。

后续圆角primitive审计发现三槽框及load/save等待框全部传style3；现代旧直角框及初次误用style4的4/8+4/8两轮可视结论依次废弃。读取 `sub_2050A` 与 `unk_54294` 后恢复palette0 3/8加目标5/8，再从 `0x25F87` 入口覆盖三个面板调用、文字、present门禁和I/O出口，除primitive像素修正外无新增差异。

## 4. 平台适配

- 原同步磁盘调用由runtime pending-I/O continuation承接。
- 现代失败报告允许回到调用方页面；合法存档域的机器调用顺序和成功出口不变。

## 5. 验证与结论

- Linux app Debug：14/14。
- UI/persistence回归证明present前编号槽字节不变、present后才执行操作。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
