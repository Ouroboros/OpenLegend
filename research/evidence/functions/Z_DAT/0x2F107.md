# 函数证据：`sub_2F107` `0x2F107..0x2F136`

状态：`implemented_pending_review`

KDEF opcode28。参数 `(role,min,max,true_offset,false_offset)`；读取角色记录 word56（VA `word_901BC`，即 morality），按 signed `value < min || value > max` 判断，区间两端均包含。返回对应 offset，caller 宽度6。

C++ case28 使用 `morality >= min && morality <= max`。真实 script636（item110 ID存在）固定 morality 79/80/100/101：仅80与100到达 dialogue2383。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
