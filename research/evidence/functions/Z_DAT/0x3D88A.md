# 函数证据：`sub_3D88A` `0x3D88A..0x3D8AD`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码。

独立汇编恢复：薄包装器先建立20字节栈检查边界，再把调用者传入的anchor x/y、CLOUD帧指针和混合权重原序转交`sub_206AD`；无条件调用、无额外裁剪，返回值直接来自粒子绘制。

现代映射：`WorldSession::render`在天气active且粒子y>`-1000`时按数组顺序调用`draw_weather_particle`；帧指针由`particle.kind`索引`CLOUD.IDX/GRP`，权重由`particle.speed`提供。固定全局buffer/pointer改为`IndexedFramebuffer`和`PackedArchive`，不改变合法资产像素。

向量覆盖三个粒子的调用顺序、y哨兵和kind 0..3。损坏kind/frame在现代实现中失败返回属于内存安全适配。最终双向逐基本块REVIEW为`not_started`。