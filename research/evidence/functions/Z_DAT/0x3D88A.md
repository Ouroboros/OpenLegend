# 函数证据：`sub_3D88A` `0x3D88A..0x3D8AD`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码。

独立汇编恢复：薄包装器先以参数20调用栈边界检查，再从自己的五个入口参数中忽略固定framebuffer指针，把anchor x、anchor y、CLOUD帧指针和混合权重原序压栈并无条件调用`sub_206AD`。包装器不增加裁剪、分支或状态写入，callee返回后只回收16字节参数并直接返回。

现代映射：固定全局buffer参数由对象持有的`IndexedFramebuffer`取代。world与scene在天气已启用且粒子y>`-1000`的caller门后，按粒子数组顺序以kind 0..3和weight 6..8进入各自`draw_weather_particle`；battle renderer以CLOUD帧4/5和weight 2/3进入`draw_cursor_overlay`。六帧均来自同一只读`PackedArchive`。

差异与适配：现代入口验证kind、weight和RLE有效性，确定性拒绝机器会产生裸指针/表越界的宿主输入；合法六帧及调用顺序不变。world、scene和battle三条实际绘制路径都有独立资产oracle与C++整帧hash。登记修正后已从`0x3D88A`入口重启最终单向汇编→C++ REVIEW，确认栈检查、四项原序转发、无条件callee、16字节回收和直接返回；三类现代caller无新增可达差异，终态为`platform_adapted`。