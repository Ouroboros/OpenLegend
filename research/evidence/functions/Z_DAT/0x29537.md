# 函数证据：`sub_29537` `0x29537..0x295D0`

状态：`implemented_pending_review`

机器码确保 `ALLSINBK.IDX` cache tag=4，打开 `ALLSINBK.GRP`，按场景号 offset 将当前49,152字节地图缓冲写回工作副本并关闭。`sub_3DAE3→sub_3F775` 证明方向是 write；IDX 缺失打印原粗口错误后进入终止链。

现代实现把工作副本表示为 `GameSnapshot::scene_maps`，`set_scene_value` 直接写目标场景固定 span，因而写回在每次字修改时已完成且无需宿主文件 I/O。当前 KDEF 无非当前 opcode38 occurrence；当前场景 script434 和 snapshot 字节访问覆盖合法域，最终 REVIEW 尚未执行。
