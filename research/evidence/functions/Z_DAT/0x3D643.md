# 函数证据：`sub_3D643` `0x3D643..0x3D68A`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；legacy sprite包装。

独立汇编恢复：legacy id按32位有符号检查`0..0x7FFE`，越界返回0；合法id以向零除2索引累计MMAP frame指针，传有符号16位anchor X/Y调用RLE sprite绘制，成功返回1。奇数id与前一偶数id映射同一frame。

现代映射：`render::legacy_sprite_index`与`WorldSession::draw_sprite`，同样上界和`/2`；`PackedArchive::entry`替代裸指针，`draw_rle_sprite`执行覆盖。真实资产entry数量额外检查防止越界。

向量覆盖-1/0/1/2/0x7FFE/0x7FFF，固定`0/1→0`、`2→1`、`0x7FFE→0x3FFF`和非空角色/建筑帧。scene/world共享该检查，真实场景六层、世界地图与结局帧覆盖固定有效资产路径；额外archive边界检查为安全适配。

最终REVIEW仍需反查全部wrapper调用者及委托`sub_20354`/stack-guard边界；双向逐基本块状态为`not_started`。
