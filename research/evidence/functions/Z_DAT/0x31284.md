# 函数证据：`sub_31284` `0x31284..0x312A6`

状态：`platform_adapted`；最终 REVIEW：`converged_no_new_differences`

来源：当前 `Z.DAT` 完整机器码与原始文件字节；headless IDA完整指令、caller、共享返回尾部和数据引用；当前1,018条KDEF。

## 1. 机器身份与caller

- 物理范围 `0x31284..0x312A6`，34 bytes，6条指令。
- loaded SHA256 `c72475ee4252e82deb7be1a007228d043f37a93b27c569b3051e98e87cfe3df0`；原始 `Z.DAT[0x2AC84:0x2ACA6]` SHA256 `48bbf62ff4dfb1627ead50e1200d66d0005434348aea081a9efd4ea19e01ffde`。
- loaded/raw唯一差异字节位于唯一角色word绝对地址operand内；完整dword为`raw+0x20000`，归一化后34 bytes逐字节相同。
- 唯一可执行caller是事件解释器jump-table case63；另一xref只是jump table函数指针。caller将`role_id,value`两个KDEF words分别movsx后压栈。
- 函数尾跳到共享 `0x2DE4D`，该块写EAX=1并return；caller不读取EAX，回收8 bytes，执行`PC += 3`后恢复dispatch。
- 当前KDEF仅script289 PC107调用一次，参数role36、value2；完整调用流SHA256 `a7554cc7164559be88b236dfcf6b92d76e2cb60678f6fe857d71d6e8d5d744f2`，script289 word流SHA256 `c191d39636c89da677c8e221d7e1df3be20ef390a7f9991612e2bc27aa387c4b`。

## 2. 不看C++的机器合同

函数执行4-byte stack probe后，以32位乘法计算 `role_id * 182`，读取value的32位sign-extended实参，并把DX低16位写至绝对基址 `0x90168 + role_id*182`。角色记录基址为`0x9014C`，因此写入byte offset28、word14 `sexual`。

机器不检查角色范围，不读取旧值，不执行钳位或算术；除该word外不写角色、队伍、库存、事件、地图、随机、画面、时间、输入或音频状态。返回常量1只来自共享尾部且被caller忽略。

## 3. 汇编→C++ REVIEW

C++ case63对合法role索引执行 `set_word(role_word::sexual, argument(2))`，随后无条件PC加3。`argument(2)`本身是int16，因此与机器把sign-extended dword的DX低16位写回完全相同。

现代代码对负数或超出角色数组的ID不写，替代机器的线性越界内存访问，归类 `platform_adapted`；唯一真实role36调用处于合法域。

第一遍单向对照未发现合法域产品差异。第二遍从入口重新覆盖6条指令、4-byte stack probe、182-byte乘法步长、word14绝对写入、低16位截断、无边界检查、共享返回1和caller回收/PC推进，零新增差异。

## 4. 验证

- 独立oracle固定原始34字节、唯一真实调用、角色记录182 bytes/91 words、byte offset28/word14和完整script289；双次生成逐字节一致；正式 `scene-goldens.json` SHA256 `099ff7ef798f4aaa62c5f863e748d658b56edd3037b72d6af164aa937a6a5126`。
- 真实script289将role36 sexual从0覆盖为2；role36相邻level保持123，前一role35 sexual保持-123，固定目标角色步长和单word写入。
- 同一script后续武功、携带物品和品德操作各由自身closure负责，不传播到本项。
- Linux app Debug根构建14/14通过（`proc_8268`）。
- 原程序动态差分：`blocked_runtime_oracle`。

```text
closure_status = platform_adapted
final_review = converged_no_new_differences
remaining =
```
