# 函数证据：`sub_31284` `0x31284..0x312A6`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；KDEF opcode63，仅 script289 PC107。

参数 `(role_id,value)`；计算 `role_id*182`，把 value 的低16位写角色记录 VA `word_90168`。角色记录基址 `0x9014C`，偏移 `0x1C` 即 word14 `sexual`。随后跳回事件解释器默认 continuation，caller 宽度3。

C++ case63 在 role ID 有效时写 `role_word::sexual`，无效时不写，PC+=3。真实 script289 从 PC0 驱动全部 dialogue 后核对 role36 sexual 从0变2。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
