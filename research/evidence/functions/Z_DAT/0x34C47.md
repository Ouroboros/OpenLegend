# 函数证据：`sub_34C47` `0x34C47..0x3505B`

状态：`pending_implementation`

部分映射：`BattleSetup::begin_ai_attack_plan`、`resume_ai_attack_after_move`。

入口先统计十个magic id中严格大于0的数量并调用`bounded(count)`，直接把结果当magic槽；随后从熟练度unsigned `/100`取得等级层，读取select distance和area type。七项特殊加成表首列是角色第一装备槽word23，不是角色ID：装备/武功/加成为`106/57/100`、`107/49/50`、`108/49/50`、`110/54/80`、`115/63/50`、`116/67/70`、`119/68/100`。表由Z.DAT file offset324920的42字节直接解析。

武功选择发生在目标策略前；seed9、两个已学槽依次得到槽0与目标roll5，终态2878571567。目标距离使用targeting图。area type0/3只要求距离<=select distance并传movement mode1；type1/2还要求同x或同y并传mode2；其他值永不直接命中并传mode0。

初次已满足射程则以automatic flag1调用攻击；不满足且round value<=0直接结束，不休息；否则请求移动。移动后先复检原目标，失败才强制改选最近目标并再次复检，命中则自动攻击，否则休息；handler最后写action_done。固定field2距离为6/8，相邻重选距离1。

现代仅生成typed计划并在移动后恢复上述决策；实际`sub_3650E`逐格移动/render/present/tick、`sub_37734`攻击动画/伤害/提交、休息调用及handler自身action_done写入尚未由BattleSession同步执行，因此不得升级为完整实现。最高攻击策略未写target时，现代安全拒绝原版对slot -1的线性越界读取，此边界留待最终REVIEW。Linux Debug 14/14和独立双重oracle通过。
