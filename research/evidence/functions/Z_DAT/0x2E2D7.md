# 函数证据：`sub_2E2D7` `0x2E2D7..0x2E2F5`

状态：`implemented_pending_review`

KDEF opcode20。函数不扫描队伍；只读取 `word_C0B82`（6个队伍槽的最后一个）。若该 signed word `<=0` 返回 false_offset，否则返回 true_offset；caller 宽度3。这依赖原队伍压缩不变量，也保留 role ID 0 在尾槽不算满的边界。

旧 C++ 错误要求六槽全部 `>=0`。现只判断 `team_member(5)>0`。真实 script11 反例：前五槽含洞但尾槽9仍 true→dialogue175；前五槽满但尾槽0仍 false→dialogue30。Linux Debug scene 通过；最终双向 REVIEW 尚未执行。
