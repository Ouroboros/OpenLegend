# 函数证据：`sub_24CE8` `0x24CE8..0x24D43`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码。

独立汇编恢复：接收32位origin X/Y，按EARTH→BUILDING→SURFACE→BUILDX→BUILDY无条件调用五个逐层128行loader；前四返回被忽略，最终返回BUILDY loader结果。调用参数每次完全相同。

现代映射：`WorldCache::reload`以显式`kLayerLoadOrder`遍历五层，每层复制128行×128个int16，只有origin在0..352且map有效才执行。层顺序审计已修正。

向量覆盖origin(293,171)、(42,171)、四角及五层独立FNV1a64。现代预验证origin是内存安全适配。最终双向逐基本块REVIEW为`not_started`。