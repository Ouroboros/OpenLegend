# 函数证据：`sub_24A02` `0x24A02..0x24C23`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；世界会话初始化。

独立汇编恢复：清提示状态与400字节临时表；复制6个party id和200组物品；从header以有符号16位读world/ship坐标、方向、in_ship，world cache origin=`clamp(world-64,0,352)`，scene view origin=`clamp(scene-11,0,36)`。玩家/船frame按方向表生成，weather inactive；随后按五层顺序加载cache、重建深度、render并present首帧。

现代映射：`WorldSession`构造器、`WorldCache::reload`、`LegacyGameRuntime::start_world/render/finish_presented_tick`。审计将`in_ship`由bool改回raw int16：移动对任意非零按船处理，render只有恰为1绘中心船，其他非零跳过玩家节点，header提交保留原值。

方向/坐标损坏输入在现代做边界检查/方向clamp，避免原全局表越界，属于安全适配；合法0..3与0..479域逐像素一致。初始framebuffer和header值由真实资产golden固定。最终双向逐基本块REVIEW为`not_started`。