# 函数证据：`sub_24A02` `0x24A02..0x24C23`

状态：`platform_adapted`

来源：当前 `Z.DAT` 机器码；审计包 `tmp/b9-review/0x24A02.txt`

## 1. 独立机器恢复

- 入口清 `word_C0BFC`，把 `word_D27C0` 起始的 `0x190` 字节填为 `0xFF`。
- 从载入后的RANGER header复制6个连续party id到运行态队伍数组；随后按原顺序复制200组 `{item_id,count}` 到运行态库存数组。
- `main_map_x/y` 以有符号16位读出并扩展为运行态坐标；每轴cache origin为 `clamp(coordinate-64,0,352)`，局部偏移为 `coordinate-origin`。
- `sub_map_x/y` 保持原16位值；每轴scene view origin为按有符号16位判断后的 `clamp(coordinate-11,0,36)`。
- 恢复 `in_ship`、船方向、船当前位置/下一格和主角方向；主角与船图号分别通过原方向表索引导出。
- 恢复当前scene id并把天气/覆盖活动字清零。
- 依次调用world cache重建、深度/天气工作态重建、world render，最后present首帧。

## 2. 汇编 → C++ 映射

- snapshot提交与唯一状态owner：`model::GameState::import_snapshot`、`LegacyGameRuntime::activate_pending_load`。
- party/inventory与header字段：`RangerHeader` typed accessors；会话直接引用当前 `RangerState`，不维护第二份业务状态。
- world坐标、船状态、方向表、清零的idle/weather工作态和cache origin：`world::WorldSession::WorldSession`、`clamped_origin`、`WorldCache::reload`。
- 首帧重建/present：`LegacyGameRuntime::start_world`、load的 `first_black_present` continuation、`WorldSession::render`、SDL present。
- scene坐标/view origin保留在header，后续由 `SceneSession`构造时按同一11/36边界恢复。

## 3. 单向终审

- 从 `0x24A02` 入口独立恢复后才读取现代实现。
- 逐块覆盖清零/fill、6项party复制、200组库存复制、两套坐标派生、方向/船/scene字段、cache与首帧present尾链。
- 本轮零新增差异，无需修正或重启。
- 启动地图owner前移后废弃旧结论并再次从入口覆盖：`start_world`改为复用标题前已建立的五层字节，但party/库存导入、坐标派生、方向/船状态、cache窗口重建及首帧present顺序和值均未改变；零新增差异。

## 4. 平台与安全适配

- 原版party/inventory运行态数组与RANGER工作副本的双份运输改为单一typed `GameSnapshot` owner；UI、脚本和保存边界仍观察同一顺序和值，保存运输另由 `sub_265AB` 终审覆盖。
- 原版每次重建的固定cache/fill表改为驻留world layers、`WorldCache`及局部深度容器；128×128窗口、五层值和首帧像素保持。
- 原方向表对损坏的非0..3值可越界；现代对损坏存档clamp到0..3，登记为安全适配。合法存档域不改变。
- 手工文件句柄和同步present改为RAII与SDL continuation；载入尾链的两个黑world present及65级淡入由调用者边界保持。

## 5. 验证与结论

- Linux app Debug `proc_96cf`：14/14。
- 真实资产回归覆盖header导入、world/ship坐标与方向、raw `in_ship`、cache origin、首个黑world帧、第二个相同黑帧和后续65级淡入；损坏方向clamp另有安全边界测试。
- 原程序动态差分：`blocked_runtime_oracle`。

`final_review = converged_no_new_differences`
