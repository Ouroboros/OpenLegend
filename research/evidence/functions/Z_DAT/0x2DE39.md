# 函数证据：`sub_2DE39` `0x2DE39..0x2DE53`

状态：`implemented_pending_review`

机器码只有一项持久副作用：把 opcode8 参数写入场景循环的离场音乐覆盖字 `word_D2952`，随后返回1；它不调用音乐 wrapper，也不立即播放或停止音乐。该语义与 opcode66 `sub_31C2F` 的立即音乐命令严格分离。

现代 `SceneSession::run_event` case8 只设置 `exit_music_override_` 并继续同帧解释；`resume(scene_exit)` 离场 continuation 优先发出 `force=true` 的 override 命令，否则发出普通 scene metadata exit-music 请求。SDL 边界仅对普通请求比较 `LegacyAudioController::current_music()`，override 与同样直接调用 wrapper 的 opcode66 不抑制。独立 oracle 扫描当前 KDEF 的15处 opcode8，参数全部为3。真实 script494 从 scene70 执行至 PC54 后立即音频队列保持为空；随后踏入出口才发出强制 music3，而不是 scene70 metadata 的 exit music19。Linux Debug BUILD 13项通过。最终双向逐基本块 REVIEW 仍未执行。
