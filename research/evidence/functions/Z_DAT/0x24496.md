# 函数证据：`sub_24496` `0x24496..0x24559`

状态：`implemented_pending_review`

来源：当前 `Z.DAT` 机器码；陆地移动的船双格碰撞边界。

独立汇编恢复：候选world坐标若等于船current或船next任一格，则设置`in_ship=1`、采用本次方向并允许移动；否则转交陆地阻挡探测。比较是两组完整X/Y相等，不接受单轴命中。上船成功后玩家提交到候选格并同步船current/next。

现代映射：`WorldSession::complete_move`中的`boarded_ship`两组坐标比较及上船分支。固定轨迹覆盖(108,100)上船、船next格和header回写。

raw `in_ship`保留16位；只有写入上船动作时规范写1。最终双向逐基本块REVIEW为`not_started`。