# 函数证据：`sub_3B238` `0x3B238..0x3B387`

状态：`implemented_pending_review`

部分映射：`openlegend::battle::BattleSetup::evaluate_outcome`。

函数先顺序扫描全部参战者：若对应角色 hp word17 <= 0 且参战者 word5==0，则 occupancy `[y*64+x]` 写-1，并把 word5写1。第二次扫描只看 word5==0的槽：side word==0表示队伍仍存活，非0表示敌方仍存活。

写结果顺序不可交换：

1. 无队伍存活写 raw result=1；Big5 文案 `戰鬥失敗`；`sub_31C75` 最终返回0。
2. 无敌方存活写 raw result=2；Big5 文案 `戰鬥勝利`；`sub_31C75` 最终返回1。
3. 双方同时为空时第二次写覆盖第一次，最终 raw result=2/胜利。

结果>0时`BattleSession`现重画战场，按原`(118,30,85,27)`绘制Big5胜败文字，形成独立present和任意非零键等待边界，再调用战后结算。真实battle4胜利/失败整帧FNV64固定为`0x52bc7b717bebdba9`与`0xf3c79c485413dfe5`；双方同时全灭覆盖为胜利的fixture继续保留。Linux Debug完整BUILD 14/14，因此推进为`implemented_pending_review`；最终完整双向REVIEW尚未开始。
