# Battle work package

## 状态

- 实现状态：`implemented_pending_review`（81/81函数均已实现并接入；战果、战后五类同步UI、音乐收尾及scene typed结果已闭环）。
- 最终 REVIEW：`in_progress`（57/81）。
- B8-WP01实现差异审计：5/5差异已修正，Linux/Windows core/app × Debug/Release完整8项BUILD矩阵通过；保留待B0→B9统一最终REVIEW。

## 范围与非范围

- 物理 battle 区间锁定为 `Z_DAT 0x31C75..0x3CBE3`，专项 headless 报告枚举81个函数，机械审计顺序见 `research/inventory/battle-closure.tsv`。
- 范围包含战斗入口、WAR/WARFLD/FIGHT 资源、参战者临时态、输入、动作、AI、伤害/状态/物品、indexed 绘制、胜负/逃跑与战后提交。
- scene 包装器 `sub_2DE03` 仍归 scene closure；battle 只返回 typed result，不直接恢复 scene PC。
- 通用文件、packed archive、字体/精灵/像素、键盘、BIOS tick、LCG 与音频后端分别由 resource/render/input/time/random/audio owner 提供，不复制其实现。
- battle 不直接链接 scene、world、ui、platform_sdl3。

## 汇编与资产证据

- 机器码报告：`research/ida/reports/Z_DAT.b8_battle_xrefs.txt`，范围首尾与81个 FUNCTION 记录由 validator 固定。
- 独立资产 oracle：`research/tools/generate_b8_battle_goldens.py` / `research/evidence/battle-goldens.json`，以及状态三帧专用的`research/tools/generate_b8_player_status_golden.py` / `research/evidence/battle-player-status-golden.json`；均不链接 OpenLegend C++。
- 当前原版资产为92对 `FIGHTnnn.IDX/GRP`、共4,992帧；`WAR.STA` 为140条×186字节；`WARFLD.IDX/GRP` 为26条累计 archive entry。
- `sub_31EB9/sub_3265C` 证明参战者池是26槽×14 word（每槽28字节），occupancy 是64×64 `int16`；固定/预置队伍先写、敌方后写，重复格由后写槽覆盖。
- battle data xref 锁定瞬时状态簇：`0xDC72C..0xDEA04`、`0xE0A04..0xE6AC0`、`0xE6CBC..0xE6CBE`、`0xE6EBA..0xE6EEA`；`0xE87BC` framebuffer 仍由 render owner，`0x9014C..` 与 `0xC0B78..` 持久记录仍由 model owner。

## 接口与状态所有权

- 输入：原 battle id、get-exp 开关、借用的 `GameSnapshot` 窄视图、共享有序 RNG、宿主无关输入/tick。
- 输出：`BattleStepResult = Stay | Victory | Defeat | Escape`、indexed frame、音频命令和已按原顺序提交的持久状态。
- battle session 独占参战者临时态、战场缓存、选择/动画/AI transient；app 只拥有 session 生命周期并同步消费结果。
- `sub_31C75` 栈参数依次为 battle id 与 get-exp，返回 `word_E6ED2 - 1`；scene `sub_2DE03` 只在返回值严格等于1时选择真偏移。

## 当前实现队列

1. `sub_31DA0` 已映射为 `BattleData`：WAR 186字节记录、WARFLD entry 前16,384字节与64×64 occupancy 清空；状态仅为 `implemented_pending_review`；
2. `sub_31C75` 已由runtime-owned `BattleSession`覆盖资源生命周期、完整主状态机、队伍确认后WDX/WMP/EFT加载、跨战battle render globals首帧、首帧present后WAR word8战斗music、scene word7恢复music、typed结果和严格exact-one scene真假边界；
3. `sub_31EB9` 的26槽、固定/预置/手选状态与 `sub_3265C` 敌方建立已映射为 `BattleSetup`；`BattleSession`实际绘制原圆角队伍选择框、Big5标题/确认文字、角色名/星号，逐键执行上下回绕与确认，并在每帧恢复冻结背景防止混色叠加；真实battle2选择像素及程序初始globals为零时`view=(0,0)`首帧像素均与独立oracle一致；runtime已驱动render/present/input，前者推进为`implemented_pending_review`；
4. `sub_3271E..sub_32E59` 已恢复：稳定速度降序、逐槽swap、回合值，以及等待动作的逐槽队尾交换；`BattleSession`已执行跨战保留render globals首帧、present后首次排序/actor居中、逐帧黑场淡入、轮首再次排序/清三个效果globals/prepare和actor-present后player/AI分界；玩家分支现按原门槛建立十项可用表，以可用项ordinal执行上下回绕和三确认键，并实际绘制圆角菜单、原Big5文字及当前actor状态面板，独立oracle与C++整帧hash一致；攻击已接入武功选择的战场重画、圆角面板、Big5名称、左右回绕、三确认键与Escape取消，并保留普通名称只扫`slot<learned_count`而确认扫10槽的稀疏显示BUG；等待会把当前actor移到队尾并继续同槽，休息提交状态后推进下一槽，两者均执行胜负/隐藏目标清理、隐藏槽跳过和下一actor present；actor present后按原顺序清word7/word10，轮末调用异常状态并等待本轮捕获的BIOS tick变化后再开始下一轮；自动动作按重绘→present→置flag→同actor AI推进；玩家移动已接入路径光标、逐格present/tick及菜单重建；用毒、解毒、医疗已按各自射程接入targeting四方向/Escape/确认、每轮路径重绘present、取消回原ordinal与确认目标保存，并在确认后完成状态、FIGHT/EFT动画、双bank音效、十帧damage、共享行动尾部和下一actor；攻击现已完成四类area的目标/方向分流、FIGHT/EFT、damage、双击逐轮提交与升级提示；物品已完成5×3 MMAP网格、分页/取消/三确认键、type3效果面板/任意键及零效果边界、type4目标/effect/damage/库存提交；状态动作已按共享`sub_22066/sub_22090/sub_22A59`合同接入队员选择、Escape/三确认键、两页角色状态和两次任意键等待，且不结束actor、不消费RNG；选择页/第一页/第二页的独立原资产oracle与C++整帧hash分别为`0xfa1b21403051335c`、`0x1c5e879ce61d5b34`、`0x9592da33a3c151d4`；战斗退出条件现进入胜败面板、战后消息队列并回送scene；
5. `sub_33599`的int16态势、候选优先级、逃跑、typed handler映射和handler后行动标记，以及`sub_33C4D..sub_34550`六个候选selector均已恢复；BattleSession已实际执行态势累计、战场重绘/present、延迟参数300对应的八次BIOS tick变化、selector、休息handler和逐actor后处理；`sub_34AD3`休息wrapper、`sub_3505B..sub_35372`四种攻击目标策略、`sub_355FF..sub_3570F`三个用毒目标selector已完整映射，`sub_34AEC`已恢复逃跑/物品重定位目的格计划，`sub_34C47`已恢复自动武功、装备加成、射程、移动后复检/重选的typed计划，`sub_3540E`已恢复用毒射程、移动后同目标复检和wrapped己方平均攻击回退typed计划，`sub_35803/sub_3582B`已恢复普通物品重定位后mode0使用和暗器目标/射程/移动复检/mode1使用或攻击回退typed计划，`sub_3598C`已恢复AI mode0共享物品23项状态、面板/等待参数与AI mode1暗器伤害/毒值，并按side扣队伍库存或敌方携带槽；`sub_36133`已按四个合法删除槽恢复敌方携带物品ID/数量同步左移并关闭；`sub_361AC`已按请求医疗正/零/负行动值、mode0/value0移动、目标坐标重读和自动攻击完成最终审计并关闭，`sub_36209`两指令请求解毒共享跳入也已按独立owner关闭，`sub_36210`已按AI医疗signed射程、mode1移动、目标重读及live actor攻击对入口冻结wrapped己方平均值的回退完成最终审计并关闭，`sub_363AC`已按AI解毒signed射程、mode1移动、目标重读、零abs调用、三路共享epilogue及入口冻结平均值回退完成最终审计并关闭；`sub_3650E`已按1418字节完整入口审计mode0..3目的格、signed本回合射程分派、exact-layer扫描与strict tie、两轮generic邻格、x优先退向actor、双可达性检查和最短路delegation，新增四组区分向量后零产品差异并独立关闭；`sub_36A98`玩家移动wrapper已按95字节完整入口审计，锁定signed行动值mode0光标、cancel word恰等于1时不标路/不移动、确认后标路及`sub_37355(actor,0,0,0)`、固定-1/0返回和唯一caller覆盖EAX，取消无状态副作用回归通过后零产品差异并独立关闭；`sub_36AF7`通用玩家光标handler已按783字节完整入口审计，锁定mode0/1图、下右左上优先级、signed 64列线性别名、occupancy悬停例外、Escape和确认条件；首轮发现首键前少一次present的产品差异，现以入口计数2和每次未结束输入后计数1修正，判别性Session回归通过后入口重审零剩余差异并独立关闭；`sub_36E06`移动寻路图wrapper已按121字节完整入口审计，锁定movement初始化后source强制归零、255槽队列读0/写2/distance0、sentinel/source种子、EAX零回环及八个caller忽略返回，source occupancy真实资产回归通过后零产品差异并独立关闭，非法source安全拒绝归类平台适配；`sub_36E7F`目标寻路图wrapper已按121字节完整入口审计，锁定targeting初始化后upper-layer阻挡source强制归零、相同队列种子/EAX回环及十五个caller覆盖返回，真实battle0/93 blocked-source整图hash通过后零产品差异并独立关闭，非法source安全拒绝归类平台适配；`sub_36EF8`移动阻挡图初始化已按257字节完整入口审计，锁定64×64预清零、x外/y内最终扫描、upper layer/occupancy/九段signed ground闭区间谓词、254/555写值、返回63及共享ABI尾，occupancy signed边界和九段端点向量通过后零产品差异并独立关闭，省略不可观察预清零、独立格扫描顺序及void返回归类平台适配；`sub_36FF9`目标阻挡图初始化已按119字节完整入口审计，锁定相同预清零与扫描、upper-only零/非零254/555、无occupancy/ground读取、自有RET及返回8190，非空occupancy和battle89 ground-only向量通过后零产品差异并独立关闭，同类中间态/扫描顺序/void返回归类平台适配；`sub_37070` flood step已按246字节完整入口审计，锁定sentinel增距signed `%128`、先重入sentinel再出队、上右左下、signed坐标0..64、path恰254先入队后写距离、正常0/连续sentinel -1与共享尾保持EAX，中心十字六步trace、一二层距离和battle0 x64线性别名通过后零产品差异并独立关闭，内联step、共享状态私有化、void返回和真正存储越界安全拒绝归类平台适配；`sub_37166` dequeue已按72字节完整入口审计，锁定255-word x/y双数组、同一signed下标三次重读、先写x再写y、signed `idiv255`余数回写与254环绕商1、两个caller忽略EAX，253→254→0 trace及battle0/93回溯前457/822个可达格通过后零产品差异并独立关闭，交错coordinate、私有index、无观察点访问顺序及省略商返回归类平台适配；`sub_371AE` enqueue已按73字节完整入口审计，锁定单次signed write-index读取、同slot先写x再写y、无full guard、signed `idiv255`余数回写与254环绕商1、sentinel/候选caller忽略EAX，253→254→0 signed极值写trace及真实图457/822个可达格通过后零产品差异并独立关闭，交错coordinate、私有index、不可观察半写状态及省略商返回归类平台适配；`sub_371F7` path write已按39字节完整入口审计，锁定signed `2*(y*64+x)`字节偏移、value低16位写入、EAX偏移返回及distance/250/250/255四caller，线性别名/一字越界向量与真实distance/marked/consume/x64 hash通过后零产品差异并独立关闭，负坐标与真正index>=4096安全拒绝归类平台适配，存储内x64别名保持；`sub_3721E` path read已按39字节完整入口审计，锁定signed线性读取、scratch原始word写、`cwde` signed EAX及判254/保存distance/判distance/判250四caller，scratch唯一写无读xref、线性别名/越界向量与真实path hash通过后零产品差异并独立关闭，省略scratch及负坐标/真正越界安全拒绝归类平台适配，存储内x64别名保持；`sub_37245` shortest-path marking已按272字节完整入口审计，锁定target先标250、signed `(d+127)%128`降距、上右左下首个前驱、source标记、共享尾及caller忽略EAX，正常/tie/环绕/source相等/malformed向量与battle0/93 marked hash通过后零产品差异并独立关闭，非法输入、无前驱false及4096步上限替代机器无界回环归类平台适配；`sub_37355` per-step movement已按991字节完整入口审计，锁定上右左下首个250邻格、path/occupancy/x/y/direction/sprite/physical-power/round写序、view夹值、每格render/present/delay40、player destination与AI mode0..3停止矩阵、两个caller忽略EAX和`0x3CBDB`共享尾，四方向/回绕/malformed及真实Session向量通过后零产品差异并独立关闭，无标记路径checked终止和非法输入拒绝归类平台适配；BattleSession现从全部AI typed handler实际进入mode0..3移动，每格提交状态、更新视图、重绘/present并等待参数40对应两次BIOS tick变化，移动后恢复原typed计划；真实battle2逃跑三格后休息并推进actor；自动攻击现完成目标差方向、直接/移动后area伤害、FIGHT/EFT双bank、十帧damage、提交/升级等待、体力和外层actor尾部，`sub_34C47`为`implemented_pending_review`；AI用毒也完成直接/移动后状态、11帧effect30、十帧damage、攻击/休息回退与外层尾部，`sub_3540E`推进为`implemented_pending_review`；AI医疗/解毒完成直接和移动后状态、无flash动画、攻击/休息回退与外层尾部，请求医疗/解毒也在可选mode0移动后无条件进入完整自动攻击；AI普通物品现完成重定位、效果面板、无输入读取、九tick等待与延后来源提交；AI暗器现完成直接/移动后复检、sample13前奏、EFT后状态提交、非零damage条件kind1与固定suppress0闪烁、延后来源提交及二次超距自动攻击回退；`sub_3598C`的队伍暗器陈旧玩家payload槽BUG和随机后signed-word回绕均已恢复并关闭；玩家movement、攻击、用毒/解毒/医疗及type3/type4物品的targeting/方向、状态、FIGHT/EFT、damage、音频和行动尾部已完成；`sub_37734`已按3690字节/837条指令完整入口审计，锁定slot0 BUG、unsigned profile、四种area、第二击缓存、伤害/动画/sprite/present提交顺序、升级与16位回绕；首轮发现`E6EC2`被错误按BattleSetup清零的跨战差异，现由runtime持有进程期scratch并透传Session/Setup，修正后入口重审零剩余差异并独立关闭；
6. `sub_3859E`已按684字节/174条指令完整入口审计，锁定35处重定位、9-call序列、四caller参数、FIGHT与attack/effect双bank在signed帧数检查前预载、阈值只启动loaded sample、同帧bank1先于bank2、19帧与零帧边界；首轮修正阈值处错误load+start，fresh xref再修正AI毒/医/解清槽、Session丢失跨战`E6ED6`及AI同步晚于移动/休息的差异，现由runtime持有进程期武功槽并在玩家攻击入口/选择和AI plan建立时同步，support仅读；slot0 sample7/slot2 sample8跨Session及菜单取消回归通过，修正后入口重审零剩余差异并独立关闭。`sub_3884A`已按198字节/56条指令完整入口审计，锁定9处重定位、9-call序列、三个caller signed effect参数、bank1 sample13与bank2 effect先预载、bank1先启动、100参数等待期间无render/present、其后仅启动bank2及逐帧render/present/delay17/frame+2；首轮修正默认play造成的加载/启动错序及前奏重绘caller帧两项差异，玩家、AI直接和AI移动后三路径现以`load, load, start_loaded`进入且前奏framebuffer不变，effect0/2/30和非法边界回归通过，修正后入口重审零剩余差异并独立关闭。`sub_38910`已按120字节/31条指令完整入口审计，锁定5个分支、7处重定位、4-call序列、7个caller suppress低字、phase0..9 render、present后phase++、delay1及normal前4帧flash/suppressed全灭；首轮修正AI暗器非零damage被错误清kind0及present确认后phase延迟递增两项差异，玩家支持/暗器与AI直接/移动暗器逐帧present/wait/cleanup回归通过，并对受影响`sub_3598C`从入口复核440条指令零剩余差异。Golden三生成一致SHA256为`a6e7c3adccb1cedd5c74296b305cba84da73dfb784906e0a28b6208974e46b1b`，Linux Debug 14/14；order53独立关闭，renderer及七个caller不传播closure；`sub_38999`已按1043字节/248条指令完整入口审计，锁定30个分支、69处重定位、5-call序列、唯一caller、四方向jump table、signed低字range、逐格bounds/side/effect/damage顺序和外部共享尾；首轮发现机器`range=32767`因BX回绕不返回而现代有限执行的畸形差异，现精确安全拒绝该值。全部15条真实line profile范围1..7，四方向、先越界后入界、友军/两敌人继续、HP0 hidden1、非法/zero/max range及双击复用方向回归通过，修正后入口重审零剩余合法域差异。Golden三生成一致SHA256为`e7c4b24495a6ddab76b449e11473d31dc982705a5bf9d37774a20fc17b9ea276`，Linux Debug 14/14；order54独立关闭，jump table、HP damage、direction UI、attack caller、动画与共享尾不传播closure；`sub_38DAC`已按988字节/259条指令完整入口审计，锁定52个跳转、45处重定位、8-call序列、唯一caller、signed十槽availability、ordinal cursor、普通名称稀疏漏画、选中名称十槽解析、Big5布局、右→左→三确认→Escape的flag优先级及本地RET；首轮发现入口及方向后可跳过必经present的产品差异，现以一次presentation门修正并从入口重审零剩余合法域差异，零available的非终止/非法slot域由现代安全拒绝。Golden三生成一致SHA256为`4c923e05b6739b8dcc097d1183517ca7f0ef6fcff58118cdde8dd20226f2141c`，Linux Debug 14/14；order55独立关闭，caller/callee/输入owner及后续targeting/方向不传播closure；`sub_39188`已按1124字节/283条指令完整入口审计，锁定27个跳转、56处重定位、5-call序列、9个caller、外部共享尾、knowledge过滤、unsigned熟练度与MP affordability cost-scale、两段伤害公式和条件四次RNG、signed/unsigned距离缩放、strict underkill奖励、hurt/poison阈值及全部写回顺序；首轮完整对照零产品差异，新增scale9/scale1、inactive knowledge、负fallback跳过修正、exact poison100、counter回绕和非法域回归后入口重审仍零差异。Golden三生成一致SHA256为`ec9ff1431f453f5fa8d2a1ec73ee87dd8c5febe63fe71a6d1d12957a4cd0c86a`，Linux Debug 14/14；order56独立关闭，caller/RNG/cost-scale消费与共享尾不传播closure；`sub_395EC`已按394字节/96条指令完整入口审计，锁定3个条件跳转、22处重定位、6-call序列、唯一caller、本地RET、unsigned熟练度/100、五次RNG与第三次条件消费、actor current/max MP低16位写回和signed夹取、target两步扣减与signed <=0清零及signed32返回；首轮完整对照零产品差异，新增add_mp消费边界、负值、MP/max/target回绕、zero clamp、seed2和共享role别名后入口重审仍零差异。Golden三生成一致SHA256为`fb4d4ec7a49114ca5361904e3f5fff1bff2f32371ba675b85f22709184f1a636`，Linux Debug 14/14；order57独立关闭，caller/RNG/栈探测不传播closure；玩家用毒/解毒/医疗wrapper已完整执行targeting取消/确认、状态先提交、动态FIGHT/EFT绘制、attack/effect双bank音效、逐帧present/tick、damage flash规则及共享尾部；玩家物品已完整执行共享5×3界面、type3效果面板与type4暗器targeting、sample13前奏、EFT/damage、延后库存/行动/sprite提交，`sub_3A29C/sub_3A30B`推进为`implemented_pending_review`；AI自动攻击、用毒、医疗与解毒已复用FIGHT/EFT/damage同步相位，AI普通物品面板及AI暗器前奏/EFT/damage调用点均已接入，直接/移动后像素、RNG、音频、帧数与延后消费由真实资产回归锁定；
7. `sub_3AA17` 等待队尾重排为 `implemented_pending_review`；`sub_3AA4B` 锁定重绘→present→置自动flag→AI顺序；`sub_3AA85`双pass renderer已覆盖全部玩家/AI移动、攻击、支持、物品、状态、战果和战后调用点；战果胜/败帧及五类战后消息帧均有固定hash；
8. `sub_3B238..sub_3C2AC`已恢复胜负判定覆盖顺序、Big5战果框、敌方状态重置、经验均分、队伍下限、角色/练功/制造经验、等级/练功/武功/制造状态与RNG；`BattleSession`实际执行战果及五类消息的render/present/任意键队列并在耗尽后产出typed结果；
9. `sub_3C563/sub_3C672`已恢复回合hurt/poison扣血及hidden目标引用清理；`sub_3C6D3`三个xref均由玩家菜单或AI prelude/wait相位执行，独立面板与Session prelude像素hash均固定；
10. 完成全部实现后逐函数执行不限次数双向 REVIEW，最后一轮零新增差异前只标 `implemented_pending_review`。

## 测试、真实资产与差分点

- 资产：92 FIGHT 包逐 archive 边界、4,992帧、140 WAR 记录、26 WARFLD entries。
- 状态：参战者建立、占位、16位回绕、RNG 消费、原 BUG、战后 commit。
- 结果：Victory / Defeat / Escape 与 scene 真/假偏移；get-exp 仅在机器码证明的路径生效。
- 像素/音频：固定战斗 trace 的 indexed framebuffer、palette 与音频命令序列。
- 动态原程序差分在本机无 DOS runtime 时登记 `blocked_runtime_oracle`，不替代静态机器码、真实资产和独立 oracle。

## Closure 统计与下一停点

- `battle-closure.tsv`：0项`pending_mapping`、0项`pending_implementation`、24项`implemented_pending_review`、57项`platform_adapted`；统一最终REVIEW为57/81。
- B7 `sub_2DE03`联合边界已随typed battle result回送推进到`implemented_pending_review`。
- 下一停点：按`audit_order=58`审计`sub_39776` poison-range kernel；实现/测试通过不替代逐owner最终汇编↔C++ REVIEW门禁。
