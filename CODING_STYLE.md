# OpenLegend C++ 编码规范

本规范适用于仓库内全部第一方 C++ 头文件、源文件与测试代码；生成文件、构建目录及第三方代码不在此范围内。

## 1. 函数之间保留一个空行

相邻函数声明之间、相邻函数定义之间，必须恰好保留一个空行。

该规则同样适用于构造函数、析构函数、运算符和重载函数。多行函数签名属于同一个声明或定义，签名内部不插入额外空行。

头文件示例：

```cpp
NODISCARD bool valid() const noexcept;

NODISCARD int scale() const noexcept;

void clear();
```

源文件示例：

```cpp
bool Renderer::valid() const noexcept {
    return error_.empty();
}

void Renderer::clear() {
    error_.clear();
}
```

禁止把相邻函数紧贴书写，也不使用两个或更多连续空行分隔函数。

## 2. 使用统一属性宏

第一方 C++ 代码不得直接书写 `[[nodiscard]]`。需要要求调用方消费返回值时，必须直接包含 `openlegend/attributes.hpp` 并使用统一的 `NODISCARD` 宏：

```cpp
#include "openlegend/attributes.hpp"

NODISCARD bool valid() const noexcept;
```

`NODISCARD` 只统一属性写法，不改变哪些返回值必须检查；已有可失败的 `bool` 渲染、绘制、加载和保存接口继续保留该语义。

## 3. 按完整职责拆分代码

代码必须围绕完整、可说明的职责组织，既不得过度集中，也不得过度分散。

### 3.1 避免过度集中

一个函数、类或文件不得同时承担多个可独立描述、具有各自状态或生命周期的职责。出现以下情况时应拆分：

- 入口函数同时处理配置、资源生命周期、输入、音频、渲染和退出保存。
- 一个 renderer 同时持有基础绘制能力和多个独立业务界面的状态与布局。
- 一个函数需要依靠大量局部 lambda 才能区分内部子系统。

拆分后，入口函数只负责创建依赖、连接模块和返回结果，不直接实现各子系统细节。

### 3.2 避免过度分散

不得仅为减少文件或函数行数而拆分代码。出现以下情况时应保留在所属模块内部：

- 只被一个模块使用、没有独立状态或生命周期的布局计算与格式化辅助逻辑。
- 只服务于一个公开操作的私有实现细节。
- 单独建文件后无法用一句完整职责说明其存在理由。

私有策略、局部数据结构和模块专用 helper 优先保留在所属 `.cpp` 的私有实现中，不采用“一个 struct 一个文件”或“一个 helper 一个文件”的拆分方式。

### 3.3 拆分判断

新增或重构代码时按以下顺序判断：

1. 该部分是否拥有独立的状态、生命周期、不变量或验证边界；如果有，应形成独立职责。
2. 该部分是否只由一个职责内部使用；如果是，应优先作为该职责的私有实现。
3. 拆分后的每个模块能否用一句话准确说明用途；不能说明时，拆分通常过细。
4. 原模块是否仍同时承担多个可独立说明的职责；如果是，拆分仍不充分。

重构不得只把超长函数原样移动或改名；必须形成真实的职责边界。
